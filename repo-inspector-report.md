# repo-inspector report — Rusty-Mill/rusty_mill

> **Stale as of 2026-09-03.** This report's own inventory (95 workspace
> members, 76 external dependencies) is pinned to commit `908138f`. The
> workspace has since grown to 183 members across 199 manifests (see the
> dependency-sovereignty review referenced from `docs/adr/
> 0002-dependency-sovereignty-policy.md`). Its duplication-cluster and
> sovereignty findings below must be regenerated by re-running
> `my_loops/repo-inspector` before being treated as current work — do not
> cite this report's counts as today's state.

Generated by `my_loops/repo-inspector` (`baileyrd/skill_pack`, v1.0.1) against
this workspace's `main` at commit `908138f` (the merge of
[#69](https://github.com/Rusty-Mill/rusty_mill/pull/69), which carried the
previous version of this report). Dry-run only — **no code changes, no
issues, no PRs, no auto-merge**. Every row below is for human review;
"recommended" never means "done."

## This is a re-run against an unchanged workspace

No crate code has changed since PR #69's report was generated: this run's
fresh `cargo metadata` inventory is byte-identical to that one (95 workspace
members, 8,063 capability-index rows, 507 duplication clusters, 76 external
dependencies, and every per-dependency "used by" crate list matches exactly).
Section 2 (sovereignty) is carried forward unchanged after re-verification —
nothing there needed a correction.

**Section 1 needed two.** The report format's own guidance
(`references/repo-inspector-report-format.md`) explicitly warns against
folding several of `find_clusters.py`'s mechanically-distinct clusters into
one narrative row, using this exact scenario as its cautionary example: "a
report once combined three unrelated clusters — an HMAC construction, an RSA
key type, and a `BigUint` type — under one row labeled 'hand-rolled crypto
primitives'... `implementation-merge` had to split that row back into three
separate merge candidates itself." PR #69's report did precisely that (its
row 1). Splitting it open in this run surfaced a second problem underneath
the first: **the "HMAC construction" claim in that fold was never actually
true.** `rusty_oauth`'s `src/crypto/hmac.rs` is HMAC-SHA256; `rusty_rdp`'s
same-named `src/crypto/hmac.rs` is HMAC-MD5 and HMAC-SHA1 (for NTLMv2 and
Kerberos respectively) — different algorithms entirely, clustered by
`find_clusters.py` purely because both files share a module *path*, not
because they share content. An initial pass at this correction re-verified
the claim with a grep pattern loose enough (`fn hmac`) to match `fn
hmac_md5`/`fn hmac_sha1` too, so it passed a spot-check that should have
caught it — worth remembering that the skill's own mandatory grep check
needs the exact symbol, not a substring. A **fourth**, genuine cluster in
the same family was also found and had been silently dropped by the
original fold entirely: a hand-rolled SHA-256 (FIPS 180-4) duplicated
between `rusty_oauth` and `rusty_rdp`, for real this time (both files
literally share the same round-constant table). Section 1 below reflects
both corrections: the RSA-key and `BigUint` clusters from the original fold
(now rows 1a/1b), the real SHA-256 duplicate (row 1c), and the false
HMAC-SHA256 claim logged as investigated-and-rejected rather than silently
dropped, so neither this run's mistake nor the original one resurfaces as
new. Everything else from PR #69's report is otherwise unchanged, restated
here rather than diffed since this is the whole deliverable.

## Before you read this: three prior duplication passes already ran here

- **[PR #10](https://github.com/Rusty-Mill/rusty_mill/pull/10)** (issues
  #1–#8): 6 fixed — `rush`'s backtracking glob now tries `rusty_regx::Glob`
  first; `rusty_git`/`rusty_term`'s independent SHA-1 merged into a new
  `rusty_sha1` crate; `rusty_win32`'s 7×-duplicated `to_wide()` hoisted;
  `rsed`/`rawk`'s shared `read_lines()` extracted; a duplicated Windows
  raw-mode flag transform (`rusty_term`/`rusty_lines`) hoisted into
  `rusty_win32::console::raw_mode_core()`; `rush`'s two IFS-splitting
  implementations merged. 2 closed `no action` — Unix termios save/restore
  (`rusty_term`/`rusty_lines`, same shape, deliberately different policy)
  and a `rusty_font` `no_std` rounding workaround (`round_nonneg` vs.
  `round_f32`). Issue #9 (a capability gap, not duplication) is still open.
- **[PR #65](https://github.com/Rusty-Mill/rusty_mill/pull/65)**: 2 more
  fixed (`rusty_rdp`'s byte cursor merged into `rusty_wire`; `rusty_ansder`
  split into itself plus a new `rusty_rag` crate). 3 more investigated and
  deferred as different-scoped tools sharing a name, not true duplication:
  `rusty_term` vs. `rusty_ansi`, `rusty_ansder`'s DER codec vs. `rusty_tls`'s
  hand-rolled DER, `rusty_http::Url` vs. `rusty_url::Url`.
- **[PR #69](https://github.com/Rusty-Mill/rusty_mill/pull/69)**: this
  report's own previous version — a dry-run audit, nothing merged from it
  (v1 never files issues or PRs on findings). Superseded by this run per the
  correction above; its Section 2 findings stand unchanged.

None of the above are re-presented as new findings below.

## Section 1 — Duplication clusters

Method: `scripts/index_workspace_capabilities.sh` indexed all 95 workspace
crates (8,063 rows); `scripts/find_clusters.py` grouped them into 507
candidate clusters. The overwhelming majority are noise by design (a
`module: src/lib.rs` cluster every crate trivially joins; generic-name
clusters like `fn: new`/`fn: get`/`fn: parse` spanning dozens of crates) —
triaged as `coincidental-similarity` and not enumerated row-by-row here.
This section covers the clusters with specific-enough names to read as real
candidates: the ones that turned out to matter, sorted worst offender
first, plus a representative "confirmed no action" sample. Every
specific-symbol claim below was spot-checked with `grep -c` against the
named file this run, per the skill's own mandatory check.

| # | Cluster | Candidate crates | Classification | What each candidate does / completeness | Recommended extraction |
| --- | --- | --- | --- | --- | --- |
| 1a | `RsaPublicKey` (`struct: rsa_public_key`) | `rusty_oauth` (`src/jwt/rsa.rs::RsaPublicKey`), `rusty_rdp` (`src/security.rs::RsaPublicKey`) | **exact/near-duplicate** | Same RSA public-key verification math, different call sites: verifying an RS256-signed JWT (`rusty_oauth`) vs. an RDP server certificate's signature (`rusty_rdp`). Neither crate depends on the other or on a shared crypto crate; each looks complete for its own use. | Extract into a shared crate (e.g. `rusty_rsa` or fold into `rusty_crypto_key`, which already exists for key *storage* but not this math). `rusty_oauth`'s version looks marginally more complete (more surrounding JWT-specific tests) but neither is obviously canonical — worth a closer read before picking which becomes the host. |
| 1b | `BigUint` (`struct: big_uint`) | `rusty_oauth` (`src/crypto/bigint.rs::BigUint`), `rusty_rdp` (`src/crypto/bignum.rs::BigUint`) | **exact/near-duplicate** | Both little-endian, base-2³² limb arbitrary-precision unsigned integers — same representation, independently implemented. This is the primitive `1a`'s RSA verification is built on in both crates. | Same host as 1a — the natural pairing, since `1a`'s RSA math is built directly on this type in both crates. |
| 1c | `SHA-256` (`module: src/crypto/sha256.rs`, `fn: sha256`) | `rusty_oauth` (`src/crypto/sha256.rs`), `rusty_rdp` (`src/crypto/sha256.rs`) | **exact/near-duplicate** | Both hand-rolled, dependency-free SHA-256 (FIPS 180-4) — same `K` round-constant table, same compression-function structure. `rusty_rdp`'s own doc comment is explicit about why it's hand-rolled despite not being a broken algorithm: "SHA-256 is not broken, but it is still implemented by hand to keep the crate dependency-free." `rusty_oauth`'s backs its own hash chain for JWT verification. Not in PR #69's report — surfaced independently this run. | Candidate for the same host as 1a/1b, though it's a genuinely separate primitive from the RSA/`BigUint` pair (RSA verification doesn't need SHA-256 directly in either crate — `rusty_oauth`'s JWT hash chain and `rusty_rdp`'s CredSSP public-key binding are the actual callers) — worth confirming during extraction whether it belongs in the same crate or a smaller sibling. |
| 1d | ~~`HMAC-SHA256`~~ — investigated, not real | `rusty_oauth` (`src/crypto/hmac.rs`), `rusty_rdp` (`src/crypto/hmac.rs`) | **coincidental-similarity** | PR #69's original fold (and this report's own first correction pass) claimed this as exact/near-duplicate HMAC-SHA256 in both crates. False: `rusty_oauth`'s `hmac.rs` is HMAC-SHA256 (backs RS256 JWT verification), but `rusty_rdp`'s same-named `hmac.rs` is HMAC-MD5 and HMAC-SHA1 (NTLMv2 and Kerberos respectively) — different algorithms, matched by `find_clusters.py` purely on module *path*, not content. Confirmed via `grep -c 'fn hmac_sha256' crates/rusty_rdp/src/crypto/hmac.rs` → 0. | None — logged here (rather than silently dropped) so this specific false claim, seen twice now across two report versions, doesn't get re-proposed a third time. |
| 2 | `f16_to_f32` (IEEE 754 half→single precision float conversion) | `rusty_simd` (`src/lib.rs`), `rusty_whisper` (`src/model.rs`) | **exact/near-duplicate** | Both implement full IEEE 754 semantics including the subnormal case (bit-shift normalization). `rusty_simd`'s own crate description is literally "for LLM and Whisper inference" — it exists for exactly `rusty_whisper`'s use case. Both look algorithmically complete and correct (same edge cases handled, just different code shape — a loop vs. a `leading_zeros()` trick). | **`rusty_whisper` should depend on `rusty_simd::f16_to_f32` directly** rather than reimplement it — this is the cleanest case in the report: the "shared crate" already exists and is already a workspace member, this is purely an unused-dependency situation. |
| 3 | `RetryPolicy` (retry/backoff configuration) | `rusty-acp` (`src/client/mod.rs`), `rusty_request` (`src/retry.rs`) | **convergent-but-diverged** | `rusty_request`'s is a general HTTP client feature (opt-in via `ClientBuilder::retry`). `rusty-acp`'s is scoped to "the deployment this crate is aimed at" (its own doc comment) — likely narrower, agent-RPC-specific assumptions about what's retriable. Didn't fully diff the retry/backoff algorithm itself (exponential? jitter? which status codes retry?) — that's the open behavioral question. | Candidate for extraction (usage count 2), but the behavioral question needs a human answer first per repo-inspector's own process — this is the "mechanism vs. policy" split `dedupe-loop`/ADR-011 exists for: does `rusty-acp` adopt `rusty_request`'s retry mechanism with its own policy layered on top, or does it have a real reason (agent-RPC semantics) to stay separate? |
| 4 | `AnsiParser` | `rusty_ansi` (`src/lib.rs`), `rusty_term` (`src/core/parser.rs`) | **coincidental-similarity** (independently confirms PR #65's finding) | `rusty_term`'s turns a shell's output byte stream into `Grid` mutations — coupled to its own terminal-state model. `rusty_ansi`'s is a zero-allocation, streaming ANSI escape iterator over a string slice with no `Grid` coupling at all — a general-purpose parsing library. Genuinely different layers (terminal emulator internals vs. a reusable escape-sequence parser), same name. | None — re-checked PR #65's deferral independently and it holds. Logged here so a future re-scan doesn't re-surface it as new. |
| 5 | `DropTable` / `AlterTable` / `DropIndex` / `ToSql` (SQL DDL vocabulary) | `rusty-db-core` (`src/query/ddl.rs`, `src/query/mod.rs`), `rusty_rusqlite` (`src/ddl.rs`, `src/tosql.rs`) | **coincidental-similarity** | Opposite data-flow directions despite identical type names: `rusty_rusqlite`'s are **parsed** statements — it's a from-scratch SQLite reimplementation, so it needs a real SQL-text parser. `rusty-db-core`'s are **builders** — a portable query builder that *generates* SQL text from a fluent API (`rusty-db-core`'s own doc: "Builds a portable `ALTER TABLE` statement"). A parser and a builder aren't the same capability even sharing a name and a domain. | None — shared SQL vocabulary, not shared code or even shared direction of data flow. |
| 6 | `SystemClock` | `reactor-core` (nested under `rustils_async/crates/reactor-core`, `src/clock.rs`), `rusty_stream` (`src/clock.rs`) | **exact/near-duplicate**, low priority | Both wrap the real system clock behind a trait for testability — `reactor-core`'s wraps `Instant::now()` (monotonic), `rusty_stream`'s wraps `std::time::SystemTime` (wall-clock). Not literally the same wrapped type, but the same one-line "real clock" pattern reimplemented. Both trivial (a handful of lines). | Low-value extraction — the whole point is a one-line wrapper; a shared crate for it likely costs more (a new dependency edge) than it saves. Noting for completeness rather than recommending action. |

**Deliberately not re-litigated** (already closed `no action` by PR #10, and
this run found nothing to reopen either decision on): Unix termios
save/restore (`rusty_term`/`rusty_lines`) and the `rusty_font` rounding
workaround (`round_nonneg`/`round_f32`).

## Section 2 — Sovereignty findings

Method: `scripts/scan_workspace_sovereignty.sh` inventoried all 76 distinct
external (non-workspace, non-dev/build) dependencies across the workspace's
95 crates via `cargo metadata`, then grepped the rest of the workspace for
internal-coverage candidates. Given the volume, this section covers the
highest-usage dependencies (by crate count) and every one with a
`rusty_<thing>` name match in `references/platform-directory.md` — not all
76 rows. Skipped entirely: single-user, no-name-match dependencies with no
grep signal (mostly `rusty-mcp`/`rusty_a2a`'s own protocol-specific crates
— `prost`, `tonic`, `opentelemetry*`, etc. — genuinely specialized, low
sovereignty relevance). Re-verified this run against a fresh scan; every
per-dependency "used by" list matched PR #69's report exactly, so this
section is unchanged.

| Dependency | Used by (crates) | Internal equivalent | Notes |
| --- | --- | --- | --- |
| `uuid` | `rusty-acp`, `rusty-db-core`, `rusty_a2a` | **`rusty_uuid`** (workspace member) | Already a proven swap, not a hypothetical: `rusty_search`'s own CHANGELOG documents completing this exact swap for its own crates, calling it "a genuine, complete sovereignty win (zero dependencies of its own)." Three more crates haven't made the same swap yet. |
| `reqwest` | `rusty-acp`, `rusty-mcp`, `rusty_a2a` | **`rusty_request`** (workspace member) | Already adopted elsewhere: `rusty_proxmox`'s own README states it uses `rusty_request` "rather than `reqwest`." Three crates still pull the external crate directly. (`rusty_search`'s several backend crates also use `reqwest` deliberately, per their own ADRs, for backend-parity reasons specific to those integrations — not included as a finding here.) |
| `url` | `rusty-mcp` | **`rusty_url`** (workspace member — from-scratch WHATWG URL implementation, the same crate PR #65 confirmed is *not* duplicative of `rusty_http::Url`) | Single user, straightforward swap candidate. |
| `rustls` | `rusty_rdp`, `rusty_tls` | **partial** — `rusty_tls` (workspace member, described as "a `rustls`-based TLS library") | `rusty_tls` is the deliberate wrapper — `rustls` itself is a floor dependency there by design, not a finding. `rusty_rdp` depends on raw `rustls` directly rather than going through `rusty_tls`; worth checking whether `rusty_rdp`'s TLS needs (its own certificate-verification path) fit `rusty_tls`'s existing API before centralizing there. |
| `base64` | `rusty-acp`, `rusty-mcp`, `rusty_a2a` | **partial** — `rusty_request` already has a hand-rolled encoder (`src/base64.rs`, private `mod base64`, encode-only, RFC 7617 Basic-Auth use case) | Not currently reusable as-is (private to `rusty_request`, encode-only — the three users may need decode too). Worth checking whether decode is actually needed before treating this as more than a partial match. |
| `thiserror` | `platform`, `rusty-acp`, `rusty-db-core`, `rusty-mcp`, `rusty_a2a`, `rusty_opnsense`, `rusty_proxmox` | **partial, flagged with a caveat** — `rusty_err` (+ `rusty_err_derive`, both workspace members — "sovereign error trait... error derive library") | `references/platform-directory.md`'s own "Known transitive-dependency note" already documents that `platform`'s `thiserror` use pulls `syn`/`quote`/`proc-macro2`/`unicode-ident` into every consumer of the platform layer, and warns a full `cargo tree -i thiserror` reachability check (`sovereignty-loop` step 2.5, out of scope for this dry-run skill) is required before treating any of these as removable — one such check already invalidated a completed hand-roll elsewhere in this ecosystem. Flagging as a real candidate, explicitly not as "safe to remove." |
| `tokio` | 18 crates (`rusty-acp`, `rusty-db-core`, `rusty-mcp`(+`-demo`), 6 `rusty-search-*` backends, `rusty_a2a`, `rusty_homelab_mcp`, `rusty_http`, `rusty_lsp`, `rusty_request`, `rusty_term`) | **none — correctly `keep external`, not a gap** | `rusty_tokio` (workspace member, hand-rolled async runtime) exists, but its own README states its purpose explicitly: *"It exists to actually understand how an async runtime works, not to replace tokio."* Treating this as a sovereignty gap would be relitigating an already-decided non-goal — noting it here only so a future scan doesn't propose it fresh. |
| `libc` | `platform-async-linux`, `platform-bsd`, `platform-linux` (3 of the 7 users — `rustils`'s own sub-crates), plus `rush`, `rusty_lines`, `rusty_term`, `rusty_tokio` (4 more, not examined this pass) | **already tracked** — `rusty_libc` (workspace member) | Not a fresh finding: `rustils/deny.toml` already documents `rusty_libc` as "a deliberate rev-pinned git dependency" (Track P) for exactly this migration — `platform-linux`/`platform-bsd` still on external `libc` is a known, in-progress state per `rustils`' own docs, not an oversight. `rush`/`rusty_lines`/`rusty_term`'s use wasn't cross-checked against the same ADR — a genuinely open question for a follow-up pass. |
| `rusqlite` | `rusty_sqlite` | **keep external, already centralized** | `rusty_sqlite` exists specifically to wrap it ("thin, ergonomic wrapper over `rusqlite`"), and `rusty-search-sqlite-fts5` already re-exports `rusty_sqlite::rusqlite` rather than depending on `rusqlite` directly (per its own `Cargo.toml` comment). Good state already — noted, not a gap. |
| `sqlx` | `rusty-acp`, `rusty-db-mysql`, `rusty-db-postgres`, `rusty-db-sqlite` | **none found** | `rusty_rusqlite`/`rusty_sqlite` cover SQLite only; no sovereign Postgres/MySQL driver exists in this workspace. **Recommend running `parity-loop` against `sqlx`'s API surface** — this is the closest thing in this pass to `sovereignty-loop`'s own worked "hand-roll L/XL, propose a dedicated effort" case, given the size of a Postgres/MySQL wire-protocol implementation. |
| `clap` | `rusty-mcp`, `rusty-mcp-demo`, `rusty_homelab_mcp` | **none found** | No sovereign CLI-parsing crate in this workspace. Foundational, large surface (subcommands, derive macros, shell completion) — low sovereignty priority relative to its engineering cost; noting rather than recommending `parity-loop` outright. |
| `unicode-width` | `rusty_ansi`, `rusty_lines`, `rusty_term` | **none found** | Small, focused crate, but its correctness depends on Unicode Character Database version-specific tables that need ongoing maintenance to stay current — a real, recurring cost a `parity-loop` hand-roll would take on indefinitely. Low priority for the same reason `sovereignty-loop`'s own rules treat maintenance burden as a real factor in the hand-roll-size call. |

## Tool notes (repo-inspector's own behavior on this run)

- **Section 1's crypto cluster took two corrections, not one, and this
  report's own first attempt at the first correction is what surfaced the
  second.** The format reference already documents the row-folding failure
  mode and names the downstream cost (`implementation-merge` having to
  un-fold a row before it could act), but the run that produced PR #69's
  report made the mistake anyway (folding an HMAC claim, an RSA key type,
  and a `BigUint` type into one row). Splitting that fold open in this
  report's *first* pass correctly separated the three claims but re-verified
  each with a grep pattern loose enough (`fn hmac`) to match `fn
  hmac_md5`/`fn hmac_sha1` as well as `fn hmac_sha256` — so it reported all
  three (plus a genuinely new fourth, SHA-256) as confirmed when the HMAC
  one was actually false. A second pass, re-verifying with the exact symbol
  (`fn hmac_sha256`) rather than a substring, caught it: `rusty_rdp` has no
  HMAC-SHA256 at all. Both corrections are folded into Section 1 above (rows
  1a–1d) rather than left implicit. The lesson for this skill generically:
  the mandatory grep spot-check needs an exact-symbol pattern, not a
  substring loose enough to match a same-prefixed sibling function.
- `index_workspace_capabilities.sh`'s nested-workspace-member pruning (fixed
  in v1.0.1) was re-confirmed live on this run: `reactor-core`
  (`crates/rustils_async/crates/reactor-core`) is a real, separately nested
  workspace member and appears correctly as its own crate in row 6, not
  double-tagged into `rustils_async`.
- Every specific-symbol claim in Section 1 (`RsaPublicKey`, `BigUint`,
  `SHA-256`, `f16_to_f32`, `SystemClock`, and — this time with an
  exact-symbol pattern — `HMAC-SHA256`) was re-verified this run with
  `grep -c '<exact symbol>' <file>` against the actual candidate files. All
  came back non-zero except `rusty_rdp`'s `fn hmac_sha256`, which is the
  correction above.
- `repo-config`'s governance file set (CONTRIBUTING, SECURITY, ARCHITECTURE,
  ADR log, templates) is already present at the workspace root from PR #69
  — reconfirmed present, not re-applied.
- This pass covers the highest-signal subset of both a 507-cluster and a
  76-dependency surface, not either exhaustively — see the method notes at
  the top of each section for what was in scope. A `FOCUS`-narrowed re-run
  on a specific capability area, or simply more review time, would surface
  more.
