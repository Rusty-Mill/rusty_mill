[package]
name = "rusty_crypto_key"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "A zeroize-on-drop key storage and file persistence micro-crate (0600 permissions on Unix)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/baileyrd/rusty_crypto_key"
keywords = ["crypto", "key", "zeroize", "secret", "no-deps"]
categories = ["cryptography", "no-std"]

[dependencies]

[features]
default = ["std"]
std = []

[dev-dependencies]
