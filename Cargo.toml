[package]
name = "rpath"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Path translation and normalization engine for MSYS2/Git Bash/POSIX to Windows interop"

[dependencies]
