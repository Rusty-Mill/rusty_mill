/target
**/*.rs.bk
*.pdb
Cargo.lock.orig

# sessionmgr's own local state root, if a dev points one at the repo
.sessionmgr/
.sessionmgr-worktrees/

# Tauri's own generated ACL/capability JSON schemas -- regenerated from
# capabilities/*.json on every build, not source.
/crates/sessionmgr-desktop/src-tauri/gen/
