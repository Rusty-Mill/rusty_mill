/target
Cargo.lock
.cargo/

# cargo-fuzz working state. The corpus is a large binary blob that grows on
# every run and is reproducible from a trust store (see fuzz/Cargo.toml);
# artifacts are crash reproducers, which are worth committing individually as
# regression tests rather than as a directory.
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
fuzz/coverage/
