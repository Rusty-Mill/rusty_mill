[package]
name = "rusty-hister-server"
version = "0.1.0"
edition = "2021"
description = "Hister's HTTP/JSON API and WebSocket search protocol (server/api.go's surface), v1 scope"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Rusty-Mill/rusty_mill"
authors = ["rusty_hister contributors"]
publish = false

[dependencies]
rusty-hister-core = { path = "../rusty-hister-core" }
rusty-hister-model = { path = "../rusty-hister-model" }
