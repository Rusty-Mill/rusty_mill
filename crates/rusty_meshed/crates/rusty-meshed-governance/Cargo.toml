[package]
name = "rusty-meshed-governance"
version = "0.1.0"
edition = "2021"
description = "Policy-as-code governance engine and built-in policies, ported from meshed.governance."
license = "MIT OR Apache-2.0"
repository = "https://github.com/baileyrd/rusty_mill"
publish = false

[package.metadata.rusty_mill]
layer = "apps"

[dependencies]
rusty_err = { workspace = true }

[dev-dependencies]
