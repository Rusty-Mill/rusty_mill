[package]
name = "rusty_json-derive"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Proc-macro #[derive(RustyJson)] attribute for rusty_json"
license = "MIT OR Apache-2.0"

[package.metadata.rusty_mill]
layer = "foundation"

[lib]
proc-macro = true

[dependencies]
