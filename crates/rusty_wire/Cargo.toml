[package]
name = "rusty_wire"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "A minimal, zero-dependency endian-explicit byte cursor Reader/Writer for Rust"
license = "MIT OR Apache-2.0"
repository = "https://github.com/baileyrd/rusty_wire"
keywords = ["wire", "cursor", "byte-cursor", "no-deps", "endian"]
categories = ["encoding", "no-std"]

[dependencies]

[features]
default = ["std"]
std = []

[dev-dependencies]
