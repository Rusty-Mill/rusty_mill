{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageNoteCreateUniqueArgs",
  "description": "Args for `com.nexus.storage::note_create_unique` (handler `83`).",
  "type": "object",
  "properties": {
    "folder": {
      "description": "Forge-relative folder for the new note. Default: forge root.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "id_format": {
      "description": "chrono `strftime` template for the id prefix. Default `%Y%m%d%H%M%S`.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "separator": {
      "description": "Inserted between id and title. Default a single space.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "title": {
      "description": "Human title; sanitized into the filename suffix. May be empty.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "title"
  ]
}
