# Specification Registry

Status vocabulary: `Proposed`, `Draft`, `Accepted`, `In Progress`,
`Implemented`, `Verified`, `Blocked`, `Deferred`, `Deprecated`,
`Superseded`.

| ID | Title | Version | Design | Implementation | Verification | Depends on | Owner | Location | Evidence |
|---|---|---:|---|---|---|---|---|---|---|
| `STORAGE-001` | Dataset generator and record model | 0.1.0 | Accepted | Implemented | Verified | — | baileyrd | `docs/specifications/storage/STORAGE-001-dataset-generator.md` | `src/generator.rs` tests |
| `STORAGE-002` | `DogStore` trait and three backend implementations | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-001` | baileyrd | `docs/specifications/storage/STORAGE-002-dogstore-backends.md` | `src/store/**` + `tests/cross_backend.rs` |
| `STORAGE-003` | Criterion benchmark suite and cache-miss instrumentation | 0.1.0 | Accepted | Implemented | Verified (wall-clock); cache-miss counters not producible in this session's environment, see ADR-0002/RESULTS.md | `STORAGE-001`, `STORAGE-002` | baileyrd | `docs/specifications/storage/STORAGE-003-benchmark-suite.md` | `cargo bench` run, `RESULTS.md` |
| `STORAGE-004` | Results and decision writeup | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-003` | baileyrd | `docs/specifications/storage/STORAGE-004-results-writeup.md` | `RESULTS.md` |
| `STORAGE-005` | `CanonicalCachedStore`: canonical store with a materialized age cache | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002` | baileyrd | `docs/specifications/storage/STORAGE-005-canonical-cached-backend.md` | `src/store/canonical_cached.rs` tests + `tests/cross_backend.rs` + `RESULTS.md` |
| `STORAGE-006` | `littermate_of` graph relationship: edge generation, `neighbors` trait method, graph-traversal benchmarks | 0.1.0 | Accepted | Implemented | Verified (wall-clock); cache-miss counters not producible in this session's environment, see ADR-0002/RESULTS.md | `STORAGE-001`, `STORAGE-002`, `STORAGE-003` | baileyrd | `docs/specifications/storage/STORAGE-006-littermate-graph-traversal.md` | `src/generator.rs`, `src/store/**`, `src/bench_support.rs` tests + `tests/cross_backend.rs` + `cargo bench` run + `RESULTS.md` |
| `STORAGE-007` | Mixed read/write workload benchmark: blended `get`/`update_age`/`scan_ages` driver, swept write ratios | 0.1.0 | Accepted | Implemented | Verified (wall-clock); cache-miss counters not producible in this session's environment, see ADR-0002/RESULTS.md | `STORAGE-001`, `STORAGE-002`, `STORAGE-003`, `STORAGE-005` | baileyrd | `docs/specifications/storage/STORAGE-007-mixed-workload-benchmark.md` | `src/bench_support.rs` tests + `cargo bench` run + `RESULTS.md` |
| `STORAGE-008` | Durability, Tier 1: WAL/snapshot/hybrid family for `CanonicalCachedStore` | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-005` | baileyrd | `docs/specifications/storage/STORAGE-008-durability-tier1.md` | `src/durability/{mod,wal_fsync,wal_buffered,snapshot_rebuild,snapshot_full,hybrid}.rs` tests + `cargo bench --bench durability` run + `RESULTS.md` |
| `STORAGE-009` | Durability, Tier 2: alternate architectures (mmap, LSM-style, `redb`) for `CanonicalCachedStore` | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-005`, `STORAGE-008` | baileyrd | `docs/specifications/storage/STORAGE-009-durability-tier2.md` | `src/durability/{mmap_store,lsm_store,embedded_store}.rs` tests + `cargo bench --bench durability` run + `RESULTS.md` |
| `STORAGE-010` | Concurrent access prototypes for `CanonicalCachedStore`: global `RwLock`, sharded locking, `dashmap`, actor/single-writer-thread | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-005` | baileyrd | `docs/specifications/storage/STORAGE-010-concurrency-prototypes.md` | `src/concurrency/{mod,global_rwlock,sharded,dashmap_store,actor}.rs` tests + `cargo bench --bench concurrency` run + `RESULTS.md` |
| `STORAGE-011` | Production default: `ProductionStore` (`CanonicalCachedStore` architecture + mmap durability + global `RwLock` concurrency) consolidated into one recommended type, `TransactionalStore` critical-section primitive | 0.2.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-005`, `STORAGE-007`, `STORAGE-008`, `STORAGE-009`, `STORAGE-010` | baileyrd | `docs/specifications/storage/STORAGE-011-production-default.md` | `src/production.rs` tests + `tests/production_integration.rs` + `cargo bench --bench workloads -- production` + `cargo bench --bench concurrency` run + `RESULTS.md` |
| `STORAGE-012` | Generic schema library: `crate::generic` (promoted record/schema/query traits + composable store layers + `Order`/`Customer` reference implementation), `GenericMmapStore`/`GenericProductionStore` (generic durability + `RwLock` concurrency), `Reversed`/`Neighbors` forwarding completion, `GenericProductionStore::with_exclusive` critical-section primitive | 0.3.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-005`, `STORAGE-009`, `STORAGE-010`, `STORAGE-011` | baileyrd | `docs/specifications/storage/STORAGE-012-generic-schema-library.md` | `src/generic/**` tests + `tests/generic_production_integration.rs` + `cargo bench --bench generic_production` run + `RESULTS.md` |
| `SERVER-001` | Network server/query layer: `Request`/`Response` protocol, length-prefixed `bincode` framing, thread-per-connection dispatch, `Dog`/`Order`-`Customer`/`Employee` domain adapters, schema discovery, throughput/latency benchmark (`GetById` + `Request::Transaction` coverage), schema-driven client library, authentication/authorization, atomic multi-operation transactions, native transport encryption via `rusty_tls` (`server` feature); since v0.9.1 the wire encoding is `STORAGE-018`'s codec; since v0.10.0 the wire shape is versioned (`PROTOCOL_VERSION = 2`, optional first-frame `Hello`, append-only compatibility rules — FR-020, ADR-0022); since v0.11.0 `SchemaDrivenClient` can authenticate (`connect_authenticated`/`authenticate` — FR-021); since v0.12.0 it can reach a `TlsConfig`-configured server (`connect_with` + `ClientTlsConfig` — FR-022); since v0.13.0 mutual TLS as an admission gate layered under the tokens (`TlsConfig::new_with_client_auth`, `ClientTlsConfig::with_identity` — FR-023, ADR-0023); since v0.14.0 transaction sessions at protocol version 3 (`Begin`/`Commit`/`Rollback`, `Staged`, `SchemaDrivenClient::begin` — FR-024, ADR-0024); since v0.15.0 opt-in crash-atomic batches via a redo journal (`with_journal`, `ErrorCode::Journal`, protocol version 4 — FR-025, ADR-0025); since v0.16.0 `SchemaDrivenClient` reconnects without a `Hello` to a pre-hello server, default-on with `require_hello()` opt-out (FR-026); since v0.17.0 the journal's `fsync` is a leader/follower group commit outside the store's exclusive section with applies kept in journal order (`CommitGroup` — FR-027, ADR-0026); since v0.18.0 opt-in read-your-writes sessions at protocol version 5 (`BeginWith { flags }`, `GetById` overlaid, `Session::get` — FR-028, ADR-0027); since v0.19.0 an opt-in audit log of admission, authentication, and authorization decisions (`AuditSink` on `AuthConfig::with_audit`, `SERVER_AUDIT_LOG` — FR-029, ADR-0029); since v0.20.0 stage-time validation in sessions at protocol version 6 (`SESSION_VALIDATE_ON_STAGE`, `ConnectionStore::validate_op`, `SessionOptions` — FR-030); since v0.21.0 class from certificate — an exact-DER pinned leaf may start a connection at a class with no `Authenticate` (`AuthConfig::with_certificate_class`, `SERVER_AUTH_READ_ONLY_CLIENT_CERTS`/`SERVER_AUTH_READ_WRITE_CLIENT_CERTS` — FR-031, ADR-0028); since v0.22.0 rate limiting and lockout — a per-connection lockout at `MAX_AUTH_FAILURES` on by default and an opt-in per-peer failure budget, both audited (`AuthConfig::with_rate_limit`, `SERVER_AUTH_RATE_LIMIT` — FR-032, ADR-0030); since v0.23.0 an opt-in per-request access log, a second sink family independent of the audit log, kind and outcome shape only, never a payload (`AuthConfig::with_access_log`, `SERVER_ACCESS_LOG` — FR-033, ADR-0031); since v0.24.0 the audit log's `Admitted` event records whether the connection's starting class came from a matched certificate (`AuditKind::Admitted.classed_by_certificate` — FR-034); since v0.25.0 `AuthConfig` is renamed `ServeOptions` and absorbs `TlsConfig` as a field, `serve` dropping from four parameters to three (`ServeOptions::with_tls`/`tls()` — FR-035, ADR-0032); since v0.26.0 opt-in session snapshot isolation at protocol version 7 — a third `BeginWith` bit records every session `GetById`'s raw committed value into a read set `Commit` re-checks atomically with the staged batch's own validate/apply, refusing the whole commit with `ErrorCode::Conflict` on any mismatch (`SESSION_SNAPSHOT_ISOLATION`, `ConnectionStore::apply_transaction`'s new `read_set` parameter, `SessionOptions::snapshot_isolation()` — FR-036, ADR-0033); since v0.27.0 a bounded, read-only SQL `SELECT` subset at protocol version 8 — `*`/named columns, `WHERE` with `AND`-only equality/ordering predicates, `LIMIT`, parsed entirely client-side against the already-discovered schema and sent as a structured `Request::Query`, every full scan answered by one centralized filter/project/limit pair reused across all three domains, every schema-described field queryable regardless of its other capability flags (`Request::Query`/`Response::Rows`, `SchemaDrivenClient::query`/`Session::query` — FR-037, ADR-0034); since v0.28.0 bounded `GROUP BY`/aggregation at protocol version 9 — `COUNT`/`SUM`/`AVG`/`MIN`/`MAX` and `GROUP BY` on top of `Query`'s own machinery, parsed client-side the same way and sent as a structured `Request::Aggregate`, one centralized filter/bucket/reduce pair reused across all three domains, `ScanValue::F64` carrying only `AVG`'s result, `SchemaDrivenClient::query`'s return type widened to `QueryResult::{Rows, Groups}` (`Request::Aggregate`/`Response::Groups` — FR-038, ADR-0035); since v0.29.0 a fourth domain, `Reminder` — the generic schema library's first front-door appearance outside its own `research`-gated reference material, `status` (an enum) as the durably-mutable `ScannableField` rather than the usual plain-number shape, no relation of either kind, a real `reminder_server` binary (`server::reminder::ReminderConnectionStore`, `src/generic/reminder.rs` — FR-039, ADR-0036); since v0.30.0 a fifth domain, `Entity` — this library's first `SymmetricRelation` outside `research`-gated reference material, one self-referential `relates_to` edge, plus a genuinely new capability, `SchemaDrivenClient::traverse` — bounded client-side breadth-first graph walking over the existing `Request::Neighbors`, no new wire primitive (`server::entity::EntityConnectionStore`, `src/generic/entity.rs` — FR-040, ADR-0037); since v0.31.0 `Entity` revised in place to its real, MCP-server-verified shape — `kind` an open `String` (was a fixed enum), a second `SymmetricRelation` (`MentionedWith`) alongside `RelatesTo`, carried by a genuinely new multi-relation primitive (`MultiSymmetric`/`MultiNeighbors`, `src/generic/{store,query}.rs`) rather than the previously-assumed `Reversed`-style forwarding fix, which was tried and found to conflict (`E0119`); protocol version 10 adds `Request::NeighborsByRelation`/`ListRelationKinds`/`Response::RelationKinds`, gated client-side only, plus a relation filter on `SchemaDrivenClient::traverse` (FR-041, ADR-0039); since v0.32.0 `Entity` gains `aliases` (durable, not yet wire-representable — `ScanValue` has no list variant) and `label`/alias lookup becomes real, case- and whitespace-insensitive, via a new secondary-index primitive (`NameIndex`/`NameIndexed`/`FindByName`, `src/generic/{store,query}.rs` — rebuilt from records at every open, no file of its own) reached through the existing `FilterEq`/`ScanValue::Str`/`RecordList` shapes with **no `PROTOCOL_VERSION` change**, only `DomainSchema` reporting `label` `filter_eq: true` (FR-042, ADR-0040); since v0.33.0 the two follow-ups from reading `rusty_remind_me`'s source directly (`ADR-0042`, ten findings cited to file:line) — `NameIndex::normalize` collapses internal whitespace runs, matching the source's own rule and replacing the trim-only one it had retired (zero migration), and `entity_id(name) -> Uuid` derives a full 128-bit id from the normalized name so a canonical name resolves in one `GetById` with `RecordId` untouched; `sha2` added, the first new unconditional dependency since `subtle`; no wire, `src/server/**`, or on-disk change (FR-043, ADR-0042); since v0.34.0 `Entity::aliases` is readable over the wire — `ScanValue::StrList`/`ValueKind::StrList` appended at protocol version 11, `FIELD_ALIASES = 3` with every capability flag `false`, and the protocol's first content-rewriting rule-3 downgrade: `downgrade_for_version` strips the pair from `Record`/`Rows` and the descriptor from `Schema` for every connection negotiated below 11, so an older client sees exactly the v0.32.0 shape (FR-044, ADR-0041); since v0.35.0 a `JOIN` over a *declared relation* within one table — `Request::Join`/`DescribeRelations` and `Response::JoinedRows`/`Relations` at protocol version 12, server-side index nested loop over the adapter's own relation methods, `ConnectionStore::describe_relations()` with a conservative default, SQL aliases and `JOIN … ON <relation>`, `QueryResult::Joined`; `Malformed` below 12; the `right_table`/`target_table` fields reserved for ADR-0045 (FR-045, ADR-0044); v0.35.1 (patch, ADR-0043) splits the `client` Cargo feature out of `server` with no path change, adds the `SERVER-002` wire specification and its enforced conformance fixture, and the Python reference client; since v0.36.0 runtime record insertion — `crate::generic::query::Insert<R>` over a new append-only insert log (`<path>.inserts`) folded into the record blob at the next open, forwarded by every composition layer, `ConnectionStore::insert_record` (default `Unsupported`; `Reminder`/`Entity` implement), `Request::Insert`, `ErrorCode::Duplicate`, and `ErrorCode::Storage` at protocol version 13, `SchemaDrivenClient::insert` and the Python client's `insert` (FR-046, ADR-0046 — proposed and implemented in one cycle, accepted as option (d)); since v0.37.0 runtime relation insertion — `Link`/`MultiLink` over an edge log beside each edge blob folded at the next open, open labels on `MultiSymmetric` with a label manifest (`<path>.relations`), `ConnectionStore::link_records` (default `Unsupported`; `Entity`/`Employee` implement), `Request::Link` at protocol version 14 answered `Ok` for a new or an existing edge, `SchemaDrivenClient::link` and the Python client's `link` (FR-047, ADR-0047 — proposed and implemented on one branch, accepted as designed); since v0.38.0 the `Memory` domain — the consumer's `memories` table as a bounded eleven-field projection on `Reminder`'s stack shape, `category` indexed, `access_count` scannable, no relation, no wire change (FR-048, ADR-0048 — proposed and implemented on one branch, accepted as designed); since v0.39.0 whole-record replacement — `Replace<R>` over the insert log (the fold applying a logged record over the blob's copy by id), forwards through every layer, `ConnectionStore::replace_record` (default `Unsupported`; `Memory`/`Reminder`/`Entity` implement), `Request::Replace` at protocol version 15 answered `Ok`/`NotFound`, `SchemaDrivenClient::{replace, upsert}` and the Python client's `replace` (FR-049, ADR-0049 — proposed and implemented on one branch, accepted as designed); since v0.40.0 more than one table on one connection — `serve_tables`, `ConnectionStore::table_name`, per-connection `Request::Use`/`ListTables` → `Response::Tables` at protocol version 16, cross-table `Join`/`Link` resolved through the relation's `target_table`, foreign labels on `MultiSymmetric`, `Memory`'s `mentions` → `entity` (FR-050, ADR-0050 implementing ADR-0045 — proposed and implemented on one branch, accepted as designed); since v0.41.0 runtime deletion — tombstones in the insert log (format version 2, backward-compatible reader) and every edge log honored by every fold, `Delete`/`Detach` through every layer, `ConnectionStore::{delete_record, detach_record}`, `Request::Delete` at protocol version 17 answered `Ok`/`NotFound`, the cross-table cascade on a `serve_tables` server, `delete` on both clients (FR-051, ADR-0051 — proposed and implemented on one branch, accepted as designed); since v0.42.0 compaction — the reopen fold done in place under the write lock, the slot files rewritten without retired slots, every log folded, `CompactionReport`, `ConnectionStore::compact`, `Request::Compact` → `Response::Compacted` at protocol version 18, `compact` on both clients (FR-052, ADR-0052 — proposed and implemented on one branch); a durable data directory — `SERVER_DATA_DIR` on `memory_server`, `open_or_create_{memory,entity}_production_stack`, no wire change (FR-053, ADR-0053 — proposed and implemented on one branch); a guarded replace — `Request::ReplaceIf` with a predicate over the stored record under the write lock, `ErrorCode::GuardFailed`, protocol 19, `replace_if` on both clients (FR-054, ADR-0054 — proposed and implemented on one branch); one ordered keyset page — `Request::Page` over an orderable field answered by `Rows`, protocol 20, `page` on both clients (FR-055, ADR-0055 — proposed and implemented on one branch); `Memory`'s sync fields `deleted_at_unix_ms`/`node_id` with sentinels for null and schema tag `memory::Memory@2`, no wire change (FR-056, ADR-0056 — design-only proposal, the owner's pick, then implemented); a global edge count — `Request::CountEdges`/`Response::Count` at protocol 21, `count_edges` on both clients (FR-057, ADR-0057 — proposed and implemented on one branch); the `Relation` domain — the hub's directed, open-label edges as a record table, `memory_server`'s third table, no wire change (FR-058, ADR-0058 — proposed and implemented on one branch); 0.49.0 a memory-only sorted index behind `Page` on `Memory`/`Relation`, a page flat at ~155 µs from 1K to 100K rows (FR-059, ADR-0059 — proposed and implemented on one branch); 0.48.1 `Page` selecting by key; 0.50.0 a runtime-write batch — `Request::WriteBatch`/`Response::BatchResults` at protocol 22, `WriteOp`/`WriteResult`, pipelined by default and precondition-/isolation-atomic under a flag (FR-060, ADR-0060 — the owner's pick of option (c), implemented on one branch); since v0.51.0 `ORDER BY <field>` in the SQL subset, compiled entirely client-side to one or more `Request::Page` calls for a plain query (no `WHERE`/`JOIN`/`GROUP BY`/aggregate — refused at parse time), no wire or protocol-version change (`FR-061`, ADR-0061 — proposed and implemented on one branch); since v0.52.0 `Request::WriteBatch`'s atomic mode is crash-atomic on `Memory`/`Entity`/`Relation` when journaled — the batch journal's format version 2 adds a leading kind byte per entry (`0` `TransactionOp`, `1` `WriteOp`), reusing `CommitGroup`'s existing fsync/replay/checkpoint discipline; `Dog`/`Order`/`Employee`/`Reminder`'s journals unaffected (`FR-062`, ADR-0063 — proposed and implemented on one branch) ; since v0.53.0 `Request::Metrics`/`Response::Metrics { text }` at protocol 23 answers a bounded, fixed set of process-wide atomic counters as Prometheus text, gated as a read (any authenticated class, not `ReadWrite`-restricted) — `FR-063`, ADR-0064; since v0.54.0 `Request::Backup { name }`/`Response::BackedUp { files, bytes }` at protocol 24 (`PROTOCOL_VERSION = 24`) copies a table's on-disk files, under its write lock, into an opt-in `SERVER_BACKUP_ROOT`-relative directory via a temp-dir-then-atomic-rename, gated as a write like `Compact` — `Dog`/`Memory`/`Entity`/`Relation` implement `ConnectionStore::backup`, `SERVER_BACKUP_ROOT` wired into `memory_server` (`FR-064`, ADR-0065); since v0.55.0 `Request::FetchSnapshot`/`Response::Snapshot { files: Vec<(String, Vec<u8>)> }` at protocol 25 (`PROTOCOL_VERSION = 25`) streams every file a table owns back over the connection — `Backup`'s reading twin, reusing its `with_exclusive` lock and file-enumeration unchanged — gated behind a new, distinct `TokenClass::Replication` never satisfied by `ReadOnly`/`ReadWrite`, refusing `ErrorCode::TooLarge` when the table's on-disk size exceeds `MAX_SNAPSHOT_BYTES` (8 MiB) before any byte is read; `SERVER_AUTH_REPLICATION_TOKEN` wired into `memory_server` (`FR-067`, ADR-0067); since v0.56.0 `Request::FilteredPage`/`Response::Rows` (reused) at protocol 26 (`PROTOCOL_VERSION = 26`) is `Page`'s own ordered-keyset contract (`order_by`, `after`, `limit`) plus a `WHERE`-shaped `filter: Vec<Predicate>` — the identical predicate evaluator `Request::Query`'s own filter and `ReplaceIf`'s guard already share, reused unchanged — evaluated by one shared default (`scan_all` → filter → `page_rows`) correct for every domain with zero adapter overrides; `WHERE` combined with `ORDER BY` now compiles instead of the parse-time refusal `ADR-0061` left (`FR-068`, ADR-0068); since v0.57.0 an opt-in HTTP `/metrics` scrape listener, entirely separate from this wire protocol — `SERVER_METRICS_HTTP_ADDR` binds a second `TcpListener` answering `GET /metrics` with `ServerMetrics::render()`'s existing text unchanged over `rusty_http` (path dependency, default features, `server`-gated), no `PROTOCOL_VERSION` change (`FR-069`, ADR-0069); since v0.58.0 query planner step one — `Request::Query` with an `Eq` predicate on a `describe()`-declared `filter_eq: true` field reads that index's bucket via the adapter's own `filter_eq` + per-id `get` instead of `scan_all`, every predicate re-checked by the unchanged `evaluate_query` so the result set is identical by construction, a `filter_eq` refusal falling back to the scan; schema-driven and value-blind, no cost model, `Query` only, no wire/protocol/trait/adapter/client change (`FR-070`, ADR-0073 — design-only proposal, the owner's pick of (a), then implemented the same session); since v0.84.0 the wire says when a reply was clamped and when a server is full — protocol 30: `Response::RowsClamped` (24) for a clamped `Query` at ≥ 30 (`Rows` below), `ErrorCode::Busy` (15) written to a connection refused at the connection cap on a plaintext listener, `SchemaDrivenClient::last_clamp`, `SERVER-002` 0.19.0 (`FR-096`, ADR-0103 — the owner's "2"); since v0.83.0 a `Query` with no `limit` under a row cap is clamped to the cap and counted, `memory_server` defaulting the cap to 10,000 (`FR-095`, ADR-0102 — the owner's "clamp"); v0.82.0 `memory_server` defaults on — idle timeout 300 s, connection cap 1,024, synced updates, `0` turning each off; row cap still opt-in (`FR-094`, ADR-0099 — the owner's "Defaults"); v0.81.0 synced updates — opt-in `msync` before an in-place `UpdateField`/non-journaled `Transaction` is acknowledged (`with_synced_updates`, `SERVER_SYNC_UPDATES`; `FR-093`, ADR-0097 — taken on the owner's word); v0.80.0 `Compact` reclaims MVCC history at the oldest open snapshot before the history flush (`MvccState::reclaim`; `FR-092`, ADR-0096 — taken on the owner's word); v0.79.0 the binaries refuse a non-loopback bind without both authentication and TLS unless `SERVER_ALLOW_INSECURE=1` (`server::exposure::check_exposure`, pure; `FR-091`, ADR-0094 — taken on the owner's word); v0.78.0 connection limits — opt-in idle timeout, connection cap with `dogserver_connections_refused_total`, row cap answering `TooLarge`, `evaluate_query` bounded at `limit`, `SERVER_IDLE_TIMEOUT_SECS`/`SERVER_MAX_CONNECTIONS`/`SERVER_MAX_QUERY_ROWS` (`FR-090`, ADR-0093 — taken on the owner's word); v0.77.0 one server process per data directory (`DataDirLock` on `SERVER_DATA_DIR`, `memory_server`), directory `fsync` after every rename install, MSRV 1.89 (`FR-089`, ADR-0092 — taken on the owner's word); v0.76.0 a contradictory filter is `Malformed` at protocol 29 or above, the empty answer below (`FR-088`, ADR-0091 — taken on the owner's word); since v0.75.0 `WHERE id = '<uuid>'` as a client-side point read, no wire change (`FR-087`, ADR-0090 — in the `ADR-0089` PR awaiting the owner's review); since v0.74.0 `ORDER BY … DESC` on the wire — protocol 28, `PageDesc`/`FilteredPageDesc`, the sorted index walked backward (`FR-086`, ADR-0089 — proposed and implemented on one branch; a wire change, awaiting the owner's review); since v0.73.0 a request latency histogram — `dogserver_request_duration_seconds`, sixteen fixed buckets, observed once per dispatched request (`FR-085`, ADR-0088 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) plus a `kind` label / (c) decline held open at review); since v0.72.0 query planner step ten — an ungrouped reduction of the range field is one fold over the walk, no key materialized (`FR-084`, ADR-0087 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) plus a run-emitting fold for the grouped walk / (c) decline held open at review); since v0.71.0 query plan metrics — `dogserver_query_plans_total{plan="…"}`, one fixed family of seven counting the path each planned read took, classified before dispatch by a pure `plan_of` (`FR-083`, ADR-0086 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) thread the counters through `dispatch` / (c) decline held open at review); since v0.70.0 a hashed bucket for `Aggregate`'s decode path — every decoded `GROUP BY` linear in its rows, not quadratic in its groups (`FR-082`, ADR-0085 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) `Hash + Eq` on `ScanValue` / (c) decline held open at review); since v0.69.0 query planner step nine — `GROUP BY` the range field over a pure range is one group per run of equal walked keys, no record read, no bucket search (`FR-081`, ADR-0084 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) plus a `HashMap` bucket in `evaluate_aggregate` / (c) decline held open at review); since v0.68.0 query planner step eight — two bounds on one side of the range field walk between the tightest two, the plan and the walk-only aggregates alike (`FR-080`, ADR-0083 — proposed and implemented on one branch under the owner's standing instruction; the fork (a) as implemented / (b) plus contradiction as a validation error / (c) decline held open at review); since v0.67.0 query planner step seven — `SUM`/`AVG`/`MIN`/`MAX` of the range field over a pure range are reductions over the walked keys, no record read (`FR-079`, ADR-0082 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); since v0.66.0 query planner step six — a `COUNT(*)` whose filter is only bounds on a range-indexed field is the sorted index's own count, no record read (`FR-078`, ADR-0081 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); since v0.65.0 `Reminder` under `Ordered` — a sorted index on `due_at_unix_ms`, the adapter declaring it as its range field and overriding `page`/`filtered_page`, so every planner round reaches the consumer's "what is due" shapes; no file-format/wire/protocol/client change (`FR-077`, ADR-0080 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); since v0.64.0 query planner step five — the intersection's range walk carries a budget (`range_by_limited`, abandoned past ten ids per bucket id, the crate's one measured cost ratio), so a filter carrying both indexes costs at most ~1.4× the bucket alone (`FR-076`, ADR-0079 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); since v0.63.0 query planner step four — a `WHERE` carrying both a declared-index equality and a range bound plans `IndexIntersect`: both id lists, neither decoded, intersected exactly, only the members read; every consumer, no estimate, no generic/wire/protocol/adapter/client change (`FR-075`, ADR-0078 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); since v0.62.0 walk-past-rejects for a mixed-filter `FilteredPage` — `Memory`/`Relation`'s override walks whenever `order_by` is the range field and the filter does not plan the declared equality index, passing any other predicate's rejects chunk by chunk until the page fills; identical sequence, `Page`'s consistency class, no generic/wire/protocol/client change (`FR-074`, ADR-0077 — proposed and implemented on one branch under the owner's standing instruction, the round after PR #276 merged ADR-0076; fork held open at review); since v0.61.0 the O(page) bounded walk for a range-filtered `FilteredPage` — `Memory`/`Relation` override `filtered_page` to `page_by` from the later of the client's cursor and the tightest lower bound, cut at the first upper-bound reject, when `order_by` is the range field and every predicate is a bound on it; the trait default's body factored into `filtered_page_by_candidates` for every other shape; identical sequence, `Page`'s consistency class; no generic/wire/protocol/client change (`FR-073`, ADR-0076 — proposed and implemented on one branch under the owner's standing instruction; fork held open at review); v0.60.0 query planner step three — a range path through the `Ordered` index: `RangeBy::range_by` on `Ordered` (guarded, pair bounds) exposed on `GenericProductionStore`, `ConnectionStore::range_field`/`range_ids` with defaults overridden by `Memory`/`Relation` for `updated_at_unix_ms`, `QueryPlan::IndexRange` after the unchanged equality rule, reached by every consumer through `indexed_candidates`; each consumer's re-check keeps its result set identical; no wire/protocol/`FieldCapabilities`/client change (`FR-072`, ADR-0075 — design-only proposal, the owner's pick of (a), then implemented the same day); v0.59.0 query planner step two — the same candidate step, through one shared `indexed_candidates` helper, behind `Request::Aggregate`, the `ConnectionStore::filtered_page` default body (signature unchanged; zero observable difference, since `page_rows` orders the filtered set), and `Request::Join`'s left side (planned against the left store's schema, right side untouched); each consumer's existing per-row re-check keeps its result set identical; no wire/protocol/trait-signature/adapter/client change (`FR-071`, ADR-0074 — design-only proposal, the owner's pick of (a), then implemented the same day) | 0.61.0 | Accepted | Implemented | Verified | `STORAGE-011`, `STORAGE-012`, `STORAGE-018` | baileyrd | `docs/specifications/server/SERVER-001-query-layer.md` | `src/server/**` tests + `tests/server_dog_integration.rs` + `tests/server_reminder_integration.rs` + `tests/server_entity_integration.rs` + `tests/server_memory_integration.rs` + `tests/server_order_integration.rs` + `tests/server_employee_integration.rs` + `tests/server_schema_driven_client.rs` + `tests/server_auth_integration.rs` + `tests/server_transaction_integration.rs` + `tests/server_tls_integration.rs` + `tests/server_protocol_version.rs` + `tests/server_sql_integration.rs` + `tests/server_metrics_integration.rs` + `tests/server_backup_integration.rs` + `tests/server_replication_integration.rs` + `tests/server_metrics_http_integration.rs` + `cargo bench --bench server` run + `RESULTS.md` |
| `SERVER-002` | Wire format for foreign clients: a language-neutral, byte-level specification of `SERVER-001`'s protocol at version 30 (0.19.0 — `RowsClamped` (24) and `ErrorCode::Busy` (15), `ADR-0103`; 0.18.0 was version 29 — no new variant, a contradictory filter `Malformed`, `ADR-0091`; 0.17.0 was version 28 — `PageDesc`/`FilteredPageDesc`, `ADR-0089`; 0.11.0 was version 22; 0.10.0 was version 21, 0.9.0 version 20, 0.8.0 version 19, 0.7.0 version 18, 0.6.0 version 17, 0.5.0 version 16, 0.4.0 version 15, 0.3.0 version 14, 0.2.0 version 13, 0.1.0 version 12) — TCP/TLS transport, `u32`-LE framing with the 16 MiB cap, every `STORAGE-018` primitive encoding, every enum's variant table and every struct's field table, the connection lifecycle (`Hello` first or never, `Authenticate` before anything on an authenticated server, the two close-without-reply cases, sessions), per-request semantics, the version table and compatibility rules 1–4 restated for an implementer, and the conformance statement over `tests/fixtures/wire-vectors.txt`; derivative of `SERVER-001`/`STORAGE-018` — the golden vectors stay authoritative (ADR-0043, `ECO-FR-004`–`006`; 0.2.0 adds `Insert`/`Duplicate`/`Storage` at 13, ADR-0046; 0.3.0 adds `Link` at 14, ADR-0047; 0.11.0 adds `WriteBatch` (31)/`BatchResults` (20) at 22, ADR-0060) | 0.11.0 | Accepted | Implemented | Verified | `SERVER-001`, `STORAGE-018` | baileyrd | `docs/specifications/server/SERVER-002-wire-format.md` | `tests/fixtures/wire-vectors.txt` (52 vectors, enforced by `src/server/protocol.rs`'s golden tests via `test_support::wire_fixture`) + `clients/python/tests/test_vectors.py` (offline conformance, CI step) + `tests/server_python_client.rs` (the reference client against a real server at 14 and 10) + `tests/server_client_only.rs` |
| `STORAGE-013` | External database benchmark comparison: `ProductionStore` vs. real SQLite/Postgres/DuckDB on `get`/`scan_ages`/one- through five-hop `littermate_of` traversal (`external-db-bench` feature) | 0.5.0 | Accepted | Implemented | Verified | `STORAGE-001`, `STORAGE-002`, `STORAGE-003`, `STORAGE-006` | baileyrd | `docs/specifications/storage/STORAGE-013-external-database-benchmark.md` | `benches/external_db.rs` + `cargo bench --features research,external-db-bench --bench external_db` run + `RESULTS.md` |
| `STORAGE-014` | `ProductionStore` file portability: a `<path>.records` companion blob (`DOGBLOB\0` magic + version header + bincode `RecordBlob { records, edges }`, crash-safe write-to-temp-then-rename) written by `create`/`open`, and a new additive `ProductionStore::open_portable(path)` reconstructing the full store from the path alone; `DurabilityError::RecordBlobUnreadable`; `MmapAgeStore` untouched; v0.2.0 adds a 64-bit FNV-1a content fingerprint to the header (blob version 2) so `open`'s steady-state check reads 20 bytes instead of serializing and comparing the whole record set, and upgrades version-1 blobs in place | 0.2.0 | Accepted | Implemented | Verified | `STORAGE-009`, `STORAGE-011`, `ADR-0016` | baileyrd | `docs/specifications/storage/STORAGE-014-production-store-file-portability.md` | `src/durability/record_blob.rs` tests (12) + `src/production.rs` portability tests (6) + release-build `create`/`open` cost measurement in `RESULTS.md`, re-measured for v0.2.0 |
| `STORAGE-015` | `GenericMmapStore` file portability: a `<path>.records` companion blob (`GENBLOB\0` magic + version + FNV-1a fingerprint header, since v0.2.0 followed by the 8-byte FNV-1a 64 hash of the new `SchemaTag::SCHEMA_TAG` under blob version 2 — a 28-byte tagged header checked magic → version → tag → fingerprint before decode, so a foreign-`R` blob is a named `schema tag mismatch` and a version-1 blob a `version` cause on the read-only paths, both rewritten by `open`; bincode `Vec<R>`, crash-safe write-to-temp-then-rename, sharing `STORAGE-014`'s now-`pub(crate)` machinery) written by `create`/kept current by `open`, new additive `read_portable_records(path)`/`open_portable(path)`, `open_order_production_stack_portable(path)`; `R: Serialize + DeserializeOwned` joins the store's bounds and `R: SchemaTag` the four file constructors' own `impl` block; `.mmap` slot format untouched | 0.2.0 | Accepted | Implemented | Verified | `STORAGE-012`, `STORAGE-014`, `ADR-0017`, `ADR-0019` | baileyrd | `docs/specifications/storage/STORAGE-015-generic-store-file-portability.md` | `src/generic/record_blob.rs` tests (14) + `src/generic/mmap_store.rs` portability tests (8) + `src/generic/order_customer.rs` stack test (1) + `production.rs` doctest + release-build `create`/`open`/`open_portable` cost measurement in `RESULTS.md` |
| `STORAGE-016` | `Symmetric` edge-list portability: a companion edge blob at a caller-supplied path (`GENEDGE\0` magic + version + FNV-1a fingerprint header, bincode `Vec<(Id, Id)>` in caller order, crash-safe write-to-temp-then-rename, sharing `STORAGE-014`'s `pub(crate)` machinery) written by `Symmetric::create`/kept current by `Symmetric::open`, new additive `read_portable_edges(edges_path)`/`open_portable(inner, edges_path)` in one `R::Id: Serialize + DeserializeOwned` (+ `R: SchemaTag` since v0.2.0)-bounded `impl` block; since v0.2.0 the header carries the 8-byte hash of `R::SCHEMA_TAG` under blob version 2 (`STORAGE-015` v0.2.0's tagged-header helpers shared; foreign-`R` and version-1 edge blobs refused by name on the read-only paths, rewritten by `open`); `Employee` helpers switched to `create`/`open` at `<path>.edges`, new `open_employee_production_stack_portable(path)`; `Symmetric::new`, `GenericMmapStore`, and `.records` untouched | 0.2.0 | Accepted | Implemented | Verified | `STORAGE-012`, `STORAGE-014`, `STORAGE-015`, `ADR-0018`, `ADR-0019` | baileyrd | `docs/specifications/storage/STORAGE-016-symmetric-edge-list-portability.md` | `src/generic/edge_blob.rs` tests (15) + `src/generic/store.rs` tests (8) + `src/generic_spike/employee_impl.rs` portability tests (5) |
| `STORAGE-017` | Multi-field mmap durability: `MmapScanned<S, R, Marker>`, the durable twin of `Scanned` — one more mutable, durable, scannable field over any inner store, backed by its own `GMMAPST\0` version-2 slot file (format unchanged) with per-file reconciliation (reuse / append-for-missing / ignore-stale / torn-slot re-append), write-through `get`, top-down `flush`; the slot-file mechanics extracted from `GenericMmapStore` into a shared `pub(crate)` `SlotFile<Id, V>` with no behaviour change; `forward_scannable_pairs!(for Layer; …)` generates the cross-marker forwards; `create`/`open` bounded `R: SchemaTag`, and `open` refuses a file whose body is not a whole number of the layer's slots with the new `DurabilityError::SlotWidthMismatch`; `OrderProductionStack` gains `DiscountCents` at `<path>.discount_cents.mmap` beside `Amount`'s file and the blob | 0.1.1 | Accepted | Implemented | Verified | `STORAGE-012`, `STORAGE-015`, `ADR-0006`, `ADR-0020` | baileyrd | `docs/specifications/storage/STORAGE-017-multi-field-mmap-durability.md` | `src/generic/mmap_scanned.rs` tests (8) + `src/generic/order_customer.rs` stack tests (1 new, 1 extended, 2 compile-time pair checks) + `src/generic/mmap_store.rs` tests (16) unchanged against the extracted `SlotFile` + before/after Criterion in `RESULTS.md` |
| `STORAGE-018` | bincode encoding stability: one `pub(crate)` `crate::codec` (`encode`/`encode_into`/`decode` over an explicit `bincode::Options` — fixint, little-endian, no limit, trailing bytes rejected) that all 23 production call sites route through; bytes unchanged from `bincode`'s free functions, proven by golden vectors captured on the pre-change code (primitives/enum/struct/`Uuid`, every `Request`/`Response` variant, one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0`); the one behavioral change is that a payload with bytes after the value is a decode error (a junk-padded frame is `FrameError::Encoding`; a junk-padded `DOGBLOB\0` body — previously silently accepted, since that blob decodes before fingerprinting — is `RecordBlobUnreadable`); evolution rules documented on the codec; v0.1.1 closes the `DOGBLOB\0` fingerprint-before-decode open question as not warranted (a `BLOB_VERSION` bump for a different error cause on files no conforming writer produces) | 0.1.1 | Accepted | Implemented | Verified | `STORAGE-008`, `STORAGE-009`, `STORAGE-014`, `STORAGE-015`, `STORAGE-016`, `SERVER-001`, `ADR-0021` | baileyrd | `docs/specifications/storage/STORAGE-018-bincode-encoding-stability.md` | `src/codec.rs` tests (9) + `src/server/protocol.rs` golden tests (2) + `src/server/framing.rs` trailing-junk test (1) + golden/junk tests in `src/durability/record_blob.rs` (2), `src/generic/record_blob.rs` (2), `src/generic/edge_blob.rs` (1) + every pre-existing test unchanged beyond the call-site rename |
| `STORAGE-019` | Schema migration tooling: a documented three-step pattern (a caller-defined old-layout struct implementing `SchemaTag` under the old tag; the existing `open_..._portable` read path, typed over it; the existing `create_..._production_stack` write path, unmodified, for a fresh new-tagged directory) proven against `Memory`'s own `@1 → @2` bump (`ADR-0056`) — `examples/migrate_memory_v1_to_v2.rs`, no new `src/` library code, no wire/protocol change | 0.1.0 | Accepted | Implemented | Verified | `ADR-0056`, `ADR-0019` | baileyrd | `docs/specifications/storage/STORAGE-019-schema-migration-tooling.md` | `tests/schema_migration.rs` (3/3) + a real `cargo run --example migrate_memory_v1_to_v2` smoke test against a genuine fixture |
| `STORAGE-020` | Restore tooling: a real `examples/restore_backup.rs` CLI closing `ADR-0065`'s own "restore needs no new code" claim — crash-safe per-file staged copy from a `Request::Backup`-produced directory, existing-target refusal, a real verification reopen via the named domain's own portable production constructor; no new `src/` library code, no wire/protocol change | 0.1.0 | Accepted | Implemented | Verified | `ADR-0065`, `STORAGE-014`, `STORAGE-015`, `STORAGE-016`, `ADR-0066`, `STORAGE-019` | baileyrd | `docs/specifications/storage/STORAGE-020-restore-tooling.md` | `tests/restore_backup.rs` (10/10) + a real `cargo run --example restore_backup` smoke test against a genuine `Request::Backup`-produced fixture |
| `STORAGE-021` | Crash-safety gate: the diagnosis harness's four `SIGKILL`-a-real-subprocess trials (flushed updates survive; a torn slot write is excluded on reopen, the control kept; a torn in-place update is one whole pattern; an unflushed kill leaves no invented value) asserted as `tests/crash_safety.rs`, research-gated so CI's `cargo test --all-features` runs it; no `src/` change | 0.1.0 | Accepted | Implemented | Verified | `STORAGE-012`, `STORAGE-017`, `ADR-0095` | baileyrd | `docs/specifications/storage/STORAGE-021-crash-safety-gate.md` | `tests/crash_safety.rs` (4/4, ~2 s) |

IDs remain stable. Link superseding artifacts rather than reusing an ID for
a new meaning.
