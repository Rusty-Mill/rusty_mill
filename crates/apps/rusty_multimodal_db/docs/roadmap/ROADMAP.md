# Roadmap

Status vocabulary: `Proposed`, `Draft`, `Accepted`, `In Progress`,
`Implemented`, `Verified`, `Blocked`, `Deferred`, `Deprecated`,
`Superseded`.

| Unit | Outcome | Depends on | Specs | Exit gate | Status | Evidence |
|---|---|---|---|---|---|---|
| `BOOTSTRAP` | Charter, architecture, ADR-0001, ADR-0002, spec tree, roadmap, AGENTS.md/WORKFLOW.md/PROJECT-STATUS.md, Cargo scaffolding, CI | — | — | Docs merged, `cargo check` passes on empty scaffold | Implemented | commit `5bfc8c5` (bootstrap, before the first PR) |
| `GENERATOR` | `DogRecord` + seeded, configurable dataset generator with unit tests | `BOOTSTRAP` | `STORAGE-001` | `cargo test` green for `src/generator.rs` | Implemented | commit `5bfc8c5` (bootstrap, before the first PR) |
| `BACKENDS` | `DogStore` trait + `AosStore`/`SoaStore`/`CanonicalStore` with unit tests | `GENERATOR` | `STORAGE-002` | `cargo test` green for `src/store/**`, cross-backend equivalence tests pass | Implemented | commit `5bfc8c5` (bootstrap, before the first PR) |
| `BENCH-SUITE` | Criterion suite: 4 workloads × 3 sizes × 3 backends, default features | `BACKENDS` | `STORAGE-003` | `cargo bench` completes all 36 cases | Implemented | commit `5bfc8c5` (bootstrap, before the first PR) |
| `CACHE-MISS` | `perf-events`-gated cache-miss benchmark target, Linux-only | `BENCH-SUITE` | `STORAGE-003` | `cargo build --features perf-events` succeeds on Linux; ADR-0002 path documented | Implemented (build/feature only — real counter numbers deferred to a `baileyai` run, see `RESULTS.md`) | commit `5bfc8c5` (bootstrap, before the first PR) |
| `RESULTS` | `RESULTS.md` with per-workload verdicts, explicit win/loss call-outs, open questions | `BENCH-SUITE`, `CACHE-MISS` | `STORAGE-004` | `RESULTS.md` merged, meets `STORAGE-004` acceptance criteria | Implemented | commit `5bfc8c5` (bootstrap, before the first PR) |
| `HYBRID-BACKEND` | `CanonicalCachedStore` (canonical store + eager write-through age cache) closing `scan_ages`'s gap; ADR-0003; `RESULTS.md` revised to 4-way comparison | `RESULTS` | `STORAGE-005` | `cargo test`/`cargo bench` green for the 4th backend; `RESULTS.md` reports `scan_ages`/`update_age` specifically | Implemented | follow-on PR |
| `GRAPH-TRAVERSAL` | `littermate_of` edge generation; `DogStore::neighbors` (one-hop); ADR-0004; generic `two_hop_neighbors`; `neighbors_one_hop`/`neighbors_two_hop` benchmarks; `RESULTS.md`'s `## Graph traversal` section | `HYBRID-BACKEND` | `STORAGE-006` | `cargo test`/`cargo bench` green for `neighbors`/`two_hop_neighbors` across all 4 backends; `RESULTS.md` reports both workloads | Implemented | follow-on PR |
| `MIXED-WORKLOAD` | `MixedWorkloadDriver` (blended `get`/`update_age`/`scan_ages`, reusing `RoundRobin`); `mixed_workload_write{10,50,90}` benchmarks; `RESULTS.md`'s `## Mixed read/write workload` section, directly answering whether `CanonicalCachedStore` ever loses to `CanonicalStore`/`SoaStore` at any tested write ratio | `GRAPH-TRAVERSAL` | `STORAGE-007` | `cargo test`/`cargo bench` green for all 36 mixed-workload cases; `RESULTS.md` reports a verdict per (write ratio × size) | Implemented | follow-on PR |
| `DURABILITY-TIER1` | `CanonicalCachedStore`-only durability, five variants (WAL fsync, WAL buffered, snapshot rebuild, snapshot save-as-is, hybrid snapshot+WAL), built around a new shared `CanonicalCachedState` core; ADR-0005; `benches/durability.rs`'s per-write/checkpoint/load suite; `RESULTS.md`'s `## Durability` section | `MIXED-WORKLOAD` | `STORAGE-008` | `cargo test`/`cargo bench --bench durability` green for all 5 Tier 1 variants; `RESULTS.md` reports per-configuration numbers, the 1,000-write recoverability comparison, and an explicit recommendation | Implemented | follow-on PR |
| `DURABILITY-TIER2` | Three alternate durability architectures for `CanonicalCachedStore` (mmap-backed ages, LSM-tree-style with no compaction, `redb`-backed embedded engine), each scoped to the mutable `age` field only; ADR-0006; `RESULTS.md`'s `## Durability` section extended to cover all eight variants | `DURABILITY-TIER1` | `STORAGE-009` | `cargo test`/`cargo bench --bench durability` green for all 3 Tier 2 variants; `RESULTS.md` reports per-configuration numbers with the same rigor as Tier 1 | Implemented | follow-on PR |
| `CONCURRENCY-PROTOTYPES` | Four concurrent access strategies for `CanonicalCachedStore` (global `RwLock`, sharded locking, `dashmap`, actor/single-writer-thread), a new `ConcurrentStore` trait, a linearizability-style stress test, and a custom multi-threaded throughput harness; ADR-0007; `RESULTS.md`'s `## Concurrency` section | `DURABILITY-TIER2` | `STORAGE-010` | `cargo test` green for all four variants' flagship stress tests (16 threads × 2,000 iterations each, no lost updates/torn reads); `cargo bench --bench concurrency` completes the full size×ratio×thread-count×variant sweep; `RESULTS.md` reports a verdict per (size × write-ratio) configuration and an explicit recommendation | Implemented | follow-on PR |
| `PRODUCTION-DEFAULT` | `ProductionStore` (`src/production.rs`): `CanonicalCachedStore`'s architecture + mmap durability + global `RwLock` concurrency, consolidated into one recommended type implementing both `DogStore` and `ConcurrentStore`; ADR-0008; a flagship integration test exercising mmap durability and `RwLock` concurrency together for the first time; `src/lib.rs`/`README.md`/`docs/architecture/SYSTEM-ARCHITECTURE.md` reorganized to lead with it; `RESULTS.md`'s `## Production recommendation` section | `CONCURRENCY-PROTOTYPES` | `STORAGE-011` | `cargo test` green including the new flagship integration test; `cargo bench --bench workloads -- production` and `cargo bench --bench concurrency` report `ProductionStore` numbers; `RESULTS.md` ties all six prior rounds together | Implemented | follow-on PR |
| `GENERIC-SCHEMA-DESIGN` | A generic record/schema/query abstraction (`Record`/`IndexedField`/`ScannableField`/`SymmetricRelation`/`ChildOf` traits, composable store wrapper layers) validated against `Dog` and a second, structurally different domain (`Order`/`Customer` — directed relation, currency-like field, enum categorical field, timestamp field); ADR-0009 (**Accepted** — see `GENERIC-SCHEMA-LIBRARY` below); `docs/design/GENERIC-SCHEMA-DESIGN.md` | `PRODUCTION-DEFAULT` | — (no spec written for the design itself; implementation tracked by `STORAGE-012`) | Four validation spikes (`src/generic_spike/`) resolved every risk this design's §4 named, then `GENERIC-SCHEMA-LIBRARY` promoted it into a real library | Accepted/Implemented | PR #12 (proposed), PR #17 (accepted and implemented) |
| `GENERIC-SCHEMA-LIBRARY` | Promotes `GENERIC-SCHEMA-DESIGN` into `crate::generic`: promoted traits/query/store (unchanged from four validation spikes), `Order`/`Customer` as the real reference implementation, `GenericMmapStore`/`GenericProductionStore` (generic mmap durability + `RwLock` concurrency — new beyond every spike), a flagship durability+concurrency integration test, a benchmark suite confirming no regression from spike to real code; ADR-0009 moved to Accepted; `STORAGE-012` | `GENERIC-SCHEMA-DESIGN` | `STORAGE-012` | `cargo test` green including the new flagship integration test (run 5× to rule out flakiness); `cargo bench --bench generic_production` completes and is reported in `RESULTS.md` alongside the spike rounds' numbers; no `src/production.rs`/`src/store/**`/`src/durability/**`/`src/concurrency/**` changes | Implemented | PR #17 |
| `SERVER-QUERY-LAYER-DESIGN` | A network server/query layer design in front of `ProductionStore`/`GenericProductionStore`: a `Request`/`Response` protocol (`GetById`/`FilterEq`/`ScanField`/`UpdateField`/`Parent`/`Children`/`Neighbors`), length-prefixed `bincode` framing, thread-per-connection dispatch reusing the existing `RwLock`-shared-store concurrency pattern; ADR-0010 (**Accepted** — owner approved as proposed); `docs/design/SERVER-QUERY-LAYER-DESIGN.md`; explicitly excludes authentication, transport encryption, transactions, and a query language | `GENERIC-SCHEMA-LIBRARY`, `PRODUCTION-DEFAULT` | — (no spec written for the design itself; a `SERVER-001` spec is registered by the implementation unit that follows, matching `GENERIC-SCHEMA-DESIGN`'s own precedent) | Request/response/dispatch shapes compiled in a standalone scratch probe (types only, not executed); owner reviewed and accepted the design and ADR-0010 without requesting changes | Accepted | PR #33 |
| `SERVER-QUERY-LAYER` | The real implementation of `SERVER-QUERY-LAYER-DESIGN`: `src/server/**` (`server` Cargo feature, off by default), `Dog`/`Order`-`Customer`/`Employee` domain adapters, a minimal server binary (`dog_server`), real end-to-end tests over a genuine socket including a flagship concurrent-client stress test, schema discovery (`DescribeSchema`/`ADR-0011`, v0.2.0), a third validation domain (`Employee`, v0.3.0 — the first with both `Parent`/`Children` and `Neighbors` real, which found and fixed a real `Reversed`/`Neighbors`-forwarding gap in `crate::generic`), a throughput/latency benchmark (`benches/server.rs`, v0.4.0), two real-hardware follow-ups retuning `benches/server.rs`'s `THREAD_COUNTS` (`Beast`, 24 cores; `baileyai`, 32 cores) that together resolve the thread-per-connection model's connection-count-ceiling open question, and a schema-driven client library (`server::client::SchemaDrivenClient`, v0.5.0 — a real, reusable, name-addressed client built purely from `DescribeSchema`, closing the "no schema-driven client library exists" gap); `SERVER-001` | `SERVER-QUERY-LAYER-DESIGN` | `SERVER-001` | `cargo test --features server` and `cargo test --features server,research` both green, including the flagship stress test, all three schema-driven tests, and the new client's own four-test integration suite; `cargo test`/`cargo test --all-features` unaffected (the `server` module adds no default-build surface); `cargo fmt`/`clippy`/`check` clean; no `src/production.rs`/`src/store/**`/`src/durability/**`/`src/concurrency/**` changes (the one exception, `src/generic/{store,production}.rs`'s `Neighbors`-forwarding completion, is named in `SERVER-001`'s own Non-goals); `cargo bench --features server,research --bench server` completes, numbers in `RESULTS.md` | Implemented | PR #35, #37, #39, #41, #43, #44, #46 |
| `SERVER-AUTH-DESIGN` | A design for authentication and coarse read/write authorization on the server/query layer: a new `Request::Authenticate` request, two static token classes (`ReadOnly`/`ReadWrite`), constant-time comparison via the `subtle` crate, and an explicit non-goal (native transport encryption, deferred to an external TLS-terminating proxy/tunnel); ADR-0012 (**Accepted** — owner approved as proposed); `docs/design/SERVER-AUTH-DESIGN.md`; explicitly does not, by itself, authorize deploying a server beyond a trusted network — closes only the access-control half of ADR-0010's own named gap, not the encryption half | `SERVER-QUERY-LAYER` | — (no spec written for the design itself; `SERVER-AUTH` below registers the implementation against `SERVER-001`, matching `SERVER-QUERY-LAYER-DESIGN`'s own precedent) | Design document written, incremental additions to `SERVER-001`'s already-compiling `protocol.rs`/`mod.rs` shapes (no standalone scratch probe built — see ADR-0012's own "Validation and revisit triggers" for why); owner reviewed and accepted the design and ADR-0012 without requesting changes | Accepted | PR #48, #50 |
| `SERVER-AUTH` | The real implementation of `SERVER-AUTH-DESIGN`: `AuthConfig`/`TokenClass` (`src/server/mod.rs`), `Request::Authenticate`/`ErrorCode::{Unauthenticated,Unauthorized}` (`src/server/protocol.rs`), constant-time token comparison via `subtle` (this crate's first new server-layer dependency), every `serve` call site updated (`AuthConfig::default()` everywhere except `src/bin/dog_server.rs`'s `AuthConfig::from_env()`), real end-to-end coverage (`tests/server_auth_integration.rs`) plus a timing measurement for the constant-time comparison claim; `SERVER-001` v0.6.0 | `SERVER-AUTH-DESIGN` | `SERVER-001` | `cargo test --features server` green including `tests/server_auth_integration.rs`'s five tests and `src/server/mod.rs`'s new `AuthConfig`/timing unit tests; `cargo test`/`cargo test --all-features` unaffected; `cargo fmt`/`clippy`/`check`/`doc` clean; no pre-existing `serve` call site's behavior changed (`AuthConfig::default()` reproduces the original unauthenticated behavior exactly, `AUTH-FR-007`); every `SERVER-AUTH-DESIGN.md` functional acceptance criterion verified over a real socket | Implemented | PR #52, #53 |
| `SERVER-TRANSACTION-DESIGN` | A design for atomic multi-operation transactions on the server/query layer: a new `Request::Transaction { updates }` request batching `UpdateField`-shaped writes, validate-then-apply atomicity (all-or-nothing, no undo log needed given this crate's own "no runtime deletion" invariant), isolation from concurrent access via a new minimal storage-layer critical-section primitive reusing each store's existing lock (no new lock at the server layer), and explicit non-goals (a multi-round-trip interactive session, crash-atomicity across a batch — both named directly, not left implicit); ADR-0013 (**Accepted** — owner approved as proposed); `docs/design/SERVER-TRANSACTION-DESIGN.md`; explicitly does not deliver ACID transactions — atomicity/isolation with respect to concurrent access only, not crash-atomicity | `SERVER-AUTH` | — (no spec written for the design itself; `SERVER-TRANSACTION` below registers the implementation against `SERVER-001`/`STORAGE-011`/`STORAGE-012`, matching `SERVER-AUTH-DESIGN`'s own precedent) | Design document and ADR written, incremental protocol additions to `SERVER-001`'s already-compiling shapes plus one new, real storage-layer primitive (flagged honestly as a bigger footprint than `SERVER-AUTH`'s purely server-layer-additive implementation); owner reviewed and accepted the design and ADR-0013 without requesting changes | Accepted | PR #54, #55, #56 |
| `SERVER-TRANSACTION` | The real implementation of `SERVER-TRANSACTION-DESIGN`: `Request::Transaction`/`TransactionOp`/`Response::TransactionFailed`/`ErrorCode::RecordNotFound` (`src/server/protocol.rs`), `ConnectionStore::apply_transaction` (`src/server/mod.rs`, plus a per-adapter implementation in `dog.rs`/`order.rs`/`employee.rs`), and the storage-layer critical-section primitive it depends on — `crate::production::TransactionalStore` (`STORAGE-011` v0.2.0) and `crate::generic::production::GenericProductionStore::with_exclusive` (`STORAGE-012` v0.3.0); `SERVER-001` v0.7.0 | `SERVER-TRANSACTION-DESIGN` | `SERVER-001`, `STORAGE-011`, `STORAGE-012` | `cargo test --features server` green including `tests/server_transaction_integration.rs`'s six tests (full success, failure at each position, malformed value, `ReadOnly` rejection, flagship concurrent stress test) and `src/server/mod.rs`'s new dispatch/protocol unit tests; `cargo test`/`cargo test --all-features` unaffected; `cargo fmt`/`clippy`/`check`/`doc` clean; every `SERVER-TRANSACTION-DESIGN.md` functional acceptance criterion verified over a real socket; no `src/store/**`/`src/durability/**`/`src/concurrency/**` changes, `src/production.rs`/`src/generic/production.rs` changes limited to the one new critical-section primitive each | Implemented | PR #57 |
| `SERVER-TRANSACTION-BENCHMARK` | Bounded, additive extension of `SERVER-QUERY-LAYER`'s own throughput/latency benchmark (`benches/server.rs`, FR-014) to cover `Request::Transaction`: a directly-comparable `{domain}-txn` row set (`measure_transaction_latency`/`run_transaction_throughput`/`bench_transaction_domain`), same environment/pool/thread-count sweep as the existing `GetById` measurement; `SERVER-001` v0.8.0 (`SERVER-001-FR-018`); no ADR — completes already-accepted FR-014 scope, not a new decision | `SERVER-TRANSACTION` | `SERVER-001` | `cargo bench --features server,research --bench server` completes without panics, prints `{domain}-txn` rows for all three domains alongside the existing `GetById` rows, numbers in `RESULTS.md`'s `## Server / query layer` section; `cargo test --all-features`/`cargo test` both unaffected (bench-only change); `cargo fmt`/`clippy`/`check`/`doc` clean; finding: no meaningful latency or throughput cost relative to `GetById` at this benchmark's scale, on this session's shared container | Implemented | PR #59 |
| `SERVER-TLS-DESIGN` | A design for native transport encryption on the server/query layer: TLS termination inside this crate's own server process via `rusty_tls` (`Rusty-Mill/rusty_mill`, this owner's own ecosystem-wide `rustls` wrapper — not a direct `rustls` dependency, not an external proxy/tunnel), an opt-in `TlsConfig` mirroring `AuthConfig`'s own shape, a per-connection stream abstraction (`rusty_tls::TlsServerStream`) so `handle_connection`/`send_response` work uniformly whether or not TLS is active — `src/server/framing.rs` needs zero changes, already generic over `Read`/`Write`; ADR-0014 (**Accepted** — owner approved as revised); `docs/design/SERVER-TLS-DESIGN.md`; explicitly does not add mTLS (client identity remains `AuthConfig`'s existing token scheme) | `SERVER-AUTH`, `SERVER-TRANSACTION-BENCHMARK` | — (no spec written for the design itself; `SERVER-TLS` below registers the implementation against `SERVER-001`, matching `SERVER-AUTH-DESIGN`'s own precedent) | Design document and ADR written, incremental additions to `SERVER-001`'s already-compiling `protocol.rs`/`mod.rs` shapes (no standalone scratch probe built — see ADR-0014's own "Validation and revisit triggers" for why); owner reviewed and accepted the design and ADR-0014 as revised, no further changes requested | Accepted | PR #61, #62, #63 |
| `SERVER-TLS` | The real implementation of `SERVER-TLS-DESIGN`: `TlsConfig`/`TlsConfigError` (`src/server/mod.rs`), a new `ReadHalf`/`WriteHalf` enum pair (`Plain`'s existing `TcpStream::try_clone` split unchanged; `Tls` shares one `Rc<RefCell<rusty_tls::TlsServerStream<TcpStream>>>` between both halves — a real implementation-time finding, since `rustls`' connection state can't be split the way a raw socket can), a small hand-written PEM/base64 decoder (`src/server/pem.rs`, new, not a new dependency), `serve`'s new `tls: Option<TlsConfig>` parameter; `SERVER-001` v0.9.0 | `SERVER-TLS-DESIGN` | `SERVER-001` | `cargo test --features server` green including `tests/server_tls_integration.rs`'s five tests (a full round trip over TLS; `TlsConfig` composed with `AuthConfig`; a plain connection to a TLS-configured server never getting a valid response; `TlsConfig: None` reproducing plaintext behavior; the `Authenticate` token verifiably absent from the raw bytes sent on the wire, via a byte-recording stream wrapper) and `src/server/pem.rs`'s ten new unit tests; `cargo test`/`cargo test --all-features` unaffected; `cargo fmt`/`clippy`/`check`/`doc` clean; every `SERVER-TLS-DESIGN.md` functional acceptance criterion verified over a real socket; no `src/store/**`/`src/durability/**`/`src/concurrency/**` changes, and every pre-existing plaintext test passes unmodified (verifying the `Plain` half of the new stream abstraction introduced no behavior change) | Implemented | PR #64 |
| `EXTERNAL-DB-BENCHMARK` | The first external (not this-crate-implemented) comparison point: `ProductionStore` benchmarked against real SQLite (`rusqlite`), Postgres (`postgres`, local server), and DuckDB (`duckdb`) on the same three access-pattern shapes already benchmarked in-repo — `get`, `scan_ages`, one-hop `littermate_of` traversal (depth-bounded recursive CTE over an adjacency table). New `benches/external_db.rs`, gated behind a new `external-db-bench` Cargo feature; ADR-0015 (Accepted); `STORAGE-013` v0.1.0 (later extended through v0.5.0, one-through-five-hop graph traversal, tracked as version bumps to this same unit rather than new rows — see `PROJECT-STATUS.md`). Stays inside `docs/FUTURE-GROWTH.md`'s existing "Path to SQLite/DuckDB parity" scope line — no SQL parser/planner/arbitrary joins built or implied | `SERVER-TLS` (most recent prior round; no functional dependency) | `STORAGE-013` | `cargo check --bench external_db --features research,external-db-bench` clean; a real `cargo bench --features research,external-db-bench --bench external_db` run against a local Postgres instance, all three workload groups × three sizes × four systems, no panics; `RESULTS.md`'s `## External database comparison` section states real numbers and a verdict per workload, including any workload where `ProductionStore` loses; `cargo fmt`/`clippy --all-targets --all-features`/`check`/`doc` clean; no `src/` changes | Implemented | PR #66 |
| `PRODUCTION-STORE-PORTABILITY-DESIGN` | A design for closing `ProductionStore`'s file-portability gap against SQLite's/DuckDB's own self-describing `.db` files: a new companion file (bincode-serialized `id`/`breed`/`littermate_of` edges, write-to-temp-then-atomic-rename crash safety reusing `MmapAgeStore`'s own established pattern), a new, additive `ProductionStore::open_portable(path)` constructor needing no caller-supplied `records`/`edges`, implemented in terms of the existing, unchanged `open`; ADR-0016 (**Accepted** — owner approved as proposed); `docs/design/PRODUCTION-STORE-PORTABILITY-DESIGN.md`; explicitly leaves `MmapAgeStore`'s own file format and `create`/`open`'s existing signatures untouched — reframes ADR-0006's declined string-heap redesign as unnecessary, since `breed`/`id`/edges are never mutated, only `age` is | `EXTERNAL-DB-BENCHMARK` (most recent prior round; no functional dependency) | — (no spec written for the design itself; a `STORAGE-014` spec would be registered by the implementation unit that follows, matching `GENERIC-SCHEMA-DESIGN`'s own precedent) | Design document and ADR written, reusing `SnapshotFullStore`'s already-proven full-`DogRecord` bincode round trip and `MmapAgeStore`'s already-proven crash-safe rewrite mechanism rather than a from-scratch string-heap format; owner reviewed and accepted the design and ADR-0016 without requesting changes | Accepted | PR #80 (proposed), PR #82 (accepted) |
| `PRODUCTION-STORE-PORTABILITY` | The real implementation of `PRODUCTION-STORE-PORTABILITY-DESIGN`: `src/durability/record_blob.rs` (new — `RecordBlob { records, edges }` bincode-serialized behind a `DOGBLOB\0` magic + `u32` version header, written to `<path>.records` via write-to-temp-then-atomic-rename), `ProductionStore::create` now also writes the blob, `ProductionStore::open` reads-and-byte-compares it and rewrites only when the record set changed (which also heals a pre-`STORAGE-014` directory holding only the ages file), a new additive `ProductionStore::open_portable(path)` reconstructing the whole store from the path alone, a new `DurabilityError::RecordBlobUnreadable { path, cause }`; `STORAGE-014` v0.1.0; `src/durability/mmap_store.rs` untouched (empty diff). One implementation-time finding the design did not predict: `open`'s common-case cost is *not* zero — `MmapAgeStore`'s rewrite decision is private to it, so `open` serializes and compares on every call (~+27% at 1M records, measured) — closed in `STORAGE-014` v0.2.0 by a 64-bit FNV-1a content fingerprint in the blob header (`BLOB_VERSION = 2`): `open` now reads the 20-byte header and compares fingerprints, +0.3-4% at 1M, and a version-1 blob is upgraded in place on its first `open` | `PRODUCTION-STORE-PORTABILITY-DESIGN` | `STORAGE-014` | `cargo test`/`cargo test --all-features` green including `record_blob.rs`'s 12 unit tests and `production.rs`'s 6 portability tests (path-alone reconstruction, both files copied to a fresh directory, missing blob a typed error naming the companion path, legacy ages-only directory healed by `open`, blob rewritten only on a changed record set, version-1 blob upgraded in place); `MmapAgeStore`'s existing suite passing unmodified; `cargo fmt`/`clippy --all-targets --all-features`/`doc` clean; a release-build `create`/`open`/`open_portable` cost table at 1K/100K/1M in `RESULTS.md`'s `### ProductionStore file portability` subsection; no benchmarked hot path affected (no Criterion group times `create`/`open` inside `b.iter()`) | Implemented | PR #84 |
| `GENERIC-STORE-PORTABILITY-DESIGN` | A design for the identical file-portability treatment for `GenericMmapStore`/`GenericProductionStore` — the generic half `ADR-0016` twice named as deliberately out of scope, and the one-durable-field wall `GENERIC-SCHEMA-DESIGN.md` §4.2 first hit with `Order`: a `<path>.records` companion blob of bincode-serialized `Vec<R>` behind the `STORAGE-014` v0.2.0 20-byte magic/version/FNV-1a header (`GENBLOB\0`, version 1), fingerprinted over the streamed encoding (no per-type hand-walking; includes the mmap-backed field, a named cost with a trait-method fingerprint as fallback); `create`/`open`'s existing bounds tightened with `Serialize + DeserializeOwned` (the one breaking change, named — every in-crate record type is one derive line away) rather than a parallel constructor pair that would leave a plain `open`'s blob silently stale; two new additive functions, `read_portable_records(path) -> Vec<R>` (persisted order preserved, so `Reversed`'s child order stays deterministic) and `open_portable(path)`; `open_order_production_stack_portable(path)` as the stack-level helper. Relation layers stay out of the blob (`Reversed` derives from records; `Symmetric`'s external edge list explicitly out of scope, a separate later decision); `GenericMmapStore`'s own `.mmap` format and `GenericProductionStore` unchanged; ADR-0017 (**Accepted** — owner approved as proposed); `docs/design/GENERIC-STORE-PORTABILITY-DESIGN.md` | `PRODUCTION-STORE-PORTABILITY` (`STORAGE-014` v0.2.0 — the header, hash, write path, and error variant it reuses) | — (no spec written for the design itself; a `STORAGE-015` spec would be registered by the implementation unit that follows, matching the `PRODUCTION-STORE-PORTABILITY-DESIGN` precedent) | Design document and ADR written from direct investigation of `src/generic/mmap_store.rs`, `order_customer.rs`, `traits.rs`, `store.rs`, `generic_spike/employee_impl.rs`, and `record_blob.rs`; no `src/` changes; `cargo fmt --all -- --check` clean; owner reviewed and accepted the design and ADR-0017 without requesting changes | Accepted | PR #88 (proposed), PR #90 (accepted) |
| `GENERIC-STORE-PORTABILITY` | The real implementation of `GENERIC-STORE-PORTABILITY-DESIGN`: `src/generic/record_blob.rs` (new — `GENBLOB\0` magic, blob version 1, the `STORAGE-014` v0.2.0 20-byte magic/version/FNV-1a header, then the bincode `Vec<R>`; `GenericRecordBlob<'a, R>` borrows the record slice and fingerprints it by streaming `bincode::serialize_into` a `Fnv1a64` that `impl`s `io::Write`), `STORAGE-014`'s header/hash/temp-then-rename helpers made `pub(crate)` and shared (`RecordBlob`'s 12 tests unchanged); `GenericMmapStore::create` writes the blob and `open` refreshes it only when the 20-byte header's fingerprint is stale, both gaining the `R: Serialize + DeserializeOwned` bound (the one breaking change, named — serde derives added to `Order`/`OrderStatus`/`Customer`/`Employee`/`Department`); new additive `read_portable_records(path)` (persisted order preserved) and `open_portable(path)` (= `open(read_portable_records(path)?, path)`); `open_order_production_stack_portable(path)` rebuilds the whole `Order`/`Customer` stack, `Reversed` included, from the two files; `STORAGE-015` v0.1.0; the `.mmap` slot format untouched (the diff removes only the six bound lines). Two deliberate departures from the design's sketch, recorded in the spec: no bound on `R::Id`, no `Employee` portable helper (its `Symmetric` edge list stays the deferred decision). Cost, measured rather than assumed: `create` roughly doubles at every size (the blob write, ~76 B/record, 3× the `.mmap` file); `open` pays the streamed-serialization fingerprint on every call — a ~0.1 ms floor at 1K, +19–33% at 100K, and at 1M the throwaway's three-sample medians and the 20-sample Criterion `generic_production_open` group disagree (near zero vs. +24–27%), both recorded in `RESULTS.md` with the Criterion figure treated as the more trustworthy one; the design's fallback (a per-type trait-method fingerprint) is therefore the named next step, the owner's call | `GENERIC-STORE-PORTABILITY-DESIGN` | `STORAGE-015` | `cargo test`/`cargo test --all-features` green including `record_blob.rs`'s 9 unit tests, `mmap_store.rs`'s 6 portability tests (round trip in creation order, both files copied to a fresh directory, missing blob a typed error naming the companion path and healed by a plain `open`, blob rewritten only on a changed record set, a `DOGBLOB\0` file a magic error, an `.mmap`-file failure leaving a valid companion untouched), and `order_customer.rs`'s stack test; `mmap_store.rs`'s 8 pre-existing tests, `tests/mmap_record_identity_keying.rs`, `RecordBlob`'s 12 tests, and `production.rs`'s 6 portability tests passing unmodified; `cargo fmt`/`clippy --all-targets --all-features`/`doc` clean; a release-build `create`/`open`/`open_portable` cost table at 1K/100K/1M plus the same-session Criterion `generic_production_create`/`generic_production_open` before/after pair in `RESULTS.md`'s `### GenericMmapStore file portability` subsection | Implemented | PR #92 |
| `GENERIC-STORE-FINGERPRINT-MEASUREMENT` | Docs-only: `STORAGE-015`'s named fallback — the per-type trait-method fingerprint that would have been `STORAGE-015` v0.2.0 — measured at the owner's request before being built, and closed as not warranted by the owner's choice. An in-place A/B at 1M `Order` records (the same binary with `is_current_at`'s `fingerprint()` stubbed out) puts the shipped streamed-bincode fingerprint at ~52 ms of a 1,222 ms `open`, 4% — not the ~300 ms the Criterion pair implied (that group, re-run on unchanged code, drifted +8.8% against itself). Isolated candidates: streamed bincode 79 ms, hand-walk every field 72 ms (−7 ms, the same ~76 B/record hashed), id + customer + status 42 ms, id only 21 ms — the larger savings come only from hashing less of the record, which reopens the silently-stale-blob gap `ADR-0016` rejected. Outcome: streamed fingerprint stays, `BLOB_VERSION` stays 1, no record-trait API, `STORAGE-015` stays v0.1.0; `RESULTS.md` gains `#### Follow-up: the trait-method fingerprint, measured and not built` and its earlier verdict is marked superseded; design doc, ADR-0017, spec, and `PROJECT-STATUS.md` item 65 record the closure | `GENERIC-STORE-PORTABILITY` | `STORAGE-015` (unchanged, v0.1.0) | Zero `src/` changes, verified by diff; `cargo fmt --all -- --check` clean; the three measurements recorded with their method in `RESULTS.md`; the owner chose "close" from a three-way measured choice | Implemented | PR #94 |
| `SYMMETRIC-EDGE-PORTABILITY-DESIGN` | A design for closing the one gap `ADR-0017` named as its own deliberate limitation — `Symmetric`'s external edge list, the only relation-layer state not derivable from records, so `Employee`'s durable stack (`Reversed` over `Symmetric` over `GenericMmapStore`) still needs the caller to hold `collaboration_edges` and got no portable helper under `STORAGE-015`: a `Symmetric`-level companion blob at a caller-supplied `edges_path` (`GENEDGE\0`, version 1, the `STORAGE-014` v0.2.0 20-byte magic/version/FNV-1a header via the already-`pub(crate)` helpers, body a bincode `Vec<(R::Id, R::Id)>` in the order given — the edge list, not the adjacency map, so bytes are deterministic and half the size), a parallel `create`/`open`/`open_portable` triple plus `read_portable_edges` in a separately bounded `impl` block with `Symmetric::new` untouched (the parallel-constructor shape `ADR-0017` rejected is right here because `new` has no path to refresh a blob from; the residual by-convention gap is named), `edges_path(path) -> <path>.edges` as the single-relation convention, `RecordBlobUnreadable` reused with `path` naming the edge blob, `open_employee_production_stack_portable(path)` as the stack-level helper. `GenericMmapStore`/`.mmap`/`.records`/`Reversed`/`dog_impl.rs` unchanged; schema tag and stack manifest deferred; ADR-0018 (**Accepted** — owner approved as proposed); `docs/design/SYMMETRIC-EDGE-PORTABILITY-DESIGN.md` | `GENERIC-STORE-FINGERPRINT-MEASUREMENT` (most recent prior round), `GENERIC-STORE-PORTABILITY` (`STORAGE-015` v0.1.0 — the header, hash, write path, error variant, and `read_portable_records` it reuses) | — (no spec written for the design itself; a `STORAGE-016` spec would be registered by the implementation unit that follows, matching the `GENERIC-STORE-PORTABILITY-DESIGN` precedent) | Design document and ADR written from direct investigation of `src/generic/store.rs`, `src/generic/record_blob.rs`, `src/generic_spike/employee_impl.rs`, `src/generic_spike/dog_impl.rs`, and every `Symmetric::new`/`create_employee_production_stack` call site; no `src/` changes; `cargo fmt --all -- --check` clean; owner reviewed and accepted the design and ADR-0018 without requesting changes | Accepted | PR #98, #99, #100 |
| `SYMMETRIC-EDGE-PORTABILITY` | The accepted `SYMMETRIC-EDGE-PORTABILITY-DESIGN` implemented and registered as `STORAGE-016` v0.1.0: `src/generic/edge_blob.rs` (new, `pub(crate)`) — `GENEDGE\0` magic, blob version 1, the `STORAGE-014` v0.2.0 20-byte magic/version/FNV-1a header via the shared `pub(crate)` helpers (their third call site), body a bincode `Vec<(Id, Id)>` in caller order, `EdgeBlob<'a, Id>` borrowing the slice and fingerprinting by streaming into `Fnv1a64`, `read(path)` verifying magic/version/body-vs-header fingerprint with every failure a `RecordBlobUnreadable { path, cause }` naming the edge blob, `edges_path(path) -> <path>.edges`; one added `impl` block in `src/generic/store.rs` bounded `R::Id: Serialize + DeserializeOwned` — `Symmetric::create(inner, edges, edges_path)`, `open(inner, edges, edges_path)` (header check, rewrite only when stale/missing/unreadable, adjacency always from the caller's edges), `read_portable_edges(edges_path)`, `open_portable(inner, edges_path)` — with `Symmetric::new`, `Neighbors`, and every forwarding impl untouched; `create_employee_production_stack`/`open_employee_production_stack` switched to `create`/`open` at `<path>.edges` with signatures unchanged, new `open_employee_production_stack_portable(path)` rebuilding the whole stack from `<path>` + `<path>.records` + `<path>.edges`; `RecordBlobUnreadable`'s doc names the edge blob, no new variant; 23 new tests (12 blob, 6 `store.rs`, 5 `employee_impl.rs`) covering every acceptance criterion the design named; one recorded departure from the sketch (`fingerprint` returns a `Result`); cost not measured per the design; `GenericMmapStore`/`.mmap`/`.records`/`Reversed`/`dog_impl.rs` untouched, `STORAGE-015` stays v0.1.0 | `SYMMETRIC-EDGE-PORTABILITY-DESIGN`, `GENERIC-STORE-PORTABILITY` | `STORAGE-016` | `STORAGE-016` registered; `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `cargo test --all-features` clean; the design's acceptance criteria each pinned by a test; every pre-existing test unmodified | Implemented | PR #102 |
| `BLOB-SCHEMA-TAG-DESIGN` | A design for the one open question `STORAGE-015` and `STORAGE-016` both hold and agreed to resolve together — neither generic companion blob records which `R` it holds, so a foreign-`R` `.records` blob passes magic/version/fingerprint and fails (if at all) inside `bincode`, and every `Uuid`-keyed domain's `.edges` blob decodes as every other's: a new opt-in `SchemaTag { const SCHEMA_TAG: &'static str }` trait in `src/generic/traits.rs` (not a supertrait of `Record`; `std::any::type_name` rejected as unstable across compiler versions, a required const on `Record` rejected as touching nine impls), an 8-byte `u64` LE FNV-1a 64 hash of the tag at offset 20 after the unchanged 20-byte shared header (`TAGGED_HEADER_LEN` 28; `durability::record_blob` and `DOGBLOB\0` untouched; new `pub(crate)` helpers in `generic::record_blob` imported by `generic::edge_blob`), `GENBLOB\0` and `GENEDGE\0` `BLOB_VERSION` 1 → 2 (`STORAGE-015` v0.2.0, `STORAGE-016` v0.2.0 at implementation), check order magic → version → tag → fingerprint → decode with a tag mismatch a `RecordBlobUnreadable` naming the expected tag string (no new variant), `is_current_at` requiring all four so `open` heals version-1 and foreign-`R` blobs (the `DOGBLOB\0` 1 → 2 story), the `R: SchemaTag` bound on exactly the blob-touching functions (`GenericMmapStore`'s four moved into their own `impl` block; `Symmetric`'s `STORAGE-016` block), `Order`/`Employee`/doc-example `Widget` the whole in-crate impl set, the edge blob tagged with `R::SCHEMA_TAG` passed as a value (a `Marker`-level tag rejected); a readable fixed-width 32-byte string offered as the alternative at acceptance; `.mmap` tag, two-relations-per-`R`, and stack manifest deferred; ADR-0019 (**Accepted** — owner approved as proposed, the hashed 8-byte tag); `docs/design/BLOB-SCHEMA-TAG-DESIGN.md` | `SYMMETRIC-EDGE-PORTABILITY` (`STORAGE-016` v0.1.0), `GENERIC-STORE-PORTABILITY` (`STORAGE-015` v0.1.0) — the two blobs it tags | — (no spec written for the design itself; the `STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0 bumps would be registered by the implementation unit that follows) | Design document and ADR written from direct investigation of `src/generic/record_blob.rs`, `src/generic/edge_blob.rs`, `src/durability/record_blob.rs`, the `GenericMmapStore` and `Symmetric` `impl` blocks, every `Record` impl, and every `GenericMmapStore::<…>` call site; no `src/` changes; `cargo fmt --all -- --check` clean; accepted as proposed by the owner | Accepted | PR #104 (proposed), PR #106 (accepted) |
| `BLOB-SCHEMA-TAG` | The accepted `BLOB-SCHEMA-TAG-DESIGN` implemented and registered as `STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0: `pub trait SchemaTag { const SCHEMA_TAG: &'static str; }` in `src/generic/traits.rs` (public, not a supertrait of `Record`, documented as on-disk format); `src/generic/record_blob.rs` gains the `pub(crate)` tagged-header helpers (`TAG_OFFSET` 20, `TAGGED_HEADER_LEN` 28, `tag_hash` = FNV-1a 64 over the tag bytes pinned to the published test vectors, `encode_tagged_image`, `parse_tagged_header` = shared header then tag) and `GENBLOB\0` `BLOB_VERSION` 1 → 2; `src/generic/edge_blob.rs` imports them, `EdgeBlob::new(edges, tag)`/`read(path, tag)` take the tag as a value, `GENEDGE\0` `BLOB_VERSION` 1 → 2; check order magic → version → tag → fingerprint → decode with a foreign-`R` blob a `RecordBlobUnreadable` whose cause names the expected tag string and both hashes, a version-1 blob a `version` cause, a file cut inside the tag a short-tagged-header cause — no new variant; `is_current_at` requires all of them so `open` rewrites version-1 and foreign-`R` blobs (the heal, no migration step); `R: SchemaTag` on exactly the blob-touching functions (`GenericMmapStore`'s four file constructors moved into their own `impl` block; `Symmetric`'s `STORAGE-016` block), `Order` (`"order_customer::Order"`), `Employee` (`"employee::Employee"`), doctest `Widget` the whole impl set; `durability::record_blob`/`DOGBLOB\0`/`.mmap` untouched; 12 new tests (5 record blob, 3 edge blob, 2 `mmap_store.rs` `Employee`-as-`Order` and version-1 heal, 2 `store.rs` second-record-type and version-1 heal) pinning every acceptance criterion the design named; both specs' shared open question resolved | `BLOB-SCHEMA-TAG-DESIGN`, `SYMMETRIC-EDGE-PORTABILITY`, `GENERIC-STORE-PORTABILITY` | `STORAGE-015` v0.2.0, `STORAGE-016` v0.2.0 | Both specs bumped and re-registered; `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `cargo test --all-features` clean; `cargo doc --no-deps` at baseline; every pre-existing test passing | Implemented | PR #108 |
| `MULTI-FIELD-MMAP-DURABILITY-DESIGN` | A design answering `ADR-0006`'s named revisit trigger ("a future record shape needs more than one mutable field persisted" — met by `Order` per `GENERIC-SCHEMA-DESIGN` §4.2, carried as "genuinely unscoped" in `PROJECT-STATUS` item 22 since): `GenericMmapStore` persists exactly one `ScanMarker` in its `.mmap`; the `.records` blob makes every other field durable but immutable. Weighs the three shapes `PROJECT-STATUS` item 68 names — (1) a multi-slot `.mmap` layout (`SCHEMA_VERSION` 3, per-domain layout type, refuses every existing file, makes every slot helper and the benchmark-pinned `scan` fast path layout-generic, the AoS stride `ADR-0001` measured as bad for single-column scans), (2) per-field `.mmap` files via a composable `MmapScanned<S, R, Marker>` layer — the durable twin of `Scanned`, one file per marker in exactly the existing `GMMAPST\0` version-2 format, `get` patching on the way up, `Flush` own-then-inner, cross-marker forwards from `forward_scannable_pairs!` generalized to the layer type, the slot/header/commit/reconcile machinery extracted into a `pub(crate)` `SlotFile` both stores share (duplication the fallback), caller-supplied path per layer (`STORAGE-016` precedent) with the domain constructor deriving `<path>.discount_cents.mmap`, a slot-width check as the weak foreign-file guard and the tagged blob as the strong one — the shape §4.2 sketched, and (3) widening the blob's role (a full encode + fingerprint + write per `update`, or a second WAL; rejected for fixed-width fields, the named fallback for a variable-width one); proposes (2) with `OrderProductionStack` gaining `DiscountCents` as the in-crate proof and `CreatedAt` deliberately left in-memory; no format changes; multi-field atomic updates, variable-width fields, the `.mmap` tag, the stack manifest, and re-expressing `GenericMmapStore` as the layer all named as non-goals or revisit triggers; nine requirements `MFMD-FR-001`–`-009` and eight acceptance criteria for a `STORAGE-017` at implementation; ADR-0020 (**Proposed** — owner's call); `docs/design/MULTI-FIELD-MMAP-DURABILITY-DESIGN.md` | `BLOB-SCHEMA-TAG` (`STORAGE-015` v0.2.0 — the tagged blob the design leans on), `GENERIC-STORE-PORTABILITY`, `GENERIC-SCHEMA` (`STORAGE-012`), `DURABILITY-TIER2` (`ADR-0006`) | — (no spec for the design itself; `STORAGE-017` v0.1.0 is registered by `MULTI-FIELD-MMAP-DURABILITY`, the implementation unit that followed acceptance) | Design document and ADR written from direct investigation of `src/generic/mmap_store.rs` (`create`, `open`, every query impl, the slot helpers), `src/generic/store.rs` (`Scanned`, `forward_scannable_pairs!`), `src/generic/order_customer.rs` (`OrderProductionStack` and its constructors), `src/generic/mmap_field.rs`, and `ADR-0006`/§4.2's exact wording; no `src/` changes; `cargo fmt --all -- --check` clean; accepted as proposed by the owner | Accepted | PR #110 (proposed), PR #112 (accepted) |
| `MULTI-FIELD-MMAP-DURABILITY` | The accepted `MULTI-FIELD-MMAP-DURABILITY-DESIGN` implemented and registered as `STORAGE-017` v0.1.0: `src/generic/slot_file.rs` (new, `pub(crate)`) — the `GMMAPST\0` constants, slot arithmetic, header read/write, committed-slot read, `create(path, slots)`, `open(path)`, `committed_pairs()`, `append_committed_slots()`, `flush()` extracted verbatim from `GenericMmapStore`, which now delegates to it with its 16 tests textually unchanged and the `chunks_exact` scan fast path kept in each owner over `slot_bytes()`; `src/generic/mmap_scanned.rs` (new, `pub`) — `MmapScanned<S, R, Marker>`, the durable twin of `Scanned`: one more mutable, durable, scannable field over any inner store in its own version-2 slot file (no format change), `create`/`open` in one `R: SchemaTag`-bounded `impl` block, per-file reconciliation with the same four cases and `O_APPEND` repair `GenericMmapStore::open` has, `get` patching its field on the way up, `Flush` own-then-inner, generic `FilterEq`/`Neighbors`/`Children` forwards; `forward_scannable_pairs!` gains `for Scanned;` / `for MmapScanned;` entry arms (the layer path carried as one `tt` through the rotating accumulator, the bare spelling preserved) so the cross-marker forwards are generated for both layers; new `DurabilityError::SlotWidthMismatch { path, body_len, slot_width }` — `MmapScanned::open` refuses a file whose body is not a whole number of its slots (the design's weak foreign-file guard, also catching a truncated-mid-slot file the blob-backed store tolerates); `OrderProductionStack` becomes `Reversed<MmapScanned<GenericMmapStore<Order, Status, Amount>, Order, DiscountCents>, …>`, the three constructors keep their signatures and derive `<path>.discount_cents.mmap` via `discount_cents_path`, `CreatedAt` deliberately in-memory (a compile-time refusal through the durable stack); two Criterion groups (`scan_layer`, `update_layer`) beside the core's `scan`/`update`, before/after run recorded in `RESULTS.md` (core `scan`/`update` within noise; `get`/`parent` +22–67% for the second layer's slot read; `create`/`open` +15–42% for the second file); 9 new tests, 1 extended, 2 compile-time pair checks — every acceptance criterion the design named pinned; six implementation calls the design left open recorded under the spec's "Traceability" | `MULTI-FIELD-MMAP-DURABILITY-DESIGN`, `BLOB-SCHEMA-TAG`, `GENERIC-STORE-PORTABILITY` | `STORAGE-017` | `STORAGE-017` registered; `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` (124 lib + 2 integration), `cargo test --all-features` (316 lib) clean; every pre-existing test unmodified; Criterion `scan`/`update` within noise of the pre-extraction baseline | Implemented | PR #114 |
| `BINCODE-ENCODING-STABILITY-DESIGN` | A design answering the limitation `ADR-0010` named when the server layer was proposed and `SERVER-001`/`PROJECT-STATUS` item 33 have carried since — *"`bincode`'s wire-format stability across crate versions is unverified"* — now that the three companion blobs (`STORAGE-014`–`016`) have raised the same cross-build bar for files: every `bincode` byte this crate writes comes from the crate's *free functions* with no configuration named in `src/`, and no test pins a byte. Verified (vendored `bincode` 1.3.3 source, `uuid` 1.25.0's serde impl, an executed probe) what the free functions pin — fixint integers at Rust width, little-endian, no limit, trailing bytes allowed on read; **not** `bincode::options()`/`DefaultOptions`, which are varint and reject trailing (a `Uuid` is 24 bytes, not 17; one probed enum value 22 bytes vs 5) — and the 1.x readme's promise (*"stable across minor revisions, provided the same configuration is used"*), whose "same configuration" condition this crate satisfies by convention only. Inventories the 23 production call sites (11 front-door: `server/framing.rs`, `durability/record_blob.rs`, `generic/record_blob.rs`, `generic/edge_blob.rs`; 12 `research`-gated or research-only-reached) and proposes: a `pub(crate)` `src/codec.rs` naming the configuration once (`DefaultOptions::new().with_fixint_encoding().with_little_endian().with_no_limit()` + `reject_trailing_bytes()`) behind `encode`/`encode_into`/`decode` with `bincode::Error` unchanged; all 23 sites routed so `bincode::` in production `src/` is a `grep`; golden byte vectors (primitives, every `Request`/`Response` variant, one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0`) captured on the pre-change code and checked in as hex so the routing is proven byte-identical; the evolution rules for pinned types documented; no format bump, no dependency change, no public API change. Acceptance question: reject trailing bytes (proposed — frames with junk after a valid message become `FrameError::Encoding`; the blobs' fingerprint already refuses such a body) vs allow (a purely no-behavior-change pin) vs document-only; `bincode` 2.x (`config::legacy()`), a wire-protocol version/handshake, a `Uuid` newtype, and `ADR-0019`'s layout fingerprint (concluded not needed now; trigger stays armed) named as non-goals or revisit triggers; eight requirements `BINENC-FR-001`–`-008` and eight acceptance criteria for a spec at implementation (`STORAGE-018`, approved at acceptance); ADR-0021 (**Accepted** — as proposed by the owner); `docs/design/BINCODE-ENCODING-STABILITY-DESIGN.md` | `SERVER-QUERY-LAYER` (`SERVER-001` — the wire protocol), `PRODUCTION-STORE-PORTABILITY` (`STORAGE-014`), `GENERIC-STORE-PORTABILITY` (`STORAGE-015`), `SYMMETRIC-EDGE-PORTABILITY` (`STORAGE-016`), `BLOB-SCHEMA-TAG` (the blob formats at their current versions) | — (no spec for the design itself; a `STORAGE-018` would be registered by the implementation unit that follows acceptance) | Design document and ADR written from direct investigation of the vendored `bincode` 1.3.3 source (`lib.rs`, `config/mod.rs`, `config/int.rs`, `config/trailing.rs`), `uuid` 1.25.0's `Serialize` impl, an executed throwaway probe (not committed), and all 23 call sites with their feature gates; no `src/` changes; `cargo fmt --all -- --check` clean | Accepted | PR #116 (proposed), PR #118 (accepted) |
| `BINCODE-ENCODING-STABILITY` | The accepted `BINCODE-ENCODING-STABILITY-DESIGN` implemented and registered as `STORAGE-018` v0.1.0: `src/codec.rs` (new, `pub(crate)`) — one private `options()` (`DefaultOptions::new().with_fixint_encoding().with_little_endian().with_no_limit().reject_trailing_bytes()`) behind `encode`/`encode_into`/`decode` with `bincode::Error` unchanged, module docs stating the configuration, the byte format, the 1.x stability promise and `Cargo.toml` range, and the evolution rules (append a variant: compatible; reorder/insert variants, change struct fields or integer widths: the owning format's version bump); all 23 production call sites routed (`server/framing.rs`, the three blob modules, `durability/{mod,snapshot_rebuild,hybrid,lsm_store}.rs`) so `grep -rn "bincode::" src` outside test modules hits only `codec.rs` and the two error-type payloads; golden byte vectors captured on the pre-change code in a first commit (primitives/enum/struct/`Uuid` in `codec.rs`, every `Request`/`Response` variant in `server/protocol.rs`, one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0` written under each blob's real header and read back through its real `read`) and passing unchanged after routing in the second — the routing proven byte-identical; `test_support::{assert_golden, assert_golden_eq, hex_literal}` printing the actual bytes as a paste-ready literal on drift; the one behavioral change pinned by tests: a frame carrying a valid `Request` plus two junk bytes is `FrameError::Encoding`, a junk-padded `GENBLOB\0` body is still a fingerprint mismatch, and — a finding against the design's "no change for the three blobs" — a junk-padded `DOGBLOB\0` body, silently accepted before because that blob decodes before it fingerprints, is now a decode-cause `RecordBlobUnreadable`; the four resolution pointers (`ADR-0010`, `SERVER-001` amended to v0.9.1, `PROJECT-STATUS` item 33, `ADR-0019`); no format bump, no dependency change, no public API change, no benchmark (no runtime path changes); 17 new tests across the two commits | `BINCODE-ENCODING-STABILITY-DESIGN`, `SERVER-QUERY-LAYER`, `PRODUCTION-STORE-PORTABILITY`, `GENERIC-STORE-PORTABILITY`, `SYMMETRIC-EDGE-PORTABILITY` | `STORAGE-018`, `SERVER-001` (v0.9.1) | `STORAGE-018` registered; `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` (136 lib + 2 integration), `cargo test --all-features` (333 lib), `cargo doc --all-features --no-deps` (zero warnings) clean; every pre-existing test unmodified beyond the call-site rename; every golden literal unchanged between the capture and routing commits | Implemented | PR #120 |
| `SERVER-PROTOCOL-VERSION-DESIGN` | A design answering `SERVER-001` v0.9.1's last open question ahead of its trigger (*a second deployed client build*): the wire *shape* — which `Request`/`Response` variants exist, at which indices — has no version and no handshake, so a request an older server does not know is a silent connection drop (`FrameError::Io` at the client) and a newer server has no way to know which response variants an older client can decode. Proposes `pub const PROTOCOL_VERSION: u32 = 2` (version 1 is retroactively the v0.9.1 shape, what a silent client speaks), an appended `Request::Hello { protocol_version }` / `Response::Hello { protocol_version }` pair at index 10 — optional, first frame if sent, answered by `handle_connection` before authentication (the `Authenticate` precedent) with `min(client, server)`; version 0 or a non-first `Hello` is `Malformed` — and four compatibility rules written into `protocol.rs` and `SERVER-001` (append-only; every variant records its introducing version; a server sends a variant introduced at *N* only on a connection negotiated at ≥ *N*; a client sends a request introduced at *N* only after negotiating ≥ *N*). `SchemaDrivenClient::connect` says `Hello` before `DescribeSchema` and exposes `server_protocol_version()`. Rejected: a per-frame version byte in the length prefix's top byte (versions the transport, caps at 255, costs the 16 MiB edge), a field on `DomainSchema` (a format change for every existing client, and pre-auth-unreachable), a feature bitmap, `min_supported`, renegotiation, and building the gating branch before a gated variant exists. Every v0.9.1 golden vector unchanged; no existing client, suite, or bench touched | `BINCODE-ENCODING-STABILITY` (`STORAGE-018`, the encoding under the shape), `SERVER-TLS` (`SERVER-001` v0.9.1, the shape at protocol version 1) | — (design only; `SERVER-001` v0.10.0 / FR-020 registered by `SERVER-PROTOCOL-VERSION`, the implementation unit below) | Design document and ADR written from `src/server/{framing,protocol,mod,client}.rs` at `main` `835d7a9` (frame layout, variant indices 0–9 on both enums, the `Authenticate` intercept's position, `connect`'s first frame) and `STORAGE-018`'s evolution rules; no `src/` changes; `cargo fmt --all -- --check` clean; owner reviewed and accepted the design and ADR-0022 as proposed (option (a); hello-required and rule-only declined) without requesting changes | Accepted | PR #124 (proposed), PR #125 (accepted) |
| `SERVER-PROTOCOL-VERSION` | The accepted `SERVER-PROTOCOL-VERSION-DESIGN` implemented and registered as `SERVER-001` v0.10.0 / FR-020: `pub const PROTOCOL_VERSION: u32 = 2` (version 1 is the v0.9.1 shape, what a silent client speaks); `Request::Hello { protocol_version }` / `Response::Hello { protocol_version }` appended at index 10 on both enums with golden vectors (`0a 00 00 00 02 00 00 00` for either `Hello { 2 }`); `handle_connection` answers a first-frame `Hello` before the `Authenticate` intercept and the auth gate with `min(client, server)`, and a version-0 or non-first `Hello` with `Malformed` leaving the connection open (one `bool` of per-connection state, no negotiated version stored until a gated variant exists); `dispatch` maps `Hello` to `Unsupported` as it does `Authenticate`; the four append-only compatibility rules and a per-version variant table in `protocol.rs`'s module docs; `SchemaDrivenClient::connect` says hello before `DescribeSchema` and exposes `server_protocol_version()`. `tests/server_protocol_version.rs` (new, all three domains) covers the design's acceptance criteria 3, 4, 7 and 8 over real sockets. Every v0.9.1 golden vector, `framing.rs`, the codec, `benches/server.rs`, and every pre-existing suite unchanged; no dependency. Recorded deviation from the design's criterion 10: one `[[test]]` line in `Cargo.toml`, as every server suite has | `SERVER-PROTOCOL-VERSION-DESIGN`, `SERVER-QUERY-LAYER`, `BINCODE-ENCODING-STABILITY` | `SERVER-001` (v0.10.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests (333 prior + 4: `protocol.rs`'s `protocol_version_is_the_one_the_table_names` and both pinned-bytes tests extended with the `Hello` golden vectors; `mod.rs`'s `dispatch_never_routes_hello_to_a_store`, `hello_is_answered_unauthenticated_with_the_min_version`, `hello_version_zero_and_a_second_hello_are_malformed_but_not_fatal`) plus every integration suite including the new `tests/server_protocol_version.rs` (4/4: pre-auth `min` negotiation, version 0 / late hello `Malformed` with the connection open and a silent client served, `SchemaDrivenClient` negotiating 2 on all three domains, an unknown request index closing the connection with no reply); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`; three intra-doc links in the new `src/server/mod.rs` section were rewritten to `crate::server::protocol` paths so they resolve); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, `tests/server_protocol_version.rs`, one `Cargo.toml` `[[test]]` entry, and docs; the resolution pointers in place (`SERVER-001` open question, `STORAGE-018` non-goal and open question, `ADR-0010`, `ADR-0021`, `SERVER-QUERY-LAYER-DESIGN`, `PROJECT-STATUS` item 33) | Implemented | PR #126 |
| `SERVER-CLIENT-AUTH` | `SchemaDrivenClient` catching up with the server's own authentication gate, registered as `SERVER-001` v0.11.0 / FR-021: `connect_authenticated(addr, token)` sends `Request::Authenticate` between the `Hello` and the `DescribeSchema` — the token has to be a constructor argument because `AUTH-FR-002` gates the schema fetch itself, so plain `connect` cannot succeed on an `AuthConfig`-configured server (it now fails with the server's own `Unauthenticated` instead of `UnexpectedResponse("Schema")`, the one change to an existing path); `authenticate(&mut self, token)` re-presents a token mid-connection, changing the class in both directions, a rejected token changing nothing; neither learns the granted `TokenClass` (the server answers with a bare `Ok`); both no-ops on a server with no tokens (`AUTH-FR-007`). No wire, protocol-version, server, or `Cargo.toml` change — `Authenticate` is a version-1 variant. Two tests appended to `tests/server_auth_integration.rs`, beside the raw-protocol coverage the client must match. Taken from `ADR-0022`'s named revisit trigger as a bounded, additive completion of FR-015/FR-016 — no new ADR or design document, per the v0.8.0 precedent | `SERVER-PROTOCOL-VERSION`, `SERVER-AUTH`, `SERVER-QUERY-LAYER` (v0.5.0, the client) | `SERVER-001` (v0.11.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests unchanged plus every integration suite including `tests/server_auth_integration.rs` now 7/7 (5 prior + `schema_driven_client_authenticates_at_connect_and_can_change_class_later`, `schema_driven_client_authentication_is_a_no_op_without_configured_tokens`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/client.rs`, `tests/server_auth_integration.rs`, and docs — no `Cargo.toml`, protocol, or server change; the resolution pointers in place (`SERVER-001` open question, `ADR-0022` revisit trigger, `SERVER-PROTOCOL-VERSION-DESIGN` open question, `PROJECT-STATUS` item 70) | Implemented | PR #128 |
| `SERVER-CLIENT-TLS` | `SchemaDrivenClient` catching up with the server's own transport encryption, registered as `SERVER-001` v0.12.0 / FR-022: `connect_with(addr, ConnectOptions)` as the general constructor (`connect`/`connect_authenticated` delegate to it), `ConnectOptions::tls(ClientTlsConfig)` wrapping the TCP connection in a `rusty_tls::TlsStream` before the `Hello` so the `Hello`, any token, the schema fetch, and every request travel encrypted; `ClientTlsConfig::new(server_name, TrustPolicy)` is the explicit client-side trust configuration `ADR-0014`'s Consequences named, with `rusty_tls::TrustPolicy` re-exported; `ClientError::Tls` for a server name or policy `rusty_tls` rejects before any I/O, handshake failures surfacing under the `Hello` as `Frame(Io(..))`. One internal stream (a `TlsStream` cannot be split by `try_clone`, the server's own v0.9.0 finding), each frame assembled before a single `write_all` so the plaintext path still does one write per request. Named, not mitigated: a TLS client against a plaintext server can block rather than fail. No wire, protocol-version, server, or `Cargo.toml` change. Three tests appended to `tests/server_tls_integration.rs`, beside the raw `rusty_tls::TlsStream` coverage the client must match. A bounded, additive completion of FR-015/FR-019 — no new ADR or design document, per the v0.8.0/v0.11.0 precedent | `SERVER-CLIENT-AUTH`, `SERVER-TLS`, `SERVER-QUERY-LAYER` (v0.5.0, the client) | `SERVER-001` (v0.12.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests unchanged plus every integration suite including `tests/server_tls_integration.rs` now 8/8 (5 prior + `schema_driven_client_connects_over_tls`, `schema_driven_client_authenticates_over_tls`, `schema_driven_client_tls_failures_are_errors_not_hangs`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/client.rs`, `tests/server_tls_integration.rs`, and docs — no `Cargo.toml`, protocol, or server change; the resolution pointers in place (`SERVER-001` v0.9.0 open question and Security section, `ADR-0014` Consequences, `PROJECT-STATUS` item 71) | Implemented | PR #130 |
| `SERVER-MTLS-DESIGN` | A design answering `ADR-0014`'s first revisit trigger (*mTLS becomes a real requirement now that this crate owns TLS natively*) and `SERVER-AUTH-DESIGN`'s open question (*would mTLS replace the token scheme or layer alongside it*): **layer** — the client certificate is an *admission* gate, the token still decides *class*. Proposes one optional input on each end and nothing else: `TlsConfig::new_with_client_auth(chain, key, client_ca_roots)` / `from_pem_files_with_client_ca` / `from_env` honoring an optional `SERVER_TLS_CLIENT_CA_PATH` (set without chain/key → a startup error, never a silent downgrade), built on `rusty_tls::TlsAcceptor::new_with_client_auth` so a client presenting no certificate, or one that does not chain to the operator's own CA roots, fails the handshake before any framed byte — `Authenticate` included — is read; `ClientTlsConfig::with_identity(chain, key)` / `with_identity_pem_files` on `rusty_tls::TlsStream::new_with_client_identity` so `SchemaDrivenClient` can reach such a server. `handle_connection`, `serve`, `AuthConfig`, `TokenClass`, the wire, `PROTOCOL_VERSION`, and `Cargo.toml` unchanged (the acceptor carries the policy; `rcgen` already issues a CA and a signed leaf for hermetic tests). Rejected: class derived from the certificate (the pinned `rusty_tls` exposes no peer certificate and this crate has no X.509 parser — named as the future revisit, with admission as its compatible base), certificate replacing tokens, optional client certificates. Explicitly out of scope: revocation (only client-side in `rusty_tls` today), issuance/distribution, rotation without restart. Requirements `MTLS-FR-001`–`008`, ten acceptance criteria, a verification plan on the existing `rcgen` footing preceded by a throwaway probe. Acceptance options: (a) as proposed, (b) hold for class-from-certificate (upstream accessor first), (c) close as not warranted | `SERVER-TLS`, `SERVER-CLIENT-TLS`, `SERVER-AUTH-DESIGN` | none yet (would be `SERVER-001` v0.13.0 / FR-023 at implementation) | `docs/design/SERVER-MTLS-DESIGN.md` and `docs/decisions/ADR-0023-server-mtls-proposal.md` written and cross-linked; every `rusty_tls` claim (both mTLS constructors' signatures, the absent peer-certificate accessor, client-side-only revocation) verified against the pinned `9fd2e27` source; the `rcgen` CA-issuance API (`signed_by`, `IsCa`, `ClientAuth`) verified against the vendored 0.13.2 source; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration; accepted as proposed (option (a)) after the design's throwaway probe confirmed the mechanism — CA-signed leaf round-trips, no identity `CertificateRequired`, wrong CA `BadSignature`, empty roots `InvalidClientCaRoots`, no hangs | Accepted | PR #133 (proposed), PR #134 (accepted) |
| `SERVER-MTLS` | The accepted `SERVER-MTLS-DESIGN` implemented and registered as `SERVER-001` v0.13.0 / FR-023: `TlsConfig::new_with_client_auth(chain, key, client_ca_roots)` (on `rusty_tls::TlsAcceptor::new_with_client_auth`), `from_pem_files_with_client_ca`, `from_env` honoring an optional `SERVER_TLS_CLIENT_CA_PATH` (set without the chain/key pair → `Some(Err(Io(NotFound)))` naming the variables, the decision table factored as `from_env_values` for a hermetic unit test), `requires_client_certificate()`; `ClientTlsConfig::with_identity` / `with_identity_pem_files` / `has_identity()` with a hand-written `Debug` that never prints the key, `connect_with` selecting `rusty_tls::TlsStream::new_with_client_identity`; `dog_server` names the variable and reports the mode. Admission only, layered under `AuthConfig` exactly as designed: `handle_connection`, `serve`, `AuthConfig`, `TokenClass`, the wire, `PROTOCOL_VERSION`, and `Cargo.toml` unchanged. Four tests appended to `tests/server_tls_integration.rs` on a throwaway `rcgen` CA (admitted; rejected without a certificate and with a foreign CA, server still healthy; admission then class with and without tokens; the PEM path both ends with a two-root CA file plus the `Io`/`Tls` construction errors) and one unit test. No deviation from the design | `SERVER-MTLS-DESIGN`, `SERVER-TLS`, `SERVER-CLIENT-TLS`, `SERVER-AUTH` | `SERVER-001` (v0.13.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 338 lib tests (337 + `tls_from_env_values_treats_a_client_ca_without_a_chain_and_key_as_an_error`) plus every integration suite including `tests/server_tls_integration.rs` now 12/12 (8 prior + `mtls_admits_a_client_whose_certificate_chains_to_the_configured_ca`, `mtls_rejects_a_client_without_a_certificate_or_with_one_from_another_ca`, `mtls_composes_with_auth_config_as_admission_then_class`, `mtls_pem_files_and_construction_errors`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/mod.rs`, `src/server/client.rs`, `src/bin/dog_server.rs`, `tests/server_tls_integration.rs`, and docs — no `Cargo.toml`, wire, protocol-version, or `handle_connection` change; every `SERVER-MTLS-DESIGN` acceptance criterion 1–10 recorded as holding in `SERVER-001`; the resolution pointers in place (`SERVER-001` v0.9.0 open question and Security section, `ADR-0012`/`ADR-0014` mTLS revisit triggers, `SERVER-AUTH-DESIGN` and `SERVER-TLS-DESIGN` open questions, `ADR-0023` implementation log) | Implemented | PR #135 |
| `SERVER-TRANSACTION-SESSION-DESIGN` | One design round answering `ADR-0013`'s two revisit triggers as two independently acceptable decisions. **Part A / `ADR-0024`** — a multi-round-trip transaction session *without* holding any lock across round trips: `Request::Begin`/`Commit`/`Rollback` (indices 11–13), `Response::Staged { index }` (11), `ErrorCode::{NoSession, SessionOpen, SessionFull}` (6–8), `PROTOCOL_VERSION = 3` — the first version-3 variants `ADR-0022` named as its own trigger, so `handle_connection` finally keeps the negotiated version and rules 3/4 get a branch; while a session is open an admitted `UpdateField` is *staged* in a per-connection `Vec<TransactionOp>` (capped at `MAX_STAGED_OPS`, nothing applied, no lock, no validation) and `Commit` runs `apply_transaction` on it exactly as one `Transaction`; reads see committed state only (no read-your-writes, named); `SchemaDrivenClient::begin()` → `Session` gated on `server_protocol_version() >= 3`. Rejected: a lock-held session (`ADR-0013`'s liveness reason, unchanged), stage-time validation, session state on `ConnectionStore`. **Part B / `ADR-0025`** — crash-atomic batches via an opt-in redo journal at the domain adapter (`with_journal(store, path)`): the validated batch is appended and `fsync`'d *before* the first slot write, replayed idempotently on the next open (no runtime deletion + fixed schema + idempotent overwrite is why redo suffices and no undo log is needed), torn tail discarded, checkpoint (`flush` then truncate) by `JOURNAL_CHECKPOINT_BYTES` and at open — `hybrid.rs`'s snapshot-plus-WAL discipline with the batch as the unit and the `.mmap` files as the snapshot. `TXNJRNL\0` + `crate::codec` entries; no store, format, lock, wire (beyond one appended `ErrorCode::Journal`), or `Cargo.toml` change. Rejected: undo log, two-phase commit across slot files, `flush`-after-batch, journaling single writes, placement in the store or on `serve`; always-on offered as an option. Requirements `SESS-FR-001`–`009` and `JRN-FR-001`–`009`, acceptance criteria and verification plans per part, the crash criterion by a file-copy replay pair. Acceptance options per ADR: `ADR-0024` (a) as proposed / (b) lock-held with timeout / (c) close; `ADR-0025` (a) as proposed, opt-in / (b) always-on / (c) close | `SERVER-TRANSACTION`, `SERVER-PROTOCOL-VERSION`, `SERVER-MTLS` (the preceding round) | none yet (would be `SERVER-001` v0.14.0 / FR-024 and v0.15.0 / FR-025 at implementation) | `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md`, `docs/decisions/ADR-0024-server-transaction-session-proposal.md`, and `docs/decisions/ADR-0025-crash-atomic-transactions-proposal.md` written and cross-linked; every claim about the current code (variant indices 0–10, the `Hello`/`Authenticate` intercept order, `apply_transaction`'s validate-then-apply under `with_exclusive`, the per-slot `COMMITTED` marker, `Flush` own-then-inner, `hybrid.rs`'s never-truncate discipline, `ProductionStore::flush` = `msync`) read from `main` `371b421`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration; both parts accepted as proposed ("1, 1") | Accepted | PR #137 (proposed), PR #138 (accepted) |
| `SERVER-TRANSACTION-SESSION` | The accepted `SERVER-TRANSACTION-SESSION-DESIGN` Part A / `ADR-0024` implemented and registered as `SERVER-001` v0.14.0 / FR-024 — the first protocol-version-3 change, `ADR-0022`'s own named trigger taken as predicted: `PROTOCOL_VERSION = 3`, `Request::Begin`/`Commit`/`Rollback` (11–13), `Response::Staged { index }` (11), `ErrorCode::NoSession`/`SessionOpen`/`SessionFull` (6–8), `MAX_STAGED_OPS = 4096`, the version table's row 3, seven golden vectors with every version-1/2 vector unchanged; `handle_connection` keeps the negotiated version per connection (1 for a silent client) and a per-connection staged buffer — an admitted `UpdateField` inside a session is pushed and answered `Staged { index }` with nothing applied and no lock taken, `Commit` runs `apply_transaction` on the buffer exactly as one `Transaction` (`Ok`, or `TransactionFailed` naming the staged index) and closes the session, `Rollback`/disconnect discards, the three are `Malformed` below version 3 (rule 3) and `Commit` joins the `ReadOnly` gate; `dispatch` maps them to `Unsupported`; `SchemaDrivenClient::begin()` → `Session` (`update` returning the staged index after the usual client-side capability checks, `commit`, `rollback`, `Drop` rolling back best-effort) refused below version 3 with no frame sent (rule 4), `ClientError::TransactionFailed`. No adapter, store, `ConnectionStore`, framing, codec, or `Cargo.toml` change; no benchmark (staging is a `Vec` push, commit is FR-018's measured path). Nine tests across three suites plus two unit tests; the unknown-index test moved from 11 to 14, as its own doc said it would. No deviation from the design | `SERVER-TRANSACTION-SESSION-DESIGN`, `SERVER-TRANSACTION`, `SERVER-PROTOCOL-VERSION`, `SERVER-CLIENT-AUTH` | `SERVER-001` (v0.14.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 340 lib tests (338 + `protocol.rs`'s `session_shapes_round_trip_through_the_codec` and `mod.rs`'s `dispatch_never_routes_session_requests_to_a_store`; `protocol_version_is_the_one_the_table_names` now pins 3 and both pinned-bytes tests carry the version-3 vectors) plus every integration suite — `tests/server_transaction_integration.rs` 10/10 (6 prior + `a_session_stages_writes_and_commits_them_as_one_batch`, `a_commit_failure_names_the_staged_index_and_applies_nothing`, `session_state_errors_leave_the_connection_open`, `a_read_only_connection_can_open_a_session_but_not_stage_or_commit`), `tests/server_protocol_version.rs` 5/5 (4 prior + `session_requests_are_malformed_below_protocol_version_3`; the unknown-index test moved from 11 to 14, as its own doc predicted), `tests/server_schema_driven_client.rs` 5/5 (4 prior + `sessions_stage_commit_and_roll_back_on_every_domain`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, the three test files, and docs — no `Cargo.toml`, adapter, store, or framing change; `benches/server.rs` untouched; every Part A acceptance criterion 1–8 recorded as holding in `SERVER-001`; the resolution pointers in place (`SERVER-001` v0.7.0 and v0.10.0 open questions, `ADR-0013` first trigger, `ADR-0022` first trigger, `SERVER-TRANSACTION-DESIGN` last open question, `ADR-0024` and design-doc implementation logs) | Implemented | PR #139 |
| `SERVER-TRANSACTION-JOURNAL` | The accepted `SERVER-TRANSACTION-SESSION-DESIGN` Part B / `ADR-0025` implemented and registered as `SERVER-001` v0.15.0 / FR-025: `src/server/journal.rs` (new) — `BatchJournal` (`TXNJRNL\0`, `u32` version 1, entries `[u32 len][crate::codec(Vec<TransactionOp>)]`; `open` replays every complete entry and drops a torn tail, `append` writes and `fsync`s, `needs_checkpoint`/`truncate` at `JOURNAL_CHECKPOINT_BYTES` = 1 MiB), `CheckpointFlush` (a server-layer trait for the three exclusive store types — no storage module touched), `JournalError`; `with_journal(store, path)` on all three adapters (replay → flush → truncate on open; inside `apply_transaction`'s existing `with_exclusive` closure: validate, append + `fsync`, apply, checkpoint past the threshold), `validate_batch`/`apply_batch` factored out so replay reuses the apply step; `SERVER_TXN_JOURNAL_PATH` in `dog_server`; `ErrorCode::Journal` (9) for a failed append with nothing applied — an appended variant, so `PROTOCOL_VERSION = 4` per rule 2 and a rule-3 downgrade to `Unsupported` below 4 (`downgrade_for_version`, the first use of the "nearest older shape" clause; the ADR named the index and gating but not the bump — recorded as a clarification). Redo suffices because every operation is an idempotent overwrite keyed by an undeletable id; placement at the adapter because only it can replay one operation. Opt-in: every unjournaled adapter is exactly v0.14.0. Seven unit tests (format, torn tail, foreign file, checkpoint; the file-copy replay pair — pre-batch files gain the batch, post-batch files are a no-op, a foreign journal is refused; the adapter-level checkpoint; the downgrade) and one integration test (a `Transaction` and a session `Commit` journaled before `Ok`; staging, a rejected batch, and a single `UpdateField` journal nothing). `benches/server.rs` gains `dog-jrnl-txn` latency/throughput rows, run on this session's container and recorded in `RESULTS.md` beside `dog-txn`: the `fsync` per batch is the whole story | `SERVER-TRANSACTION-SESSION-DESIGN`, `SERVER-TRANSACTION-SESSION`, `SERVER-TRANSACTION`, `PRODUCTION-STORE-PORTABILITY` (the `flush`/`msync` and per-slot `COMMITTED` durability the journal builds on) | `SERVER-001` (v0.15.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 347 lib tests (340 + `journal.rs`'s four — header, replay with a torn tail dropped, foreign file/unknown version, checkpoint threshold and truncate — `dog.rs`'s `with_journal_replays_onto_pre_batch_files_and_is_a_no_op_on_post_batch_files` and `a_journaled_adapter_checkpoints_past_the_threshold`, `mod.rs`'s `journal_error_code_is_downgraded_below_version_4`; `protocol_version_is_the_one_the_table_names` now pins 4 and the `Err` vector loop carries `Journal` at 9) plus every integration suite — `tests/server_transaction_integration.rs` 11/11 (10 prior + `a_journaled_server_journals_transactions_and_session_commits_only`), every other suite unchanged apart from the two `server_protocol_version() == 4` assertions; `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `cargo bench --features server,research --bench server` completes with the new `dog-jrnl-txn` rows (numbers in `RESULTS.md`); `git diff --stat` limited to `src/server/{journal,dog,order,employee,mod,protocol}.rs`, `src/bin/dog_server.rs`, `benches/server.rs`, three test files, `RESULTS.md`, and docs — no `Cargo.toml`, store, storage-format, framing, or `ConnectionStore`-signature change; every Part B acceptance criterion 1–6 recorded as holding in `SERVER-001`; the resolution pointers in place (`SERVER-001` v0.7.0 open question and Security section, `ADR-0013` second trigger, `TXN-FR-007`, `MULTI-FIELD-MMAP-DURABILITY-DESIGN`'s atomic-multi-field non-goal, `ADR-0025` and design-doc implementation logs) | Implemented | PR #141 |
| `SERVER-CLIENT-HELLO-FALLBACK` | `ADR-0022`'s third revisit trigger — a reconnect-without-hello fallback in the client library for a pre-hello server — taken at the owner's explicit call ("3": default-on) over the design's own "not while none is deployed" and the offered recommendation to close it; registered as `SERVER-001` v0.16.0 / FR-026. `SchemaDrivenClient::connect_with` treats the peer closing the connection under the `Hello` with no reply (an end-of-stream/reset/abort-class I/O error, the one shape a pre-hello server produces) as that server: one silent second connection with no `Hello`, `server_protocol_version()` reported as 1, `begin` refused with `Unsupported`. Bounded: at most once per connect, only under the `Hello`, only on that error class, never on a reply, and a second failure is returned as is; `ConnectOptions::require_hello()` restores the pre-v0.16.0 EOF error. `open_transport` factored so both attempts are identical (TCP, `TCP_NODELAY`, the TLS wrap and identity). The one change to an existing path: `connect` against a pre-hello server now succeeds; the named cost: a server that genuinely dies under the first frame costs one extra connect. One test in `tests/server_protocol_version.rs` with a pre-hello fake server built on the public `dispatch` (fallback, opt-out, bounded retry). No wire, server, `Cargo.toml`, or `PROTOCOL_VERSION` change; a bounded, additive client change, so no new ADR or design document | `SERVER-PROTOCOL-VERSION`, `SERVER-CLIENT-TLS` (the `open_transport` this factors) | `SERVER-001` (v0.16.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 347 lib tests unchanged plus every integration suite including `tests/server_protocol_version.rs` now 6/6 (5 prior + `a_pre_hello_server_is_reconnected_to_without_a_hello_unless_hello_is_required`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/client.rs`, `tests/server_protocol_version.rs`, and docs — no wire, server, `Cargo.toml`, or `PROTOCOL_VERSION` change; the resolution pointers in place (`ADR-0022` trigger and consequence, `SERVER-PROTOCOL-VERSION-DESIGN` open question and compatibility matrix, `SERVER-001` v0.10.0 open question, `client.rs` module docs) | Implemented | PR #143 |
| `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` | A design answering `ADR-0025`'s third revisit trigger (*the journal's `fsync` cost dominates a real workload — a group commit is the standard answer and a separate design*), fired by `RESULTS.md`'s FR-025 rows (latency +254 µs, throughput flat at ~3.3k batches/s from 1 to 64 connections because the `fsync` runs inside the store's exclusive section, stalling every other connection's batch, read, and single write). Proposes: the `fsync` leaves the exclusive section — validate with per-call reads (sound by the no-runtime-deletion invariant), append under the journal's own mutex taking a sequence, a leader/follower group commit (the first waiter with no sync in flight `sync_data`s once for everyone appended so far; no timer, no delay, no thread — a lone connection pays exactly today's one `fsync`), apply under `with_exclusive` in sequence order (a turn gate, free because durability is monotone — so replay order equals apply order and a crash cannot re-decide a winner a reader already saw), checkpoint only when no later entry is appended. Failure semantics per batch unchanged (an `fsync` failure fails the covered group with `ErrorCode::Journal`, nothing applied). Evidence from a throwaway probe on this container (never committed): per-batch `fsync` flat at ~4.6k/s at every thread count; group commit 5.3k / 8.5k / 23–26k / 29k at 1 / 4 / 32 / 64 threads with average groups of 1 / 2.3 / 12 / 16; a 100 µs commit delay doubles the group size, adds no throughput, and halves the lone thread. Rejected: a fixed commit delay (offered as option (b)), a committer thread, unordered apply, one journal per connection, prefix truncation. No format, store, wire, `PROTOCOL_VERSION`, lock-type, `Cargo.toml`, or unjournaled-path change; requirements `GRP-FR-001`–`008`, acceptance criteria 1–8 (hook-driven held-leader/grouping/failure tests, a many-thread ordering test, the file-copy replay pair reused, the `dog-jrnl-txn` rows re-measured). Acceptance options: (a) as proposed / (b) with a fixed commit delay / (c) close as not warranted | `SERVER-TRANSACTION-JOURNAL`, `SERVER-TRANSACTION-SESSION-DESIGN` | none yet (would be `SERVER-001` v0.17.0 / FR-027 at implementation) | `docs/design/SERVER-JOURNAL-GROUP-COMMIT-DESIGN.md` and `docs/decisions/ADR-0026-journal-group-commit-proposal.md` written and cross-linked; every claim about the current code (`apply_transaction`'s validate → lock journal → `append` (write + `sync_data`) → apply → checkpoint all inside `with_exclusive`, the never-contended journal mutex, `BatchJournal`'s format and `open`, `RESULTS.md`'s FR-025 rows) read from `main` `3987f9c`; the probe run twice and both runs recorded; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration ; accepted as proposed | Accepted | PR #145 (proposed), PR #153 (accepted) |
| `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` | A design answering `ADR-0024`'s first revisit trigger (*read-your-writes inside a session becomes wanted — a per-connection overlay on the read paths, a real design with a cost on every read*), the owner's second pick of four. Two facts fix the shape: `SESS-FR-007` (reads in a session see committed state) is a numbered requirement a version-3/4 client may rely on, and `ADR-0022`'s rules version shapes, not meanings — so the mode must be *asked for* with a shape, not switched on; and only `GetById` is keyed by id and carries fields, so only it can be overlaid exactly (`ScanField` is by position, `FilterEq` by value). Proposes: an appended `Request::BeginWith { flags: u32 }` (index 14, protocol version 5; `SESSION_READ_YOUR_WRITES = 1`; unknown bits `Malformed`; `flags: 0` is exactly `Begin`); while such a session is open the connection's own `GetById` is served as today and then overlaid by a pure function in `handle_connection` — last staged write per `(id, field)` wins, only for fields the record carries, only when the staged value's kind matches the committed one (a write that would fail at `Commit` never produces a misleading read); `NotFound` stays `NotFound`; set reads, plain `Begin` sessions, older clients, and every other connection unchanged (`SESS-FR-007` restated for them); cost linear in the buffer, only on the sessions and reads that asked. Client: `SchemaDrivenClient::begin_read_your_writes()` gated on ≥ 5 and `Session::get(id)` on every session — found while reading: a `Session` borrows the client mutably, so today no read at all is possible through the library while one is open. No new `Response`/`ErrorCode`, no store, adapter, `ConnectionStore`, or `Cargo.toml` change; the unknown-index probe moves to 15. Rejected: changing `Begin`'s meaning, gating on the negotiated version alone, a unit variant per option, placement on `ConnectionStore`, overlaying a kind-mismatched value. Requirements `RYW-FR-001`–`008`, acceptance criteria 1–7, verification plan. Acceptance options: (a) as proposed / (b) with `FilterEq` adjusted by the buffer too / (c) close as not warranted | `SERVER-TRANSACTION-SESSION`, `SERVER-PROTOCOL-VERSION` | none yet (would be `SERVER-001`'s next minor / FR at implementation — v0.17.0 / FR-027 if it lands before `ADR-0026`'s implementation) | `docs/design/SERVER-SESSION-READ-YOUR-WRITES-DESIGN.md` and `docs/decisions/ADR-0027-session-read-your-writes-proposal.md` written and cross-linked; every claim about the current code (`handle_connection`'s session intercept and the fall-through of `GetById` to `dispatch`, `Session`'s mutable borrow and `update`/`commit`/`rollback`-only surface, `Request` indices 0–13 and the unknown-index probe at 14, `PROTOCOL_VERSION = 4`) read from `main` `8edaf7e`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration ; accepted as proposed | Accepted | PR #147 (proposed), PR #153 (accepted) |
| `SERVER-MTLS-CLASS-DESIGN` | A design answering `ADR-0023`'s first revisit trigger (*class-from-certificate becomes wanted — first an upstream `rusty_tls` peer-certificate accessor, then an X.509 reading decision in this crate*) and `SERVER-MTLS-DESIGN`'s "Class from certificate" open question, the owner's third pick of four. Both halves decided: the upstream accessor is `TlsServerStream::peer_certificate_der()`, a mirror of the accessor the pinned `rusty_tls` already has on the client stream — an eight-line patch spelled out verbatim and *run* against this crate through a temporary local `[patch]` (discarded): after `complete_handshake()` the server sees exactly the client's leaf DER, and a client without a certificate fails the eager handshake with a typed `NoCertificatesPresented` (while the client's own eager handshake returns `Ok` under TLS 1.3 — so `ADR-0023`'s typed-client-error trigger stays armed, with that finding). The X.509 question is answered by not reading X.509: `AuthConfig` gains a certificate class map matched by **exact DER bytes** of the presented leaf (`with_certificate_class(der, class)`, `with_certificate_class_pem_file(path, class)`; `is_configured()` true with classes only; `Debug` prints counts), `handle_connection` completes the handshake eagerly before the frame loop and seeds `authenticated` from the classed leaf — a valid token later replaces it, an admitted leaf not in the map behaves exactly as today; `dog_server` reads `SERVER_AUTH_READ_ONLY_CLIENT_CERTS`/`SERVER_AUTH_READ_WRITE_CLIENT_CERTS` and refuses classes without `SERVER_TLS_CLIENT_CA_PATH`. No parser, no new dependency (the one `Cargo.toml` change is the `rusty_tls` rev bump), no wire or `TokenClass` change. Rejected: SPKI pinning (offered as option (b) — the first X.509 code in this crate for rotation convenience), subject/SAN patterns, class from the issuing CA, the map on `TlsConfig`, a lazy handshake, an immutable certificate class. Requirements `CLS-FR-001`–`008`, acceptance criteria 1–7, verification plan. The upstream repository could not be attached from this session (refused by the session's permission policy), so the implementation unit's first step — the `rusty_tls` PR — needs that permission or the owner applying the patch. Acceptance options: (a) as proposed / (b) with SPKI pinning / (c) close as not warranted | `SERVER-MTLS`, `SERVER-AUTH`, `SERVER-TLS` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-MTLS-CLASS-DESIGN.md` and `docs/decisions/ADR-0028-mtls-class-from-certificate-proposal.md` written and cross-linked; every claim about the current code (`handle_connection`'s `accept` then frame loop with the handshake under the first read, `authenticated` seeded from `auth.is_configured()` alone, `AuthConfig`'s two tokens and `subtle` comparison, the pinned `rusty_tls` `9fd2e27` having `TlsStream::peer_certificate_der` and `complete_handshake` on both streams but no server-side accessor) read from `main` and the pinned checkout; the patch probe run under `--features server` (2/2) and discarded; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration ; accepted as proposed — implementation gated on the upstream `rusty_tls` accessor | Accepted | PR #149 (proposed), PR #153 (accepted) |
| `SERVER-AUTH-AUDIT-DESIGN` | A design answering the last third of the gap `SERVER-AUTH-DESIGN` named and every security round since carried forward unchanged (*rate-limiting failed authentication attempts, locking out a connection after N failures, and any audit log of authentication attempts*), the owner's fourth pick of four. Proposes an audit record for every decision the server's three gates take — admission (the TLS handshake, with or without a client certificate), authentication (`Authenticate`, and a certificate-derived class if `ADR-0028` lands), authorization (every `Unauthenticated`/`Unauthorized` refusal) — plus `Disconnected`, and nothing else: no successful request, no token or any part of one, no certificate, no record id or value; the peer address and a Unix-seconds timestamp are the identifiers. `AuditSink` (one method) with `NoAudit` (the default, free), `StderrAudit`, and `FileAudit` (append-only, one documented `key=value` line per event, no `fsync`); the sink hangs on `AuthConfig::with_audit` — the policy object every gate already consults — so `serve`'s signature is unchanged (`ADR-0025`'s precedent against a fifth parameter); called at the existing gates after the decision and before the response, outside every lock; fail-open — a sink write failure drops the event, counts it, and prints one stderr notice per process, never failing or blocking a connection; the eager server-side handshake shared with `ADR-0028` (adopted by whichever lands first) so a rejected admission has a typed reason to record; `dog_server` reads `SERVER_AUDIT_LOG` (`stderr` | path). Rejected: a fifth `serve` parameter or a `ServeOptions` struct, a `log`/`tracing` facade (a dependency, and filterable — the wrong guarantee), access logging, failures-only, silent drop; fail-closed offered as option (b). Rate limiting and lockout stay named — now with the record they would be tuned from — as the ADR's first revisit trigger. No `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change; requirements `AUD-FR-001`–`008`, acceptance criteria 1–7 (including a secrecy assertion over every variant's line and the ordered event sequence for one connection), verification plan. Acceptance options: (a) as proposed / (b) fail-closed / (c) close as not warranted | `SERVER-AUTH`, `SERVER-TLS`, `SERVER-MTLS`, `SERVER-MTLS-CLASS-DESIGN` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-AUTH-AUDIT-DESIGN.md` and `docs/decisions/ADR-0029-auth-audit-log-proposal.md` written and cross-linked; every claim about the current code (the four gates in `handle_connection` and their order, `auth.check`'s constant-time comparison, the lazy handshake under the first read, `serve`'s four parameters, the binary's single startup `eprintln!`, no logging dependency in `Cargo.toml`) read from `main`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration ; accepted as proposed | Accepted | PR #151 (proposed), PR #153 (accepted) |
| `SERVER-JOURNAL-GROUP-COMMIT` | The accepted `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` / `ADR-0026` implemented and registered as `SERVER-001` v0.17.0 / FR-027: `src/server/journal.rs` gains `CommitGroup` — append under the journal's own mutex taking a sequence (`BatchJournal::append_unsynced`), a leader/follower `fsync` (the first waiter with no sync in flight leads, reads how far to sync as late as possible, syncs once for everything appended before its `sync_data`; followers wait on a condvar; no timer, delay, or thread), a turn gate so applies happen in journal order (free — durability is monotone), a checkpoint offered only to the last-appended batch and re-checked under the lock after the flush, v0.15.0's failure semantics per group (a failed sync refuses every batch it covered with `Journal`, nothing applied, the next batch syncs again), a turn guard that releases the gate on unwind, and a `#[cfg(test)]` pre-sync hook; the three adapters' journaled `apply_transaction` becomes validate-by-per-call-reads → `commit` with `with_exclusive` only inside the apply closure, the unjournaled path untouched. Measured: `dog-jrnl-txn` latency 304.3 µs (v0.15.0: 320.7) and 3,169 / 5,906 / 14,276 / 15,322 batches/s at 1 / 4 / 32 / 64 connections (v0.15.0: 3,182 / 3,843 / 3,395 / 3,337). No `Cargo.toml`, journal-format, storage-format, wire, `PROTOCOL_VERSION`, `TransactionalStore`, or `ConnectionStore` change; no deviation from the design | `SERVER-JOURNAL-GROUP-COMMIT-DESIGN`, `SERVER-TRANSACTION-JOURNAL` | `SERVER-001` (v0.17.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 352 lib tests (347 + `journal.rs`'s four `CommitGroup` tests — ordered apply across eight threads equals journal order, a held leader syncing once for three batches, a failed sync refusing its group and the next batch succeeding, a checkpoint deferred until no later batch is appended — and `dog.rs`'s `the_fsync_never_holds_the_store_lock`) plus every integration suite including `tests/server_transaction_integration.rs` now 12/12 (11 prior + `concurrent_journaled_transactions_replay_to_the_live_state`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `cargo bench --features server,research --bench server` re-run with the `dog-jrnl-txn` rows recorded in `RESULTS.md`; `git diff --stat` limited to `src/server/{journal,dog,order,employee,mod}.rs`, `src/bin/dog_server.rs` (docs), `tests/server_transaction_integration.rs`, `RESULTS.md`, and docs — no `Cargo.toml`, journal-format, storage-format, wire, `PROTOCOL_VERSION`, `TransactionalStore`, or `ConnectionStore` change; every acceptance criterion 1–8 recorded as holding in `SERVER-001`; the resolution pointers in place (`ADR-0025` third trigger, `SERVER-TRANSACTION-SESSION-DESIGN` Part B lock-discipline invariant, `ADR-0026` and design-doc implementation logs) | Implemented | PR #155 |
| `SERVER-SESSION-READ-YOUR-WRITES` | The accepted `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` / `ADR-0027` implemented and registered as `SERVER-001` v0.18.0 / FR-028 — the second version-5 change is the first: `Request::BeginWith { flags: u32 }` appended at index 14, `PROTOCOL_VERSION = 5`, `SESSION_READ_YOUR_WRITES = 1`, unknown bits `Malformed`, `flags: 0` exactly `Begin`, `Malformed` below 5 (rule 3), sent only after negotiating ≥ 5 (rule 4); while such a session is open the connection's own `GetById` is served as usual and then passed through a pure `overlay_staged` — last staged write per `(id, field)` wins, only for fields the record carries, only when the staged value's kind matches and the schema marks the field updatable (the `update`-capable tags read once at `BeginWith`) — so a missing id stays `NotFound` and a write `Commit` would refuse is never shown; set reads, plain sessions, older clients, and other connections keep committed-state reads; `dispatch` maps `BeginWith` to `Unsupported`. Client: `begin_read_your_writes()` gated on ≥ 5, `Session::get` on every session (the only read reachable while a session borrows the client), `Session::read_your_writes()`. One clarification recorded: read-only fields are not overlaid, for the reason the kind rule exists. No new `Response`/`ErrorCode` (so `downgrade_for_version` is unchanged), no store, adapter, `ConnectionStore`, or `Cargo.toml` change; the unknown-index probe moves to 15 | `SERVER-SESSION-READ-YOUR-WRITES-DESIGN`, `SERVER-TRANSACTION-SESSION`, `SERVER-PROTOCOL-VERSION` | `SERVER-001` (v0.18.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 353 lib tests (352 + `mod.rs`'s `overlay_staged_replaces_only_matching_updatable_fields`; the `BeginWith` golden vector, codec round trip, and dispatch check extend existing tests; `protocol_version_is_the_one_the_table_names` pins 5) plus every integration suite including `tests/server_transaction_integration.rs` now 15/15 (12 prior + `a_read_your_writes_session_sees_its_own_staged_writes_and_nobody_else_does`, `read_your_writes_never_shows_a_write_that_commit_would_refuse`, `begin_with_is_gated_by_version_and_refuses_unknown_flags`), `tests/server_schema_driven_client.rs` 6/6 (5 prior + `read_your_writes_sessions_see_their_own_staged_writes_through_the_client`), `tests/server_protocol_version.rs` 6/6 (the unknown-index probe moved to 15, the client gate asserted in the pre-hello test); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, three test files, and docs — no `Cargo.toml`, store, adapter, `ConnectionStore`, `dispatch`-signature, `Response`, or `ErrorCode` change; every acceptance criterion 1–7 recorded as holding in `SERVER-001`; the resolution pointers in place (`ADR-0024` first trigger and consequence, `SESS-FR-007`, `SERVER-TRANSACTION-SESSION-DESIGN` open question, `ADR-0027` and design-doc implementation logs) | Implemented | PR #157 |
| `SERVER-AUTH-AUDIT` | The accepted `SERVER-AUTH-AUDIT-DESIGN` / `ADR-0029` implemented and registered as `SERVER-001` v0.19.0 / FR-029: `src/server/audit.rs` (new) — `AuditEvent { at, peer, kind }` over the closed `AuditKind` set (`Admitted`, `HandshakeFailed`, `Authenticated`, `AuthenticationFailed`, `Refused`, `Disconnected`; never a token, certificate, id, or value), `Transport`, an exhaustive `RequestKind`, `AuditSink` (one method), `NoAudit` (default), `StderrAudit`, `FileAudit` (append-only, no `fsync`), the documented `key=value` line built in one place; `AuthConfig::with_audit` / `audit()` so `serve` keeps its four parameters; `handle_connection` takes the peer once, completes the TLS handshake eagerly (a refused admission is `HandshakeFailed` with the TLS error's text), records at its existing gates after the decision and before the response outside every lock, and emits exactly one `Disconnected` per admitted connection through a drop guard; fail-open — a failing sink drops, counts, and prints one notice per process; `dog_server` reads `SERVER_AUDIT_LOG` (`stderr` | path; unopenable is a startup error, the table factored for a unit test) and reports the mode. No `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, adapter, `ConnectionStore`, or `serve`-signature change; no deviation from the design. Rate limiting and lockout stay named | `SERVER-AUTH-AUDIT-DESIGN`, `SERVER-AUTH`, `SERVER-TLS`, `SERVER-MTLS` | `SERVER-001` (v0.19.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 356 lib tests (353 + `audit.rs`'s three — the documented line format per variant, no line can carry a token or a certificate, `FileAudit` appending across opens and a read-only handle dropping without failing) plus `dog_server`'s `audit_sink_from_follows_the_documented_table` and every integration suite including `tests/server_auth_integration.rs` now 10/10 (7 prior + `every_gate_decision_is_recorded_in_order_with_the_peer`, `unauthenticated_refusals_and_open_servers_are_recorded_as_designed`, `a_file_sink_appends_one_line_per_event_through_the_server`) and `tests/server_tls_integration.rs` now 13/13 (12 prior + `handshake_outcomes_are_audited_with_a_reason`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/audit.rs` (new), `src/server/mod.rs`, `src/bin/dog_server.rs`, two test files, and docs — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, adapter, `ConnectionStore`, or `serve`-signature change; every acceptance criterion 1–7 recorded as holding in `SERVER-001`; the resolution pointers in place (`SERVER-AUTH-DESIGN` gap line, `ADR-0012` consequence, `SERVER-001` non-goals line, `ADR-0029` and design-doc implementation logs) | Implemented | PR #159 |
| `SERVER-AUTH-RATE-LIMIT-DESIGN` | A design answering the remaining two thirds of the gap `SERVER-AUTH-DESIGN` named (*rate-limiting failed authentication attempts, locking out a connection after N failures*) and `ADR-0029`'s first revisit trigger, the owner's second pick of four after the audit log landed. Two deliberately unequal mechanisms: a **per-connection lockout** — after `MAX_AUTH_FAILURES = 5` failed `Authenticate`s on one connection the server answers the fifth `Unauthenticated` as today, records `LockedOut`, and closes the connection; on by default, a constant not a knob (the `MAX_STAGED_OPS` precedent), because five wrong tokens on one connection is a bug or an attack and a reconnect costs a legitimate client nothing — and an **opt-in per-peer failure budget** (`AuthConfig::with_rate_limit(RateLimit { failures, window })`): failures counted per peer IP over a fixed window in a table bounded by `MAX_TRACKED_PEERS` (expired then oldest evicted, so an address flood degrades toward no budget, never toward no service); an over-budget peer's `Authenticate` is refused before any comparison, answered `Unauthenticated`, recorded `Throttled`. Nothing on the wire changes — a throttled or locked-out attempt is indistinguishable from a wrong token, extending `SERVER-AUTH-DESIGN`'s own rule — so no `ErrorCode`, no `PROTOCOL_VERSION` change; no delays (a thread held on the attacker's schedule); state on `AuthConfig`, `serve` unchanged; `SERVER_AUTH_RATE_LIMIT=<failures>/<seconds>` in `dog_server`. `AuditKind`/`RequestKind` marked `#[non_exhaustive]` as they gain variants — a clarification of `ADR-0029`. Rejected: both opt-in and a typed `ErrorCode::RateLimited` (offered together as option (b)), delays, per-token or global budgets, token bucket, state on `serve`. Out of scope, named: connection and handshake floods (an accept-level design), distributed guessing across addresses (token strength still matters), NAT false positives (why the budget is opt-in). Requirements `RL-FR-001`–`008`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) both opt-in with a typed error at protocol 6 / (c) close as not warranted | `SERVER-AUTH-AUDIT`, `SERVER-AUTH` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-AUTH-RATE-LIMIT-DESIGN.md` and `docs/decisions/ADR-0030-auth-rate-limit-proposal.md` written and cross-linked; every claim about the current code (the `Authenticate` arm's check-and-continue with the connection open, `AuthConfig`'s shape and `Arc` sharing in `serve`, the audit enums without `#[non_exhaustive]`) read from `main` `fce0762`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration; accepted as proposed | Accepted | PR #161 (proposed), PR #166 (accepted) |
| `SERVER-SESSION-VALIDATE-ON-STAGE` | `ADR-0024`'s second revisit trigger and `ADR-0027`'s stage-time trigger, taken together as the bounded additive completion both described (no new ADR) and registered as `SERVER-001` v0.20.0 / FR-030: `Request::BeginWith` learns `SESSION_VALIDATE_ON_STAGE = 2` at **protocol version 6 with no new variant** — a flag bit is introduced at a version exactly as a variant is (clarification recorded against `ADR-0022`): below 6 the bit is unknown and `BeginWith` is `Malformed`, a client sends it only after negotiating ≥ 6; `ConnectionStore` gains `validate_op(&TransactionOp) -> Result<(), ErrorCode>`, implemented by every adapter as `validate_batch` on one operation with the journaled path's per-call existence read; in a validating session every `UpdateField` is validated as it is staged and a bad one is answered `Err` with the code `Commit` would have given (`RecordNotFound`, `Unsupported`, `Malformed`, `UnknownField`), nothing staged; the two bits compose. Client: `SessionOptions::new().read_your_writes().validate_on_stage()`, `begin_with(options)` gated per option, `Session::validate_on_stage()`, a refused `update` as `ClientError::Server`. No `Response`/`ErrorCode`, store, `serve`, or `Cargo.toml` change | `SERVER-SESSION-READ-YOUR-WRITES`, `SERVER-TRANSACTION-SESSION`, `SERVER-PROTOCOL-VERSION` | `SERVER-001` (v0.20.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 357 lib tests (356 + `dog.rs`'s `validate_op_reports_exactly_what_commit_would`; `protocol_version_is_the_one_the_table_names` pins 6) plus every integration suite including `tests/server_transaction_integration.rs` now 17/17 (15 prior + `a_validating_session_refuses_bad_writes_when_staged`, `the_validate_bit_is_unknown_below_protocol_6`; the unknown-flag probe moved from bit 2 to bit 4), `tests/server_schema_driven_client.rs` 7/7 (6 prior + `validating_sessions_report_bad_writes_at_update_through_the_client`), `tests/server_protocol_version.rs` 6/6 (the `begin_with` gate asserted in the pre-hello test); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/{protocol,mod,client,dog,order,employee}.rs`, three test files, and docs — no `Cargo.toml`, `Response`/`ErrorCode`, store, or `Cargo.toml` change; the resolution pointers in place (`ADR-0024` second trigger, `ADR-0027` stage-time trigger, `SERVER-TRANSACTION-SESSION-DESIGN` change history, `ADR-0022` / `SERVER-PROTOCOL-VERSION-DESIGN` flag-bit clarification) | Implemented | PR #163 |
| `SERVER-ACCESS-LOG-DESIGN` | A design answering `ADR-0029`'s second revisit trigger (*access logging is wanted — a volume and privacy decision, a second sink or a second event family*) and `SERVER-AUTH-AUDIT-DESIGN`'s own access-logging non-goal, the owner's fourth pick of four ("1, 2, 3, 4": land the rusty_tls accessor, rate limiting/lockout, stage-time validation, this round). Proposes a **second, independent sink family** — `AccessSink`/`AccessEvent` in a new `src/server/access.rs` — rather than folding into `AuditKind`, so an operator's choice to audit admission/authentication decisions never implies accepting per-request log volume and vice versa. `AccessEvent { at, peer, class, request: RequestKind (reused from audit.rs), outcome: Outcome::{Ok, Err(ErrorCode)} }` — kind and outcome shape only, never a record id, field, or value; its own documented line format distinct from the audit log's; `NoAccessLog`/`StderrAccessLog`/`FileAccessLog` mirroring the audit sinks' fail-open shapes. Hung on `AuthConfig::with_access_log` alongside the (possible) audit sink and rate limiter, keeping `serve` unchanged; fires once per *dispatched* request (never for `Hello`/`Authenticate`/a gate refusal — the audit log's territory, so the two streams are disjoint by construction); `SERVER_ACCESS_LOG` in `dog_server`, independent of `SERVER_AUDIT_LOG`. Rejected: extending `AuditKind` with a `Handled` variant (couples the two switches — exactly what this document exists to avoid), a verbosity level, logging the record id or full request/response (a data log, not an access log), a new `ServeOptions` (restates `ADR-0029`'s own deferred trigger rather than pulling it). Out of scope, named: payloads, timing, sampling, session/transaction correlation. Requirements `ACC-FR-001`–`008`, acceptance criteria 1–5 (including a test that both sinks configured together stay disjoint by kind), verification plan. Acceptance options: (a) as proposed / (b) fold into `AuditKind` instead / (c) close as not warranted | `SERVER-AUTH-AUDIT`, `SERVER-AUTH-RATE-LIMIT-DESIGN` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-ACCESS-LOG-DESIGN.md` and `docs/decisions/ADR-0031-access-log-proposal.md` written and cross-linked; every claim about the current code (`handle_connection`'s dispatch path unobserved, `AuthConfig`'s shape and `Debug`, `RequestKind`'s exhaustiveness in `audit.rs`) read from `main` `24690db`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 integration; accepted as proposed | Accepted | PR #165 (proposed), PR #166 (accepted) |
| `SERVER-MTLS-CLASS` | The accepted `SERVER-MTLS-CLASS-DESIGN` / `ADR-0028` implemented and registered as `SERVER-001` v0.21.0 / FR-031, once its upstream prerequisite (`CLS-FR-001`, `Rusty-Mill/rusty_mill` PR #148) merged: `Cargo.toml`'s `rusty_tls` rev bumped to that commit (no new dependency); `AuthConfig` gains `certificate_classes: Vec<(Vec<u8>, TokenClass)>`, `with_certificate_class(der, class)`, `with_certificate_class_pem_file(path, class)`, `pub(crate) class_for_certificate(der)` (exact byte equality), and `is_configured()` now considers certificate classes too; `handle_connection`'s already-eager TLS handshake now also reads `peer_certificate_der()` and seeds the connection's starting class from a matched leaf (`certificate_class` precedence: a match wins outright, otherwise `is_configured()` decides as before), a later valid `Authenticate` still replacing it; `AuthConfig`'s `Debug` prints per-class certificate counts, never bytes; `dog_server` reads `SERVER_AUTH_READ_ONLY_CLIENT_CERTS`/`SERVER_AUTH_READ_WRITE_CLIENT_CERTS` (`:`-separated PEM files) via the factored `certificate_classes_from_env_values`, refusing either without `SERVER_TLS_CLIENT_CA_PATH`. No wire, `PROTOCOL_VERSION`, `TokenClass`, `TlsConfig`, `ConnectionStore`, or store change; no deviation from the design | `SERVER-MTLS-CLASS-DESIGN`, `SERVER-MTLS`, `SERVER-AUTH-AUDIT` | `SERVER-001` (v0.21.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 360 lib tests (357 + `class_for_certificate_matches_by_exact_der_bytes_only`, `is_configured_is_true_with_only_a_certificate_class`, `auth_config_debug_prints_certificate_counts_not_bytes`) plus `dog_server`'s `certificate_classes_from_env_values_follows_the_documented_table` and every integration suite including `tests/server_tls_integration.rs` now 17/17 (12 prior + `a_classed_leaf_starts_at_its_class_with_no_authenticate`, `a_valid_token_replaces_the_certificate_class_an_invalid_one_does_not`, `certificates_only_server_leaves_an_unclassed_admitted_leaf_unauthenticated`, `with_certificate_class_pem_file_classes_every_block_and_surfaces_errors`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `cargo tree --features server` shows no new package name — the same five transitive crates, now sourced from the merged `Rusty-Mill/rusty_mill` monorepo at newer versions; `git diff --stat` limited to `Cargo.toml`/`Cargo.lock`, `src/server/mod.rs`, `src/bin/dog_server.rs`, `tests/server_tls_integration.rs` — no wire, `PROTOCOL_VERSION`, `TokenClass`, `TlsConfig`, or `ConnectionStore` change; every acceptance criterion 1–7 recorded as holding in `SERVER-001`; the resolution pointers in place (`ADR-0023`'s first trigger, `SERVER-MTLS-DESIGN.md`'s open question, `ADR-0028` and design-doc implementation logs) | Implemented | PR #166 |
| `SERVER-AUTH-RATE-LIMIT` | The accepted `SERVER-AUTH-RATE-LIMIT-DESIGN` / `ADR-0030` implemented and registered as `SERVER-001` v0.22.0 / FR-032: `MAX_AUTH_FAILURES = 5` per-connection lockout on by default (a counter never reset by success; on the fifth failure the server answers `Unauthenticated` as always, records `LockedOut { failures }`, and closes the connection); an opt-in per-peer budget, `AuthConfig::with_rate_limit(RateLimit { failures, window })`, backed by a `FailureTable` (`Mutex<HashMap<IpAddr, Window>>`, monotonic `Instant`s) bounded at `MAX_TRACKED_PEERS = 4096` (expired-then-oldest eviction on insert, fail-open on a poisoned mutex); an over-budget peer's `Authenticate` is refused before any comparison, recorded `Throttled { failures }`, and still counts toward the per-connection lockout; `AuditKind` gains `LockedOut`/`Throttled`, and `AuditKind`/`RequestKind` are marked `#[non_exhaustive]` (a clarification of `ADR-0029`); `dog_server` reads `SERVER_AUTH_RATE_LIMIT="<failures>/<seconds>"` via the factored `rate_limit_from_env_value`. No wire, `PROTOCOL_VERSION`, `Cargo.toml`, store, or `serve`-signature change; no deviation from the design | `SERVER-AUTH-RATE-LIMIT-DESIGN`, `SERVER-AUTH-AUDIT`, `SERVER-MTLS-CLASS` | `SERVER-001` (v0.22.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 364 lib tests (360 + `rate_limit_parse_accepts_valid_and_rejects_malformed_input`, `failure_table_tracks_each_peer_independently`, `failure_table_forgets_a_peer_once_its_window_elapses`, `failure_table_never_exceeds_max_tracked_peers`) plus `dog_server`'s `rate_limit_from_env_value_follows_the_documented_table` and every integration suite including `tests/server_auth_integration.rs` now 13/13 (10 prior + `the_fifth_failed_authenticate_locks_out_the_connection`, `four_failures_then_a_success_leaves_the_connection_open`, `a_throttled_peer_is_refused_even_with_the_correct_token_and_recovers_after_the_window`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/{audit,mod}.rs`, `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change; every acceptance criterion 1–8 recorded as holding in `SERVER-001`; the resolution pointers in place (`SERVER-AUTH-DESIGN.md`'s named gap, `ADR-0029`'s first revisit trigger and its `#[non_exhaustive]` clarification, `ADR-0030` and design-doc implementation logs) | Implemented | This PR |
| `SERVER-ACCESS-LOG` | The accepted `SERVER-ACCESS-LOG-DESIGN` / `ADR-0031` implemented and registered as `SERVER-001` v0.23.0 / FR-033: `src/server/access.rs` (new, public) — a second, independent sink family, deliberately not sharing a type with `audit.rs`: `AccessEvent { at, peer, class, request: RequestKind, outcome: Outcome }`, `Outcome::{Ok, Err(ErrorCode)}`, `AccessSink`, `NoAccessLog` (default), `StderrAccessLog`, `FileAccessLog` — the same fail-open shapes as the audit sinks; `AuthConfig::with_access_log`/`access_log()`, keeping `serve`'s signature unchanged. `handle_connection` records exactly one `AccessEvent` per dispatched request — every request reaching the final `match req` dispatch, session-management errors included — after the response is decided and outside every lock; `Hello`, `Authenticate`, and every gate-refused request are never logged here, only on the audit log, so the two streams are disjoint by construction. `dog_server` reads `SERVER_ACCESS_LOG` (`stderr` \| path), independent of `SERVER_AUDIT_LOG`, via the factored `access_sink_from`. No wire, `PROTOCOL_VERSION`, `Cargo.toml`, store, or `serve`-signature change; no deviation from the design | `SERVER-ACCESS-LOG-DESIGN`, `SERVER-AUTH-AUDIT`, `SERVER-AUTH-RATE-LIMIT` | `SERVER-001` (v0.23.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests (364 + `lines_follow_the_documented_format_and_are_distinct_from_audit_lines`, `no_line_can_contain_a_marker_value`, `file_access_log_appends_and_a_failing_file_drops_without_failing`) plus `dog_server`'s `access_sink_from_follows_the_documented_table` and every integration suite including `tests/server_auth_integration.rs` now 14/14 (13 prior + `access_log_and_audit_log_streams_stay_disjoint`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/{access (new),mod}.rs`, `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change; every acceptance criterion 1–5 recorded as holding in `SERVER-001`; the resolution pointers in place (`ADR-0029`'s second revisit trigger, `SERVER-AUTH-AUDIT-DESIGN.md`'s access-logging non-goal, `ADR-0031` and design-doc implementation logs) | Implemented | this PR |
| `SERVER-AUDIT-ADMITTED-CERT-FIELD` | `ADR-0029`'s fourth revisit trigger (*`ADR-0028` lands — `Admitted` may gain an explicit "classed by certificate" field*), taken now that FR-031 has, registered as `SERVER-001` v0.24.0 / FR-034 — a bounded, additive completion (the v0.8.0/v0.16.0/v0.20.0 precedent: no new ADR or design document). `AuditKind::Admitted` gains `classed_by_certificate: bool`, `true` exactly when `initial_class` came from a matched, configured certificate (`CLS-FR-004`'s precedence match) rather than the unauthenticated/`ReadWrite` default; `handle_connection` passes `certificate_class.is_some()`, the value the TLS arm already computes. `AuditEvent::line`'s `Admitted` arm gains ` classed_by_certificate=<true\|false>`. No new `Request`, `Response`, `ErrorCode`, wire, `PROTOCOL_VERSION`, `Cargo.toml`, store, or `serve`-signature change; every pre-existing `Admitted` literal updated | `SERVER-AUTH-AUDIT`, `SERVER-MTLS-CLASS` | `SERVER-001` (v0.24.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests unchanged plus every integration suite including `tests/server_tls_integration.rs` now 18/18 (17 prior + `admitted_records_classed_by_certificate_for_a_matched_leaf_only`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/{audit,mod}.rs`, `tests/server_auth_integration.rs`, `tests/server_tls_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change; the resolution pointer in place (`ADR-0029`'s fourth revisit trigger) | Implemented | this PR |
| `SERVER-SERVE-OPTIONS-DESIGN` | A design answering the trigger four ADRs named and each deferred (`ADR-0029` ×2, `ADR-0030`, `ADR-0031`: *a second cross-cutting `serve` option appears*/*a fourth or fifth cross-cutting `AuthConfig` knob appears*), evaluated directly for the first time rather than deferred a fifth. `AuthConfig` carries five independent concerns (tokens, certificate-derived classes, audit, rate limit, access log) under a name describing only the first; `TlsConfig` has sat outside it as `serve`'s own second parameter since v0.9.0. Proposes consolidation: `AuthConfig` renamed to `ServeOptions`, `TlsConfig` folded in as a field, `serve` drops from four parameters to three (`listener`, `store`, `options`); every existing builder method's name and behavior unchanged, one new `with_tls`; `TlsConfig` construction kept its own separately-fallible step, composed via `.with_tls(..)`, not folded into one fallible `from_env`. No new capability — `SRV-FR-006` requires the change behavior-preserving bit for bit, verified by the existing ~85-call-site test suite passing with a mechanical rename and zero assertion changes. Rejected: leaving `TlsConfig` separate (keeps the asymmetry this round exists to resolve), splitting into identity vs. observability/policy structs (recreates the "many `serve` parameters" shape `ADR-0025` first ruled against). Requirements `SRV-FR-001`–`006`, acceptance criteria 1–5, verification plan. Acceptance options: (a) as proposed / (b) consolidate but leave `TlsConfig` separate / (c) close as not yet warranted | `SERVER-AUTH-AUDIT`, `SERVER-AUTH-RATE-LIMIT`, `SERVER-ACCESS-LOG`, `SERVER-MTLS-CLASS` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-SERVE-OPTIONS-DESIGN.md` and `docs/decisions/ADR-0032-serve-options-proposal.md` written and cross-linked; every claim about the current code (`AuthConfig`'s field list and ~85 call-site count, `TlsConfig`'s shape, `rusty_tls::TlsAcceptor`'s own `Clone` derive, `serve`'s exact parameter passing) read from `main` `1fe23f9` and, for the upstream claim, `rusty_tls`'s own vendored source; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 doctests unchanged, `cargo test --all-features` 367 lib tests unchanged; accepted as proposed, `ADR-0032` option (a), no changes requested | Accepted | PR #169 (proposed), PR #170 (accepted) |
| `SERVER-SERVE-OPTIONS` | The accepted `SERVER-SERVE-OPTIONS-DESIGN` / `ADR-0032` implemented and registered as `SERVER-001` v0.25.0 / FR-035: `AuthConfig` renamed `ServeOptions` throughout `src/server/mod.rs`, gaining `tls: Option<TlsConfig>` with `with_tls`/`tls()` in the same builder shape every other opt-in concern already has; `TlsConfig` construction stays its own separately-fallible step, folded in via `.with_tls(..)` after the caller handles its `Result`. `serve` drops from four parameters to three; `handle_connection` takes one `options: &ServeOptions`, reading `let tls = options.tls();` in place of its former second parameter — every downstream `match tls { .. }` site unchanged under Rust's match ergonomics. A straight rename, no deprecation shim: no `AuthConfig` alias or re-export survives. `ServeOptions` drops its unused `Clone` derive (no call site anywhere cloned a value of this type, checked directly; `TlsConfig` did not implement `Clone` either, and `serve` needs no `Clone` bound since it moves the value once into `Arc::new`). Every internal caller updated across sixteen files — `src/bin/dog_server.rs`, `benches/server.rs`, eight test files — with zero assertion changes anywhere. No `Cargo.toml`, wire, `PROTOCOL_VERSION`, or store change; no deviation from the design | `SERVER-SERVE-OPTIONS-DESIGN` | `SERVER-001` (v0.25.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests unchanged, every integration suite unchanged (`tests/server_tls_integration.rs` 18/18, `tests/server_auth_integration.rs` 14/14); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); `git diff --stat` across all sixteen touched files limited to `use`/type-name/parameter-passing lines — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, or store change; every acceptance criterion 1–5 recorded as holding in `SERVER-001`; the resolution pointer in place (the trigger `ADR-0029` first named, `ADR-0030`/`ADR-0031` each restated) | Implemented | PR #171 |
| `SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN` | A design answering the owner's "Future Growth here we come" / "Real transactions" pick from `docs/FUTURE-GROWTH.md`'s "Path to SQLite/DuckDB parity" section, scoped down from that document's own "MVCC or log-based design" framing to a bounded, session-machinery-native slice: `SESS-FR-007`'s second half, left standing on purpose by `ADR-0027`'s read-your-writes (which closed only "does a session see its own pending writes," not "has anyone else's commit landed since I looked"). Proposes optimistic read-set validation: a third `BeginWith` bit, `SESSION_SNAPSHOT_ISOLATION`, protocol version 7; every session `GetById` records the raw, pre-overlay committed value into a per-session read set (bounded at `MAX_TRACKED_READS = 4096`, the `MAX_STAGED_OPS` precedent); at `Commit`, `ConnectionStore::apply_transaction` (gaining one parameter, `read_set`) re-checks every tracked key inside the same exclusive section its own write validation and apply already use, refusing the whole batch atomically on any mismatch (`Response::TransactionFailed { index: 0, code: Conflict }`, the `ErrorCode::Journal` sentinel-index precedent reused). No storage-engine change, no phantom-read protection, no `FilterEq`/`ScanField` isolation — named as explicit non-goals, not silently assumed. Rejected: holding a read lock for the session's lifetime (blocks every writer for as long as any session is open, exactly what `ADR-0024` designed the session mechanism to avoid), full MVCC (the real "multi-year effort" `docs/FUTURE-GROWTH.md` itself names, disproportionate to what has actually been asked for and the first round that would touch `src/store/**`). Requirements `ISO-FR-001`–`008`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) track only the single most recent read instead of a full read set / (c) close as not warranted | `SERVER-TRANSACTION-SESSION`, `SERVER-SESSION-READ-YOUR-WRITES`, `SERVER-JOURNAL-GROUP-COMMIT` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN.md` and `docs/decisions/ADR-0033-session-snapshot-isolation-proposal.md` written and cross-linked; every claim about the current code (`SESS-FR-007`'s exact wording, the `GetById` session-intercept's raw-then-overlay order, `apply_transaction`'s current signature and each adapter's exclusive section, `ErrorCode::Journal`'s sentinel-index precedent, `outcome_of`'s exhaustive match) read from `main` `649d708`; docs-only, `cargo test` (default features) 136 lib + 2 doctests unchanged; accepted as proposed, `ADR-0033` option (a), no changes requested | Accepted | PR #173 (proposed), PR #174 (accepted) |
| `SERVER-SESSION-SNAPSHOT-ISOLATION` | The accepted `SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN` / `ADR-0033` implemented and registered as `SERVER-001` v0.26.0 / FR-036: a third `BeginWith` bit, `SESSION_SNAPSHOT_ISOLATION = 4`, at protocol version 7 (`PROTOCOL_VERSION = 7`, the table gains row 7, `ErrorCode::Conflict` at index 10, downgraded to `Unsupported` below 7). While such a session is open, every `GetById` records the raw, pre-overlay committed `(id, field) → value` pairs into a per-connection `snapshot_reads` map (`record_read_set`, mirroring `overlay_staged`'s own shape; bounded at `MAX_TRACKED_READS = 4096`, past which a *new* key is simply not added while an already-tracked one keeps updating). `ConnectionStore::apply_transaction` gains a `read_set: &[(RecordId, FieldRef, ScanValue)]` parameter, empty when unused; each of `DogConnectionStore`/`OrderConnectionStore`/`EmployeeConnectionStore` re-checks every tracked key against current state inside the same `with_exclusive`/`with_journal` section its own write validation and apply already use, refusing the whole commit atomically with `(0, ErrorCode::Conflict)` on any mismatch — nothing applied. The three `BeginWith` bits compose independently. Client: `SessionOptions::snapshot_isolation()`/`Session::snapshot_isolation()`, required version 7. No `Cargo.toml`, store, or `serve`-signature change; no deviation from the design | `SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN` | `SERVER-001` (v0.26.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 371 lib tests (367 + 4), `tests/server_transaction_integration.rs` 22/22 (17 + 5); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); every acceptance criterion 1–8 recorded as holding in `SERVER-001`; `SESS-FR-007`'s second half and the v0.7.0 "isolation levels beyond full serialization by lock" gap resolved by pointer | Implemented | this PR |
| `SERVER-SQL-SELECT-DESIGN` | A design answering the owner's "Future Growth here we come" / "SQL" pick from `docs/FUTURE-GROWTH.md`'s "Path to a server / query layer" ("the query language itself") and "Path to SQLite/DuckDB parity" ("SQL. A parser, a query planner, a cost-based optimizer, and an execution engine... a multi-year effort on its own") sections, narrowed the same way `ADR-0033` narrowed full MVCC: a bounded, read-only `SELECT` subset, parsed client-side (real tokenizing/parsing, not string splitting) and compiled to a new full-table-scan primitive, `ConnectionStore::scan_all` plus a new `crate::generic::query` trait (`AllIds<R>`) forwarded through `Reversed`/`Symmetric` exactly as `GetById`/`FilterEq`/`ScanField`/`UpdateField`/`Neighbors` already are — verified directly that neither `DogStore` nor `src/generic/query.rs` exposes an "every id" primitive today, and that both `MmapAgeStore` and `GenericMmapStore` already hold every record in a `HashMap` keyed by id, so this needs no storage-layer rework and touches none of `src/store/**`'s four `research`-gated backends. `Request::Query`/`Response::Rows` at protocol version 8; `=`/`!=` on every field kind, `<`/`<=`/`>`/`>=` on `U32`/`I64` only; `AND`-only conjunction; `LIMIT`; every described field queryable regardless of its `filter_eq`/`scan`/`update` capability flags (no index is needed, so no reason to restrict). No new `ErrorCode`; not overlaid by read-your-writes and not snapshot-isolation-tracked, the same "only `GetById`" line `RYW-FR`/`ISO-FR-002` already draw. Explicit non-goals: a query planner/cost-based optimizer (every `Query` is an unconditional full scan, even where `GetById`/`FilterEq` could answer more cheaply), joins, aggregation, `ORDER BY`, writes via SQL, prepared statements, a server-side parser. Requirements `SQL-FR-001`–`010`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) skip the parser, ship only a structured compound filter (not literally SQL) / (c) close as not warranted | `SERVER-QUERY-LAYER`, `SERVER-SCHEMA-DISCOVERY` (ADR-0011), `SERVER-SESSION-READ-YOUR-WRITES`, `SERVER-SESSION-SNAPSHOT-ISOLATION` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-SQL-SELECT-DESIGN.md` and `docs/decisions/ADR-0034-sql-select-subset-proposal.md` written and cross-linked; every claim about the current code (the exact `Request`/`Response`/`ConnectionStore` shape, that `MmapAgeStore`/`GenericMmapStore` already hold every record in a `HashMap`, that no "every id" primitive exists today, the `GetById`-forwarding pattern `Reversed`/`Symmetric` already use) read from `main` `cd46017` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) 136 lib + 2 doctests unchanged; accepted as proposed, `ADR-0034` option (a), no changes requested | Accepted | PR #176 (proposed and accepted) |
| `SERVER-SQL-SELECT` | The accepted `SERVER-SQL-SELECT-DESIGN` / `ADR-0034` implemented and registered as `SERVER-001` v0.27.0 / FR-037: `Request::Query { select, filter, limit }`/`Response::Rows { rows }` at protocol version 8 (`PROTOCOL_VERSION = 8`, table row 8), `Selection::All`/`Fields`, `Predicate`/`CompareOp` with `is_ordering()`. `ConnectionStore::scan_all` is backed by a new `AllIds`/`AllIds<R>` trait — added to `crate::production` (mirroring `TransactionalStore`'s server-facing-only shape) and `crate::generic::query` (mirroring `GetById`'s own per-capability-trait pattern, forwarded through `Reversed`/`Symmetric`/`MmapScanned`) — deliberately not added to `DogStore` or the domain-specific generic traits, since only the server layer needs it. `validate_query`/`evaluate_query` in `src/server/mod.rs` do the filter/project/limit work once, reused identically by all three adapters; every schema-described field is queryable regardless of its `filter_eq`/`scan`/`update` flags (`Dog::breed`, every flag `false`, is the concrete test). No new `ErrorCode` — `UnknownField`/`Malformed` cover every rejection. The SQL text itself is parsed entirely client-side (`src/server/sql.rs`, new — a real tokenizer/recursive-descent parser, no server-side counterpart) against the already-discovered `DomainSchema`; `SchemaDrivenClient::query`/`Session::query` are the only entry points, gated to protocol version 8. `Query` is never overlaid by read-your-writes and never snapshot-isolation read-set tracked, the same line `RYW-FR`/`ISO-FR-002` already draw for `GetById`. No `Cargo.toml`, store, or `serve`-signature change; no deviation from the design | `SERVER-SQL-SELECT-DESIGN` | `SERVER-001` (v0.27.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 389 lib tests (371 + 18), `tests/server_sql_integration.rs` 14/14 (new); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); every acceptance criterion 1–10 recorded as holding in `SERVER-001`; `docs/FUTURE-GROWTH.md`'s "SQL" item narrowed and resolved by pointer, a full parser/planner/optimizer restated as the real, disproportionate, still-open answer | Implemented | this PR |
| `SERVER-SQL-AGGREGATE-DESIGN` | A design answering the owner's "Future Growth here we come" / "Aggregation / GROUP BY" pick from `docs/FUTURE-GROWTH.md`'s "Path to SQLite/DuckDB parity" section ("A query optimizer for aggregation (`GROUP BY`, `AVG`, multi-table joins) — DuckDB's core identity is vectorized execution over exactly this; nothing comparable exists here"), narrowed to aggregation alone the same way `ADR-0034` narrowed "SQL" — leaves multi-table joins as that document's own separate, still-open item. Extends `Request::Query`/`ADR-0034` rather than duplicating it: `GROUP BY <field>(, <field>)*` and `SELECT`-list aggregate calls (`COUNT(*)`, `SUM`/`AVG`/`MIN`/`MAX(<field>)`) parsed client-side by extending `src/server/sql.rs`'s existing grammar, compiled to a new `Request::Aggregate`/`Response::Groups` pair at protocol version 9, reusing `Predicate`/`CompareOp` unchanged for `WHERE`. Verified directly that `ConnectionStore::scan_all` (`ADR-0034`) already returns every filtered row's full field set, so this design needs **zero new storage-adjacent primitives** — grouping/reduction is one new centralized pure function (`evaluate_aggregate`) over data already in hand. One new `ScanValue::F64` variant carries `AVG`'s result; `COUNT` is `COUNT(*)`-only since this schema has no `NULL` concept, making `COUNT(field)` and `COUNT(*)` provably identical. A key matching zero rows never appears as a group; `GROUP BY` with zero aggregate functions is allowed (the natural `DISTINCT`-equivalent degenerate case). No new `ErrorCode`; not overlaid by read-your-writes, not snapshot-isolation-tracked, the same line `RYW-FR`/`ISO-FR-002`/`SQL-FR-009` already draw. Explicit non-goals: `HAVING`, a query planner/cost-based optimizer (inherited unchanged from `ADR-0034`), `COUNT(DISTINCT field)`, nested/composite aggregate expressions, multi-table joins. Requirements `AGG-FR-001`–`010`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) accept without `AVG`/`ScanValue::F64` — `COUNT`/`SUM`/`MIN`/`MAX` only / (c) close as not warranted | `SERVER-SQL-SELECT-DESIGN`, `SERVER-SQL-SELECT` | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-SQL-AGGREGATE-DESIGN.md` and `docs/decisions/ADR-0035-sql-aggregate-proposal.md` written and cross-linked; every claim about the current code (the exact `Request::Query`/`Response::Rows`/`Predicate`/`CompareOp` shape, that `scan_all` already returns every filtered row's full field set, the declaration indices `Request::Query` (15)/`Response::Rows` (12) occupy, that no `ScanValue` variant represents an absent/`NULL` value) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0035` option (a), no changes requested | Accepted | PR #178 (proposed), PR #179 (accepted) |
| `SERVER-SQL-AGGREGATE` | The accepted `SERVER-SQL-AGGREGATE-DESIGN` / `ADR-0035` implemented and registered as `SERVER-001` v0.28.0 / FR-038: `Request::Aggregate { group_by, filter, aggregates, limit }`/`Response::Groups { groups }` at protocol version 9 (`PROTOCOL_VERSION = 9`, table row 9), `AggregateFn::{Count,Sum,Avg,Min,Max}`, `AggregateSpec`/`AggregateGroup`, `ScanValue::F64` carrying only `AVG`'s result. `validate_aggregate`/`evaluate_aggregate`/`reduce`/`reduce_extreme` in `src/server/mod.rs` do the filter/bucket/reduce work once, reused identically by all three adapters, over `ConnectionStore::scan_all` (`ADR-0034`) unchanged — the zero-new-storage-primitive claim the design made held exactly. `group_by` empty is one implicit whole-table bucket that always exists, even holding zero rows (acceptance criterion 4, `SELECT COUNT(*) FROM t WHERE false`-equivalent still returns one group whose `Count` is `0`) — a real edge case this round's own implementation surfaced beyond the design's worked examples: `Min`/`Max` on that empty bucket have no real value to pass through, so they fall back to a `schema`-typed zero rather than panicking, `Sum`/`Avg` already well-defined at `0`/`0.0`. The SQL text is parsed entirely client-side, extending `src/server/sql.rs`'s existing grammar (`agg_call`/`group_by_clause`); `SchemaDrivenClient::query`'s return type widens to `QueryResult::{Rows, Groups}` — a deliberate, documented breaking change, no shim, matching this project's own `FR-035` precedent — routing to a new `query_aggregate` method that performs the functional-dependency check, the `SELECT *`-with-`GROUP BY` rejection, and the aggregate-field-kind check entirely client-side before any round trip, then translates `Response::Groups` back into the original `SELECT`-list column order. No `Cargo.toml`, store, or `serve`-signature change; no deviation from the design | `SERVER-SQL-AGGREGATE-DESIGN` | `SERVER-001` (v0.28.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 404 lib tests (389 + 15), `tests/server_sql_integration.rs` 29/29 (14 prior + 15); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); every acceptance criterion 1–8 recorded as holding in `SERVER-001`; `docs/FUTURE-GROWTH.md`'s aggregation/`GROUP BY`/`AVG` item narrowed and resolved by pointer, multi-table joins restated as the real, separate, still-open answer | Implemented | this PR |
| `SERVER-REMINDER-DOMAIN-DESIGN` | A design answering the owner's own question ("could we use this DB for `rusty_remind_me`?") — not sourced from `docs/FUTURE-GROWTH.md`. A scoping discussion found `rusty_remind_me`'s real surface (semantic search, schema-less memory content, an entity graph, provenance/history) genuinely out of scope for one round; narrows to the one slice that fits this crate's existing architecture with zero new engine capability — reminders themselves, a small fixed-schema record (`title`/`due_at_unix_ms`/`status`). Proposes a fourth domain, `Reminder`, on the already-accepted generic schema library (`crate::generic::production::GenericProductionStore`), departing from `Order`/`Employee`'s own precedent in two ways: front-door rather than `research`-gated (the library's first real, deployable appearance outside `research`), and `status` — not a plain number — as the durably-mutable `ScannableField`, so marking a reminder done needs no SQL round trip (safe because `Request::Query`/`Request::Aggregate` already support range search on any field regardless of `filter_eq` capability). No relation of either kind (the one combination no existing adapter has), so `ReminderProductionStack` needs no `Symmetric`/`Reversed` layer at all — the simplest domain shape this library supports. A real `reminder_server` binary is proposed alongside the adapter. No wire-protocol, `PROTOCOL_VERSION`, or `ErrorCode` change. Explicit non-goal, named plainly: `rusty_remind_me`'s own source was not read this session — the three-field guess is inferred from its MCP tool names alone, an open question for a later, separate integration unit to verify. Requirements `RMD-FR-001`–`009`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) accept but keep `Reminder` behind `research`, matching `Order`/`Employee` / (c) close as not warranted | `SERVER-SQL-SELECT`, `SERVER-SQL-AGGREGATE` (the generic library and `Query`/`Aggregate` this design reuses unchanged) | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-REMINDER-DOMAIN-DESIGN.md` and `docs/decisions/ADR-0036-reminder-domain-proposal.md` written and cross-linked; every claim about the current code (that `crate::generic` is already front-door/unconditional while every domain built on it is `research`-gated, `GenericMmapStore`'s exactly-one-`IndexedField`-plus-one-`ScannableField` structural constraint, that `UpdateField` is only ever implemented for `ScannableField` not `IndexedField`, that `Query`/`Aggregate` already filter/sort on any field regardless of capability flags) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0036` option (a), no changes requested | Accepted | PR #181 (proposed and accepted) |
| `SERVER-REMINDER-DOMAIN` | The accepted `SERVER-REMINDER-DOMAIN-DESIGN` / `ADR-0036` implemented and registered as `SERVER-001` v0.29.0 / FR-039: `Reminder { id, title, due_at_unix_ms, status }` (`src/generic/reminder.rs`, new, front-door — the generic schema library's first appearance outside its own `research`-gated reference material), `ReminderStatus::{Pending, Done, Snoozed, Cancelled}`. `status` — an enum, not a plain number, the shape every existing generic-schema domain uses — is the durably-mutable `ScannableField` (`StatusField`, encoded as its `u32` discriminant); `due_at_unix_ms` is the equality-filterable `IndexedField` (`DueAtField`); `title` is read-only over the wire. No relation of either kind, so `ReminderProductionStack = GenericMmapStore<Reminder, DueAtField, StatusField>` needs no `Symmetric`/`Reversed` layer at all — the simplest domain shape this library supports; `CheckpointFlush` implemented for it directly, unconditionally. `server::reminder::ReminderConnectionStore` (`src/server/reminder.rs`, new) implements `ConnectionStore` exactly as `OrderConnectionStore`/`EmployeeConnectionStore` already do, gated by `server` alone — matching `server::dog`'s precedent, not `server::order`/`server::employee`'s. `scan_field`/`update_field`/`apply_transaction`/`validate_op` on `status` validate the incoming discriminant before ever reaching the store (`ErrorCode::Malformed` on an unrecognized value) — the one genuinely new validation path this domain needed, the first enum-typed `ScannableField` this library has ever had (every prior enum field has been an `IndexedField`, validated only on `filter_eq`). `src/bin/reminder_server.rs` (new) mirrors `dog_server.rs`'s shape and every env-var-driven operational knob exactly, confirmed by a real `cargo run --bin reminder_server --features server` smoke check. `dispatch`/`serve` needed no change — already generic over `ConnectionStore`; no new `Request`/`Response`/`ErrorCode`/`PROTOCOL_VERSION`/wire change of any kind; no deviation from the design | `SERVER-REMINDER-DOMAIN-DESIGN` | `SERVER-001` (v0.29.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 412 lib tests (404 + 8: 3 `generic::reminder` + 5 `server::reminder`), `tests/server_reminder_integration.rs` 9/9 (new); `cargo test` (default features) 139 lib tests (136 + 3, the `generic::reminder` unit tests, run even under zero features — confirming genuine front-door status) + 2 doctests; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after — ten transient new warnings from this round's own module-header doc comments caught and fixed in place); every acceptance criterion 1–8 recorded as holding in `SERVER-001`; the owner's own `rusty_remind_me` question answered with a real, deployable domain, the schema-shape assumption against the real target left as this round's own named open question for a later integration unit | Implemented | this PR |
| `SERVER-ENTITY-DOMAIN-DESIGN` | A design answering the owner's "Start Unit 39: entities + entity_traverse design round" — the second round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Investigates `rusty_remind_me`'s `entity`/`entity_upsert`/`entity_traverse` tool names against this crate's existing relation machinery directly rather than assuming it fits, and finds a real gap: `Symmetric<S, R, Marker>` (the `Neighbors`-providing layer) has no forwarding impl re-exposing a *different* marker's `Neighbors` from an inner layer, unlike `Reversed` (which gained exactly that forwarding for `FR-012`, `Employee`'s directed-plus-symmetric combination) — so two independent symmetric relations on one record type isn't supported today without a `FR-012`-shaped fix to `Symmetric` itself, named precisely, not proposed this round. Proposes a fifth domain, `Entity` (`label`/`kind`/`mention_count`), one self-referential `SymmetricRelation` (`relates_to`) — `Dog`'s own shape, not `Employee`'s dual-relation one — front-door, `server`-gated alone, mirroring `Reminder`'s own precedent. Plus a genuinely new capability this crate has never had at any layer: `SchemaDrivenClient::traverse`, bounded breadth-first graph walking built entirely client-side over the existing `Request::Neighbors` RPC (`max_depth`/`max_nodes` both caller-supplied, a cycle-safe visited set required for correctness not just efficiency) — no new `Request`/`Response`/`PROTOCOL_VERSION`/`ErrorCode`, the same "new client-side capability, zero new wire primitive" shape `ADR-0034`'s SQL parsing already established. Requirements `ENT-FR-001`–`008`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) accept and also fix `Symmetric`'s forwarding gap this round, delivering multiple relation types now / (c) close as not warranted | `SERVER-REMINDER-DOMAIN-DESIGN`, `SERVER-REMINDER-DOMAIN` (the `rusty_remind_me`-motivated line this continues) | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-ENTITY-DOMAIN-DESIGN.md` and `docs/decisions/ADR-0037-entity-domain-proposal.md` written and cross-linked; every claim about the current code (that `Symmetric` has no forwarding `Neighbors` impl for a second marker while `Reversed` does, `Dog`'s `littermate_of`/`Employee`'s dual-relation shapes, that no multi-hop traversal exists anywhere in this crate's real engine — `benches/external_db.rs`'s `two_hop_neighbors`-style helpers are throwaway, non-reusable bench code) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0037` option (a), no changes requested | Accepted | PR #183 (proposed and accepted) |
| `SERVER-ENTITY-DOMAIN` | The accepted `SERVER-ENTITY-DOMAIN-DESIGN` / `ADR-0037` implemented and registered as `SERVER-001` v0.30.0 / FR-040: `Entity { id, label, kind, mention_count }` (`src/generic/entity.rs`, new, front-door), `EntityKind::{Person, Place, Organization, Concept, Event}`. `kind` is the equality-filterable `IndexedField` (`KindField`, not inverted the way `Reminder::status` was — `kind` is expected to change rarely if ever); `mention_count` is the durably-mutable `ScannableField` (`MentionCountField`); `label` is read-only over the wire. One self-referential `SymmetricRelation`, `RelatesTo` — `Dog::littermate_of`'s own shape — so `EntityProductionStack = Symmetric<GenericMmapStore<Entity, KindField, MentionCountField>, Entity, RelatesTo>`, one composition layer; `CheckpointFlush` implemented for it directly, unconditionally, delegating to `Symmetric`'s own `Flush` (which forwards to its inner `GenericMmapStore`). `server::entity::EntityConnectionStore` (`src/server/entity.rs`, new) implements `ConnectionStore` exactly as `ReminderConnectionStore` already does, gated by `server` alone; `filter_eq`/`update_field` on `kind` validate the incoming discriminant before ever reaching the store (`ErrorCode::Malformed` on an unrecognized value); `parent`/`children` `Unsupported` (no `ChildOf`), `neighbors` real. `SchemaDrivenClient::traverse` (`src/server/client.rs`, new) — bounded client-side breadth-first walking over the existing `Request::Neighbors`, both `max_depth`/`max_nodes` caller-supplied with no crate-side default, a `HashMap`-backed visited-with-depth guard for cycle safety and true shortest-path distances, `Err(ClientError::Unsupported("traverse"))` locally with no round trip when `relations.neighbors` is `false`. `src/bin/entity_server.rs` (new) mirrors `reminder_server.rs`'s shape exactly. `dispatch`/`serve` needed no change — already generic over `ConnectionStore`; no new `Request`/`Response`/`ErrorCode`/`PROTOCOL_VERSION`/wire change of any kind. As proposed, option (a): the `Symmetric`-forwarding gap for a second, independent relation type stays named but unfixed — no deviation from the design | `SERVER-ENTITY-DOMAIN-DESIGN` | `SERVER-001` (v0.30.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` all lib tests plus `tests/server_entity_integration.rs` 9/9 (new); `cargo test` (default features) 143 lib tests (139 + 4, the `generic::entity` unit tests, run even under zero features — confirming genuine front-door status) + 2 doctests; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); every acceptance criterion 1–8 recorded as holding in `SERVER-001` | Implemented | this PR |
| `SERVER-ENTITY-INTEGRATION-VERIFICATION` | A verification round answering the owner's "Start Unit 41: entities + entity_traverse integration verification round" — the third round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Reads the now-connected `rusty-remind-me` MCP server's real tool schemas (`remind_me_entity`/`remind_me_entity_upsert`/`remind_me_entity_traverse`) directly — a channel neither `ADR-0036` nor `ADR-0037` had — closing the open question both explicitly named: whether `rusty_remind_me`'s real shape matches this line's own three-field, one-relation guess. Findings: the traversal shape (bounded, bidirectional, relation-edge-based) was directionally right; the entity schema was not — real entities are addressed by `name`/`aliases` (not `Uuid`), `kind` is an open string (not a fixed enum), there is no real `mention_count` (only linked-memory lists), no entity-enumeration tool exists, and — most consequentially — `remind_me_entity_traverse`'s own `relation`-label filter proves multiple named relation types are real, already-exercised capability, confirming the `Symmetric`-forwarding gap `ADR-0037` named is not speculative. Proposes no engine change; three options offered: (a) revise `Entity`/`traverse` to match now / (b) accept as informational, no change this round / (c) deprecate `Entity`. Acceptance criteria 1–5, verification plan | `SERVER-REMINDER-DOMAIN-DESIGN`, `SERVER-REMINDER-DOMAIN`, `SERVER-ENTITY-DOMAIN-DESIGN`, `SERVER-ENTITY-DOMAIN` (the `rusty_remind_me`-motivated line this continues and whose own named open question it closes) | none — proposes no engine change | `docs/design/SERVER-ENTITY-INTEGRATION-VERIFICATION-DESIGN.md` and `docs/decisions/ADR-0038-entity-integration-verification.md` written and cross-linked; every finding traced to a specific real tool-schema field/description this session loaded via `ToolSearch`, not assumed; every claim about `Entity`/`traverse`'s own current shape re-verified against merged `src/generic/entity.rs`/`src/server/entity.rs`/`src/server/client.rs`; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted, `ADR-0038` option (a) — revise `Entity`/`traverse` to match the real shape, including the `Symmetric`-forwarding fix; (b)/(c) declined; the concrete mechanism follows as its own design round | Accepted | PR #185 (proposed and accepted) |
| `SERVER-ENTITY-V2-REDESIGN` | The concrete mechanism for `ADR-0038`'s accepted option (a) — the fourth round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Investigation found two real constraints: `RecordId = Uuid` is crate-wide (`protocol.rs`'s own doc comment), so `Entity` alone going `name`-keyed would break more than this round's mandate; `bincode`'s positional struct encoding means relation-type filtering can only reach the wire via new, appended `Request`/`Response` variants (`ADR-0022`'s own rules), never new fields on `Request::Neighbors`/`DomainSchema`. Proposes: `Entity` stays `Uuid`-keyed, gains a real equality-filterable `name` field; `kind` becomes an open `String` filling both `IndexedField`/`ScannableField` roles (retiring `mention_count`'s synthetic role); a second relation, `MentionedWith` (an honest mechanism-validation label, not a claimed real `rusty_remind_me` term), proves the `Symmetric`-forwarding fix (`FR-012`-shaped, now applied to `Symmetric`) over a nested `Symmetric<Symmetric<..>, ..>` stack; `PROTOCOL_VERSION` 10 adds `Request::NeighborsByRelation`/`ListRelationKinds`/`Response::RelationKinds`, all append-only, no existing variant's byte layout touched; `traverse` gains an optional relation filter, `None` unchanged from today. `aliases`/case-insensitive names/server-side traversal all explicitly deferred. Requirements `ENT2-FR-001`–`007`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) accept but defer the wire-protocol half (engine fix only, no protocol bump) / (c) close as not warranted | `SERVER-ENTITY-DOMAIN-DESIGN`, `SERVER-ENTITY-DOMAIN`, `SERVER-ENTITY-INTEGRATION-VERIFICATION` (the `rusty_remind_me`-motivated line this continues) | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-ENTITY-V2-REDESIGN-DESIGN.md` and `docs/decisions/ADR-0039-entity-v2-redesign.md` written and cross-linked; every claim about the current code (`RecordId = Uuid`'s crate-wide doc comment, `bincode`'s positional struct encoding, `Reversed`'s existing `Neighbors`-forwarding impl as the precedent `Symmetric` lacks, `GenericMmapStore`'s exactly-one-`IndexedField`-plus-one-`ScannableField` constraint) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0039` option (a), no changes requested. Implemented as `SERVER-001-FR-041` (v0.31.0): two real blockers caught before code was written, both resolved with the owner via `AskUserQuestion`, neither assumed away — `kind` could not fill both `IndexedField`/`ScannableField` roles (`ScanValue: Copy`, `GenericMmapStore`'s fixed-width mmap slots), so it stays read-only rather than retiring `mention_count`; the `Symmetric`-forwarding mechanism this line named since `ADR-0037` does not compile (`E0119`, confirmed directly with `rustc`), so a genuinely new primitive (`MultiSymmetric`/`MultiNeighbors`) was built instead of patching `Symmetric`; `label`'s identifier was also not renamed to `name` as the accepted prose described. Full sweep green (`fmt`/`clippy -D warnings`/`test`/`test --all-features`/`doc` at the 64-warning baseline) | Implemented | PR #185 |
| `SERVER-ENTITY-ALIASES` | Closes `ADR-0039`'s own paired `aliases`/case-insensitive-name-resolution Non-goals — the sixth round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Investigation of the real, merged `Entity` v2 shape found: `label` carries every capability flag `false` because `GenericMmapStore`'s two type parameters admit exactly one `IndexedField` marker, already occupied by `kind`; `GenericMmapStore`'s `records: HashMap` already holds the full struct and the record blob serializes `Vec<R>` whole, so a new plain field needs no mmap/blob change; the one real new limitation is `ScanValue` has no list variant, so `aliases: Vec<String>` cannot appear in any `GetById`/`Query` response this round at all — a genuinely new "durable but not wire-representable" category. Proposes a new secondary-index primitive, `NameIndex` (the `MultiSymmetric`/`MultiNeighbors` naming precedent applied to indices, rebuilt from records at construction, no new blob file since index keys are fully derivable unlike relation edges), normalizing by lowercase+trim, making `FilterEq` on `label` real against `label` or any alias — needing **no `PROTOCOL_VERSION` change**, since it reuses `Request::FilterEq`/`ScanValue::Str`/`Response::RecordList` exactly as they exist today (only a `DomainSchema` data value changes). Requirements `ENT3-FR-001`–`007`, acceptance criteria 1–7, verification plan. Options offered: (a) accept as proposed / (b) accept the lookup capability only, defer `aliases` as a field entirely / (c) close as not warranted | `SERVER-ENTITY-DOMAIN-DESIGN`, `SERVER-ENTITY-DOMAIN`, `SERVER-ENTITY-INTEGRATION-VERIFICATION`, `SERVER-ENTITY-V2-REDESIGN` (the `rusty_remind_me`-motivated line this continues) | none yet (would be `SERVER-001`'s next minor / FR at implementation) | `docs/design/SERVER-ENTITY-ALIASES-DESIGN.md` and `docs/decisions/ADR-0040-entity-aliases-and-name-lookup.md` written and cross-linked; every claim about the current code (`GenericMmapStore`'s single-`IndexedField`-marker constraint, its `records: HashMap` holding the full struct, the record blob's whole-`Vec<R>` serialization, `ScanValue`'s five-variant enum) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0040` option (a), no changes requested. Implemented as `SERVER-001-FR-042` (v0.32.0), landed as proposed with no deviation: `aliases` field (durable, no wire representation), `NameIndex`/`NameIndexed`/`FindByName` in `crate::generic` (rebuilt from records, no file of its own — confirmed by a directory-listing assertion), `GenericProductionStore::find_by_name`, `FilterEq` on `label` real against `label` or any alias, `DomainSchema` `label` `filter_eq: true`; **`src/server/protocol.rs` untouched** — no `PROTOCOL_VERSION` change, the first `SERVER-001` minor since v0.30.0 with none. Full sweep green (`fmt`/`clippy -D warnings`/`test` 144+2/`test --all-features` 423 + `server_entity_integration` 10/10/`doc` at the 64-warning baseline after fixing three own-doc-comment link warnings) | Implemented | PR #186 (proposed and accepted); implementation PR #187 |
| `SERVER-ENTITY-ALIASES-WIRE` | Closes the one piece `ADR-0040` deferred — reading `Entity::aliases` back over the wire — the seventh round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Investigation of the real merged shape found the variant itself routine (`ScanValue` derives everything `Vec<String>` satisfies; append at index 5, rule 1; every exhaustive-looking match has a safe fallthrough, checked site by site) and the compatibility story the real decision: every prior appended variant was a new request or rode only inside a gated response (`F64` inside protocol-9 `Groups`); a list inside `Response::Record`/`Rows`/`Schema` reaches protocol-1/8 requests a silent client can send. Proposes `ScanValue::StrList`/`ValueKind::StrList` at protocol 11 (the latter a reasoned divergence from `F64`'s no-`ValueKind` precedent — `aliases` is a stored field with a descriptor), `FIELD_ALIASES = 3` with every capability flag `false`, and rule-3 content stripping in `downgrade_for_version` — the single funnel every dispatched response already passes through — so a pre-11 client sees exactly the three-field `Entity` of `FR-042`. **The first content-rewriting downgrade in this protocol's history**, named plainly. Zero new client API. Requirements `ENT4-FR-001`–`006`, acceptance criteria 1–7, verification plan. Options: (a) accept as proposed / (b) separator-joined `Str`, no bump, lossy / (c) close as not warranted | `SERVER-ENTITY-ALIASES` (the field this reads back), `SERVER-PROTOCOL-VERSION-DESIGN` (rule 3, the load-bearing clause), `SERVER-SQL-AGGREGATE-DESIGN` (the `F64` precedent diverged from on one point) | `SERVER-001` v0.34.0 / `SERVER-001-FR-044` | `docs/design/SERVER-ENTITY-ALIASES-WIRE-DESIGN.md` and `docs/decisions/ADR-0041-aliases-wire-readability.md` written and cross-linked; every claim about the current code (`ScanValue`/`ValueKind`'s derives and variants, `downgrade_for_version`'s two existing arms and its single call site, each `ScanValue`/`ValueKind` match site's fallthrough, the three hardcoded version pins) read from `main` by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0041` option (a), no changes requested. Implemented as `SERVER-001-FR-044` (v0.34.0), landed as proposed: `ScanValue::StrList`/`ValueKind::StrList` at protocol 11, `FIELD_ALIASES = 3` all-`false`, the raw list in `GetById`/`Query`, and the protocol's first content-rewriting rule-3 downgrade proven over a real socket against a hand-negotiated `Hello { 10 }` client and a silent client (each sees exactly the `FR-042` three-field shape). One additive completion recorded, not folded in: `GROUP BY` a `StrList` field refused server- (`Malformed`) and client-side (`Sql`), the one path the design's own "never in `Groups`" invariant left unguarded. Wire-only — no `Cargo.toml`, `src/generic/store.rs`, or on-disk change. Full sweep green (`fmt`/`clippy -D warnings`/`test` 147+2/`test --all-features` 429 + `server_entity_integration` 14/14 + `server_protocol_version` 7/7/`doc` at the 64-warning baseline, no new warning) | Implemented | PR #188 (proposed and accepted); implementation this PR |
| `SERVER-ENTITY-SOURCE-VERIFICATION` | The owner's ordered pick "1b" — read `rusty_remind_me`'s *source*, not its tool schemas, and compare against the merged `Entity` shape — the eighth round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md`. Shallow-cloned `baileyrd/rusty_remind_me` at `29602f1`; read `entity.rs` (908 lines), the SQL schema/indexes, the traverse/id tests, the MCP handler, and the owner's own graph ADR. Ten findings, each cited to a file:line; three reframe earlier inferences: no relation vocabulary exists (free-form `TEXT` per triple — `ADR-0039`'s fixed-set model, not its placeholder strings, is the mismatch); edges are directed triples with timestamps, walked bidirectionally, returned with direction/hop (ours are symmetric, id-only); identity is a derived content-hash id (canonical-name lookup is one indexed hit; ours is two round trips). One unambiguous small delta: their normalization collapses internal whitespace, ours only trims — the exact version their own doc comment says they retired after it split one person into two. Proposes two bounded follow-ups (`ENT5-FR-001` normalization, zero migration; `ENT5-FR-002` a derived `entity_id(name) -> Uuid`, `RecordId` untouched, adds `sha2`) and names open-label/directed-edge/edges-with-hops divergences for a future round. Options: (a) accept, take both / (b) accept, normalization only / (c) informational only | `SERVER-ENTITY-INTEGRATION-VERIFICATION` (the same question asked of the schemas), `SERVER-ENTITY-V2-REDESIGN`, `SERVER-ENTITY-ALIASES`, `SERVER-ENTITY-ALIASES-WIRE` (every round carrying the "source unread" caveat this retires) | none yet (`SERVER-001`'s next minor / FR if (a) or (b) is accepted) | `docs/design/SERVER-ENTITY-SOURCE-VERIFICATION-DESIGN.md` and `docs/decisions/ADR-0042-entity-source-verification.md` written and cross-linked; every finding traced to a file:line in the `29602f1` checkout, not to a schema or memory; `sync/graph.rs` and the hub/API crates named as unread (out of scope), not skipped silently; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0042` option (a) — both follow-ups authorized as one unit, F3/F4/F5 named for a future round. Implemented as `SERVER-001-FR-043` (v0.33.0), landed as proposed: `NameIndex::normalize` collapses internal whitespace (zero migration); `entity_id(name) -> Uuid` derived from the normalized name, one-round-trip `GetById` proven over a real socket, `RecordId` untouched; `sha2` added — the first new *unconditional* dependency since `subtle` (this row's earlier "since `rusty_tls`" corrected: the optional ADR-0015 bench crates came between); every "source unread" caveat retired with a pointer to the findings. Full sweep green (`fmt`/`clippy -D warnings`/`test` 147+2/`test --all-features` 426 + `server_entity_integration` 11/11/`doc` at the 64-warning baseline after fixing one own-doc-comment link warning) | Implemented | PR #189 (proposed and accepted); implementation this PR |
| `SERVER-CLIENT-ECOSYSTEM` | The owner's ordered pick "3" — the client ecosystem, the first round in this session sourced from `docs/FUTURE-GROWTH.md` ("drivers for other languages, a CLI, general tooling") rather than the `rusty_remind_me` line, answering the revisit trigger `ADR-0010` set on day one ("a non-Rust or cross-language client becomes a real requirement"). Read the merged wire directly and found the reframing fact: the protocol is already trivially implementable in any language (4-byte LE framing; `bincode` fixint/LE — `u64` counts, `u32` enum indices, positional structs, `Uuid` as a 16-byte length-prefixed string; every rule in `src/codec.rs`'s doc; 41 golden vectors pin every variant), but is specified only in Rust doc comments and a `#[cfg(test)]` module. Two adjacent facts: the Rust client is not separable from the server (one `server` feature, `client.rs` importing the server's `TlsConfigError` and a private `sql`, `publish = false`); and `rusty_remind_me` is all Rust — no non-Rust consumer exists. Proposes, with zero wire change and zero new runtime dependency: a `client` Cargo feature `server` implies (`ECO-FR-001`–`003`); `SERVER-002` v0.1.0, a language-neutral wire specification, plus a checked-in plain-text conformance fixture the existing golden-vector tests read and enforce (`ECO-FR-004`–`006`); one stdlib-only Python reference client verified offline against the fixture and live against a real `Entity` server from a Rust test that fails loudly without `python3` (`ECO-FR-007`–`009`). gRPC/JSON gateway reconsidered per `ADR-0010` and declined. Options: (a) all three / (b) spec + fixture / (c) spec + fixture + `client` feature / (d) close as not warranted | `SERVER-QUERY-LAYER-DESIGN` (the Non-goal and revisit trigger), `BINCODE-ENCODING-STABILITY-DESIGN` (the pinned codec — "the precondition, not the client"), `SERVER-PROTOCOL-VERSION-DESIGN` (the table and rules a foreign client must speak), `SERVER-ENTITY-ALIASES-WIRE` (the rule-3 strip the live test proves from the other side) | `SERVER-002` v0.1.0; `SERVER-001` v0.35.1 (patch) | `docs/design/SERVER-CLIENT-ECOSYSTEM-DESIGN.md` and `docs/decisions/ADR-0043-client-ecosystem.md` written and cross-linked; every claim about the current code (the codec rules, the 41 vectors, the framing cap, the feature layout, `client.rs`'s imports, `publish = false`, `rusty_remind_me`'s language mix) read from `main` at `89d7c81` and the `29602f1` clone by direct inspection, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed, `ADR-0043` option (a), no changes requested. **Implemented as accepted — no deviation from the accepted text**: `client = ["dep:rusty_tls"]` with `server = ["client"]` (the server body moved verbatim to `src/server/serve.rs`, `client.rs`/`framing.rs`/`protocol.rs`/`pem.rs` compile under `client` alone, `cargo test --features client` proves it and is a CI step); `SERVER-002` v0.1.0 — the byte-level wire specification a non-Rust reader can implement from — with `tests/fixtures/wire-vectors.txt` (48 vectors, `name\tversion\thex`) written by and checked against every golden-vector test through `test_support::wire_fixture`, so the fixture cannot drift from the codec; a stdlib-only Python reference client (`clients/python/`, protocol 12, `Hello`/`DescribeSchema`/`DescribeRelations`/`GetById`/`FilterEq`/`Query`/`Aggregate`/`Update`/`Neighbors`/`Join`) verified offline against the fixture (`unittest`, a CI step) and live over a real socket against the `Entity` server at 12 and at a negotiated 10 (`tests/server_python_client.rs`, `server`-gated). Full sweep green (`fmt`/`clippy -D warnings`/`test` 147+2/`test --features client` 195+2 + `server_client_only` 2/2/`test --all-features` 439 + every suite incl. `server_python_client` 1/1/`doc` at the 64-warning baseline). `PROJECT-STATUS` item 38 closes | Implemented | PR #192 (proposed and accepted); implementation this PR |
| `SERVER-SQL-JOIN` | The owner's ordered pick "2" — `docs/FUTURE-GROWTH.md`'s "arbitrary joins," the last of the four-item queue, answering `ADR-0035`'s revisit trigger (*multi-table joins are ever taken up*). Read the consumer's nine real `JOIN`s (all relation joins — FK or link table, never a computed predicate) and the crate's two-type domain (`Reversed` stores no parent records, so `Customer` needs a store, not a wire variant). Reframed as two parts, two ADRs (the `SERVER-TRANSACTION-SESSION-DESIGN` precedent): **Part A / `ADR-0044`** — a relation join within one table (`FROM entity a JOIN entity b ON relates_to`), `Request::Join`/`DescribeRelations` + `Response::JoinedRows`/`Relations` at protocol 12, server-side index nested loop over the adapter's existing relation methods, one round trip for what costs `traverse` + N `GetById`s today, `describe_relations()` with a conservative default, SQL aliases and `JOIN … ON <relation>`, `QueryResult::Joined`, no new `ErrorCode`; **Part B / `ADR-0045`** — more than one table per connection (`serve_tables`, per-connection `Use`, `ListTables`, cross-table `Join`), proposed as gated direction because no second table exists. Theta joins declined with reasons. Options: `ADR-0044` (a) as proposed / (b) wire + server only / (c) plus indexed equi-joins / (d) close; `ADR-0045` (a) gated direction / (b) schedule `Order → Customer` now / (c) decline | `SERVER-SQL-SELECT`, `SERVER-SQL-AGGREGATE` (both deferred joins with the one-table reason), `SERVER-ENTITY-DOMAIN`/`SERVER-ENTITY-V2-REDESIGN` (`traverse`, the client-side composition Part A replaces for one hop), `SERVER-ENTITY-SOURCE-VERIFICATION` (the source reading behind "every consumer join is a relation join"), `SERVER-CLIENT-ECOSYSTEM` (`SERVER-002` will carry the new shapes) | Part A: `SERVER-001` v0.35.0 / `SERVER-001-FR-045`; Part B: none yet (gated on a second table, ADR-0045) | `docs/design/SERVER-SQL-JOIN-DESIGN.md`, `docs/decisions/ADR-0044-relation-join.md`, `docs/decisions/ADR-0045-multi-table-connections.md` written and cross-linked; every claim about the current code (`serve`'s one-store signature, `DomainSchema`'s shape, `sql.rs`'s ignored `FROM`, `Reversed`'s fields and impls, each adapter's relation methods) and about the consumer (the nine `JOIN`s, cited to file) read by direct inspection at `6fd4783`/`29602f1`, not assumed; docs-only, `cargo fmt --all -- --check` clean, `cargo test` (default features) unchanged; accepted as proposed — `ADR-0044` option (a), `ADR-0045` option (a) as gated direction, no changes requested. Part A implemented as `SERVER-001-FR-045` (v0.35.0), landed as proposed: `Request::Join`/`DescribeRelations` at protocol 12, server-side index nested loop over the adapter's existing relation methods, `describe_relations()` with a conservative default and `Employee`'s override, SQL aliases and `JOIN … ON <relation>`, `QueryResult::Joined`; proven over real sockets on `Entity` (the consumer's one-hop-with-names query in one round trip, `aliases` riding as a `StrList`), `Employee` (`parent`/`children`/`collaborates_with`), `Dog`, with `Order`/`Reminder` refusing and `Hello { 11 }`/silent clients answered `Malformed`. Two consequences recorded (three exhaustive `QueryResult` matches gained an arm; four unplanned protocol-11 pins from Unit 49 rewritten against `PROTOCOL_VERSION`). Wire-and-client only — no `Cargo.toml`, `src/generic/**`, or on-disk change. Full sweep green (`fmt`/`clippy -D warnings`/`test` 147+2/`test --all-features` 439 + every suite incl. `server_entity_integration` 16/16, `server_protocol_version` 8/8/`doc` at the 64-warning baseline). Part B stays gated | Implemented (Part A); Accepted, gated (Part B) | PR #193 (proposed and accepted); Part A implementation this PR |
| `SERVER-INSERT-DESIGN` | The eleventh round in the `rusty_remind_me`-motivated line `ADR-0036` started, and the first that changes what a running store can *hold*. Read the consumer's source again at `29602f1` and found that all three of its creating tool calls (`add_memory` at `db/queries.rs:98`, `set_reminder`, `entity_upsert`) mint an id client-side and `INSERT` — and that this crate has never had a runtime insert anywhere: no request, no adapter method, no trait, no store method; the invariant `ADR-0036` named as "no runtime deletion, fixed schema" was three clauses, and the unnamed one is the one a backend cannot live with. Names this the strictly-prior gap to `ADR-0045`'s `Memory` domain (a table nothing can write to is useless; insertion alone makes `Reminder`/`Entity` usable for two of the three calls today). Proposes, end to end: `Insert<R>` in `crate::generic::query`; `GenericMmapStore::insert` over a new append-only **insert log** (`<path>.inserts`, the blob's tagged header under `GENINSL\0`, length-prefixed `bincode` records, `fsync` per entry, torn tail dropped) folded into the record blob by the rewrite `open` already performs for a changed set, so a reopen compacts for free and no existing format changes; every composition layer forwarding after the inner insert succeeds; `ConnectionStore::insert_record` with a default of `Unsupported`; `Request::Insert { id, fields }` in `Response::Record`'s own shape at protocol 13 with a new `ErrorCode::Duplicate`, server-gated below 13 (a write, so the session/`Join` precedent rather than `Query`'s), `Unauthorized` for `ReadOnly`, `SessionOpen` in a session; `SchemaDrivenClient::insert` and the Python client's. Four considered-options tables (where the content lives — a log vs. an O(n) blob rewrite vs. a format change vs. memory-only; who mints the id; how the wire carries a record; where the gate sits). Non-goals named: deletion, relation insertion (the next round), a server-minted id, SQL `INSERT`, staging inside a session, journaling, runtime compaction, the `Memory` domain. **Proposed and implemented in one cycle** under the owner's standing mandate — the `ADR-0011` precedent, reasoned through in `ADR-0046`'s own Status rather than defaulted to; acceptance options (a)–(d) still the owner's | `SERVER-SQL-JOIN` | `ADR-0046` (Accepted, option (d)) | Design and ADR in `docs/design/SERVER-INSERT-DESIGN.md` / `docs/decisions/ADR-0046-runtime-record-insertion.md`; accepted by the owner as (d) in this same PR | Accepted | this PR |
| `SERVER-INSERT` | `docs/design/SERVER-INSERT-DESIGN.md` implemented as `SERVER-001` v0.36.0 / FR-046 in the same cycle: `src/generic/insert_log.rs` (new), `Insert<R>`/`InsertError`, `GenericMmapStore::insert` + `merge_log` + the fold in `open`/`read_portable_records` + the clear in `create`, seven `store.rs` forwards + `MmapScanned`'s, `GenericProductionStore::insert`; `InsertOutcome`, `ConnectionStore::insert_record`, `dispatch`'s arm and the three gates, `RequestKind::Insert`, `Reminder`/`Entity` adapters validating the whole field list before any write (`Entity::aliases` as a `StrList` — the first write of one); `Request::Insert` (21), `ErrorCode::Duplicate` (11), `ErrorCode::Storage` (12), `PROTOCOL_VERSION` 13, three vectors (51 pinned), the literal pins moved; `SchemaDrivenClient::insert`, Python `Client.insert` at 13 with the version checked before names resolve; `SERVER-002` v0.2.0. The one reuse the proposal named — a durability failure on insert answering `ErrorCode::Journal` — was resolved at acceptance: the owner picked option (d), and `ErrorCode::Storage` (12) was appended in the same PR, so `Journal` keeps its one producer. Landed as designed; no deviation. Proven over real sockets on `Reminder` (every refusal with nothing inserted, `SessionOpen` with the session still committing, **a second server started on the same directory serving the insert**) and `Entity` (a derived-id insert with aliases resolved by label, alias, and derived id; joinable; duplicate), `Malformed` below 13 with nothing inserted, `Unsupported` on `Dog`, `Unauthorized` for `ReadOnly`, the Python client inserting a record the Rust client reads back | `SERVER-INSERT-DESIGN` | `SERVER-001` (v0.36.0), `SERVER-002` (v0.2.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean (and the default and `client` feature sets); `cargo test --all-features` 459 lib tests + every integration suite green (`server_reminder_integration` 11/11, `server_entity_integration` 17/17, `server_protocol_version` 9/9, `server_dog_integration` 6/6, `server_auth_integration` 14/14, `server_transaction_integration` 22/22, `server_python_client` 1/1, the rest unchanged); `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | this PR |
| `SERVER-LINK-DESIGN` | The twelfth round in the `rusty_remind_me`-motivated line, and the one `ADR-0042` F3 named ("open-label edges created at write time would be a redesign of `MultiSymmetric`'s construction model and of `Request::ListRelationKinds` … a design round of its own"). After `ADR-0046` a running store could gain records but not edges — both relation layers built adjacency once from a caller-supplied list and kept no path to write to, so an inserted `Entity` was an island. Read the consumer's edge path again (`upsert_entity_relation`, `entity.rs:770-826`: free-form label, insert-or-ignore on a derived id). Proposes: `Link<R, Marker>`/`MultiLink<R>` over an **edge log** beside each edge blob (the insert log's format, folded at the next open); **open labels** — a `MultiSymmetric` creates a relation at first use, writing an empty blob and a **label manifest** (`<path>.relations`) first so a portable reopen discovers runtime labels, labels validated to a filesystem-safe charset at the boundary; `ConnectionStore::link_records` (default `Unsupported`; `Entity` open-label, `Employee` its one fixed label); `Request::Link { left, right, relation }` at protocol 14 answered `Ok` for a new *or* an existing edge (the consumer's insert-or-ignore) with the insert round's gates; `SchemaDrivenClient::link` and the Python client's, re-fetching the relation list after a new label. Five considered-options tables (label model, where an edge lives, duplicate edge, label validation, direction). Non-goals named: directed edges, edge metadata, unlinking, cross-table links, parent/child edges, label normalization. Proposed on this branch, implementation following in its own commit | `SERVER-INSERT` | `ADR-0047` (Accepted, option (a)) | Design and ADR in `docs/design/SERVER-LINK-DESIGN.md` / `docs/decisions/ADR-0047-relation-insertion.md`; accepted by the owner as designed on PR #199 | Accepted | this PR |
| `SERVER-LINK` | `docs/design/SERVER-LINK-DESIGN.md` implemented as `SERVER-001` v0.37.0 / FR-047 on the same branch: `insert_log::{append_item, read_items}`, `Link`/`MultiLink`/`LinkOutcome`/`LinkError`, `valid_relation_label`, `merge_edge_lists`, `Symmetric::link` + `edges_path` + the fold in `open`/`read_portable_edges`/`open_portable`, `label_manifest`, `MultiSymmetric::link` + `base_path` + label creation + the per-label fold + manifest union in `open`/`open_portable`, six forwards, `GenericProductionStore::{link, link_by_relation}`; server `LinkOutcome`, `ConnectionStore::link_records`, `dispatch`'s arm, the three gates, `RequestKind::Link`, `Entity`/`Employee` adapters; `Request::Link` (22), `PROTOCOL_VERSION` 14, one vector (52 pinned), the four literal pins moved plus one pre-existing file-list pin (an `Entity` directory now has five files); `SchemaDrivenClient::link`, Python `Client.link` at 14; `SERVER-002` v0.3.0. Landed as designed; no deviation. Proven over real sockets on `Entity` (an existing and a new label, every read — `neighbors`, `neighbors_by_relation`, `ListRelationKinds`, `relations()`, `JOIN … ON <new label>`, `traverse` — seeing it at once, the repeat `Ok`, every refusal, `SessionOpen` with the session still committing, **a second server on the same directory serving the edge and the label**), `Employee` (the fixed-label `Symmetric` path, joinable), `Malformed` below 14 with nothing created, `Unsupported` on `Dog`/`Reminder`, `Unauthorized` for `ReadOnly`, the Python client linking under a new label the Rust client sees from the far end | `SERVER-LINK-DESIGN` | `SERVER-001` (v0.37.0), `SERVER-002` (v0.3.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` 471 lib tests + every integration suite green; `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | this PR |
| `SERVER-MEMORY-DOMAIN-DESIGN` | The thirteenth round in the `rusty_remind_me`-motivated line, and the one every prior round pointed at: the consumer's `memories` table, scoped out by `ADR-0036` ("a genuinely different kind of database") and named by `ADR-0045` as gate (ii). Buildable now because `ADR-0046`/`ADR-0047` made a table writable. Read the consumer's source again at `29602f1` (`schema_tables.sql:56-64`, `add_memory`, `list_filters`, the `access_count` bump) and proposes a **bounded eleven-field projection** — what a memory *is* at creation and what every list reads back — on `Reminder`'s one-index/one-scan stack: `category` indexed (open `String`), `access_count` scannable and non-negative, `tags` a `StrList`, `metadata_json` carried as unparsed text, everything else read-only after insert, no relation of either kind, **no wire change**. Names each of the nineteen omitted columns with its reason: the `f64` scoring triple (no float `ValueKind`), three nullable columns (no null on the wire), sync bookkeeping, chunking, `remind_at`, `deleted_at`, whole-record replacement (the next round), and both edge tables (`memory_entities` cross-table per `ADR-0045`, `memory_associations` deferred to the same round). Options: (a) accept as designed, (b) the whole table, (c) also `memory_associations` now, (d) replacement first, (e) decline | `SERVER-LINK` | `ADR-0048` (Accepted, option (a)) | Design and ADR in `docs/design/SERVER-MEMORY-DOMAIN-DESIGN.md` / `docs/decisions/ADR-0048-memory-domain.md`; accepted by the owner as designed after PR #200 | Accepted | PR #200 |
| `SERVER-MEMORY-DOMAIN` | `docs/design/SERVER-MEMORY-DOMAIN-DESIGN.md` implemented as `SERVER-001` v0.38.0 / FR-048 on the same branch: `src/generic/memory.rs` (`Memory`, `CategoryField`, `AccessCountField`, `MemoryProductionStack`, the three constructors), `src/server/memory.rs` (`MemoryConnectionStore`, eleven field tags, `READ_ONLY_FIELDS`, `valid_access_count`, `fields_of` as the one source of the wire shape), `CheckpointFlush` for the stack, `src/bin/memory_server.rs`, `tests/server_memory_integration.rs`; `Cargo.toml` entries only, no dependency. Protocol stays 14, `SERVER-002` 0.3.0, every vector untouched. Landed as designed; no deviation. Proven over real sockets: `DescribeSchema` names in tag order, `get` with every field, `filter_eq category`, `WHERE sensitive = false`, `WHERE source = 'import'`, `GROUP BY category`, `insert` with all eleven fields, the `access_count` bump, a negative `Malformed`, a `content` update `Unsupported`, **a second server on the same directory serving the inserted memory**, every relation request `Unsupported` | `SERVER-MEMORY-DOMAIN-DESIGN` | `SERVER-001` (v0.38.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; `cargo test` (default, proving front-door status) and `--features client` green; Python vectors 5/5 unchanged; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | PR #200 |
| `SERVER-REPLACE-DESIGN` | The fourteenth round in the `rusty_remind_me`-motivated line, the one `ADR-0048` named as next: after inserts, edges, and a `memories` table, a record could still change in one field only (`UpdateField`), while the consumer's `update_memory` rewrites five at once, `entity_upsert` is insert-or-replace, and `reclassify` rewrites `memory_type`. Proposes **whole-record replacement** — `Replace<R>` appending the new version to the insert log (no new file), the scannable slot rewritten in place, the index bucket moved; the fold applying a logged record *over* the blob's copy by id (the log is the later fact — no existing directory reopens differently); forwards through every layer with the old and new record in hand so `NameIndex` moves keys and `Reversed` moves a child; `Request::Replace` at 23 with `Insert`'s exact body and gates, answered `Ok`/`NotFound` (`UpdateField`'s shape — no new response or code); `SchemaDrivenClient::replace` and a client-side `upsert`; Python `Client.replace`. The one crash window named (the scannable field, bounded to one field). Non-goals: partial update, id change, deletion, edge change, staging, compaction. Options: (a) accept as designed, (b) partial update, (c) delete-then-insert, (d) also a server-side `Upsert`, (e) decline | `SERVER-MEMORY-DOMAIN` | `ADR-0049` (Accepted, option (a)) | Design and ADR in `docs/design/SERVER-REPLACE-DESIGN.md` / `docs/decisions/ADR-0049-whole-record-replacement.md`; accepted by the owner as designed | Accepted | this PR |
| `SERVER-REPLACE` | `docs/design/SERVER-REPLACE-DESIGN.md` implemented as `SERVER-001` v0.39.0 / FR-049 on the same branch: `Replace`/`ReplaceError`, `GenericMmapStore::replace` + `merge_log`'s overwrite, seven `store.rs` forwards + `MmapScanned`'s, `GenericProductionStore::replace`; `ReplaceOutcome`, `ConnectionStore::replace_record`, `dispatch`'s arm, the three gates, `RequestKind::Replace`, `Memory`/`Reminder`/`Entity` adapters; `Request::Replace` (23), `PROTOCOL_VERSION` 15, one vector (53 pinned), the literal pins moved; `SchemaDrivenClient::{replace, upsert}`, Python `Client.replace` at 15; `SERVER-002` v0.4.0. Landed as designed; no deviation. Proven over real sockets on `Memory` (every field replaced, every read seeing it, `upsert` replacing then creating, refusals writing nothing, **a restart serving the replaced version**), `Entity` (`upsert` replacing whole with the new alias resolving, the old label not, `neighbors`/`JOIN` unchanged, a restart), `Malformed` at 14 with nothing replaced, `Unsupported` on `Dog`, `Unauthorized` for `ReadOnly`, `SessionOpen` in a session, the Python client replacing and the Rust client seeing the new alias | `SERVER-REPLACE-DESIGN` | `SERVER-001` (v0.39.0), `SERVER-002` (v0.4.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | this PR |
| `SERVER-TABLES-DESIGN` | The fifteenth round in the `rusty_remind_me`-motivated line: the implementation unit `ADR-0045` gated on "a second table someone needs", now that `ADR-0048` built `Memory` and named `memory_entities` as the first cross-table link. Reads the consumer's link path again (`entity.rs:145`, `327-347`, `expansion.rs:200` — memory → entity, read from both ends, every join crossing the two tables) and finds Part B had every piece but one: a relation whose far endpoint is another table's record. Proposes Part B as accepted (`serve_tables`, `table_name`, per-connection `Use`, `ListTables`/`Tables` at protocol 16, cross-table `Join` with the right table's schema and adapter, `Use` in a session refused) plus **foreign labels** on `MultiSymmetric` — `link` skips only the far-end check, the server checks that end in the table the descriptor names — and `Memory`'s one relation `mentions` → `entity`. Non-goals: per-table auth, multi-table sessions, directed edges, edge metadata, `Order → Customer`, cross-table `Aggregate`, the access log's table field. Options: (a) accept as designed, (b) a new directed cross-table layer, (c) mirror the link on the entity side, (d) `Order → Customer` first, (e) decline | `SERVER-REPLACE`, `SERVER-SQL-JOIN-DESIGN` | `ADR-0050` (Accepted, option (a)), implementing `ADR-0045` (Accepted) | Design and ADR in `docs/design/SERVER-TABLES-DESIGN.md` / `docs/decisions/ADR-0050-tables-on-one-connection.md`; accepted by the owner as designed after PR #202 | Accepted | PR #202 |
| `SERVER-TABLES` | `docs/design/SERVER-TABLES-DESIGN.md` implemented as `SERVER-001` v0.40.0 / FR-050 on the same branch: `MultiSymmetric::{with_foreign_labels, is_foreign}`, `MemoryProductionStack` over `MultiSymmetric` with `MEMORY_RELATION_LABELS`/`MEMORY_FOREIGN_TABLE`; `ConnectionStore::table_name`, `serve_tables`, `handle_connection` over a table list, `Use`/`ListTables`, `join_across`, `link_across`, two-adapter `validate_join`/`evaluate_join`, `RequestKind::{Use, ListTables}`, six `table_name` overrides, `Memory`'s relation methods; `Request::Use` (24), `Request::ListTables` (25), `Response::Tables` (17), `PROTOCOL_VERSION` 16, three vectors (56 pinned), the literal pins moved; `SchemaDrivenClient::{list_tables, use_table, table}` and cross-table SQL `JOIN`, Python `Client.{list_tables, use_table}` and a table-crossing `join` at 16; `memory_server` serving `memory` and `entity`; `SERVER-002` v0.5.0. Landed as designed; no deviation. Proven over real sockets: a two-table server — `ListTables`, `SELECT m.content, e.label FROM memory m JOIN entity e ON mentions` with a right-side `WHERE`, `mentions` from both ends, `Use` switching schema/`get`/`Query` and back, an unknown table `Malformed`, a runtime link checked in the entity table, **a second server on the same directory serving the edge and the join**; a one-table server listing itself, `Use` of its own name `Ok`, another `Malformed`, a foreign link `Unsupported`; `Malformed` at 15 with the table intact; `SessionOpen` in a session; the Python client listing and using tables | `SERVER-TABLES-DESIGN` | `SERVER-001` (v0.40.0), `SERVER-002` (v0.5.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | PR #202 |
| `SERVER-DELETE-DESIGN` | The sixteenth round in the `rusty_remind_me`-motivated line, and the last of `ADR-0036`'s three fixed-shape clauses ("no runtime deletion"). Reads the consumer's `delete_memory` (`db/queries.rs:397` — a hard `DELETE` when sync is off), entity delete (`entity.rs:610`) and its `DELETE FROM memory_entities WHERE entity_id = ?` (`:622`). Proposes a **tombstone** in the ordered insert log — format version 2 with a per-entry kind byte, a version-1 log read unchanged and upgraded on its next append — honored by every fold (a record tombstone removes the record, an edge tombstone every edge touching the id, a later item re-adds); `GenericMmapStore::delete` as tombstone-then-retire-the-slot; `Delete` through every layer with relation layers dropping every edge of the id; `MultiSymmetric::detach` for a foreign id; `Request::Delete` at 26, protocol 17, answered `Ok`/`NotFound` with `Insert`'s gates; the cross-table cascade (`detach_record` on every other table's relation targeting this one); `delete` on both clients. Non-goals: soft deletion, compaction, cascading record deletion, single-edge unlink, batch delete, staging. Named: a pre-round build cannot read a version-2 log; the cascade's two steps. Options: (a) accept as designed, (b) a separate deletes log, (c) soft delete as a folded flag, (d) rewrite the blob per delete, (e) decline | `SERVER-TABLES` | `ADR-0051` (Accepted, option (a)) | Design and ADR in `docs/design/SERVER-DELETE-DESIGN.md` / `docs/decisions/ADR-0051-runtime-deletion.md`; accepted by the owner as designed after PR #204 | Accepted | PR #204 |
| `SERVER-DELETE` | `docs/design/SERVER-DELETE-DESIGN.md` implemented as `SERVER-001` v0.41.0 / FR-051 on the same branch: insert log version 2 (`LogEntry`, `append_tombstone`, `read_entries`, the lazy upgrade), `SlotFile::clear_marker`, `Delete`/`Detach`/`DeleteError`, `GenericMmapStore::delete` + `merge_log` over entries, `merge_edge_lists` over entries + `drop_edges_of`, eight `store.rs` impls + `MmapScanned`'s, `GenericProductionStore::{delete, detach}`; `DeleteOutcome`, `ConnectionStore::{delete_record, detach_record}`, `dispatch`'s arm, the three gates, `delete_across`, `RequestKind::Delete`, `Memory`/`Reminder`/`Entity` adapters; `Request::Delete` (26), `PROTOCOL_VERSION` 17, one vector (57 pinned), the literal pins moved; `SchemaDrivenClient::delete`, Python `Client.delete` at 17; `SERVER-002` v0.6.0. Landed as designed; no deviation. Proven over real sockets on the two-table server: a deleted memory gone from every read and from the entity's side of `mentions`, the repeat `Ok(false)`, a deleted entity detaching every memory's edge to it with the memories intact and the cross-table join shrinking, the deleted id inserted again with no edges, **a second server on the same directory serving the deletions**; `Malformed` at 16 with nothing deleted, served at 17 (`Ok` then `NotFound`), `Unsupported` on `Dog`, `Unauthorized` for `ReadOnly`, `SessionOpen` in a session, a delete from Python | `SERVER-DELETE-DESIGN` | `SERVER-001` (v0.41.0), `SERVER-002` (v0.6.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | PR #204 |
| `SERVER-COMPACT-DESIGN` | The seventeenth round in the `rusty_remind_me`-motivated line, and the operational gap `ADR-0046`, `ADR-0047`, and `ADR-0051` each named: every runtime write appends, only a reopen folds, and nothing ever drops a retired slot — the mmap file never shrinks and a long-running server never folds. Proposes `Compact`: the reopen fold done in place under the table's write lock — the blob rewritten from the live set, the slot file rewritten gaplessly via temp-and-rename with the mapping released (valid on every platform), the insert log removed, every edge log folded into its blob (edges sorted), in a crash-safe order; a `CompactionReport` summed across the stack; `Request::Compact` → `Response::Compacted` at protocol 18 with `Insert`'s gates; `compact` on both clients. No threshold, no timer, no thread: an operator's request. Non-goals: automatic/background/online compaction, the journal, reordering; named: readers and writers wait, neighbor order after a later reopen is sorted. Options: (a) accept as designed, (b) automatic past a threshold, (c) reopen-only with retired slots dropped, (d) online with a shadow file, (e) decline | `SERVER-DELETE` | `ADR-0052` (Accepted, option (a)) | Design and ADR in `docs/design/SERVER-COMPACT-DESIGN.md` / `docs/decisions/ADR-0052-compaction.md`; accepted by the owner as designed after PR #206 | Accepted | PR #206 |
| `SERVER-COMPACT` | `docs/design/SERVER-COMPACT-DESIGN.md` implemented as `SERVER-001` v0.42.0 / FR-052 on the same branch: `SlotFile::rewrite`, `Compact`, `CompactionReport`, `GenericMmapStore::compact`, `sorted_edges`/`compact_edge_blob`, seven `store.rs` impls + `MmapScanned`'s, `GenericProductionStore::compact`; `ConnectionStore::compact`, `dispatch`'s arm, the three gates, `RequestKind::Compact`, `Memory`/`Reminder`/`Entity` adapters; `Request::Compact` (27), `Response::Compacted` (18), `PROTOCOL_VERSION` 18, two vectors (59 pinned), the literal pins moved; `SchemaDrivenClient::compact`, Python `Client.compact` at 18; `SERVER-002` v0.7.0. Landed as designed; no deviation. Proven over real sockets on the two-table server: the exact counts after a link and a delete, every read agreeing before and after, a second compaction reclaiming nothing, the entity table after a cascading delete, **a restart on the compacted files serving the same data**; `Malformed` at 17, `Compacted` at 18, `Unsupported` on `Dog`, `Unauthorized` for `ReadOnly`, `SessionOpen` in a session, the exact counts from Python | `SERVER-COMPACT-DESIGN` | `SERVER-001` (v0.42.0), `SERVER-002` (v0.7.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; `cargo test` (default) and `--features client` green; Python vectors 5/5; `cargo doc --all-features --no-deps` at the pre-round warning baseline | Implemented | PR #206 |
| `SERVER-DATA-DIR-DESIGN` | The eighteenth round in the `rusty_remind_me`-motivated line, found by an integration spike's first restart: `memory_server` seeded the sample dataset into a per-process scratch directory on every start, so no runtime write outlived the process — every survival test reopened in-process, the binary never did. Proposes `open_or_create_{memory,entity}_production_stack` (portable reopen when the slot file exists, empty creation when not) and one variable on the binary, `SERVER_DATA_DIR` (set: both tables durable there; unset: unchanged), the startup line naming which; no seeding, no repair, no default journal placement, the other binaries an open question | — | `SERVER-001` (v0.43.0) | Design and ADR in `docs/design/SERVER-DATA-DIR-DESIGN.md` / `docs/decisions/ADR-0053-durable-data-directory.md`; accepted by the owner as designed after PR #210 | Accepted | PR #210 |
| `SERVER-DATA-DIR` | `docs/design/SERVER-DATA-DIR-DESIGN.md` implemented as `SERVER-001` v0.43.0 / FR-053 on the same branch: `open_or_create_memory_production_stack`, `open_or_create_entity_production_stack`, `memory_server`'s `DataLocation`/`open_stores`/`SERVER_DATA_DIR` and startup line. Landed as designed; no wire change. Proven over a real socket by hand: an empty directory, a memory and an entity inserted and linked through the Python client, the process killed, a restart on the same directory reading everything back, `ListTables` unchanged; the scratch default still seeding the samples | `SERVER-DATA-DIR-DESIGN` | `SERVER-001` (v0.43.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` green (two new unit tests); default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #210 |
| `SERVER-GUARDED-REPLACE-DESIGN` | The nineteenth round in the `rusty_remind_me`-motivated line, the hub spike's first gap: the consumer's last-writer-wins merge is one atomic SQL statement, and against this crate it had to be get-compare-`Replace` — not atomic, so a concurrent writer can be overwritten by an older version. Proposes a guarded replace: `Request::ReplaceIf`, `Replace`'s body plus one `Predicate` (a `Query` filter's own type) the server evaluates against the stored record under the table's write lock, replacing only if it holds; validated as a query predicate; answered `Ok`/`NotFound`/`Err { GuardFailed }`; one method on the production store, no new store trait, no version counter, no format change; options (b) a dedicated `ReplaceIfNewer`, (c) a server-maintained version counter, (d) client-side, (e) decline | — | `SERVER-001` (v0.44.0), `SERVER-002` (v0.8.0) | Design and ADR in `docs/design/SERVER-GUARDED-REPLACE-DESIGN.md` / `docs/decisions/ADR-0054-guarded-replace.md`; accepted by the owner as designed after PR #212 | Accepted | PR #212 |
| `SERVER-GUARDED-REPLACE` | `docs/design/SERVER-GUARDED-REPLACE-DESIGN.md` implemented as `SERVER-001` v0.44.0 / FR-054 on the same branch: `GenericProductionStore::replace_if`, `GuardedReplace`; `ConnectionStore::replace_record_if` on `Memory`/`Reminder`/`Entity` (`fields_of` factored on the latter two), `validate_predicate` factored out of `validate_query`, `dispatch`'s arm, the three gates, `RequestKind::ReplaceIf`; `Request::ReplaceIf` (28), `ErrorCode::GuardFailed` (13), `PROTOCOL_VERSION` 19, two vectors (61 pinned), the literal pins moved; `SchemaDrivenClient::replace_if`, Python `Client.replace_if` at 19; `SERVER-002` v0.8.0. Landed as designed; no deviation. Proven over real sockets: a newer version replaced, an older and an equal one refused with nothing written, `Eq` as compare-and-swap, an unknown id, the server's `Malformed` for a wrong-kind guard, the client's local refusals, **the winner surviving a restart**; `Malformed` at 18, served at 19, `Unsupported` at 1 and on `Dog`, `Unauthorized` for `ReadOnly`, `SessionOpen` in a session, the three outcomes from Python | `SERVER-GUARDED-REPLACE-DESIGN` | `SERVER-001` (v0.44.0), `SERVER-002` (v0.8.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #212 |
| `SERVER-PAGE-DESIGN` | The twentieth round in the `rusty_remind_me`-motivated line, the hub spike's second gap: nothing on the wire had an order, so a sync puller fetched and sorted the whole table for every page. Proposes `Request::Page` — one ordered keyset page over an orderable (`U32`/`I64`) field with the id as tie-break, strictly after a `(value, id)` cursor, at most `limit` rows, answered by the existing `Rows`; the order written once (`page_rows`) with `ConnectionStore::page` defaulting to it over `scan_all` so an adapter may answer more cheaply later; validated before any scan; a read gated as `Query`; no index, no descending, no `Str` order, no `Since` request, no `ORDER BY`, no server sequence; options (b) `ORDER BY`/`LIMIT`/`OFFSET` in `Query`, (c) a second scannable slot first, (d) a server-side sequence, (e) decline | — | `SERVER-001` (v0.45.0), `SERVER-002` (v0.9.0) | Design and ADR in `docs/design/SERVER-PAGE-DESIGN.md` / `docs/decisions/ADR-0055-ordered-page.md`; accepted by the owner as designed after PR #214 | Accepted | PR #214 |
| `SERVER-PAGE` | `docs/design/SERVER-PAGE-DESIGN.md` implemented as `SERVER-001` v0.45.0 / FR-055 on the same branch: `PageRow`, `page_rows`, `validate_page`, `ConnectionStore::page` with its default, `dispatch`'s arm, the gate, `RequestKind::Page`; `Request::Page` (29), `PROTOCOL_VERSION` 20, one vector (62 pinned), the literal pins moved; `SchemaDrivenClient::page`, Python `Client.page` at 20; `SERVER-002` v0.9.0; no adapter touched. Landed as designed; no deviation. Proven over real sockets: `Memory` walked in pages of two by `updated_at` with a tie broken by id and "after *t*" as a cursor at the maximum id, `Dog` by `age` through the default disjoint and complete, every local and server refusal; `Malformed` at 19, served at 20, `Unsupported` at 1; two pages from Python sorted and disjoint | `SERVER-PAGE-DESIGN` | `SERVER-001` (v0.45.0), `SERVER-002` (v0.9.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #214 |
| `SERVER-MEMORY-SYNC-FIELDS-DESIGN` | The twenty-first round in the `rusty_remind_me`-motivated line, the hub spike's third gap: the projection lacks `deleted_at` (a nullable soft-delete timestamp) and `node_id` (a nullable writing node), so `compact_tombstones`, `stats.tombstones`, and `count_by_origin_node` cannot be honest. Two decisions bundled: how the wire says "no value" (N1 sentinels — `0` live, `""` unattributed — recommended; N2 a nullable `ScanValue` at a protocol bump; N3 presence fields) and how a record layout may change now that directories are durable (L1 a schema-tag bump with a distinct failure and no upgrade — recommended now, a re-push being the remedy; L2 a layout version on the record type with an in-place upgrade — the named follow-up; L3 tolerant decoding, impossible; L4 rewrite every companion). **Design only**: not implemented on this branch, per the working agreement's ask-before-a-migration | — | `SERVER-001` (unchanged until implemented) | Design and ADR in `docs/design/SERVER-MEMORY-SYNC-FIELDS-DESIGN.md` / `docs/decisions/ADR-0056-memory-sync-fields.md`; proposed design-only in PR #216, the owner picked option (a) | Accepted | PR #216 |
| `SERVER-MEMORY-SYNC-FIELDS` | `docs/design/SERVER-MEMORY-SYNC-FIELDS-DESIGN.md` option (a) implemented as `SERVER-001` v0.46.0 / FR-056: `deleted_at_unix_ms` (`0` live) and `node_id` (`""` unattributed) on `Memory` at tags 11/12, read-only over the wire, `deleted_at_unix_ms >= 0`; `SCHEMA_TAG` `memory::Memory@2`; `describe` at thirteen fields; the samples. No wire, client, or fixture change. Landed as designed; no deviation. Proven: a blob stamped with the old tag refused distinctly on both reopen paths with nothing rewritten or recreated; over a real socket the sentinels on a fresh record, a tombstone and a node stamped by `Replace`, `Page` by `deleted_at_unix_ms` listing live rows first, the purge set by `Query`, per-node counts by `GROUP BY` with `""` its own group, a negative stamp `Malformed`, both fields surviving a restart | `SERVER-MEMORY-SYNC-FIELDS-DESIGN` | `SERVER-001` (v0.46.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | — |
| `SERVER-COUNT-EDGES-DESIGN` | The twenty-second round in the `rusty_remind_me`-motivated line, the hub spike's fifth gap: the number of edges under one label cost one `NeighborsByRelation` round trip per record. Proposes `MultiNeighbors::edge_count` (the degree sum halved — every edge is kept under both endpoints, foreign labels included), `ConnectionStore::count_edges` on `Memory`/`Entity`, and `Request::CountEdges`/`Response::Count` at protocol 21, a read gated as `Query`; per-record counts, `Employee`'s single-label stack, every-label-at-once, and cross-table totals named non-goals; options (b) counts on `RelationDescriptor`, (c) an `Aggregate` over edges, (d) decline | — | `SERVER-001` (v0.47.0), `SERVER-002` (v0.10.0) | Design and ADR in `docs/design/SERVER-COUNT-EDGES-DESIGN.md` / `docs/decisions/ADR-0057-count-edges.md`; accepted by the owner as designed after PR #218 | Accepted | PR #218 |
| `SERVER-COUNT-EDGES` | `docs/design/SERVER-COUNT-EDGES-DESIGN.md` implemented as `SERVER-001` v0.47.0 / FR-057 on the same branch: `edge_count` on `MultiSymmetric` and forwarded by `NameIndex`, `GenericProductionStore::count_edges`, `ConnectionStore::count_edges` on `Memory`/`Entity`, `dispatch`'s arm, the gate, `RequestKind::CountEdges`; `Request::CountEdges` (30), `Response::Count` (19), `PROTOCOL_VERSION` 21, two vectors (64 pinned), the literal pins moved; `SchemaDrivenClient::count_edges`, Python `Client.count_edges`; `SERVER-002` v0.10.0. Landed as designed; no deviation. Proven: at the store, two seeded mentions, a link, a delete dropping both of a record's edges, a portable reopen; over real sockets on the two-table server the three sample mentions, four after a link, an unknown label `Malformed`, `relates_to` on `entity` zero, two left after a cascading entity delete; `Malformed` at 20, served at 21, `Unsupported` at 1 and on `Dog`; from Python the one runtime edge and an unknown label | `SERVER-COUNT-EDGES-DESIGN` | `SERVER-001` (v0.47.0), `SERVER-002` (v0.10.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #218 |
| `SERVER-RELATION-DOMAIN-DESIGN` | The twenty-third round in the `rusty_remind_me`-motivated line, the hub spike's fourth and last gap: `entity_relations` is a directed edge under an open label with four sync columns, and every edge layer here is undirected, label-only, and metadata-free. Proposes modelling the table as a table — a seventh domain, `Relation`, on `Reminder`'s stack shape (`subject` indexed, `updated_at_unix_ms` scannable, `ADR-0056`'s sentinels, endpoints the consumer's id strings verbatim), served as `memory_server`'s third table; every hub method over it a request that exists; a directed edge layer, referential integrity, triple uniqueness, and a fixed label set named non-goals; options (b) a directed edge layer with metadata, (c) directed edges without metadata, (d) decline | — | `SERVER-001` (v0.48.0) | Design and ADR in `docs/design/SERVER-RELATION-DOMAIN-DESIGN.md` / `docs/decisions/ADR-0058-relation-domain.md`; accepted by the owner as designed after PR #220 | Accepted | PR #220 |
| `SERVER-RELATION-DOMAIN` | `docs/design/SERVER-RELATION-DOMAIN-DESIGN.md` implemented as `SERVER-001` v0.48.0 / FR-058 on the same branch: `src/generic/relation.rs` (`Relation`, `SubjectField`, `UpdatedAtField`, the stack, three constructors), `src/server/relation.rs` (`RelationConnectionStore`, seven fields), `CheckpointFlush`, `memory_server`'s third table. Landed as designed; no deviation; no wire change. Proven: at the store, out-edges by subject, stamps by scan, an insert and a delete across a portable reopen and open-or-create; at the adapter, the shape, every validation rule, replace moving the index, the guarded merge, delete; over a real socket on a three-table server, `ListTables`, three edges and an empty subject refused, out-edges by `FilterEq`, in-edges and one label by `Query`, a page by `updated_at`, last-writer-wins by `ReplaceIf`, a count by `Aggregate`, a delete, a restart | `SERVER-RELATION-DOMAIN-DESIGN` | `SERVER-001` (v0.48.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #220 |
| `SERVER-ORDERED-INDEX-DESIGN` | The twenty-fourth round in the `rusty_remind_me`-motivated line: `Page` measured for the first time — 292 ms per page of 50 at 100K `Memory` records, 100 ms after selecting by key, all of it one decode per record per page; SQLite's indexed `ORDER BY … LIMIT` 14 µs, flat. Proposes a memory-only sorted index layer, `Ordered<S, R, Marker>` over one `OrderedField` — a `BTreeSet<(key, id)>` built from the records at open as `NameIndex` builds its map, exact through every write, answering `PageBy` as a range walk — wrapping `Memory`'s and `Relation`'s stacks over `updated_at_unix_ms`, their adapters answering that field from the index and every other from the scan; a durable index, an index per field, `Entity`, descending order and a range named non-goals; options (b) a second scannable slot, (c) a persisted index, (d) decline | — | `SERVER-001` (v0.49.0) | Design and ADR in `docs/design/SERVER-ORDERED-INDEX-DESIGN.md` / `docs/decisions/ADR-0059-ordered-index.md`; accepted by the owner as designed after PR #225 | Accepted | PR #225 |
| `SERVER-ORDERED-INDEX` | `docs/design/SERVER-ORDERED-INDEX-DESIGN.md` implemented as `SERVER-001` v0.49.0 / FR-059 on the same branch: `OrderedField` (`traits.rs`), `PageBy` (`query.rs`), `Ordered` and its forwards (`store.rs`), `GenericProductionStore::page_by`, `UpdatedAtOrder` and the two wrapped stack aliases, `page_by_scan` and the two adapters' `page`. Landed as designed; no deviation; no wire change. Proven: at the store, five seeded `Memory` records with a tie paged from a strict cursor, an insert, a replace, a non-ordered update, a delete, and a portable reopen; on `Relation`, an `update` of the scannable slot re-keying the index; over a real socket every existing `Page` test unchanged and a page by `created_at` (the scan path) agreeing with one by `updated_at`. Measured: 152–160 µs per page of 50 at 1K, 10K, and 100K records (was 0.63 ms / 5.1 ms / 100 ms), the 100K walk 0.44 s (was 50 s) | `SERVER-ORDERED-INDEX-DESIGN` | `SERVER-001` (v0.49.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #225 |
| `SERVER-WRITE-BATCH-DESIGN` | The twenty-fifth round in the `rusty_remind_me`-motivated line, the owner's "let's incorporate Transactions": the hub's sync push applies heterogeneous records one at a time (~2 round trips each), and the only batch on the wire (`Request::Transaction`, ADR-0013) carries field updates only, not the runtime writes (`Insert`/`Replace`/`ReplaceIf`/`Delete`/`Link`) the push is made of. Proposes `Request::WriteBatch { ops: Vec<WriteOp> }` applied under one write-lock acquisition, and puts the atomicity guarantee to the owner as the round's fork — (a) pipelined per-op results, not atomic, no journal change (recommended, since the hub is per-record LWW and `ReplaceIf` already made each record atomic); (b) all-or-nothing, needing `JOURNAL_FORMAT_VERSION` 2 with idempotent redo and still not atomic against a mid-apply I/O failure without a storage rollback primitive; (c) both under a flag; (d) extend the session; (e) decline. Design only; implementation follows the pick | — | `SERVER-001` (v0.50.0 on the pick) | Design and ADR in `docs/design/SERVER-WRITE-BATCH-DESIGN.md` / `docs/decisions/ADR-0060-write-batch.md`; the owner picked option (c), implemented on one branch | Accepted | PR #229 |
| `SERVER-WRITE-BATCH` | `docs/design/SERVER-WRITE-BATCH-DESIGN.md` option (c) implemented as `SERVER-001` v0.50.0 / FR-060 on the same branch: `Request::WriteBatch` (31)/`Response::BatchResults` (20) and the `WriteOp`/`WriteResult` enums at protocol 22, `MAX_BATCH_OPS`; `ConnectionStore::{apply_write_op, write_batch}` (pipelined default) with an atomic single-`with_exclusive` override on `Memory`/`Entity`/`Relation` (validate all, then apply all, abort `TransactionFailed` applying nothing); Rust `SchemaDrivenClient::write_batch`/`BatchOp`, Python `Client.write_batch`; `SERVER-002` v0.11.0, 66 vectors. Pipelined and atomic; not crash-atomic across the batch (named follow-on). Proven: the 22 gate (`server_protocol_version`), and pipelined/atomic-success/atomic-abort-applies-nothing on a real socket (`server_memory_integration`) | `SERVER-WRITE-BATCH-DESIGN` | `SERVER-001` (v0.50.0), `SERVER-002` (v0.11.0) | `cargo fmt --all -- --check` clean; clippy `-D warnings` clean on all-features, default, and `client`; `cargo test --all-features` every lib test + every integration suite green; default and `client` green; Python vectors 5/5; rustdoc at the baseline | Implemented | PR #229 |
| `SERVER-SQL-ORDER-BY-DESIGN` | The twenty-sixth round in the `rusty_remind_me`-motivated line, selected by a planning-session reconciliation pass after finding no roadmap/spec/traceability row short of `Implemented`/`Verified` (`docs/PROJECT-STATUS.md`, 2026-09-14): `Request::Page`/`ADR-0055` already gives the wire a real ordered keyset walk, made fast by `Ordered`/`ADR-0059`, but SQL text still has no `ORDER BY` — `ADR-0034` named it a Non-goal before `Page` existed, and `ADR-0055` named this exact gap as its own open revisit trigger; `docs/FUTURE-GROWTH.md`'s SQL-parity checklist still lists it "still absent." Proposes `ORDER BY <field>` in the client-side `SELECT` grammar for a plain query only (`WHERE`/`JOIN`/`GROUP BY`/aggregate combinations refused at parse time), compiled to one `Request::Page` round trip when `LIMIT` is given or a client-side loop of `Page` calls when it is not — zero wire, protocol, or server change. `docs/design/SERVER-SQL-ORDER-BY-DESIGN.md`, `ADR-0061`. No live consumer named this gap (unlike every prior round in this line); the trigger is two governing documents' own explicit open language. Owner picked option (a) — one field, `U32`/`I64` only, ascending only, `WHERE`/`JOIN`/aggregate combinations refused; (b) a filter-bearing `Page` and (c) decline both declined. | `SERVER-WRITE-BATCH` | — (no spec written for the design itself; `SERVER-001` v0.51.0 registered by the implementation unit below) | Owner accepts one option; implementation proven by `cargo test --all-features` including a new SQL/`ORDER BY` integration suite, `cargo clippy`/`fmt` clean, no protocol-version change | Accepted | `docs/decisions/ADR-0061-sql-order-by.md`, `docs/design/SERVER-SQL-ORDER-BY-DESIGN.md` (2026-09-14, this pass) |
| `SERVER-SQL-ORDER-BY` | `docs/design/SERVER-SQL-ORDER-BY-DESIGN.md` option (a) implemented as `SERVER-001` v0.51.0 / FR-061 on the same branch: `src/server/sql.rs` (`order_by_clause` grammar, `ParsedQuery::order_by`, `is_keyword`'s `"ORDER"`, `SqlParseError::{OrderByWithFilter,OrderByWithJoin,OrderByWithAggregate}` and `validate_order_by`, all parse-time), `src/server/client.rs` (`SchemaDrivenClient::query`'s fourth branch, `query_ordered`, `fetch_page`, `ORDER_BY_PAGE_CHUNK = 1_000`). No wire, protocol-version, or server-side change — reuses `Request::Page` (protocol 20) exactly as it already existed. Landed as designed; no deviation. Proven: 5 new `sql.rs` unit tests (grammar plus each exclusion plus the reserved keyword); 6 new `tests/server_sql_integration.rs` tests over a real socket — a `LIMIT`-bearing round trip byte-identical in row order to `SchemaDrivenClient::page`'s own result, named-column projection despite `Page` returning every field, a 2,500-record no-`LIMIT` walk across several internal `Page` calls with no duplicate/missing row across a chunk seam, every client-side refusal (`WHERE`/`JOIN`/`GROUP BY` combined, non-`U32`/`I64` field, `LIMIT 0`, unknown field), and the protocol-20 gate. | `SERVER-SQL-ORDER-BY-DESIGN` | `SERVER-001` (v0.51.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-features -- -D warnings` clean (lib/bins/tests/examples — `--all-targets` unusable this session on Windows due to a pre-existing, documented Linux-only bench target, `benches/scan_ages_crossover.rs`); `cargo test --all-features` 521 lib tests (520 passing + 1 pre-existing unrelated Windows file-locking failure in `generic::insert_log`, reproduced identically under default features with none of this round's code even compiled, named not fixed); `cargo test` (default features) 186 lib tests (same pre-existing failure) + 2 doctests; `cargo test --features server,research --test server_sql_integration` 34/34 (28 prior + 6 new) | Implemented | this pass, 2026-09-14 |
| `DEFINITION-OF-DONE` | `ADR-0062`: defines "Done" for this project as "a solid backend for `rusty_remind_me`/sibling `rusty_*` consumers on Linux," not SQLite/DuckDB parity. Names what's already met (every `HubStore` method the 2026-09-07 hub spike exercised has a real primitive, all five spike-named gaps closed; CI gates every push on real Linux across three feature sets plus MSRV; durability/concurrency measured against a real process restart, not assumed) and the three concrete remaining items, tracked below as their own rows (`SERVER-WRITE-BATCH-CRASH-ATOMICITY`, `DIFFERENTIAL-TEST-SUITE-PORT`, `ENTITY-ID-MAPPING-DECISION`). Explicit non-goals named so they are never silently chased: a SQL planner/optimizer, general MVCC, `ALTER TABLE`, `NULL`, arbitrary-predicate joins — all stay in `docs/FUTURE-GROWTH.md` as directions the project *could* take, not commitments. Docs-only; no code change. | `SERVER-SQL-ORDER-BY` | — (a scoping decision, not an implementation unit) | Owner accepts the bar | Accepted | `docs/decisions/ADR-0062-definition-of-done.md` (2026-09-14, this pass) |
| `SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN` | `ADR-0062` item 4: `Request::WriteBatch`'s atomic mode (`ADR-0060`) is precondition- and isolation-atomic but not crash-atomic across a mid-apply process failure. Proposes extending the existing batch journal to format version 2 (a leading kind byte per entry: `0` `TransactionOp`, `1` `WriteOp`), reusing `CommitGroup`'s already-proven fsync/replay/checkpoint discipline as-is — no new journal file, no new fsync machinery. `docs/design/SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN.md`, `ADR-0063`. Owner picked option (a); (b) a separate journal file and (c) decline both declined. | `SERVER-WRITE-BATCH` | — (`SERVER-001` version registered by the implementation unit below) | Owner accepts one option; implementation proven by a real crash-injection test | Accepted | `docs/decisions/ADR-0063-write-batch-crash-atomicity.md` (2026-09-14, this pass) |
| `SERVER-WRITE-BATCH-CRASH-ATOMICITY` | `docs/design/SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN.md` option (a) implemented as `SERVER-001` v0.52.0 / FR-062 on the same branch: `src/server/journal.rs` (`JOURNAL_FORMAT_VERSION` 2, the kind byte, `JournalEntry`/`JournaledBatch`, `CommitGroup::commit_write`), `src/server/{memory,entity,relation}.rs` (`write_batch`'s journaled atomic branch, `replay_write_batch`, a `schema()` associated function). **Mechanism corrected during implementation** (see `ADR-0063`'s own "Acceptance and implementation"): journals the raw, already-validated `WriteOp` list rather than a pre-decided type — `CommitGroup`'s append-before-exclusive-section discipline made guard pre-resolution impossible without giving up group-commit pipelining; replay re-runs `prepare_write`/`apply_prepared` instead, safe because replay only ever runs cold-start from the exact pre-crash state. `Dog`/`Order`/`Employee`/`Reminder`'s journals are provably unaffected (kind-`0` bytes unchanged). Proven: `journal.rs`'s 8 existing tests pass unmodified; a new kind-0/kind-1 round-trip test; a real crash-injection test (`write_batch_atomic_is_crash_atomic_via_the_journal`) replaying a genuinely committed atomic batch's journal onto fresh pre-batch store files, both an `Insert` and a `Replace` landing correctly. | `SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN` | `SERVER-001` (v0.52.0) | `cargo fmt --all -- --check` clean; `cargo clippy --all-features -- -D warnings` clean; `cargo test --all-features --no-fail-fast` 523 lib tests + every integration target green except the pre-existing, unrelated `server_python_client` failure (`python3` missing from `PATH`) | Implemented | this pass, 2026-09-14 |
| `DIFFERENTIAL-TEST-SUITE-PORT` | `ADR-0062` item 5, re-scoped: the original plan (port `rusty_remind_me`'s own ~60-test SQLite/Postgres differential suite) was abandoned after finding that suite lives in a separate repo and the 2026-09-07 hub spike's own `MultimodalHubStore` adapter was never merged anywhere in this one — nothing to port *from* inside this crate. Owner-approved re-scope: build the equivalent differential/behavioral coverage directly in this crate's own `tests/`, exercising a real `SchemaDrivenClient` against a real `memory_server`-shaped three-table socket server, proving every portable consumer behavior a scout enumerated against protocol 22's real primitives. Implemented as `tests/server_hub_differential.rs`, `server` feature only (no `research`): 6 of 8 named behaviors got a new test (sensitive-flag round trip on insert, pipelined-batch malformed-op isolation, `node_id` cross-node isolation via `Query`, the entity alias union-merge primitive round-tripping through whole-record `Replace`, an identical-edge reinsert on `Relation` refused as `Duplicate` not a second edge, and a `Memory`-domain `count_since`-equivalent `WHERE`+`COUNT(*)`); 2 were found already fully proven by `tests/server_memory_integration.rs` and cited rather than duplicated (LWW `ReplaceIf` semantics; `Relation`'s own `FilterEq subject`/`Query object` retrieval). One item's premise was checked and corrected rather than assumed: `Compact` reclaims hard-deleted slots/logs only and has no relationship at all to the soft-delete field `deleted_at_unix_ms`, so the test proves the real composition (a soft-deleted record excluded from a live `Query` yet untouched by `Compact`) instead of a misleading "`Compact` purges tombstones" claim. `hub_seq` ordering and every Postgres-specific behavior confirmed genuinely not portable, named explicitly rather than silently dropped. | `SERVER-SQL-ORDER-BY` | — (a test-coverage unit, not a new spec) | The ported suite runs against a live `memory_server`-shaped server, passing or naming exactly which tests don't apply and why | Implemented | `ADR-0062`, `docs/reports/2026-09-07-hub-spike-report.md`, `tests/server_hub_differential.rs` — `cargo fmt -- --check` clean, `cargo clippy --all-features -- -D warnings` clean, `cargo test --features server,research --test server_hub_differential`: 7/7 passing |
| `ENTITY-ID-MAPPING-DECISION` | `ADR-0062` item 6: the hub spike's zero-pad-and-strip workaround for the consumer's 48-bit `entity_id` into this crate's 128-bit `Uuid` was named "a bigger, cross-repo decision" the spike explicitly declined to make. Cross-repo — needs a decision in `rusty_remind_me` too, not just here. Not started. | — | — (a cross-repo decision, not an implementation unit here) | A decision recorded in both repos (this crate already has its own collision-resistant `entity_id()`, full SHA-256 → 128 bits — the candidate the spike flagged as the likely right answer, not yet adopted by the consumer) | Deferred | `ADR-0062`, `docs/reports/2026-09-07-hub-spike-report.md` |
| `SERVER-METRICS-DESIGN` | A design for process-wide observability: `Request::Metrics`/`Response::Metrics { text }`, a bounded, fixed set of atomic counters (requests total/ok/error, connections accepted/active) rendered as hand-formatted Prometheus text, gated as a read (not restricted to `ReadWrite`) at protocol 23, incremented at the same `handle_connection` call site `ADR-0031`'s access log already uses. Sourced from `docs/FUTURE-GROWTH.md`'s new "Operational maturity" section (added this session) rather than a benchmark finding. ADR-0064 (**Accepted as designed**); `docs/design/SERVER-METRICS-DESIGN.md`; explicitly declines per-`RequestKind` cardinality and a separate HTTP `/metrics` listener for now. | `SERVER-WRITE-BATCH-CRASH-ATOMICITY` | — (no spec written for the design itself) | Owner reviewed and accepted option (a) | Implemented | `docs/design/SERVER-METRICS-DESIGN.md`, `ADR-0064` |
| `SERVER-METRICS` | The accepted `SERVER-METRICS-DESIGN`/`ADR-0064` implemented and registered as `SERVER-001` v0.53.0 / FR-063: `src/server/metrics.rs` (new) — `ServerMetrics` (six atomics: requests total/ok/err, connections total/active, `started_at`), `render()` (Prometheus text), `ConnectionMetricsGuard` (the `DisconnectAudit` drop-guard precedent, for `connections_active`); `Request::Metrics`/`Response::Metrics { text }` at protocol 23 (`PROTOCOL_VERSION` 22 → 24, shared bump with `SERVER-BACKUP`'s protocol 24 in the same PR); gated as a read — any authenticated class, not just `ReadWrite`, never `SessionOpen`-gated. **One real correction found during implementation**: the design proposed a sibling `Arc<ServerMetrics>` parameter beside `ServeOptions`; implementing it found `ServeOptions::rate_limit` (`Option<Arc<FailureTable>>`, `ADR-0030`) already holds exactly this shape of live, mutated-per-request state, so `ServerMetrics` became a plain `ServeOptions` field instead — no `serve`/`serve_tables` signature change at all. `SchemaDrivenClient::metrics()`. | `SERVER-METRICS-DESIGN` | `SERVER-001` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --all-features -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast`: 527 lib tests + `tests/server_metrics_integration.rs` (3/3) + every other integration target green, `server_python_client` included (1/1, fixed in a same-session follow-up round — `ADR-0043`'s own final addendum) | Implemented | `ADR-0064`, `src/server/metrics.rs`, `tests/server_metrics_integration.rs` |
| `SERVER-BACKUP-DESIGN` | A design for a live, consistent server-side backup: `Request::Backup { name }`/`Response::BackedUp`, an in-process snapshot copy under the table's existing write lock (`ADR-0052`'s `Compact` precedent reused), write-to-temp-then-rename per file (`STORAGE-014`'s discipline reused), into an opt-in `SERVER_BACKUP_ROOT`-configured server-local directory; restore needs no new code (the produced directory is exactly what `open_portable` already reads). Sourced from the same `FUTURE-GROWTH.md` audit as `SERVER-METRICS-DESIGN`. **Real finding**: this is the first request whose input would choose a server-local filesystem write target — a genuinely new attack-surface category, closed by construction (`BAK-FR-004`: a single path component joined to a server-configured root, never a caller path) rather than glossed over. ADR-0065 (**Accepted as designed**); `docs/design/SERVER-BACKUP-DESIGN.md`. | `SERVER-WRITE-BATCH-CRASH-ATOMICITY` | — (no spec written for the design itself) | Owner reviewed and accepted option (a) | Implemented | `docs/design/SERVER-BACKUP-DESIGN.md`, `ADR-0065` |
| `SERVER-BACKUP` | The accepted `SERVER-BACKUP-DESIGN`/`ADR-0065` implemented and registered as `SERVER-001` v0.54.0 / FR-064: `Request::Backup { name }`/`Response::BackedUp { files, bytes }` at protocol 24; `ConnectionStore::backup` (default `Unsupported`), `BackupReport`, `copy_table_files` (server-layer), `handle_backup` (path validation, temp-dir-then-atomic-rename, kept out of `dispatch` since it needs `ServeOptions::backup_root`); `ServeOptions::{with_backup_root, backup_root}`; `backup_source: Option<PathBuf>`/`with_backup_source`/`backup()` on `Dog`/`Memory`/`Entity`/`Relation`; `SERVER_BACKUP_ROOT` wired into `memory_server.rs` (all three tables, only when `SERVER_DATA_DIR` is also set); `SchemaDrivenClient::backup(name)`. **Three real corrections found during implementation** (see `ADR-0065`'s own "Acceptance and implementation" for the full account): (1) a single adapter-level `backup_source` plus one shared prefix-glob copy replaces the design's per-layer file-enumeration sketch — correct by construction against every companion file this crate has or adds later, not hand-maintained; (2) `Dog` is mechanically backup-capable (`DogConnectionStore::backup` implemented) but `dog_server.rs` has no durable-directory story at all, so no binary wires it — the design's own "no non-durable backup" non-goal, discovered to already cover `Dog` entirely; (3) the target directory is produced by one whole-directory atomic rename rather than per-file temp-then-rename — simpler, and a strictly stronger guarantee (the target name is never visible in a partial state). | `SERVER-BACKUP-DESIGN` | `SERVER-001` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --all-features -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast`: 527 lib tests + `tests/server_backup_integration.rs` (5/5) + every other integration target green, `server_python_client` included (1/1) | Implemented | `ADR-0065`, `src/server/serve.rs`, `tests/server_backup_integration.rs` |
| `SCHEMA-MIGRATION-DESIGN` | A design for reading an old-`SchemaTag`-tagged directory forward: not a generic diffing engine, but a documented three-step pattern (a caller-defined old-layout struct implementing `SchemaTag` under the old tag; the existing `open_..._portable` read path, typed over it; the existing `create_..._production_stack` write path, unmodified, for a fresh new-tagged directory) proven against the one real bump this crate has ever shipped. Sourced from `docs/FUTURE-GROWTH.md`'s "Schema migration tooling" bullet and `Memory`'s own `SCHEMA_TAG` doc comment ("the trigger for a layout version with an in-place upgrade — the design's option L2," `ADR-0056`). ADR-0066 (**Accepted as designed**); `docs/design/SCHEMA-MIGRATION-DESIGN.md`; explicitly declines a generic `migrate_portable<Old, New>` helper and a wire `Request::Migrate` for now. | `SERVER-BACKUP` | — (no spec written for the design itself; implementation registers `STORAGE-019`) | Owner reviewed and accepted option (a) | Implemented | `docs/design/SCHEMA-MIGRATION-DESIGN.md`, `ADR-0066` |
| `SCHEMA-MIGRATION` | The accepted `SCHEMA-MIGRATION-DESIGN`/`ADR-0066` implemented and registered as `STORAGE-019` v0.1.0: `examples/support/migrate_memory_v1_to_v2_lib.rs` (new — `MemoryV1` and its `Record`/`SchemaTag`/`IndexedField`/`ScannableField` impls, `open_memory_v1_stack_portable`, `migrate_memory_v1_to_v2`, `migrate`), `#[path]`-included by both `examples/migrate_memory_v1_to_v2.rs` (the real CLI) and `tests/schema_migration.rs` (3 tests, new) so both exercise the exact same code. `examples/support/` (not a direct child of `examples/`) specifically so Cargo's example autodiscovery does not also try to build the `main`-less lib file as its own example target. Zero new `src/` file, zero new dependency, zero wire/protocol change, exactly as designed — no deviation. Proven: the flagship round trip through `Memory`'s real, unmodified `open_memory_production_stack_portable` with every field and `mentions` edge checked; the existing-destination refusal; the already-`@2`-tagged-source refusal with the existing schema-tag-mismatch `RecordBlobUnreadable`; a real `cargo run --example migrate_memory_v1_to_v2` smoke test against a genuine on-disk fixture (usage message + exit 1 with no args, the existing `RecordBlobUnreadable` message + exit 1 against a nonexistent source, a real four-file `Memory@2` directory written and reported against a real two-record fixture). | `SCHEMA-MIGRATION-DESIGN` | `STORAGE-019` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --all-features -- -D warnings` clean (including `--example migrate_memory_v1_to_v2 --test schema_migration` explicitly, since this crate's own `clippy` checkpoint skips `--all-targets` for a pre-existing, unrelated Linux-only bench); `cargo test -p rusty_multimodal_db --all-features --no-fail-fast`: 527 lib tests (unchanged) + every integration target green, `schema_migration` included (3/3, new) | Implemented | `ADR-0066`, `examples/support/migrate_memory_v1_to_v2_lib.rs`, `examples/migrate_memory_v1_to_v2.rs`, `tests/schema_migration.rs` |
| `SERVER-REPLICATION-DESIGN` | Round one of replication/high availability, named by `docs/FUTURE-GROWTH.md` as "realistically a multi-round effort, not a single ADR": `Request::FetchSnapshot`, gated behind a new `Replication` token class (never satisfied by `ReadOnly`/`ReadWrite`), streaming every file a table owns back over the wire — reusing `Backup`'s `with_exclusive` lock and `copy_table_files` file-enumeration unchanged. Directly reopens `ADR-0065`'s own rejected option (c) ("a wire-level streaming backup... a new bulk-exfiltration primitive") and names precisely what changed (a separate, opt-in credential, not `ReadWrite` itself). Explicitly not full replication: no continuous shipping, no automatic failover/promotion, no write forwarding, no cluster membership — a manually-refreshed cold standby only. Owner picked option (a); ADR-0067 (**Accepted as designed and implemented**); `docs/design/SERVER-REPLICATION-DESIGN.md`. | `SCHEMA-MIGRATION` | — (no spec written for the design itself; implementation extends `SERVER-001`) | Owner picked option (a); implemented the same session | Implemented | `docs/design/SERVER-REPLICATION-DESIGN.md`, `ADR-0067` |
| `SERVER-REPLICATION` | The accepted `SERVER-REPLICATION-DESIGN`/`ADR-0067` implemented and registered as `SERVER-001` v0.55.0 / FR-067: `Request::FetchSnapshot` (34, fieldless)/`Response::Snapshot { files: Vec<(String, Vec<u8>)> }` (23) at protocol 25; `ConnectionStore::fetch_snapshot` (default `Unsupported`), `read_table_files`/`ReadTableFilesError` (`copy_table_files`'s reading twin, size-ceiling-checked before any byte is read), `MAX_SNAPSHOT_BYTES` (8 MiB); a third `TokenClass::Replication` variant (compile-time-exhaustive, not a separate `AuthConfig` field) and its own `handle_connection` gate requiring exactly that class, refusing every other class including `ReadWrite`; `ServeOptions::{with_replication_token, replication_token}`; `fetch_snapshot()` on `Dog`/`Memory`/`Entity`/`Relation` (reusing each adapter's existing `backup_source`); `SERVER_AUTH_REPLICATION_TOKEN` wired into `memory_server.rs`; `SchemaDrivenClient::fetch_snapshot()`; Python `protocol.py`/`client.py`; `driver.py`'s refusal-path exercise only (the success path is Rust-side, see Evidence). **One real correction found during implementation**: `ServeOptions::is_configured()` originally missed `replication_token`, so a replication-token-only server would silently leave `Authenticate` a no-op and every connection defaulting to `ReadWrite` — caught immediately by the integration test's own success-path case, fixed before it shipped. Proven: `tests/server_replication_integration.rs` (5 tests — a full snapshot reopened via `open_memory_production_stack_portable` with the identical record set; no token, a `ReadWrite` token, and a `ReadOnly` token all refused `Unauthorized`; a table over `MAX_SNAPSHOT_BYTES` refused `TooLarge`); `tests/server_python_client.rs` extended (the refusal path at both the current version and a hand-negotiated 10); `tests/server_client_only.rs`'s pinned `Hello` example updated 24 → 25; `SERVER-002` v0.14.0, 73 vectors. | `SERVER-REPLICATION-DESIGN` | `SERVER-001` FR-067 | `cargo fmt`/`cargo clippy --all-features -- -D warnings` clean; `cargo test --all-features --no-fail-fast` 529 lib tests + every integration target green | Implemented | `ADR-0067`, `docs/design/SERVER-REPLICATION-DESIGN.md`, `tests/server_replication_integration.rs` |
| `SERVER-FILTERED-PAGE-DESIGN` | `docs/FUTURE-GROWTH.md`'s SQL-parity item 1: "Still absent: ... `ORDER BY` combined with a filter" — a new `Request::FilteredPage { order_by, after, limit, filter }`, answered `Response::Rows` reused, evaluated by one shared default (`scan_all` → filter via the existing `predicate_matches` → `Page`'s own key selection) correct for every domain with zero adapter overrides needed. Named, not hidden: `Memory`/`Relation`'s `Ordered` index gives no speed advantage to a *filtered* request this round — only the unfiltered `Page` fast path keeps it, untouched. `ADR-0061`'s own option (b), the real evaluation-strategy question it left open, resolved here as filter-then-order via the shared default. Owner picked option (a); ADR-0068 (**Accepted as designed and implemented**); `docs/design/SERVER-FILTERED-PAGE-DESIGN.md`. | `SERVER-REPLICATION` | — (no spec written for the design itself; implementation extends `SERVER-001`) | Owner picked option (a); implemented the same session | Implemented | `docs/design/SERVER-FILTERED-PAGE-DESIGN.md`, `ADR-0068` |
| `SERVER-FILTERED-PAGE` | The accepted `SERVER-FILTERED-PAGE-DESIGN`/`ADR-0068` implemented and registered as `SERVER-001` v0.56.0 / FR-068: `Request::FilteredPage` (35: `order_by`, `after`, `limit`, `filter: Vec<Predicate>`), answered `Response::Rows` (reused, no new response variant) at protocol 26; `ConnectionStore::filtered_page` (a trait method with a working default — `page`'s own precedent, not `Unsupported`-by-default — `scan_all()` → filter via `predicate_matches` → `page_rows`), `validate_filtered_page` (composes `validate_page` + per-predicate `validate_predicate`); `sql.rs`'s `OrderByWithFilter` exclusion removed, `WHERE` + `ORDER BY` compiles to `FilteredPage`; `client.rs`'s `fetch_filtered_page`/`fetch_ordered_page` dispatcher; Python `protocol.py`/`client.py` gain `FilteredPage`/`Client.filtered_page`, exercised in `driver.py`. **Two real corrections found during implementation** (see `ADR-0068`'s "Acceptance and implementation"): `FPG-FR-004`'s self-contradictory "default `Unsupported`" text corrected to match its own "Proposed shape" code sample (a working default); the design's Non-goals wrongly claimed no Python client change was needed, corrected. Proven: `tests/server_sql_integration.rs` (40 tests, a filtered-ordered round trip cross-checked against `page()` and `Query`, a no-`LIMIT` filtered multi-page walk over 2,500 records, every client-side refusal, the protocol-26 gate, an explicit unfiltered-`Page`-unaffected check); `tests/server_python_client.rs` extended; `tests/server_client_only.rs`'s pinned `Hello` example updated 25 → 26; `SERVER-002` v0.15.0, 74 vectors. | `SERVER-FILTERED-PAGE-DESIGN` | `SERVER-001` FR-068 | `cargo fmt`/`cargo clippy --all-features -- -D warnings` clean; `cargo test --all-features --no-fail-fast` 529 lib tests + every integration target green | Implemented | `ADR-0068`, `docs/design/SERVER-FILTERED-PAGE-DESIGN.md`, `tests/server_sql_integration.rs` |
| `SERVER-METRICS-HTTP-DESIGN` | `docs/FUTURE-GROWTH.md`'s Metrics/observability bullet: "Still absent: ... any HTTP `/metrics` endpoint" — an opt-in second listener, `crates/rusty_http` (path dependency, default features, zero new transitive dependency — the exact `rusty_tls`-for-`rustls` reuse template `ADR-0014` already established), answering exactly `GET /metrics` with `ServerMetrics::render()`'s existing text unchanged. Revisits `ADR-0064`'s own declined option (b) with new information: `rusty_http` did not exist/wasn't cited when that call was made. No `PROTOCOL_VERSION`/`SERVER-002` change; no auth on the new listener in this round, a named tradeoff. Owner picked option (a); ADR-0069 (**Accepted as designed and implemented**); `docs/design/SERVER-METRICS-HTTP-DESIGN.md`. | `SERVER-FILTERED-PAGE` | — (no spec written for the design itself; implementation extends `SERVER-001`) | Owner picked option (a); implemented the same session | Implemented | `docs/design/SERVER-METRICS-HTTP-DESIGN.md`, `ADR-0069` |
| `SERVER-METRICS-HTTP` | The accepted `SERVER-METRICS-HTTP-DESIGN`/`ADR-0069` implemented and registered as `SERVER-001` v0.57.0 / FR-069 (no `PROTOCOL_VERSION` change, stays 26): new optional `rusty_http` path dependency (default features, `server`-gated only) mirroring `ADR-0014`'s `rusty_tls` reuse; new `src/server/metrics_http.rs` — an opt-in accept loop over `rusty_http::sync::SyncTransport`, one route (`GET /metrics` → 200 + `ServerMetrics::render()` verbatim + `Content-Type: text/plain; version=0.0.4` + `Connection: close`, else 404), malformed/oversized/truncated heads close with no reply and no panic, never mutates `ServerMetrics`; `ServeOptions::metrics_http`/`with_metrics_http` (taken out before `Arc::new(options)` in `serve_tables`, run on its own thread alongside the existing wire accept loop); all four `src/bin/*_server.rs` binaries wired to `SERVER_METRICS_HTTP_ADDR`, fatal bind error on a typo'd address. Built by Codex via `codex-build`, independently reviewed and re-verified by Claude (every file read, proof re-run from a clean checkout). **One correction found before the build, not during it**: the design's own acceptance criterion asserting the HTTP scrape body byte-identical to a same-instant wire `Metrics` response was unsatisfiable as written (`Metrics` renders before counting its own request; `dogserver_uptime_seconds` can cross a wall-clock second boundary) — corrected to compare individual counter values, excluding uptime. Proven: 5 new integration tests (`tests/server_metrics_http_integration.rs` — unchanged default behavior, live counters exposed over HTTP without the scrape itself mutating them, 404s, bad-head recovery on both listeners, fatal invalid-address startup in all four binaries) + 5 new unit tests in `metrics_http.rs` + 1 in `serve.rs`. | `SERVER-METRICS-HTTP-DESIGN` | `SERVER-001` FR-069 | `cargo fmt`/`cargo clippy -p rusty_multimodal_db --all-features --tests --bins --lib -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` 769 tests green (up from 758), 0 failed; `cargo tree` confirms no new transitive dependency | Implemented | `ADR-0069`, `docs/design/SERVER-METRICS-HTTP-DESIGN.md`, `tests/server_metrics_http_integration.rs` |
| `SERVER-RESTORE-DESIGN` | `docs/FUTURE-GROWTH.md`'s Backup/restore bullet: "Still absent: any `RESTORE` request or documented restore procedure" — a real CLI, `examples/restore_backup.rs`, closing `ADR-0065`'s own Decision-text claim that restore needs no new code by actually building the operator-facing tool: crash-safe per-file staged copy (stage everything, then rename each file individually into place — a named limitation, not one whole-group atomic swap, since the target may already hold sibling tables' own files), existing-target refusal, and a real verification reopen via the named domain's own `open_..._production_stack_portable`. No `PROTOCOL_VERSION`/`SERVER-002` change, no new `src/` library code, no wire operation — restore is architecturally an offline, directory-level operation for this crate's mmap-backed server model, not a live one (see the design doc's own Context). Owner picked option (a); ADR-0070 (**Accepted as designed and implemented**); `docs/design/SERVER-RESTORE-DESIGN.md`. | `SERVER-METRICS-HTTP` | `STORAGE-020` | Owner picked option (a); implemented the same session | Implemented | `docs/design/SERVER-RESTORE-DESIGN.md`, `ADR-0070` |
| `SERVER-RESTORE` | The accepted `SERVER-RESTORE-DESIGN`/`ADR-0070` implemented and registered as `STORAGE-020` v0.1.0 (no wire/protocol change): new `examples/support/restore_backup_lib.rs` (no `main`) — `Domain`/`RestoreReport`/`RestoreError`, `restore(backup_dir, target_stem, domain)` staging every backup file into a fresh temp directory first, then renaming each staged file individually into the target parent (per-file atomic, not one whole-group swap), refusing outright if any file already exists at the target's own prefix, verifying via a real reopen through the named domain's own portable production constructor and `AllIds::all_ids().len()`; new thin `examples/restore_backup.rs` CLI wrapper printing the `SERVER_DATA_DIR`/restart next step. Built by Codex via `codex-build`, independently reviewed and re-verified by Claude — clean on the first attempt, the design's per-file-atomicity semantics and `examples/support/` testability split having been made precise before handoff. Proven: `tests/restore_backup.rs` (10 tests — 5 offline: domain parsing, target-prefix refusal, missing-backup cleanup, empty-backup verification failure per domain, no-filename error; 5 live over a real socket: a full `Memory`/`Entity`/`Relation` round trip each via a real `Request::Backup` and an independent production reopen checking every field and relation/`mentions` edge, retry refusal, `Entity`'s sibling-table coexistence, a staging-copy-failure case, a wrong-domain `DurabilityError` case with every copied file retained). | `SERVER-RESTORE-DESIGN` | `STORAGE-020` | `cargo fmt`/`cargo clippy -p rusty_multimodal_db --all-features --tests --bins --lib --example restore_backup -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` 779 tests green (up from 769), 0 failed | Implemented | `ADR-0070`, `docs/design/SERVER-RESTORE-DESIGN.md`, `tests/restore_backup.rs` |
| `MVCC-SPIKE-DESIGN` / `MVCC-SPIKE` | Real MVCC correctness spike, `ADR-0071` (Accepted as designed), `docs/design/MVCC-SPIKE-DESIGN.md` (Accepted): research-gated, in-memory Dog-like field values, append-only side history, snapshot reads, atomic write-write conflict rejection, explicit minimum-snapshot-bounded GC with typed reclaimed-history errors. One txn id per nonempty commit; no production storage, server, protocol, or binary wiring. Full implementation remains a separately authorized unit. Built by Codex via `codex-build`, independently reviewed and re-verified by Claude before merge. | `SERVER-RESTORE` | `MVCC-SPIKE-FR-001..006` | Nine unit tests in `src/generic_spike/mvcc_spike.rs`; `cargo fmt -p rusty_multimodal_db -- --check` / `cargo clippy -p rusty_multimodal_db --all-features -- -D warnings` / `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` all clean, 779 tests green, 0 failed (Codex's build-sandbox-reported Python-client failure did not reproduce independently — no runnable `python3`/`python` in that sandbox, not a real defect); `cargo test -p rusty_multimodal_db --lib --features research --no-fail-fast` 361/361 green including all nine new tests; the bare `--features research` (no `--lib`) command still fails to compile on an unrelated pre-existing `server`/`client`-feature-gating mismatch in `tests/server_replication_integration.rs`, confirmed to reproduce identically on the pre-change `main` baseline | Accepted/Implemented/Verified | `ADR-0071`, `docs/design/MVCC-SPIKE-DESIGN.md`, this PR |
| `MVCC-PRODUCTION-DESIGN` / `MVCC-PRODUCTION` | Real, production MVCC wiring for `SESSION_MVCC_ISOLATION` (protocol 27) against `Memory`/`Entity`/`Relation`, `ADR-0072` (Accepted as designed, revised through nine rounds of Codex-found and one Claude-found specification/implementation correction before and during the build — see that ADR's own "Acceptance and implementation" for the full account), `docs/design/MVCC-PRODUCTION-DESIGN.md`. New `src/server/mvcc.rs`: per-`(record, field)` version chains adapted from `MVCC-SPIKE`'s proven `(old_value, superseded_by_txn)` shape to a no-before-image `(new_value, txn_id)` shape, a shared per-table `AtomicU64` txn counter assigned at *apply* time (inside whichever exclusive section each write path already runs — the load-bearing round-nine correction: assigning at journal-append time instead left a cross-path ordering race), and a persistent `<mmap_path>.mvcc` history store flushed only at existing reclamation boundaries (`Compact`, journal checkpoint). No new insert-log/journal entry kinds needed at all — round nine's fix removed that requirement entirely. `Memory`/`Entity`/`Relation` each gain: `with_mvcc(path)` (opt-in, `with_backup_source`'s own "unset by default" precedent), activation with a one-time baseline scan, full mutation-path coverage (`Insert`/`Replace`/`Delete`/`ReplaceIf`/session `Commit`/atomic `WriteBatch`, journaled and non-journaled), a write-write conflict check reusing `ErrorCode::Conflict`, and `Compact`'s own flush-before-clear. `Dog`/`Order`/`Employee` untouched — `ConnectionStore` gained four new methods, all defaulting to `Unsupported`/no-op, so their existing code and tests needed zero changes. Implemented directly by Claude, not Codex: Codex's sandbox execution environment became persistently unusable partway through scoping (a local IPC/process-spawn timeout unrelated to this spec, confirmed via direct log inspection, not resolved by a real CLI-path fix or a full machine restart) — owner picked direct implementation per this crate's own host-takeover convention; a fresh Codex session should independently review once its sandbox is restored. **Completed across PRs #234–#241, all merged 2026-09-18** (the text above describes PR #234; the follow-ons closed every gap it named): #235 `Entity`/`Relation` wire-level tests; #236 client support — Rust `SessionOptions::mvcc_isolation()` and the Python client (`MVCC2-FR-011`); #237/#238 the insert-log restart-recovery gap closed by a per-domain `open_with_mvcc` (`docs/design/MVCC-OPEN-HOOK-PROPOSAL.md`, option (b) — a pre-open read via existing `pub(crate)` primitives, so the feared `GenericMmapStore::open` hook was never needed); #239 `Dog`/`Order`/`Employee` wired at the owner's call, reversing the ADR's deferral; #240 a real bug found only by #241's deployment test — no non-journaled write path flushed `<mmap_path>.mvcc` (only `Compact`/journal checkpoints did), so an unjournaled table's session `Commit` vanished from MVCC history across a restart — fixed in all six domains by flushing after every MVCC-recording write; #241 `SERVER_MVCC_ISOLATION` on `memory_server` with real-binary restart tests. **Still open, named**: reconstruction from a pending *journal* remainder; MVCC and the crash-atomic journal on one table (`memory_server` refuses the pair at startup); independent Codex review — its sandbox could not spawn processes throughout. | `MVCC-SPIKE-DESIGN`, `MVCC-SPIKE` | `MVCC2-FR-001..012` | Per PR: `cargo fmt -p rusty_multimodal_db -- --check` / `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean, full `cargo test -p rusty_multimodal_db --features server,research` green, 0 failed. At `main` after #243: 604 lib tests; 6+6+6 real-socket MVCC tests (`tests/server_{memory,entity,relation}_mvcc_integration.rs`); 3 real-binary `memory_server` tests (`tests/memory_server_mvcc_integration.rs`, including the restart case that exposed #240); 10 `mvcc.rs` unit tests; MVCC unit tests in all six adapters. Two pre-existing tests updated for the `PROTOCOL_VERSION` 26→27 bump plus `SERVER-002` | Implemented/Verified (independent review owed) | `ADR-0072`, `docs/design/MVCC-PRODUCTION-DESIGN.md`, `tests/server_memory_mvcc_integration.rs` |
| `SERVER-QUERY-PLANNER-DESIGN` | `docs/FUTURE-GROWTH.md`'s SQL-parity item 1: "Still absent: any query planner or optimizer" — step one, and exactly the step `SERVER-SQL-SELECT-DESIGN.md`'s own Open questions named ("whether `Query` should ever consult an existing index... the first real step toward the query planner"): when a `Request::Query` `filter` carries an `Eq` predicate on a field the domain's `describe()` reports `filter_eq: true` for, `dispatch` fetches candidates via the adapter's existing `filter_eq` + per-id `get` instead of `scan_all`, falls back to `scan_all` on any `filter_eq` error, and re-checks every predicate through the unchanged `evaluate_query` — the index narrows what is read, never what is returned, so the result set is identical by construction (`QPL-FR-003`/`005`). Zero trait, adapter, wire, protocol, or client change. Named, not hidden: no cost model (use the index whenever declared; never more reads than the scan, no win when the value is near-universal); "`WHERE id = …` via `GetById`" is not expressible in today's `Predicate` and stays a grammar/schema question; row order and the `LIMIT`-selected subset may differ from before within the existing "unspecified order" contract. Options: (a) `Query` only, as proposed / (b) the same step for `Query`/`Aggregate`/`FilteredPage`/`Join` together / (c) decline. Owner picked option (a), same session; ADR-0073 (**Accepted as designed and implemented**); `docs/design/SERVER-QUERY-PLANNER-DESIGN.md`. Implemented as `SERVER-001` v0.58.0 / `FR-070` in `src/server/serve.rs` alone (`QueryPlan`/`plan_query`/`query_candidates`, the `Query` dispatch arm as validate → plan → candidates → evaluate); one recorded deviation (`QPL-FR-004`'s `debug_assert!` omitted — the fixture test proving the fallback would trip it). Proven: 5 new `serve.rs` unit tests over a `PlannerFixture` (exact/superset/refusing index, `get`/`scan_all` counters), 4 new real-socket `tests/server_sql_integration.rs` tests (all seven shipped indexed fields plus the `Dog` control cross-checked against the full scan; `Entity::label` variants matched by `FilterEq` and rejected by `Query`; two-predicate conjunction both orders; runtime `Insert`/`Replace`/`Delete`), every pre-existing test unmodified; `benches/server.rs` `memory-planner` rows in `RESULTS.md`. No independent review yet — built directly while Codex's sandbox could not spawn processes; owed. | `SERVER-SQL-SELECT`, `SERVER-FILTERED-PAGE` | `SERVER-001` v0.58.0 / `FR-070` | Owner picked (a); implemented and verified the same session (`fmt`/`clippy -D warnings` clean, lib 604 up from 599, `server_sql_integration` 44 up from 40, 0 failed); a fresh Codex review of design and code once its sandbox is restored | Implemented | `docs/design/SERVER-QUERY-PLANNER-DESIGN.md`, `ADR-0073` |
| `SERVER-QUERY-PLANNER-CONSUMERS-DESIGN` | Query planner step two — `ADR-0073`'s own option (b), taken as the next round after step one merged and measured: one private `indexed_candidates(store, schema, filter)` helper (= `query_candidates(store, plan_query(schema, filter), filter)`) substituted for `scan_all()` in the three remaining `scan_all`-then-`predicate_matches` consumers — the `Aggregate` dispatch arm, the `ConnectionStore::filtered_page` default *body* (signature unchanged, no adapter override), and `evaluate_join`'s left side — each consumer's existing per-row re-check keeping the result set identical by construction (`QPC-FR-001`–`005`). Named, not hidden: `FilteredPage` has zero observable difference (`page_rows` sorts the filtered set); `Aggregate` group order / `LIMIT`-kept groups and `Join` pair order / `LIMIT`-kept pairs may differ, inside their designs' own "unspecified order" contracts (`QPC-FR-006`); still no cost model; `Join`'s right side has nothing to narrow. Options: (a) all three consumers / (b) `FilteredPage` only / (c) decline. Owner picked option (a), same session; ADR-0074 (**Accepted as designed and implemented**); `docs/design/SERVER-QUERY-PLANNER-CONSUMERS-DESIGN.md`. Implemented as `SERVER-001` v0.59.0 / `FR-071` in `src/server/serve.rs` alone, no deviation: `indexed_candidates`, the `Aggregate` arm, the `filtered_page` default body, `evaluate_join`'s left loop, `Query` refactored onto the helper. Proven: 3 new `serve.rs` unit tests (each consumer over `PlannerFixture` with an exact/superset/refusing index and `get`/`scan_all` counters), 3 new real-socket `tests/server_sql_integration.rs` tests (aggregate tally, exact filtered-page sequence, within-table join pair set), 1 new cross-table join test in `tests/server_memory_integration.rs`; every pre-existing test passed unmodified — no order-pin needed relaxing. `benches/server.rs`'s `memory-planner` rows extended to `count(*)` and `fpage-50` (`RESULTS.md`). No independent review yet — built directly while Codex's sandbox could not spawn processes; owed. | `SERVER-QUERY-PLANNER` | `SERVER-001` v0.59.0 / `FR-071` | Owner picked (a); implemented and verified the same session (`fmt`/`clippy -D warnings` clean, lib 607 up from 604, `server_sql_integration` 47 up from 44, `server_memory_integration` 14 up from 13, 0 failed); a fresh Codex review once its sandbox is restored | Implemented | `docs/design/SERVER-QUERY-PLANNER-CONSUMERS-DESIGN.md`, `ADR-0074` |
| `SERVER-QUERY-PLANNER-RANGE-DESIGN` | Query planner step three — the first of `ADR-0073`'s two named open questions, and `docs/FUTURE-GROWTH.md`'s "still absent: a range path through the `Ordered` index": when a filter carries an ordering predicate (`Lt`/`Le`/`Gt`/`Ge`) or an `Eq` on the one field an adapter reports as range-indexed (`Memory`/`Relation`'s `updated_at_unix_ms`, the `Ordered` index of `ADR-0059`), the shared candidate step walks the `BTreeSet<(key, id)>` between the predicate's bounds instead of `scan_all`, and every consumer re-checks every predicate as today — result set identical by construction. Needs exactly what step one said: a new `RangeBy::range_by(lower, upper)` on `Ordered` (a guarded `BTreeSet::range` over pairs — empty, never a panic, for an inverted or both-`Excluded` range; sentinel-free in the generic layer, `Uuid::nil()`/`max()` supplied by the adapter) exposed on `GenericProductionStore`, and a server-side declaration — `ConnectionStore::range_field()`/`range_ids()` with defaults, overridden by the two adapters — chosen over a `FieldCapabilities` flag because that is wire shape (protocol 28, downgrade rewrite, `SERVER-002`, vectors, Python client) for a fact no client can act on. `QueryPlan::IndexRange` is chosen only after the unchanged `IndexEq` rule, so no request that planned `IndexEq` changes plan; reaches `Query`/`Aggregate`/`FilteredPage`/`Join` through the existing `indexed_candidates` with no call-site change (`QPR-FR-001`–`007`). Named, not hidden: `Query`/`Aggregate`/`Join` rows now arrive in ascending `(updated_at, id)` order (still unspecified); `FilteredPage` zero difference; no bound tightening or index intersection; the `since`-shaped `FilteredPage` still reads every in-range record — the O(page) walk is option (b); `Reminder::due_at_unix_ms` stays a full scan. Options: (a) every consumer, as proposed / (b) (a) plus the O(page) `FilteredPage` walk on `Memory`/`Relation` / (c) decline. Owner picked option (a), same session (PR #273); ADR-0075 (**Accepted as designed and implemented**); `docs/design/SERVER-QUERY-PLANNER-RANGE-DESIGN.md`. Implemented as `SERVER-001` v0.60.0 / `FR-072` on `claude/planner-range-impl`, no deviation: `RangeBy` + `impl RangeBy for Ordered` + `GenericProductionStore::range_by` (`src/generic/`), `range_field`/`range_ids` defaults, `QueryPlan::IndexRange`, `plan_query`/`range_bounds`/`query_candidates`/`indexed_candidates`, `uuid_pair_bounds` (`serve.rs`), the two adapter overrides (`memory.rs`/`relation.rs`). Proven: 1 new `generic/memory.rs` unit test (every bound shape including the `BTreeSet::range` panic inputs), 6 new `serve.rs` unit tests (lib 614, up from 607 — the range rule, the bound mapping, the sentinel mapping, the walk reading only the walked ids, the refusal fallback, every consumer on the walk vs. the scan), 4 new real-socket `tests/server_sql_integration.rs` tests (51, up from 47 — every comparator, two-sided, `=`, inverted/equal-exclusive, two lower bounds, runtime `Insert`/re-keying `Replace`/`Delete` on `Memory` and `Relation`, each against the scan and the `created_at` twin; the aggregate tally; the filtered-page sequence; the no-range-field control on `Dog`/`Entity`), 1 new `tests/server_memory_integration.rs` test (15, up from 14 — the cross-table join with a range left filter). Every pre-existing test passed unmodified. `benches/server.rs`'s `memory-planner` rows extended with the `index-range` pair (`RESULTS.md`). No independent review — Codex's sandbox still cannot spawn processes; owed. | `SERVER-QUERY-PLANNER-CONSUMERS`, `SERVER-ORDERED-INDEX` | `SERVER-001` v0.60.0 / `FR-072` | Owner picked (a); implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 614 (up from 607), `server_sql_integration` 51 (up from 47), `server_memory_integration` 15 (up from 14), every other target unchanged and green, 892 tests across 39 targets, 0 failed); a fresh Codex review once its sandbox is restored | Implemented | `docs/design/SERVER-QUERY-PLANNER-RANGE-DESIGN.md`, `ADR-0075` |
| `SERVER-FILTERED-PAGE-WALK` | `ADR-0075`'s own option (b), taken as the next round under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch, `ADR-0059`'s precedent; the fork held open at review): a `FilteredPage` whose `order_by` is the range field and whose filter is only bounds on it is the bounded walk of the `Ordered` index — `page_by` from the later of the client's cursor and the tightest lower bound (`Gt k` → `(k, max)`, `Ge`/`Eq k` → `(k - 1, max)`), `get` each id, keep the longest prefix every predicate matches (the walk is ascending on the walked key, so the first upper-bound reject ends the page) — at most `limit` pairs walked and records read, however many the bounds admit; every other shape answers through the trait default's body, factored into `filtered_page_by_candidates` (`FPW-FR-001`–`006`). Identical sequence to the default, proven in-process against it (26 shapes, 3 pinned) and over a socket via SQL on `Memory`/`Relation` through runtime writes. Named, not hidden: `Page`'s consistency class (a concurrently deleted id shortens the page rather than being back-filled); a mixed filter (`… AND category = …`) still pays the `ADR-0075` cost — option (b). No generic (`page_by` reused), wire, protocol, or client change. Options: (a) as implemented / (b) plus walk-past-rejects for mixed filters / (c) decline and revert. ADR-0076 (**Accepted as implemented** — merged unchanged as PR #276, 2026-09-21, option (a); option (b) taken as `ADR-0077`); `docs/design/SERVER-FILTERED-PAGE-WALK-DESIGN.md`. Implemented as `SERVER-001` v0.61.0 / `FR-073`. No independent review — Codex's sandbox still cannot spawn processes; owed. | `SERVER-QUERY-PLANNER-RANGE`, `SERVER-FILTERED-PAGE`, `SERVER-ORDERED-INDEX` | `SERVER-001` v0.61.0 / `FR-073` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 617 (up from 614), `server_sql_integration` 52 (up from 51), every other target unchanged and green, 896 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-FILTERED-PAGE-WALK-MIXED` | `ADR-0076`'s own option (b), taken as the next round under the owner's standing "keep working the future-growth list" instruction once PR #276 merged (a) (proposed and implemented on one branch; the fork held open at review): a `FilteredPage` ordered by the range field walks whenever its filter does not plan the declared equality index — bounds on the walked field start and cut the walk as before, every other predicate (another field's, `Ne` on the walked field) is passed over chunk by chunk of `limit` through `page_by`, resuming strictly after the last `(key, id)` read, until `limit` rows match, a bound rejects, or the index ends — the page divided by the rejects' selectivity, never more than the default read (`FPM-FR-001`–`006`). Identical sequence to the default, proven on an `I64` fixture with `get` counts, on `Memory` (32 shapes, 5 pinned), and over a socket via SQL on `Memory`/`Relation` through runtime writes. Named, not hidden: `Page`'s consistency class (a vanished id now back-filled); the worst case is every in-range record; a filter planning the equality index keeps the bucket even where a tight bound would beat it (measured). Fixed on the way: `bounded_walk_start` would have answered `Malformed` for a `Str` equality on another field; the bench's control pages re-ordered by `created_at_unix_ms` since a control ordered by the range field would itself walk. No generic, wire, protocol, or client change. Options: (a) as implemented / (b) plus intersecting the equality index with the range / (c) decline and revert. ADR-0077 (**Proposed and implemented**); `docs/design/SERVER-FILTERED-PAGE-WALK-MIXED-DESIGN.md`. Implemented as `SERVER-001` v0.62.0 / `FR-074`. No independent review — owed. | `SERVER-FILTERED-PAGE-WALK`, `SERVER-QUERY-PLANNER-RANGE`, `SERVER-QUERY-PLANNER` | `SERVER-001` v0.62.0 / `FR-074` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 619 (up from 617), `server_sql_integration` 53 (up from 52), every other target unchanged and green, 899 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-INTERSECT` | Query planner step four — `ADR-0077`'s own option (b) and the "index intersection" open question `ADR-0075` carried, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): a `WHERE` carrying both an equality on the declared index and a bound on the range field plans `IndexIntersect` — the bucket's ids and the range walk's ids, neither decoded, intersected exactly (the smaller hashed), only the members read; every consumer through `indexed_candidates` with no call-site change; the `FilteredPage` walk stands aside for it; a walk refusal reads the bucket alone, a bucket refusal scans (`QPI-FR-001`–`006`). No estimate needed — both indexes answer ids without a decode — and none taken: the wide-range case pays the walk's id list unconditionally, measured and slower than the bucket alone there. Set identity proven on `PlannerFixture` with `get`/scan counts, on `Memory`, and over a socket via SQL on `Memory`/`Relation` through runtime writes. `serve.rs` alone; no generic, wire, protocol, adapter, or client change. Options: (a) as implemented / (b) plus a width guard (the first estimate) / (c) decline and revert. ADR-0078 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-INTERSECT-DESIGN.md`. Implemented as `SERVER-001` v0.63.0 / `FR-075`. No independent review — owed. | `SERVER-FILTERED-PAGE-WALK-MIXED`, `SERVER-QUERY-PLANNER-RANGE`, `SERVER-QUERY-PLANNER` | `SERVER-001` v0.63.0 / `FR-075` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 622 (up from 619), `server_sql_integration` 54 (up from 53), 903 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-INTERSECT-BUDGET` | Query planner step five — `ADR-0078`'s own option (b), taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review), read as a *budget* on the intersection's range walk rather than an estimate of the range: `RangeBy::range_by_limited` abandons the walk at the first id past the budget with nothing materialized, `ConnectionStore::range_ids_limited` exposes it on `Memory`/`Relation`, and the `IndexIntersect` arm walks with ten ids per bucket id (`INTERSECT_WALK_BUDGET`, the crate's one measured cost ratio: a decode ~1 µs, an id visit ~40 ns) — the intersection when the range fits, the bucket alone past it, nothing walked for an empty bucket (`QPB-FR-001`–`006`). The worst case for a filter carrying both indexes is bounded at ~1.4× the bucket alone, whatever the range's width; no count, no size threshold, no estimate. Set identity on every path proven on `PlannerFixture` with counts (the over-budget path forced), `Memory`'s adapter, the generic `Ordered`, and the socket. `plan_query`, the wire, and the clients untouched. Options: (a) as implemented / (b) the budget as an operator setting / (c) decline and revert. ADR-0079 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-INTERSECT-BUDGET-DESIGN.md`. Implemented as `SERVER-001` v0.64.0 / `FR-076`. No independent review — owed. | `SERVER-QUERY-PLANNER-INTERSECT`, `SERVER-QUERY-PLANNER-RANGE` | `SERVER-001` v0.64.0 / `FR-076` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 625 (up from 622), 906 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-REMINDER-DUE-INDEX` | `Reminder` under `Ordered` — the "a wrap and a `range_field` change, when a consumer asks" open question `ADR-0059`/`ADR-0075` carried, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): `ReminderProductionStack` becomes the mmap core under a sorted index on `due_at_unix_ms`, rebuilt at every open, the files unchanged; the adapter declares it as its range field, walks `range_ids`/`range_ids_limited` over it, and overrides `page`/`filtered_page` exactly as `Memory`'s does — so the consumer's own "what is due" shapes (`due_at <= now AND status = … ORDER BY due_at LIMIT n`, a due count, a due window) take every planner round at once (`RDO-FR-001`–`006`). Identity proven at the generic, adapter, and socket levels through writes. Named, not hidden: one decode per record at open; two indexes over `due_at` until option (b). No file-format, wire, protocol, or client change. Options: (a) as implemented / (b) plus retiring `DueAtField`'s `HashMap` / (c) decline and revert. ADR-0080 (**Proposed and implemented**); `docs/design/SERVER-REMINDER-DUE-INDEX-DESIGN.md`. Implemented as `SERVER-001` v0.65.0 / `FR-077`. No independent review — owed. | `SERVER-ORDERED-INDEX`, `SERVER-QUERY-PLANNER-INTERSECT-BUDGET`, `SERVER-REMINDER-DOMAIN` | `SERVER-001` v0.65.0 / `FR-077` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 627 (up from 625), `server_sql_integration` 55 (up from 54), 909 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-COUNTED-WALK` | Query planner step six — the decode-free count `ADR-0080` named from its own `due-count` measurement, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): a `COUNT(*)` with no `GROUP BY` whose filter is nothing but bounds on the range field (at most one per side; empty allowed) is the sorted index's own count between the bounds — `RangeBy::range_count` on `Ordered`, `ConnectionStore::range_count` on the three range-indexed adapters, `counted_walk` asked first in the `Aggregate` arm — and reads no record; every other aggregate shape decodes as before (`QCW-FR-001`–`006`). Identical counts, a better consistency class (one count under one lock). Named, not hidden: an equality bucket is never counted (`Entity`'s `label` bucket is a superset); two bounds on one side still decode. No wire, protocol, or client change. Options: (a) as implemented / (b) plus `MIN`/`MAX` of the range field (the walk's first and last key) / (c) decline and revert. ADR-0081 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-COUNTED-WALK-DESIGN.md`. Implemented as `SERVER-001` v0.66.0 / `FR-078`. No independent review — owed. | `SERVER-REMINDER-DUE-INDEX`, `SERVER-QUERY-PLANNER-RANGE`, `SERVER-AGGREGATE` | `SERVER-001` v0.66.0 / `FR-078` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 630 (up from 627), `server_sql_integration` 56 (up from 55), 913 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-KEYED-WALK` | Query planner step seven — `ADR-0081`'s own option (b), taken under the owner's standing "keep working the future-growth list" instruction and widened once read against the index (proposed and implemented on one branch; the fork held open at review): the walk yields every key in range, so `SUM`/`AVG`/`MIN`/`MAX` of the range field over a pure range — alone or beside `COUNT(*)` — are reductions over the walked keys with no record read (`RangeBy::range_keys` on `Ordered`, `ConnectionStore::range_keys` on the three range-indexed adapters, `keyed_walk` over `counted_walk`), each reduced exactly as `evaluate_aggregate` reduces decoded rows, empties in the field's own kind (`QKW-FR-001`–`006`). The consumer's "next due" (`MIN(due_at) WHERE due_at > now`) is one such. Identity proven on the fixture with counts, the generic index, and over a socket. Named, not hidden: an aggregate over any other field and every `GROUP BY` still decode. No wire, protocol, or client change. Options: (a) as implemented / (b) plus `GROUP BY` the range field / (c) decline and revert. ADR-0082 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-KEYED-WALK-DESIGN.md`. Implemented as `SERVER-001` v0.67.0 / `FR-079`. No independent review — owed. | `SERVER-QUERY-PLANNER-COUNTED-WALK`, `SERVER-AGGREGATE` | `SERVER-001` v0.67.0 / `FR-079` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 633 (up from 630), `server_sql_integration` 57 (up from 56), 917 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-BOUND-TIGHTENING` | Query planner step eight — the "bound tightening" open question every planner round since `ADR-0075` carried, taken under the owner's standing "keep working the future-growth list" instruction once read as a comparison between literals rather than a cost model (proposed and implemented on one branch; the fork held open at review): two bounds on one side of the range field walk between the tightest two (`tightest_bounds` — greatest lower / least upper literal, exclusive over inclusive at a tie), in `plan_query`'s `IndexRange`/`IndexIntersect` and in the counted/keyed walks, which now accept any number of bounds per side; a contradiction walks an empty range; every result unchanged, proven on the fixture, the unit matrix, and over a socket (`QBT-FR-001`–`005`). Named, not hidden: the redundant predicate stays in the filter and is still re-checked on the decode paths; two pre-existing assertions rewritten to the new rule. No generic, adapter, wire, protocol, or client change. Options: (a) as implemented / (b) plus contradiction as a validation error / (c) decline and revert. ADR-0083 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-BOUND-TIGHTENING-DESIGN.md`. Implemented as `SERVER-001` v0.68.0 / `FR-080`. No independent review — owed. | `SERVER-QUERY-PLANNER-RANGE`, `SERVER-QUERY-PLANNER-COUNTED-WALK`, `SERVER-QUERY-PLANNER-KEYED-WALK` | `SERVER-001` v0.68.0 / `FR-080` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 635 (up from 633), `server_sql_integration` 58 (up from 57), 920 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-KEYED-GROUPS` | Query planner step nine — `ADR-0082`'s own option (b), taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): a `GROUP BY` of exactly the range field over a pure range is one group per run of equal walked keys — the key in the field's kind, each column (`COUNT(*)`, `SUM`/`AVG`/`MIN`/`MAX` of the field) reduced over the run as `evaluate_aggregate` reduces a bucket — with no record read and no bucket search, linear in the range where the decode path is quadratic in the groups (`QKG-FR-001`–`005`). Sequence-identical to the decode path under a bound, the same set with none; the one shape where a `limit` would truncate a different set (grouped, no bound, a `limit`) still decodes. Named, not hidden: `GROUP BY` any other field, or the key beside another, still decodes; `evaluate_aggregate`'s own linear bucket search is left as it was. No generic, adapter, wire, protocol, or client change. Options: (a) as implemented / (b) plus a `HashMap` bucket in `evaluate_aggregate` / (c) decline and revert. ADR-0084 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-KEYED-GROUPS-DESIGN.md`. Implemented as `SERVER-001` v0.69.0 / `FR-081`. No independent review — owed. | `SERVER-QUERY-PLANNER-KEYED-WALK`, `SERVER-AGGREGATE` | `SERVER-001` v0.69.0 / `FR-081` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 637 (up from 635), `server_sql_integration` 59 (up from 58), 923 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-AGGREGATE-HASHED-BUCKET` | `ADR-0084`'s own option (b), taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): `evaluate_aggregate` finds each decoded row's bucket through a hash of its key (a private `BucketKey` mirroring `ScanValue` with `Eq + Hash`, `F64` by bits — a kind no group key can hold) instead of a linear search over the buckets so far, so every decoded `GROUP BY` — the shapes the planner's walks cannot answer — is linear in its rows rather than quadratic in its groups; the same buckets, first-seen order, values, and `limit` truncation (`AGB-FR-001`–`003`). Not a planner change: which rows are decoded is untouched. No generic, adapter, wire, protocol, or client change. Options: (a) as implemented / (b) `Hash + Eq` on `ScanValue` itself / (c) decline and revert. ADR-0085 (**Proposed and implemented**); `docs/design/SERVER-AGGREGATE-HASHED-BUCKET-DESIGN.md`. Implemented as `SERVER-001` v0.70.0 / `FR-082`. No independent review — owed. | `SERVER-AGGREGATE`, `SERVER-QUERY-PLANNER-KEYED-GROUPS` | `SERVER-001` v0.70.0 / `FR-082` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 638 (up from 637), `server_sql_integration` 60 (up from 59), 925 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLAN-METRICS` | The "plan metrics" open question every planner design since `ADR-0073` carried, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): `ServerMetrics` gains one fixed family, `dogserver_query_plans_total{plan="full_scan"|"index_eq"|"index_range"|"index_intersect"|"bounded_walk"|"counted_walk"|"keyed_walk"}`, incremented once per planned read (`Query`/`Aggregate`/`FilteredPage`/`Join`) answered without an error. The path is classified before dispatch by a pure `plan_of` over the same predicates the arms consult (`plan_query`, `bounded_walk_applies`, `counted_walk_applies`/`keyed_walk_applies`), so `dispatch` and every planner function are untouched (`QPM-FR-001`–`005`). Two approximations named: a read refused at validation is never counted; a walk-only aggregate whose adapter refuses the walk (no shipped adapter) counts as the walk it asked for. `MET-FR-002`'s bounded, fixed set amended by one fixed family. No wire, protocol, adapter, or client change. Options: (a) as implemented / (b) thread a `&PlanCounters` through `dispatch` / (c) decline and revert. ADR-0086 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLAN-METRICS-DESIGN.md`. Implemented as `SERVER-001` v0.71.0 / `FR-083`. No independent review — owed. | `SERVER-METRICS`, `SERVER-QUERY-PLANNER` | `SERVER-001` v0.71.0 / `FR-083` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 640 (up from 638), `server_metrics_integration` 4 (up from 3), 928 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-QUERY-PLANNER-RANGE-FOLD` | Query planner step ten — the "fold that never materializes the keys" open question `ADR-0082` named from its own measurement, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): every reduction of the range field is a fold, so `RangeBy::range_fold` walks the `Ordered` index applying a closure to each key with nothing collected, `KeyStats` reduces count/sum/min/max in one pass, and an ungrouped `SUM`/`AVG`/`MIN`/`MAX` over a pure range answers from one `ConnectionStore::range_stats` instead of a `Vec` of every key in range (`QRF-FR-001`–`005`). The grouped walk (`ADR-0084`) still materializes its keys, named. Every answer unchanged, proven on the fixture, the generic index, and over a socket. No wire, protocol, or client change. Options: (a) as implemented / (b) plus a run-emitting fold for the grouped walk / (c) decline and revert. ADR-0087 (**Proposed and implemented**); `docs/design/SERVER-QUERY-PLANNER-RANGE-FOLD-DESIGN.md`. Implemented as `SERVER-001` v0.72.0 / `FR-084`. No independent review — owed. | `SERVER-QUERY-PLANNER-KEYED-WALK`, `SERVER-QUERY-PLANNER-COUNTED-WALK` | `SERVER-001` v0.72.0 / `FR-084` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 642 (up from 640), `server_sql_integration` 61 (up from 60), 931 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-REQUEST-LATENCY` | The first "still absent" item of `docs/FUTURE-GROWTH.md`'s metrics entry, taken under the owner's standing "keep working the future-growth list" instruction (proposed and implemented on one branch; the fork held open at review): `ServerMetrics` gains `dogserver_request_duration_seconds`, one Prometheus histogram with sixteen fixed buckets from 100 µs to 10 s (chosen against `benches/server.rs`'s own numbers) plus `+Inf`, observed once per dispatched request from its frame's arrival to its response being ready — at the same post-dispatch site as `requests_total`, so `_count` is that population — and rendered before `uptime_seconds` (`RLH-FR-001`–`005`). `ADR-0064`'s "bucketing machinery and a real decision about bucket boundaries" made: seventeen atomics, a cumulative render, and the bench's numbers. `MET-FR-002`'s bounded, fixed set amended by one histogram of fixed width. Named, not hidden: one histogram for every request kind; the socket write is not measured; queue depth and journal size remain absent. No wire, protocol, adapter, or client change. Options: (a) as implemented / (b) plus a `kind` label / (c) decline and revert. ADR-0088 (**Proposed and implemented**); `docs/design/SERVER-REQUEST-LATENCY-DESIGN.md`. Implemented as `SERVER-001` v0.73.0 / `FR-085`. No independent review — owed. | `SERVER-METRICS`, `SERVER-QUERY-PLAN-METRICS` | `SERVER-001` v0.73.0 / `FR-085` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 643 (up from 642), `server_metrics_integration` 5 (up from 4), 933 tests across 39 targets, 0 failed); owner's confirmation of (a) at review; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-PAGE-DESCENDING` | The "descending walks" open question carried since `ADR-0055`, taken under the owner's standing "keep working the future-growth list" instruction as the first wire change of the line (proposed and implemented on one branch; **awaiting the owner's review** — the working agreement reserves public API changes for the owner): `Request::PageDesc` (36) and `Request::FilteredPageDesc` (37), `Page`/`FilteredPage`'s exact fields with the cursor named `before`, walked from the greatest `(order_by, id)` downward, answered `Rows`; protocol 28, `Malformed` below it; `PageBy::page_by_desc` answers `Memory`/`Relation`/`Reminder`'s ordered field at the page's cost, every other shape pages the scan's keys backward, the filtered twin takes the planner's candidate step then the descending selection; SQL `ORDER BY x [ASC|DESC]`; `page_desc` on both clients; `SERVER-002` 0.17.0; the vectors extended (`PGD-FR-001`–`008`). Every descending answer is the ascending answer reversed, proven in-process, over a socket, and from Python. Named, not hidden: a protocol bump; no descending bounded walk (option (b)). Options: (a) as implemented / (b) plus a descending bounded walk / (c) decline and revert. ADR-0089 (**Proposed and implemented; awaiting review**); `docs/design/SERVER-PAGE-DESCENDING-DESIGN.md`. Implemented as `SERVER-001` v0.74.0 / `FR-086`. | `SERVER-PAGE`, `SERVER-FILTERED-PAGE`, `SERVER-ORDERED-INDEX`, `SERVER-SQL-ORDER-BY`, `SERVER-PYTHON-CLIENT` | `SERVER-001` v0.74.0 / `FR-086`; `SERVER-002` 0.17.0 | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 646 (up from 643), `server_sql_integration` 62 (up from 61), 937 tests across 39 targets, 0 failed; the Python vectors suite green); **the owner's review and merge**; a fresh Codex review once its sandbox is restored | Implemented; awaiting review |
| `SERVER-SQL-ID-PREDICATE` | The "`WHERE id = …` (not expressible in today's `Predicate`)" SQL-parity item, taken under the owner's standing "keep working the future-growth list" instruction and read as a client-side compilation rather than a wire change (proposed and implemented on one branch; in `ADR-0089`'s PR, awaiting the owner's review): the SQL compiler splits the one `id = '<uuid>'` condition out of the `WHERE` (only when the schema has no field `id`; `Eq` and a UUID literal, at most once, else a client-side `Sql` error), answers it with `GetById`, re-checks the other predicates with the server's own evaluator (`predicate_matches`, moved to `protocol.rs`), applies the `SELECT` list, honours `LIMIT 0` (`SID-FR-001`–`004`). `ORDER BY`/`GROUP BY`/`JOIN` with `id` keep their refusal. No server, wire, or Python change. Options: (a) as implemented / (b) an `id` pseudo-field on the wire / (c) decline and revert. ADR-0090 (**Proposed and implemented**); `docs/design/SERVER-SQL-ID-PREDICATE-DESIGN.md`. Implemented as `SERVER-001` v0.75.0 / `FR-087`. | `SERVER-SQL-SELECT`, `SERVER-PAGE-DESCENDING` (the same PR) | `SERVER-001` v0.75.0 / `FR-087` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo check --no-default-features --features client` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — `server_sql_integration` 63 (up from 62), 938 tests across 39 targets, 0 failed); the owner's review of the PR it shares with `ADR-0089` | Implemented; awaiting review |
| `SERVER-QUERY-CONTRADICTION` | `ADR-0083`'s option (b), taken on the owner's word ("contradiction") as a protocol round (28 → 29, no new variant): a filter no record can satisfy — two `Eq` with different literals or an `Eq` beside an `Ne` of the same literal on one field; on `U32`/`I64` a lower bound above an upper bound, or equal with either side exclusive — is refused with `Malformed` before any read on a connection negotiated at 29 or above; below 29 the empty answer every earlier version gave (rule 3's nearest older shape applied to a semantics change, as version 27 did). Decided between the literals (`protocol::contradicted`, pure), never against the table; `Query`, `Aggregate`, `FilteredPage`/`FilteredPageDesc`, both sides of `Join` (`QCX-FR-001`–`004`). Proven by a unit matrix and over a socket at 29 and 28. The last concrete item of the future-growth list. ADR-0091 (**Proposed and implemented**); `docs/design/SERVER-QUERY-CONTRADICTION-DESIGN.md`. Implemented as `SERVER-001` v0.76.0 / `FR-088`; `SERVER-002` 0.18.0. | `SERVER-QUERY-PLANNER-BOUND-TIGHTENING`, `SERVER-PROTOCOL-VERSION` | `SERVER-001` v0.76.0 / `FR-088`; `SERVER-002` 0.18.0 | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 647 (up from 646), `server_sql_integration` 64 (up from 63), 940 tests across 39 targets, 0 failed; the Python vectors suite green); the owner's merge; a fresh Codex review once its sandbox is restored | Implemented |
| `SERVER-DATA-DIR-LOCK` | The release-readiness review's first "do now" item, taken on the owner's word ("harden"): one server process per data directory — `server::data_lock::DataDirLock` (`File::try_lock` on `<dir>/.rusty_multimodal_db.lock`, released by the kernel however the process ends), claimed by `memory_server` in durable mode before any store opens, a second server on the same directory refusing to start and naming the lock file; and `durability::sync_parent_dir` after every write-to-temp-then-rename install so the rename itself is on disk. Library, harness, wire untouched; `rust-version` 1.88 → 1.89 for `File::try_lock` (the toolchain call, named for the owner). | `SERVER-DATA-DIR`, `SERVER-COMPACTION` | `SERVER-001` v0.77.0 / `FR-089`; `STORAGE-017` 0.1.1 | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 650 (up from 647), `memory_server_data_dir_lock` 1 (new), 944 tests across 40 targets, 0 failed) | Implemented | `ADR-0092`, `docs/design/SERVER-DATA-DIR-LOCK-DESIGN.md` |
| `SERVER-CONNECTION-LIMITS` | The release-readiness review's second "do now" item, taken on the owner's word ("harden"): three opt-in limits on `ServeOptions` — an idle timeout on the accepted socket (a stalled peer closed as a disconnect is, its session and MVCC snapshot released), a connection cap checked at accept against an in-flight count (a refused socket closed unread and counted in `dogserver_connections_refused_total`), and a row cap refusing a `Query` with no `limit` or one above it, and any page above it, with `TooLarge` before any read; `evaluate_query` stops at `limit` while filtering, cap or no cap; `memory_server` reads `SERVER_IDLE_TIMEOUT_SECS`/`SERVER_MAX_CONNECTIONS`/`SERVER_MAX_QUERY_ROWS`. No wire change; defaults reproduce the unbounded server — the fork (b) defaults on held open. | `SERVER-QUERY-LAYER`, `SERVER-METRICS`, `SERVER-REPLICATION` | `SERVER-001` v0.78.0 / `FR-090` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 651 (up from 650), `server_limits_integration` 3 (new), 948 tests across 41 targets, 0 failed) | Implemented | `ADR-0093`, `docs/design/SERVER-CONNECTION-LIMITS-DESIGN.md` |
| `SERVER-EXPOSURE-CHECK` | The release-readiness review's third "do now" item, taken on the owner's word ("3"): the README's rule — do not expose a server without both authentication and TLS — enforced by the binaries. A pure `server::exposure::check_exposure(addr, &options)`: a loopback bind needs nothing; any other bind needs auth configured and TLS, else `NoAuth`/`NoTls` naming the variables to set; each of the four binaries refuses to start on it after the TLS fold, `SERVER_ALLOW_INSECURE=1` turning the refusal into a warning. `serve`/`ServeOptions` unchanged (the library default still reproduces the original open server — the fork (b) into the library held open); no wire change. | `SERVER-AUTH`, `SERVER-TLS`, `SERVER-OPTIONS` | `SERVER-001` v0.79.0 / `FR-091` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 654 (up from 651), `memory_server_exposure` 3 (new), 954 tests across 42 targets, 0 failed) | Implemented | `ADR-0094`, `docs/design/SERVER-EXPOSURE-CHECK-DESIGN.md` |
| `STORAGE-CRASH-SAFETY-GATE` | The release-readiness review's fourth "do now" item, taken on the owner's word ("4"): the crash-safety diagnosis harness's four trials — a real `crash_writer` subprocess `SIGKILL`ed at a chosen line, the file reopened cold through the public API — as asserted integration tests (`tests/crash_safety.rs`, research-gated so CI's `cargo test --all-features` runs it, about two seconds): flushed updates survive; a torn slot write is excluded on reopen and the caller's record reseeded while the control keeps the attempted value; a torn in-place update is exactly one written pattern; an unflushed kill leaves no invented value. The harness stays the hand-run diagnostic; `SIGKILL` still proves process-crash survival only, never power loss. | `STORAGE-012`, `STORAGE-017` | `STORAGE-021` v0.1.0 | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — `crash_safety` 4 (new), 958 tests across 43 targets, 0 failed) | Implemented | `ADR-0095`, `docs/design/STORAGE-CRASH-SAFETY-GATE-DESIGN.md` |
| `SERVER-MVCC-HISTORY-RECLAIM` | The release-readiness review's fifth "do now" item, taken on the owner's word ("5"): `ADR-0072`'s `MvccIndex::gc` — written, unit-tested, and called by no production code — wired to its named boundary. `MvccState::reclaim` reclaims every chain entry below the oldest open snapshot (everything below current when none is open; a no-op before activation), and `Memory`/`Entity`/`Relation`'s `compact` call it inside the exclusive section before the history flush, so the persisted `.mvcc` store is the reclaimed index. An open snapshot keeps exactly what it can still read; the current value is never dropped. No wire change; no automatic trigger (the fork (b) held open). | `MVCC-PRODUCTION`, `SERVER-COMPACTION` | `SERVER-001` v0.80.0 / `FR-092` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 657 (up from 654), 961 tests across 43 targets, 0 failed) | Implemented | `ADR-0096`, `docs/design/SERVER-MVCC-HISTORY-RECLAIM-DESIGN.md` |
| `SERVER-SYNCED-UPDATES` | The release-readiness review's remaining High finding, taken on the owner's word ("durability"): the one write whose acknowledgement preceded its durability — the in-place field update (`UpdateField`, and a `Transaction` batch on a table with no journal; a bounded copy into a mapped page, on disk at the next `Flush`/checkpoint/write-back) — gains an opt-in `msync` before the acknowledgement on `Memory`/`Entity`/`Relation` (`with_synced_updates`, `SERVER_SYNC_UPDATES=1`), a failure withheld as `Storage`; journaled arms and every other write (already `fsync`ed through the insert log or the journal) unchanged. Measured: `update_field` on `Memory`, release build, this container, 2,000 updates each: 0.1 → 105.4 µs per update at 1K rows, 0.2 → 99.7 µs at 100K rows (unsynced → synced); the `msync` costs what the insert log's per-entry `sync_data` costs, and does not scale with the table. The forks — a ranged `msync`, journaling `UpdateField`, defaults on — held open. | `SERVER-TRANSACTION`, `SERVER-JOURNAL`, `STORAGE-017` | `SERVER-001` v0.81.0 / `FR-093` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 658 (up from 657), 962 tests across 43 targets, 0 failed) | Implemented | `ADR-0097`, `docs/design/SERVER-SYNCED-UPDATES-DESIGN.md`, `RESULTS.md` |
| `CI-FEATURE-SETS` | `ADR-0097`'s finding closed: the monorepo built this crate with `--all-features` only, so `--features server` (broken by `ADR-0092`) and `--features client` (broken by `ADR-0094`) merged green and stayed broken; a `multimodal-db-feature-sets` job now checks every shipped feature set with `--all-targets`, and the five server-only test targets never registered with `required-features` are. No code change. | — | `ADR-0098` | Implemented and verified the same session (all five feature sets check clean; `cargo test -p rusty_multimodal_db --features server,research` — 962 tests across 43 targets, 0 failed, unchanged) | Implemented | `ADR-0098` |
| `SERVER-MEMORY-SERVER-DEFAULTS` | `ADR-0093`'s and `ADR-0097`'s option (b), taken together on the owner's word ("Defaults"): `memory_server` starts with a 300 s idle timeout, a 1,024-connection cap, and synced updates unless told otherwise (`0` turns each off; a malformed value is a startup error naming the variable); the row cap stays opt-in because under it a `Query` with no `limit` is refused, which every `SELECT` without `LIMIT` is — a clamping cap is the fork held open. The library's defaults and the other three binaries unchanged. | `SERVER-CONNECTION-LIMITS`, `SERVER-SYNCED-UPDATES` | `SERVER-001` v0.82.0 / `FR-094` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — `memory_server_defaults` 3 (new), 965 tests across 44 targets, 0 failed) | Implemented | `ADR-0099` |
| `CI-STANDALONE-FOLDED-IN` | The separate call `ADR-0098` named, taken on the owner's word ("msrv"): the crate's standalone workflow — which GitHub never reads inside the monorepo — has its four uncovered checks folded into the root workflow: clippy `-D warnings` per shipped feature set (replacing `ADR-0098`'s `check`), `cargo test --features client`, the Python client's offline wire-vector conformance test, `cargo doc` (warnings allowed; ~80 rustdoc warnings are a named follow-up), and an MSRV job on 1.89.0. The standalone file stays, annotated, for the mirror repository. No code change. | `CI-FEATURE-SETS` | `ADR-0100` | Implemented and verified the same session (every step run locally; `cargo +1.89.0 check -p rusty_multimodal_db --all-features --all-targets` clean) | Implemented | `ADR-0100` |
| `RUSTDOC-CLEAN` | `ADR-0100`'s named follow-up, taken on the owner's word ("rustdoc"): the 82 rustdoc warnings fixed — most from one cause, a module with outer docs on its `pub mod` line has its inner-doc links resolved in the parent's scope, so those links are now qualified from the crate root; `Self` named in module docs; private targets as code spans; moved paths corrected — and the monorepo's `cargo doc` step now denies warnings. Docs-only. | `CI-STANDALONE-FOLDED-IN` | `ADR-0101` | Implemented and verified the same session (`RUSTDOCFLAGS=-D warnings cargo doc` clean) | Implemented | `ADR-0101` |
| `SERVER-ROW-CAP-CLAMP` | The fork `ADR-0099` held open, taken on the owner's word ("clamp"): under a row cap a `Query` that names no `limit` — the one shape the cap exists to bound — is answered as if it had asked for the cap (the first `cap` matches in scan order, exactly `limit: Some(cap)`'s answer) and counted in `dogserver_query_rows_clamped_total`; an explicit `limit` or page above the cap is still `TooLarge`. With that, `memory_server` defaults the cap to 10,000, so all three limits default on. A "truncated" mark on the reply would be a wire change — the fork held open. | `SERVER-CONNECTION-LIMITS`, `SERVER-MEMORY-SERVER-DEFAULTS` | `SERVER-001` v0.83.0 / `FR-095` | Implemented and verified the same session (`cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — 966 tests across 44 targets, 0 failed) | Implemented | `ADR-0102` |

## Sequencing notes

This roadmap is intentionally linear (each unit depends on the previous)
because the task specifies this exact working order: "bootstrap → dataset
generator → trait + three backends (with unit tests) → benchmark suite →
run it → write up results," with an explicit check-in point before the
benchmark run if the record shape, workload list, or dataset sizes need to
change. `CACHE-MISS` runs alongside `BENCH-SUITE` rather than strictly
after it in practice (they land in the same PR), but is listed separately
because it has its own exit gate (ADR-0002 compliance) distinct from the
default suite's.

`HYBRID-BACKEND` was itself an out-of-scope item from the original
roadmap, promoted to an actual unit once `RESULTS.md`'s first pass made
the case for it concrete (`scan_ages` losing to both baselines). That's
the expected lifecycle for this section: an item here becomes a real unit
when a finding motivates it, not on a fixed schedule.

`GRAPH-TRAVERSAL` follows the same lifecycle: it was the untested third
leg of the original row/column/graph hypothesis, promoted once a real
edge relationship (`littermate_of`) and 1-2 hop traversal became the
concrete, scoped next step per the task that motivated it — not a general
graph-query-layer unit (see ADR-0004 and `STORAGE-006`'s non-goals).

`MIXED-WORKLOAD` follows the same lifecycle again: "mixed read/write
workload benchmarking" was an explicit out-of-scope item on this roadmap
until it became the concrete next step for testing whether
`HYBRID-BACKEND`'s eager write-through tax (ADR-0003) changes the
recommendation once reads and writes are actually blended — it doesn't
(see `RESULTS.md`), so no ADR revision followed, per `STORAGE-007`'s own
scoping.

`DURABILITY-TIER1`/`DURABILITY-TIER2` are the first units in this
roadmap not motivated by a prior finding — every workload above them is
purely in-memory, and durability was always the next open question once
`CanonicalCachedStore` was settled as the recommendation. Split into two
units (rather than one) to match the two rigor levels the motivating task
specified: `DURABILITY-TIER1` covers five fully-tested, fully-benchmarked
WAL/snapshot/hybrid variants (ADR-0005); `DURABILITY-TIER2` covers three
lighter proof-of-concept alternate architectures (ADR-0006), explicitly
not held to the same production-hardening bar. Both apply to
`CanonicalCachedStore` only, per the same "don't build for backends
nobody would deploy" reasoning `HYBRID-BACKEND` established for choosing
which backend gets new capability.

`CONCURRENCY-PROTOTYPES` follows `DURABILITY-TIER1`/`DURABILITY-TIER2` as
the next previously-out-of-scope axis: every workload up to and including
durability assumes single-threaded access, and reader/writer safety was
the natural next open question once `CanonicalCachedStore` had both a
recommendation and a durability story. Deliberately kept as one unit,
unlike the durability split, because the task's own two-tier rigor split
(three full-rigor Tier 1 variants, one lighter Tier 2 variant) fit inside
a single roadmap entry without needing a second one — and deliberately
*not* combined with `DURABILITY-TIER1`/`DURABILITY-TIER2` into a
"concurrent durable store" unit, since the motivating task named that
combination as a distinct, later follow-up round rather than part of this
one (see ADR-0007's Context).

`PRODUCTION-DEFAULT` is that distinct follow-up round, once it actually
arrived: six rounds of empirical work — row/column/graph,
`DURABILITY-TIER1`/`TIER2`, and `CONCURRENCY-PROTOTYPES` (measured three
times, across a container, a Windows desktop, and `baileyai`) — had all
converged on the same combination (`CanonicalCachedStore` architecture +
mmap + global `RwLock`), so this unit's job was to wire that one
already-justified combination together, verify it as a composed stack for
the first time, and make it the crate's documented default — not to
re-derive or re-benchmark any of the three picks, and not to build a
general "any concurrency strategy × any durability variant" combinatorial
matrix (see ADR-0008's Context and Non-goals in `STORAGE-011`).

`GENERIC-SCHEMA-DESIGN` follows `PRODUCTION-DEFAULT` as the natural next
question once the recommendation was fully realized on `Dog`: should the
crate's schema/query surface generalize beyond one domain at all, and if
so, to what shape? Deliberately staged as design-only, not an
implementation unit — the motivating task named this the most
hard-to-reverse decision the project has faced (the abstraction would
become the crate's public API surface), and required stopping for review
before any implementation code, per this project's own working-style
convention of checking in before large or hard-to-reverse changes. Its
"Depends on" `PRODUCTION-DEFAULT` reflects that the recommendation needed
to exist and be settled before it was worth asking whether it should
generalize, not that the design touches `ProductionStore` itself (it
doesn't — see ADR-0009 and the design doc's own Context).

`GENERIC-SCHEMA-LIBRARY` follows once every risk `GENERIC-SCHEMA-DESIGN`'s
own §4 named had been individually resolved: four validation spikes (kept
as historical record in `src/generic_spike/`, not deleted) tested the
design against real code and real benchmarks — Dog-overhead measurement,
an associated-type-ambiguity diagnosis and fix, macro-generated per-marker-
pair forwarding, and directed-relation-generalization measurement — before
any of it was treated as accepted. This unit promotes that validated
design into `crate::generic`, a real public library, and builds the piece
none of the four spikes attempted: a generic equivalent of `ProductionStore`
(`GenericMmapStore`/`GenericProductionStore`), verified by a flagship
durability-plus-concurrency integration test on `Order`/`Customer` — the
same bar `PRODUCTION-DEFAULT` set for `Dog`. Deliberately does *not*
follow the original design doc's §5 staged migration literally (port `Dog`
onto the generic core as a third validation domain, only then build a
production store) — `Dog` stays a benchmark fixture, not a target domain,
so `Order`/`Customer` became the real reference implementation directly;
see ADR-0009's "Acceptance and implementation" section for why this
deviation was made rather than followed as originally written.

`SERVER-QUERY-LAYER-DESIGN` follows `GENERIC-SCHEMA-LIBRARY` as this
project's first roadmap unit motivated by `docs/FUTURE-GROWTH.md` rather
than by a benchmark finding — the owner chose it over the other named
direction (SQLite/DuckDB-tier parity) when asked which of the two, if
either, to pursue after the project reached a fully-`Implemented` roadmap
with no unit in progress. Deliberately staged as design-only, matching
`GENERIC-SCHEMA-DESIGN`'s own precedent: a network-facing protocol is a
comparably hard-to-reverse public-surface decision, and this unit stopped
for owner review before any server implementation code was written. The
owner reviewed and accepted the design as proposed (ADR-0010, Accepted).
`SERVER-QUERY-LAYER` (below) is the real implementation that followed,
registering `SERVER-001`, matching how `STORAGE-012` followed
`GENERIC-SCHEMA-DESIGN`'s own acceptance. Its
"Depends on" `GENERIC-SCHEMA-LIBRARY`/`PRODUCTION-DEFAULT` reflects that
both store types it wraps (`ProductionStore`, `GenericProductionStore`)
need to already exist, not that either is modified by this design.

`SERVER-QUERY-LAYER` follows `SERVER-QUERY-LAYER-DESIGN` once the owner
accepted it: the real `src/server/**` implementation, behind a new
`server` Cargo feature kept deliberately separate from `research` (new,
additive capability, not a benchmarked alternative). Validated against
both domains the design's own acceptance criteria named — `Dog`
(`Neighbors`, a real symmetric relation) and `Order`/`Customer`
(`Parent`/`Children`, a real directed relation) — over a genuine
`TcpListener`/`TcpStream` pair, not just `dispatch`'s in-process logic.
Surfaced and fixed two real issues along the way, both documented in
`docs/PROJECT-STATUS.md`'s own entry for this unit: a Nagle/delayed-ACK
interaction that made every request/response round trip cost ~40ms until
`TCP_NODELAY` was set, and a test-isolation bug (two integration tests
racing on the same mmap-backed temp path) unrelated to the server itself.
Deliberately does not include a throughput benchmark (`benches/server.rs`)
— this round's acceptance criteria are correctness, not performance; see
`SERVER-001`'s own "Open questions."

`SERVER-QUERY-LAYER`'s own follow-up round added schema discovery
(`DescribeSchema`/`Response::Schema`, ADR-0011, `SERVER-001` v0.2.0) —
closing the one item ADR-0010's original design explicitly deferred
rather than rejected. The owner picked this from a short list of concrete
next directions offered after the initial implementation landed. Bounded
and additive (field *tags* stay the addressing scheme; this only adds
runtime discovery of names/capabilities), so it followed this project's
more common ADR-and-implementation-together cadence rather than the
design-only-first treatment `SERVER-QUERY-LAYER-DESIGN` itself got —
see ADR-0011's own Context for why that distinction was made explicitly,
not just assumed.

A second follow-up round added a third validation domain, `Employee`
(`server::employee`, `SERVER-001` v0.3.0), again from the same short list
of next directions and again treated as bounded/additive rather than a
new design-review gate — this domain doesn't change the protocol, the
framing, or the concurrency model, only adds a third adapter and (as it
turned out) completes existing `crate::generic` capability. `Employee` was
purpose-built (unlike `Order`/`Customer`, which had an external reference
domain) specifically to combine `SymmetricRelation` and `ChildOf` on one
self-referential record type — a combination ADR-0009's own "revisit if"
bullet named as untested. It found a real gap: `Reversed` (the
`ChildOf`-forwarding wrapper) never forwarded `Neighbors`, so no domain
stacking `Symmetric` beneath `Reversed` could reach a symmetric relation
from outside the stack. Fixed directly in `src/generic/{store,production}.rs`
(the forwarding impl plus a new `GenericProductionStore::neighbors`
method) — recorded as an addendum to already-Accepted ADR-0009, per the
same "completion of accepted capability, not a new decision" treatment
`GENERIC-SCHEMA-WRITE-THROUGH-FIX` established, not a new ADR. `Employee`
is the first `ConnectionStore` domain adapter where every relation-kind
request (`Parent`/`Children`/`Neighbors`) is a real operation, none
`Unsupported` — verified both at the `crate::generic` layer (a durable
flush-plus-reopen test) and over the real wire protocol
(`tests/server_employee_integration.rs`).

A third follow-up round closed the throughput/latency gap `SERVER-001`'s
own "Open questions" had named since its v0.1.0 acceptance criteria —
the "2" in the owner's "3 then 2" ("this domain, then a throughput
benchmark next"). `benches/server.rs` (`SERVER-001` v0.4.0) is a custom,
non-Criterion harness matching `benches/concurrency.rs`'s own shape (a
`Barrier`-synchronized thread sweep, aggregate ops/sec from the slowest
thread), the first benchmark in this crate to put a real
`TcpListener`/`TcpStream` pair in its timed path rather than measuring
`dispatch` in-process or a real socket only for pass/fail correctness.
Measures single-connection `GetById` round-trip latency and aggregate
throughput under a 1/4/8/16-thread sweep, across all three domains.
Headline finding: at this record-count scale, the network/framing
layer's own cost (~37 µs per round trip, this session's container) so
thoroughly dominates any per-domain in-process operation cost (tens to
low-hundreds of nanoseconds, per `RESULTS.md`'s existing sections) that
all three domains' numbers land in the same band — see `RESULTS.md`'s
`## Server / query layer` section for the full account and its own
caveats (this session's 4-core container bounds the thread-count sweep;
a real-hardware follow-up would be needed for a real per-core ceiling,
matching `## Concurrency`'s own established container-then-real-hardware
precedent).

A real-hardware follow-up round closed that remaining caveat: run
directly on the owner's Windows dev machine (`Beast`, confirmed via
`hostname`/`whoami`/`nproc` — 24 real cores, not a container),
`benches/server.rs`'s `THREAD_COUNTS` retuned from `[1, 4, 8, 16]` to
`[1, 4, 24, 48]` (the same array `benches/concurrency.rs` already
established for this machine). Throughput genuinely keeps climbing from
4 to 24 threads on real hardware — confirming the container's flat
4-through-16 plateau was that environment's own ceiling, not the
model's — then flattens or degrades at 48 (2× cores, deliberate
oversubscription), the same signature `## Concurrency`'s own
real-hardware passes already documented. See `RESULTS.md`'s real-hardware
subsection for the full tables and one honest, unexplained surprise this
pass surfaced (this machine's peak throughput running lower than the
container's own plateau).

A second real-hardware follow-up round, run directly on `baileyai` itself
(32 real cores, no SSH substitution needed this time), retuned
`THREAD_COUNTS` again, from `Beast`'s `[1, 4, 24, 48]` to
`[1, 4, 32, 64]` (matching `benches/concurrency.rs`'s own `baileyai`
sweep). Same qualitative shape as the `Beast` pass — throughput climbs
strongly from 4 to 32 threads, then flattens to mildly negative at 64
(2× cores) — confirming the ceiling again sits at or near real core
count. This pass also resolved the `Beast` pass's own open surprise:
`baileyai`'s peak throughput (over 1M ops/sec) is far higher than both
the container's and `Beast`'s own plateaus, and its latency is a third
to a quarter of either — real dedicated Linux hardware genuinely is
faster end to end, meaning `Beast`'s Windows loopback-TCP/scheduling
stack was the actual outlier, not the container. See `RESULTS.md`'s
second real-hardware subsection for the full tables.

A fourth follow-up round, picked from the same short list of concrete
next directions offered once "3 then 2" and both real-hardware passes
were done, closed a different named gap: `PROJECT-STATUS.md`'s own
"no schema-driven client *library* exists" item. `server::client::
SchemaDrivenClient` (`SERVER-001` v0.5.0) promotes the one-off logic
each domain's own schema-driven integration test had reimplemented by
hand (FR-011/FR-013) into real, reusable API — `connect` sends
`Request::DescribeSchema` first and keeps the result, and every
subsequent method is addressed by field *name*, never a domain's own
`FIELD_*` constant. Capability checks (`filter_eq`/`scan`/`update`
against `FieldCapabilities`, `parent`/`children`/`neighbors` against
`RelationCapabilities`) run client-side first, matching what the schema
already reports, rather than paying a round trip to learn what a typed
`Response::Err` would have said anyway. Bounded and additive — no new
`Request`/`Response` variant, no wire-format change — so this followed
ADR-0011's own bounded/additive-cadence precedent rather than opening a
new ADR: it completes an already-accepted decision (schema discovery)
rather than deciding something new. Verified against all three domains
in one test file (`tests/server_schema_driven_client.rs`) that imports
no domain-specific `FIELD_*` constant at all, proving the promotion
didn't quietly reintroduce compile-time domain knowledge through the
test's own back door.

`SERVER-AUTH-DESIGN` picks up the second of the owner's three directions
— authentication/encryption — and, unlike the client library, did not
ship straight to implementation: `docs/FUTURE-GROWTH.md` names this
"genuinely new," not incremental, the same category ADR-0010 itself was
in before `SERVER-QUERY-LAYER-DESIGN` was written, so this followed that
exact same design-first, stop-for-review treatment. ADR-0012 (Accepted)
and `docs/design/SERVER-AUTH-DESIGN.md` propose a shared-secret token
(`Request::Authenticate`), checked once per connection before any other
request is served, with two coarse classes (`ReadOnly`/`ReadWrite`) and
a constant-time comparison (a new, narrow dependency, `subtle` — the one
place this design departs from this project's usual "avoid new
dependencies" posture, deliberately, since hand-rolling constant-time
comparison is a well-known place DIY security code silently regresses).
**Explicitly, this design does not close the transport-encryption half
of the gap** — it requires pairing with an external TLS-terminating
proxy/tunnel for any non-localhost deployment, native TLS (`rustls`)
named as a real, larger follow-up rather than added by default, matching
the same dependency-weight reasoning ADR-0010 used to defer `tokio` and
gRPC. The owner reviewed and accepted the design as proposed, with no
changes requested.

`SERVER-AUTH` is that implementation unit, following how `SERVER-QUERY-LAYER`
followed `SERVER-QUERY-LAYER-DESIGN`'s own acceptance: `SERVER-001` v0.6.0
(`SERVER-001-FR-016`) implements `SERVER-AUTH-DESIGN` essentially as
proposed, no design decision reopened. `AuthConfig::new`/`AuthConfig::from_env`
resolve the design's own left-open "exact environment-variable naming"
question (`SERVER_AUTH_READ_ONLY_TOKEN`/`SERVER_AUTH_READ_WRITE_TOKEN`);
every pre-existing `serve` call site now passes `AuthConfig::default()`
(the one exception, `src/bin/dog_server.rs`, uses `::from_env()` so the
real binary can opt in) and required no other change — direct evidence
for `AUTH-FR-007`'s backward-compatibility claim, not just an assertion of
it. The one acceptance criterion needing empirical rather than
read-through evidence — that constant-time comparison actually holds — is
measured directly against `AuthConfig::check` (mean latency over 20,000
iterations, first-byte-mismatch vs. last-byte-mismatch tokens) rather than
over a real TCP round trip: network jitter at the millisecond scale would
swamp a signal this small, so a network-level timing test would pass
regardless of whether the comparison were actually constant-time — no real
evidence at all.

`SERVER-TRANSACTION-DESIGN` picks up the third and last of the owner's
three directions — session/transaction semantics — and, like
`SERVER-AUTH-DESIGN` before it, did not ship straight to implementation:
`docs/FUTURE-GROWTH.md` names this "genuinely new" too, so it got the
same design-first, stop-for-review treatment. ADR-0013 (**Accepted**)
and `docs/design/SERVER-TRANSACTION-DESIGN.md` propose the smallest real
slice of what `docs/FUTURE-GROWTH.md`'s broader "session/transaction
semantics across multiple requests" framing names: one new request kind,
`Request::Transaction`, batching several `UpdateField`-shaped writes into
one all-or-nothing operation, isolated from concurrent connections by
holding each store's existing internal lock for the whole batch instead
of once per write — no second lock introduced, matching ADR-0010's own
"no new lock at this layer" principle. **Deliberately does not propose
the literal multi-round-trip "session" half of the framing** — a
`BeginTransaction`/several-requests/`Commit` design would hold a
connection's exclusive lock open across an unbounded number of client
round trips, a real liveness/denial-of-service risk this project has
never accepted anywhere else in the server layer; named as a real
revisit trigger, not ruled out forever. **Also does not deliver crash-
atomicity** — a process crash between two of a batch's writes landing on
stable storage can leave a partial batch durably applied; this proposal
delivers atomicity/isolation with respect to concurrent access only, a
real but narrower guarantee than a full ACID transaction. Honestly
flags a real cost beyond `SERVER-AUTH`'s own purely server-layer-
additive implementation: the atomicity mechanism needs a new, minimal
primitive on the storage layer itself (`ProductionStore`/
`GenericProductionStore`), already-accepted and "closed" modules — the
second time this project would deliberately reopen them by design,
after the `Employee` round's real `Neighbors`-forwarding fix found a gap
there by accident. The owner reviewed and accepted the design as
proposed, with no changes requested.

`SERVER-TRANSACTION` is that implementation unit, following how
`SERVER-AUTH` followed `SERVER-AUTH-DESIGN`'s own acceptance:
`SERVER-001` v0.7.0 (`SERVER-001-FR-017`) implements
`SERVER-TRANSACTION-DESIGN` essentially as proposed, no design decision
reopened. The storage-layer critical-section primitive the design named
as a real cost up front landed exactly as scoped: `crate::production::TransactionalStore`
(`STORAGE-011` v0.2.0, `Dog`) and `crate::generic::production::GenericProductionStore::with_exclusive`
(`STORAGE-012` v0.3.0, `Order`/`Employee`) each let a caller hold the
store's existing lock across multiple logical operations, purely
additive — no existing `ProductionStore`/`GenericProductionStore` method's
behavior changed, verified by diff. `tests/server_transaction_integration.rs`'s
flagship concurrent test uses sequential-replay linearizability (the same
established pattern `SERVER-001-FR-009` uses), not a per-instant "never
observed half-written" check — that approach was tried first and dropped
once it became clear two independent, un-synchronized client round trips
can't distinguish a real atomicity bug from legitimately straddling two
different, fully-completed transactions, since this protocol has no
multi-field read to observe two fields at one consistent instant; the
single-threaded before/after tests prove batch atomicity deterministically
instead, with no such confound.

`SERVER-TRANSACTION-BENCHMARK` closes the one open follow-up question
`SERVER-TRANSACTION`'s own implementation left implicit: does
`Request::Transaction`'s longer-held lock — the real, named cost
`SERVER-TRANSACTION-DESIGN`'s own "Architecture" section flagged up
front — actually show up in the numbers? `benches/server.rs` (`SERVER-001`
v0.8.0, `SERVER-001-FR-018`) was extended with a directly-comparable
`{domain}-txn` row set, same environment/pool/thread-count sweep as the
existing `GetById` measurement, no ADR needed (bounded, additive
completion of already-accepted `FR-014` scope, matching how `SERVER-001`
v0.5.0's client library and the two real-hardware follow-ups were each
reasoned about). The answer: no meaningful latency or throughput cost was
found relative to plain `GetById`, on this session's shared container —
the longer-held lock is real in principle but swamped by the same
network/framing cost `SERVER-QUERY-LAYER`'s own original benchmark pass
already identified as this whole harness's dominant cost at this
record-count scale. See `RESULTS.md`'s `## Server / query layer`,
`### \`Request::Transaction\` follow-up` subsection for the full numbers.

`SERVER-TLS-DESIGN` picks up the second of the two options the owner
selected alongside the transaction benchmark — transport encryption —
and, matching `SERVER-AUTH-DESIGN`/`SERVER-TRANSACTION-DESIGN`'s own
precedent, does not ship straight to implementation: this is the last
remaining half of the "no auth, no encryption" gap ADR-0010 named at
acceptance, and ADR-0012's own "Validation and revisit triggers" named
exactly this proposal as the condition for revisiting native TLS rather
than deferring it again by default. ADR-0014 (Accepted) and
`docs/design/SERVER-TLS-DESIGN.md` propose native TLS terminated inside
this crate's own server process, rather than continuing to require an
external TLS-terminating proxy/tunnel — reversing ADR-0012's original
rejection of native TLS once the dependency-weight objection is checked
on its actual merits: a synchronous, `Read`/`Write`-compatible TLS API
composes with the existing thread-per-connection model with **zero**
changes to `src/server/framing.rs` (already generic over `Read`/`Write`,
a verified finding, not an assumption), undercutting the earlier
"disproportionate dependency" framing that had implicitly bundled TLS
together with an async-runtime shift. **Revised mid-review**: the owner
asked whether this owner's own ecosystem already had a hand-rolled or
wrapped solution before this got proposed as a fresh `rustls`
dependency — it does (`rusty_tls`, `Rusty-Mill/rusty_mill`, purpose-built
so no consumer in that ecosystem depends on `rustls` directly), so this
proposal depends on `rusty_tls` rather than `rustls` directly, inheriting
its own tested, fuzzed rejection-path coverage rather than rebuilding
it. **Explicitly does not add mTLS** — client identity remains exactly
`AuthConfig`'s existing shared-secret token scheme, now traveling
encrypted (though the mechanism, `rusty_tls::TlsAcceptor::new_with_client_auth`,
already exists if that's ever revisited). The owner reviewed and
accepted the design as revised, no further changes requested.

`SERVER-TLS` is that implementation unit, following how `SERVER-AUTH`/
`SERVER-TRANSACTION` each followed their own design's acceptance:
`SERVER-001` v0.9.0 (`SERVER-001-FR-019`) implements
`SERVER-TLS-DESIGN` essentially as revised, no design decision reopened.
The one real implementation-time finding this round surfaced: the
existing plaintext path splits a connection into independent read/write
halves via `TcpStream::try_clone` (two real OS-level socket handles),
but `rusty_tls::TlsServerStream`'s `rustls::ServerConnection` state
can't be split that way — a read and a write both need to reach through
the *same* connection object. Resolved with a new `ReadHalf`/`WriteHalf`
enum pair: the `Plain` variant keeps the existing split completely
unchanged (zero behavior change, verified by the full existing
plaintext test suite passing unmodified); the `Tls` variant shares one
`Rc<RefCell<TlsServerStream<TcpStream>>>` between both halves — `Rc`/
`RefCell`, not `Arc`/`Mutex`, since each connection is served by exactly
one OS thread, so a single-threaded runtime borrow check is enough.
`tests/server_tls_integration.rs`'s flagship acceptance test captures
the real bytes a TLS client sends on the wire (a byte-recording
`TcpStream` wrapper) and asserts the plaintext `Authenticate` token is
genuinely absent — the direct evidence the design's whole purpose
depends on, not an assumption.

`EXTERNAL-DB-BENCHMARK` is a different kind of round from everything
before it in this roadmap: not a new backend, durability variant,
concurrency strategy, or server capability this crate itself implements,
but the first comparison against real, external, general-purpose
databases — SQLite, Postgres, DuckDB — on the same three access-pattern
shapes (`get`, `scan_ages`, one-hop `littermate_of` traversal) this
project has always benchmarked. ADR-0015 (Accepted) and `STORAGE-013`
record the decision; `RESULTS.md`'s `## External database comparison`
section has the numbers. `ProductionStore` wins every cell — expected,
since it is a purpose-built in-memory store with none of a general-purpose
engine's parsing/planning/serialization overhead to pay — but the more
interesting finding is where each external engine gets *close*: DuckDB
closes to within 3.3× of `ProductionStore` on `scan_ages` at 1M records
(exactly the columnar-scan strength the task predicted), while being the
*worst* of the three external engines on both `get` and graph traversal
at every size — a narrow, not general, strength. A real methodology bug
was found and fixed mid-round, not glossed over: Postgres's graph-traversal
numbers initially showed a ~1,400× cliff between 1K and 100K records,
traced to a missing `ANALYZE` after the bulk `COPY` load driving Postgres's
recursive-CTE cost estimate high enough to trigger LLVM JIT compilation on
every execution — confirmed directly via `EXPLAIN ANALYZE` (53 ms before
the fix, 0.196 ms after, same query, same data). See `RESULTS.md` for the
full investigation and the corrected numbers it produced.

## Out of scope for this roadmap (see architecture doc "where this can go
next")

- A fifth backend/mode implementing lazy (dirty-flag) cache invalidation,
  as an alternative to `HYBRID-BACKEND`'s eager write-through — see
  ADR-0003's revisit triggers. `MIXED-WORKLOAD` tested the scenario most
  likely to motivate this (a write-heavy blended workload) and found no
  crossover, so this remains out of scope, not promoted.
- General N-hop (3+) or typed/weighted-edge graph traversal — `GRAPH-TRAVERSAL`
  scoped this to one relationship type (`littermate_of`) and 1-2 hops only;
  see ADR-0004 and `STORAGE-006`'s open questions.
- Memory-overhead-per-backend measurement.
- Dataset sizes beyond 1M.
- Write ratios beyond 90%, and bursty/correlated (as opposed to
  independently-drawn) mixed-workload access patterns — see
  `STORAGE-007`'s open questions.
- LSM-tree compaction — `DURABILITY-TIER2`'s `LsmStore` explicitly ships
  without it, per the motivating task's own instruction to scope this
  down rather than let it become its own multi-day project; see
  ADR-0006 and `RESULTS.md`'s durability open questions.
- Durability for `AosStore`/`SoaStore`/`CanonicalStore` — both
  `DURABILITY-TIER1` and `DURABILITY-TIER2` apply to `CanonicalCachedStore`
  only, per ADR-0005's Context.
- Extending any durability variant to persist more than `age` (i.e. full
  record durability for the Tier 2 mmap/`redb` variants, which currently
  persist only the mutable field) — see ADR-0006's revisit triggers.
  **Scoped, not built**, for the generic library's mmap store only, by
  `MULTI-FIELD-MMAP-DURABILITY-DESIGN`/ADR-0020 (Accepted): more than
  one *fixed-width* mutable durable field via a per-field `MmapScanned`
  layer; `redb`, LSM, and variable-width fields stay out.
- A wire-protocol version field or hello handshake, and a `bincode` 2.x
  migration — `SERVER-001`'s frames carry no version, and `Cargo.toml`
  pins `bincode = "1"`. **Scoped, not built**, by
  `BINCODE-ENCODING-STABILITY-DESIGN`/ADR-0021 (Accepted): the
  *encoding* under the `Request`/`Response` shape and the three blob
  bodies is named once and pinned with golden vectors; the shape's
  negotiation, 2.x (`config::legacy()` against those vectors), and a
  smaller `Uuid` encoding are its named revisit triggers. The shape's
  negotiation is now **built**, as `SERVER-001` v0.10.0 / FR-020
  (`SERVER-PROTOCOL-VERSION`, from `SERVER-PROTOCOL-VERSION-DESIGN`/
  ADR-0022): a `PROTOCOL_VERSION` constant at 2, an optional
  first-frame `Hello`, and append-only compatibility rules; 2.x and the
  `Uuid` encoding stay as they were, now with a version to be
  introduced under.
- An explicit physical-disk (`fsync`) guarantee on `checkpoint()` for the
  Tier 1 variants whose `checkpoint` doesn't currently call `sync_all` —
  see ADR-0005's Consequences and revisit triggers.
- Batching multiple writes into one `redb` transaction — `RedbStore`
  commits one transaction per `update_age` call; see ADR-0006.
- A concurrency strategy paired with a specific durability variant (e.g.
  a sharded store where each shard also owns its own WAL) — named
  explicitly by the motivating task as a distinct future round, not part
  of `CONCURRENCY-PROTOTYPES`; see ADR-0007's Context. **One specific
  instance of this — global `RwLock` + mmap — is now implemented as
  `PRODUCTION-DEFAULT`'s `ProductionStore`**; every *other* pairing (e.g.
  sharded + a WAL variant) remains out of scope, per `STORAGE-011`'s own
  Non-goals.
- `ShardedStore`'s shard count swept against alternatives to its fixed
  `SHARD_COUNT = 64` — see `RESULTS.md`'s concurrency open questions.
- Scaling `ActorStore` beyond one owning thread (e.g. sharding the actors
  themselves) — its measured throughput ceiling is a structural property
  of the single-writer-thread pattern this pass didn't attempt to lift;
  see ADR-0007's revisit triggers.
- `same_breed`/`neighbors` support for the sharded-locking and `dashmap`
  concurrency variants — both cover only `get`/`update_age`/`scan_ages`,
  the `ConcurrentStore` trait's scope; see ADR-0007's Considered options
  and `STORAGE-010`'s Non-goals.

These may become future roadmap units if `RESULTS.md`'s open questions
motivate them, but are not committed to now.
