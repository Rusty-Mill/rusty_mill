# Traceability

| Requirement | Roadmap | Decision/interface | Implementation | Verification | PR/release | State |
|---|---|---|---|---|---|---|
| `STORAGE-001-FR-001..005` | `GENERATOR` | `DogRecord`, `GeneratorConfig`/`generate` | `src/record.rs`, `src/generator.rs` | unit tests in `src/generator.rs` (8 tests, all passing) | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-002-FR-001..007` | `BACKENDS` | ADR-0001, `DogStore` trait | `src/store/mod.rs`, `src/store/{aos,soa,canonical}.rs` | unit tests (18) + cross-backend tests (4), all passing | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-003-FR-001..005` | `BENCH-SUITE`, `CACHE-MISS` | ADR-0002 | `benches/workloads.rs`, `benches/cache_events.rs`, `src/bench_support.rs` | `cargo bench` run (36/36 cases); `--features perf-events` build verified on Linux, runtime deferred to `baileyai` | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-004-FR-001..006` | `RESULTS` | ADR-0001, ADR-0002 | `RESULTS.md` | manual acceptance review against `STORAGE-004` criteria | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-005-FR-001..006` | `HYBRID-BACKEND` | ADR-0003, `DogStore` trait | `src/store/canonical_cached.rs` | unit tests (8, staleness test highest-priority) + cross-backend tests (5, incl. `all_backends_reflect_update_age_in_scan_ages_immediately`) + `cargo bench` 4-way run, all passing | follow-on PR | Implemented/Verified |
| `STORAGE-006-FR-001..010` | `GRAPH-TRAVERSAL` | ADR-0004, `DogStore` trait | `src/generator.rs` (`generate_littermates`), `src/store/{aos,soa,canonical,canonical_cached}.rs` (`neighbors`), `src/bench_support.rs` (`two_hop_neighbors`), `benches/workloads.rs` + `benches/cache_events.rs` | generator/backend unit tests + cross-backend tests (`all_backends_agree_on_neighbors_one_hop` highest-priority, `all_backends_agree_on_neighbors_two_hop`) + `two_hop_neighbors` unit tests + `cargo bench` 4-way run for both new workloads, all passing; cache-miss counters not producible in this session's environment (see ADR-0002) | follow-on PR | Implemented/Verified (wall-clock); cache-miss deferred |
| `STORAGE-007-FR-001..006` | `MIXED-WORKLOAD` | ADR-0003 (the write-through tax this benchmark tests under blended load) | `src/bench_support.rs` (`MixedWorkloadConfig`, `MixedWorkloadDriver`), `benches/workloads.rs` + `benches/cache_events.rs` | `src/bench_support.rs` unit tests (bounds validation incl. `NaN`, determinism, ratio-distribution statistical check, never-errors-against-its-own-dataset highest-priority, write-only-actually-mutates end-to-end check) + `cargo bench` 36-case run (3 ratios × 3 sizes × 4 backends), all passing; cache-miss counters not producible in this session's environment (see ADR-0002) | follow-on PR | Implemented/Verified (wall-clock); cache-miss deferred |
| `STORAGE-008-FR-001..010` | `DURABILITY-TIER1` | ADR-0005, shared `CanonicalCachedState` core, `DogStore` trait | `src/durability/{mod,wal_fsync,wal_buffered,snapshot_rebuild,snapshot_full,hybrid}.rs`, `src/store/mod.rs` (`StoreError::Durability`), `benches/durability.rs` | per-variant unit tests (WAL reconstruction-from-log highest-priority for variants 1/2; checkpoint-then-open-matches-fresh-store for variants 3/4; snapshot-plus-partial-replay-across-cutoff highest-priority for variant 5) + shared-core tests (WAL round-trip, torn-write recovery) + `cargo bench --bench durability` run (5 variants × 3 sizes × up to 3 metrics), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-009-FR-001..006` | `DURABILITY-TIER2` | ADR-0006, `DogStore` trait | `src/durability/{mmap_store,lsm_store,embedded_store}.rs`, `benches/durability.rs` | per-variant unit tests (flush-then-reopen round-trip for mmap; newest-generation-wins-across-multiple-flushes highest-priority for LSM; reopen-sees-every-committed-write for `redb`) + `cargo bench --bench durability` run (3 variants × 3 sizes; checkpoint/flush for mmap/LSM only, `redb` excluded by design), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-010-FR-001..008` | `CONCURRENCY-PROTOTYPES` | ADR-0007, new `ConcurrentStore` trait | `src/concurrency/{mod,global_rwlock,sharded,dashmap_store,actor}.rs`, `src/bench_support.rs` (`run_one_concurrent`), `benches/concurrency.rs` | per-variant unit tests (construction/read/write/scan sanity + sharded's cross-shard-reachability check) + all four variants' `concurrent_stress_matches_sequential_replay` flagship stress test (16 threads × 2,000 iterations each against a 20-id contended pool, highest priority — no lost updates/torn reads) + `cargo bench --bench concurrency` run (2 sizes × 3 ratios × 4 thread counts × 4 variants), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-011-FR-001..007` | `PRODUCTION-DEFAULT` | ADR-0008, `DogStore`/`ConcurrentStore` traits, `MmapAgeStore`/`GlobalRwLockStore` (unmodified) | `src/production.rs` (new), `src/lib.rs` (lead doc comment + re-export), `benches/workloads.rs`/`benches/concurrency.rs` (additive `ProductionStore` variant), `tests/production_integration.rs` (new) | `src/production.rs` unit tests (create/open/flush round-trip, `DogStore`/`ConcurrentStore` behavior, both `From` impls, reused `run_concurrency_stress_test`) + flagship integration test (highest priority — two 16-thread × 2,000-iteration concurrent contention phases separated by a genuine drop + reopen, verified via sequential-replay linearizability and a third, post-drop reopen for persistence) + `cargo bench --bench workloads -- production` + `cargo bench --bench concurrency` run, all passing | follow-on PR | Implemented/Verified |
| `STORAGE-012-FR-001..009` | `GENERIC-SCHEMA-LIBRARY` | ADR-0009 (Accepted), promoted `crate::generic::{traits,query,store}` (unmodified from the four spikes), new `Flush`/`ScannableField::set_scannable_value` | `src/generic/{mod,traits,query,store,mmap_field,mmap_store,order_customer,production}.rs` (new/promoted), `src/generic_spike/{mod,dog_impl,order_naive,order_bench_support}.rs` (import-path updates), `src/lib.rs` (new module + doc comment), `benches/generic_production.rs` (new), `tests/generic_production_integration.rs` (new) | `src/generic/**` unit tests (12: `MmapFieldValue` round-trip, `GenericMmapStore` create/open/flush + all four query traits, `order_customer`'s full-stack + production-stack tests, `GenericProductionStore`'s lock-forwarding tests) + flagship integration test (highest priority — two 16-thread × 2,000-iteration concurrent contention phases against `GenericProductionStore<OrderProductionStack>` separated by a genuine drop + reopen, verified via sequential-replay linearizability against a plain `HashMap` reference and a third, post-drop reopen for persistence, run 5× to rule out flakiness) + `cargo bench --bench generic_production` run (5 operations × 3 sizes), cross-checked against the `directed-relation-spike` round's and `ProductionStore`'s own established numbers, all passing | PR #17 | Implemented/Verified |
| `STORAGE-012-FR-010` | `GENERIC-SCHEMA-LIBRARY`, `SERVER-QUERY-LAYER` | ADR-0009 (Acceptance and implementation addendum), `Neighbors` forwarding on `Reversed` | `src/generic/store.rs` (`Neighbors` impl on `Reversed`), `src/generic/production.rs` (`GenericProductionStore::neighbors`) | `src/generic_spike/employee_impl.rs`'s three tests (in-memory stack, durable production stack, flush-then-reopen — each exercising `Neighbors` alongside `Parent`/`Children` on the same self-referential `Employee` record), all passing | PR #39 | Implemented/Verified |

| `SERVER-001-FR-001..009` | `SERVER-QUERY-LAYER` | ADR-0010 (Accepted), `ConnectionStore` trait, `Request`/`Response` protocol | `src/server/{mod,protocol,framing,dog}.rs` (unconditional under `server`), `src/server/order.rs` (additionally behind `research`), `src/bin/dog_server.rs` | unit tests (wire-format round trip, framing round trip + oversized/truncated-frame handling, `dispatch`'s per-request-kind mapping against a fixture store, both domain adapters against a real `ProductionStore`/`GenericProductionStore`) + `tests/server_dog_integration.rs` (real socket, including the flagship concurrent-client stress test — 8 connections × 200 requests, sequential-replay linearizability) + `tests/server_order_integration.rs` (real socket, `Parent`/`Children`), all passing | PR #35 | Implemented/Verified |
| `SERVER-001-FR-010..011` | `SERVER-QUERY-LAYER` | ADR-0011 (Accepted), `DomainSchema`/`ConnectionStore::describe` | `src/server/{mod,protocol,dog,order}.rs` (`describe`/`DescribeSchema`/`Response::Schema` additions) | unit tests (`server::tests::describe_schema_returns_the_fixture_store_own_shape`, `server::{dog,order}::tests::describe_names_*`) + `tests/server_dog_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_age_field` + `tests/server_order_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_status_field` (both real-socket, schema-driven end to end — discover a field by name, then complete a real request using only the discovered tag), all passing | PR #37 | Implemented/Verified |
| `SERVER-001-FR-012..013` | `SERVER-QUERY-LAYER` | ADR-0009 (Acceptance and implementation addendum), third domain adapter `server::employee::EmployeeConnectionStore` | `src/server/employee.rs` (new), `src/generic_spike/employee_impl.rs` (new), `src/generic/{store,production}.rs` (`Neighbors` forwarding — see `STORAGE-012-FR-010`), `tests/server_employee_integration.rs` (new) | `server::employee::tests::*` (5 tests, incl. `parent_children_and_neighbors_are_all_real_for_the_first_time`) + `tests/server_employee_integration.rs`'s three tests (the all-relations-real round trip, the schema-driven test, the empty-but-real `Parent`/`Neighbors` cases), all passing | PR #39 | Implemented/Verified |
| `SERVER-001-FR-014` | `SERVER-QUERY-LAYER` | Throughput/latency benchmark, closing `SERVER-001`'s own v0.1.0 "Open questions" gap; two real-hardware follow-ups retuned the thread-count sweep and resolved the connection-count-ceiling open question | `benches/server.rs` (new; `THREAD_COUNTS` retuned `[1,4,8,16]` → `[1,4,24,48]` → `[1,4,32,64]`) | `cargo bench --features server,research --bench server` completes without panics, all three domains, single-connection latency + thread-per-connection throughput sweep — container pass (1/4/8/16), `Beast` real-hardware pass (1/4/24/48, the owner's Windows dev machine), and `baileyai` real-hardware pass (1/4/32/64, 32 real cores), each run twice to characterize its own environment's run-to-run variance — numbers in `RESULTS.md`'s `## Server / query layer` section | PR #41, #43, #44 | Implemented/Verified |
| `SERVER-001-FR-015` | `SERVER-QUERY-LAYER` | Schema-driven client library, completing ADR-0011's own schema-discovery decision rather than opening a new one | `src/server/client.rs` (new, `SchemaDrivenClient`), `tests/server_schema_driven_client.rs` (new) | `tests/server_schema_driven_client.rs`'s four tests (one per domain — `Dog`, `Order`/`Customer`, `Employee` — driving every request kind purely by discovered field name, plus the unknown-field-name client-side-error case), importing no domain-specific `FIELD_*` constant, all passing | PR #46 | Implemented/Verified |
| `SERVER-001-FR-016` | `SERVER-QUERY-LAYER` | ADR-0012 (Accepted), `docs/design/SERVER-AUTH-DESIGN.md` (Accepted) — `AuthConfig`/`TokenClass`, `Request::Authenticate`, `ErrorCode::{Unauthenticated,Unauthorized}` | `src/server/mod.rs` (`AuthConfig`/`TokenClass`, `handle_connection`/`serve` auth gating), `src/server/protocol.rs` (`Request::Authenticate`, two new `ErrorCode` variants), `src/bin/dog_server.rs` (`AuthConfig::from_env`), every other `serve` call site (`AuthConfig::default()`), `tests/server_auth_integration.rs` (new), `Cargo.toml` (`subtle` dependency) | unit tests (`server::protocol::tests::authenticate_request_and_new_error_codes_round_trip_through_bincode`, `server::tests::auth_config_*`, `server::tests::dispatch_never_routes_authenticate_to_a_store`, `server::tests::token_comparison_time_does_not_depend_on_where_the_mismatch_is` — the `AUTH-FR-006` timing measurement) + `tests/server_auth_integration.rs`'s five real-socket tests (unauthenticated rejection incl. `DescribeSchema`, wrong-token indistinguishability, `ReadOnly` read/write split, `ReadWrite` full access, no-tokens-configured backward compatibility), all passing | PR #52, #53 | Implemented/Verified |
| `STORAGE-011-FR-008`, `STORAGE-012-FR-011` | `SERVER-QUERY-LAYER` | ADR-0013 (Accepted), `docs/design/SERVER-TRANSACTION-DESIGN.md` (Accepted) — `TransactionalStore`, `GenericProductionStore::with_exclusive` | `src/production.rs` (`TransactionalStore` trait + `ProductionStore` impl), `src/generic/production.rs` (`GenericProductionStore::with_exclusive`) | `src/production.rs`'s existing test suite (unaffected — purely additive) + exercised indirectly by every `server::{dog,order,employee}::tests::*apply_transaction*` case and `tests/server_transaction_integration.rs` below, all passing | PR #57 | Implemented/Verified |
| `SERVER-001-FR-017` | `SERVER-QUERY-LAYER` | ADR-0013 (Accepted), `docs/design/SERVER-TRANSACTION-DESIGN.md` (Accepted) — `Request::Transaction`, `TransactionOp`, `Response::TransactionFailed`, `ErrorCode::RecordNotFound`, `ConnectionStore::apply_transaction` | `src/server/protocol.rs` (new request/response/error shapes), `src/server/mod.rs` (`ConnectionStore::apply_transaction`, `dispatch`'s new arm, `handle_connection`'s auth gating extended to `Transaction`), `src/server/{dog,order,employee}.rs` (`apply_transaction` per adapter, each using `TransactionalStore`/`GenericProductionStore::with_exclusive`), `tests/server_transaction_integration.rs` (new) | unit tests (`server::protocol::tests::transaction_request_and_new_shapes_round_trip_through_bincode`, `server::tests::transaction_all_pass_reports_ok`, `server::tests::transaction_reports_the_first_failing_operations_index_and_code`) + `tests/server_transaction_integration.rs`'s six real-socket tests (full success; failure at first/last operation; a malformed value; `ReadOnly` rejection before any operation is evaluated; the flagship concurrent stress test, sequential-replay linearizability against a contended id pool), all passing | PR #57 | Implemented/Verified |
| `SERVER-001-FR-018` | `SERVER-QUERY-LAYER` | Transaction benchmark coverage, completing FR-014's already-accepted benchmark scope rather than opening a new decision | `benches/server.rs` (`{domain}-txn` row set: `measure_transaction_latency`, `run_transaction_throughput`, `bench_transaction_domain`) | `cargo bench --features server,research --bench server` completes without panics, all three domains, `Request::Transaction` latency + thread-per-connection throughput sweep directly comparable to the existing `GetById` rows — this session's 4-core container, run twice to characterize run-to-run variance; no meaningful cost found relative to `GetById` — numbers in `RESULTS.md`'s `## Server / query layer` section, `### \`Request::Transaction\` follow-up` subsection | PR #59 | Implemented/Verified |
| `SERVER-001-FR-019` | `SERVER-QUERY-LAYER` | ADR-0014 (Accepted), `docs/design/SERVER-TLS-DESIGN.md` (Accepted, revised to depend on `rusty_tls`) — `TlsConfig`, `ReadHalf`/`WriteHalf` | `src/server/mod.rs` (`TlsConfig`, `TlsConfigError`, `ReadHalf`/`WriteHalf`, `serve`'s new `tls: Option<TlsConfig>` parameter, `handle_connection`'s handshake wiring), `src/server/pem.rs` (new, PEM/base64 decoder), `src/bin/dog_server.rs` (`TlsConfig::from_env`), every other `serve` call site (`None`), `tests/server_tls_integration.rs` (new), `Cargo.toml` (`rusty_tls` dependency, this spec's first git dependency; `rcgen` dev-dependency) | `src/server/pem.rs`'s unit tests (10: base64 known-vectors/whitespace-tolerance/malformed-input, PEM block decoding including multi-block chains, label-agnostic decoding, every malformed-input case) + `tests/server_tls_integration.rs`'s five real-socket tests (a full request/response round trip over TLS; `TlsConfig` composed with `AuthConfig`; a plain connection to a TLS-configured server never getting a valid response; `TlsConfig: None` reproducing plaintext behavior; the `Authenticate` token not present in the raw bytes a TLS client actually sends, via a byte-recording `TcpStream` wrapper), all passing | PR #64 | Implemented/Verified |
| `SERVER-001-FR-020` | `SERVER-PROTOCOL-VERSION` | ADR-0022 (Accepted), `docs/design/SERVER-PROTOCOL-VERSION-DESIGN.md` (Accepted) — `PROTOCOL_VERSION`, `Request::Hello`/`Response::Hello` | `src/server/protocol.rs` (`PROTOCOL_VERSION = 2`, the `Hello` variants at index 10, the compatibility rules and version table in the module docs), `src/server/mod.rs` (the first-frame `Hello` intercept, `dispatch` → `Unsupported`), `src/server/client.rs` (`connect` says hello first, `server_protocol_version()`), `tests/server_protocol_version.rs` (new), one `[[test]]` line in `Cargo.toml` | `protocol.rs`'s `Hello` golden vectors and `protocol_version_is_the_one_the_table_names`, `mod.rs`'s three `Hello` unit tests, `tests/server_protocol_version.rs` (4/4 over real sockets: pre-auth `min` negotiation, version 0 / late hello `Malformed` with the connection open, `SchemaDrivenClient` negotiating 2 on all three domains, an unknown index closing the connection) — every v0.9.1 golden vector and pre-existing suite unchanged | PR #126 | Implemented/Verified |
| `SERVER-001-FR-021` | `SERVER-CLIENT-AUTH` | `SERVER-001` v0.11.0 (bounded, additive completion of FR-015/FR-016, taken from ADR-0022's revisit trigger — no new decision) — `SchemaDrivenClient::connect_authenticated`, `SchemaDrivenClient::authenticate` | `src/server/client.rs` (`connect_authenticated(addr, token)` sending `Authenticate` between `Hello` and `DescribeSchema`; `authenticate(&mut self, token)`; `connect` on an auth-configured server reporting the server's own `Unauthenticated`) | `tests/server_auth_integration.rs`'s two appended tests (7/7: token-less `connect` and wrong-token `connect_authenticated` both `Unauthenticated`; a read token connecting, reading, refused a write; `authenticate` promoting, a rejected re-authentication changing nothing, demotion; the no-tokens no-op) — `tests/server_schema_driven_client.rs` and `tests/server_protocol_version.rs` unchanged | PR #128 | Implemented/Verified |
| `SERVER-001-FR-022` | `SERVER-CLIENT-TLS` | `SERVER-001` v0.12.0 (bounded, additive completion of FR-015/FR-019, the explicit client-side trust configuration ADR-0014's Consequences named — no new decision) — `SchemaDrivenClient::connect_with`, `ConnectOptions`, `ClientTlsConfig`, `TrustPolicy` re-export, `ClientError::Tls` | `src/server/client.rs` (`connect_with` wrapping the TCP connection in a `rusty_tls::TlsStream` before the `Hello`; the private `Transport` enum; one stream with each frame assembled before a single `write_all`) | `tests/server_tls_integration.rs`'s three appended tests (8/8: connect/negotiate/read/write/scan over TLS; composed with `AuthConfig` — plaintext `connect_authenticated` failing before the token is written, token-less TLS `Unauthenticated`, read token refused a write, `authenticate` promoting; the failure shapes — plaintext `connect` `Frame`, unparseable server name `Tls`, self-signed certificate refused under `TrustPolicy::System`) — `tests/server_schema_driven_client.rs`, `tests/server_protocol_version.rs`, `tests/server_auth_integration.rs` unchanged | PR #130 | Implemented/Verified |
| `SERVER-001-FR-023` | `SERVER-MTLS` | ADR-0023 (Accepted), `docs/design/SERVER-MTLS-DESIGN.md` (Accepted) — `TlsConfig::new_with_client_auth`, `ClientTlsConfig::with_identity` | `src/server/mod.rs` (`TlsConfig::{new_with_client_auth, from_pem_files_with_client_ca, from_env, from_env_values, requires_client_certificate}`), `src/server/client.rs` (`ClientTlsConfig::{with_identity, with_identity_pem_files, has_identity}`, the identity branch in `connect_with`, a key-redacting `Debug`), `src/bin/dog_server.rs` (doc, startup line) | `tests/server_tls_integration.rs`'s four appended mTLS tests (12/12: admitted CA-signed client raw and via `SchemaDrivenClient`; no certificate and foreign CA rejected with an error, server still serving; admission then class with and without tokens; PEM on both ends with a two-root CA file, `Io`/`Tls` construction errors) + `src/server/mod.rs`'s `tls_from_env_values_treats_a_client_ca_without_a_chain_and_key_as_an_error` — every pre-existing suite unchanged | PR #135 | Implemented/Verified |
| `SERVER-001-FR-024` | `SERVER-TRANSACTION-SESSION` | ADR-0024 (Accepted), `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md` Part A (Accepted); ADR-0022's first revisit trigger (the first version-3 variant) — `Request::{Begin, Commit, Rollback}`, `Response::Staged`, `ErrorCode::{NoSession, SessionOpen, SessionFull}`, `PROTOCOL_VERSION = 3`, `MAX_STAGED_OPS`, `SchemaDrivenClient::begin` / `Session` | `src/server/protocol.rs` (constant, seven variants, table row 3, vectors), `src/server/mod.rs` (`handle_connection`'s `negotiated`/`session` state and intercepts, the `ReadOnly` gate's `Commit`, `dispatch` → `Unsupported`, `error_message`), `src/server/client.rs` (`begin`, `Session::{update, commit, rollback}` + `Drop`, `ClientError::TransactionFailed`) | `protocol.rs`'s vectors and `session_shapes_round_trip_through_the_codec`, `mod.rs`'s `dispatch_never_routes_session_requests_to_a_store`, `tests/server_transaction_integration.rs`'s four session tests (10/10), `tests/server_protocol_version.rs`'s `session_requests_are_malformed_below_protocol_version_3` (5/5), `tests/server_schema_driven_client.rs`'s `sessions_stage_commit_and_roll_back_on_every_domain` (5/5) — every pre-existing suite and bench unchanged | PR #139 | Implemented/Verified |
| `SERVER-001-FR-025` | `SERVER-TRANSACTION-JOURNAL` | ADR-0025 (Accepted), `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md` Part B (Accepted); ADR-0013's second revisit trigger — `with_journal` on the three adapters, `BatchJournal`, `CheckpointFlush`, `JournalError`, `ErrorCode::Journal` with `PROTOCOL_VERSION = 4` | `src/server/journal.rs` (new), `src/server/{dog,order,employee}.rs` (`with_journal`, `validate_batch`/`apply_batch`, the journal steps in `apply_transaction`), `src/server/protocol.rs` (`Journal` at 9, version 4, table row 4), `src/server/mod.rs` (`downgrade_for_version`, message), `src/bin/dog_server.rs` (`SERVER_TXN_JOURNAL_PATH`), `benches/server.rs` (`dog-jrnl-txn`) | `journal.rs`'s four unit tests, `dog.rs`'s replay-pair and checkpoint tests, `mod.rs`'s downgrade test, `protocol.rs`'s `Journal` vector and version pin, `tests/server_transaction_integration.rs`'s `a_journaled_server_journals_transactions_and_session_commits_only` (11/11), the `dog-jrnl-txn` rows in `RESULTS.md` — every pre-existing suite unchanged | PR #141 | Implemented/Verified |
| `SERVER-001-FR-026` | `SERVER-CLIENT-HELLO-FALLBACK` | `SERVER-001` v0.16.0 (ADR-0022's third revisit trigger, taken at the owner's call as default-on — no new decision) — `ConnectOptions::require_hello`, the fallback in `connect_with` | `src/server/client.rs` (`connect_with`'s fallback arm, `open_transport`, `peer_closed`, `ConnectOptions::require_hello`, module docs) | `tests/server_protocol_version.rs`'s `a_pre_hello_server_is_reconnected_to_without_a_hello_unless_hello_is_required` (6/6; a pre-hello fake server on the public `dispatch`: fallback to version 1 with a read served and `begin` refused, `require_hello()` restoring the EOF, a server dying under any first frame still an error) — every pre-existing suite unchanged | PR #143 | Implemented/Verified |
| `SERVER-001-FR-027` | `SERVER-JOURNAL-GROUP-COMMIT` | ADR-0026 (Accepted), `docs/design/SERVER-JOURNAL-GROUP-COMMIT-DESIGN.md` (Accepted); ADR-0025's third revisit trigger — `CommitGroup`, the `fsync` outside the exclusive section, ordered apply, quiescent checkpoint | `src/server/journal.rs` (`CommitGroup`, `Turn`, `CommitError`, `BatchJournal::append_unsynced`), `src/server/{dog,order,employee}.rs` (`journal: Option<CommitGroup>`, `validate_batch` with an existence closure, the two-path `apply_transaction`), `src/server/mod.rs` and `src/bin/dog_server.rs` (docs) | `src/server/journal.rs`'s four `CommitGroup` tests (ordered apply across eight threads equals the journal's replay order; a held leader syncs once for three batches; a failed sync refuses its group and the next batch succeeds; a checkpoint is deferred until no later batch is appended) + `src/server/dog.rs`'s `the_fsync_never_holds_the_store_lock` + `tests/server_transaction_integration.rs`'s `concurrent_journaled_transactions_replay_to_the_live_state` (12/12) + the `dog-jrnl-txn` rows re-measured in `RESULTS.md` — every pre-existing suite unchanged | PR #155 | Implemented/Verified |
| `SERVER-001-FR-028` | `SERVER-SESSION-READ-YOUR-WRITES` | ADR-0027 (Accepted), `docs/design/SERVER-SESSION-READ-YOUR-WRITES-DESIGN.md` (Accepted); ADR-0024's first revisit trigger; ADR-0022's rules (a version-5 variant) — `Request::BeginWith`, `SESSION_READ_YOUR_WRITES`, `overlay_staged`, `Session::get` | `src/server/protocol.rs` (`BeginWith` at 14, `PROTOCOL_VERSION = 5`, `SESSION_READ_YOUR_WRITES`, table row 5, golden vector), `src/server/mod.rs` (the `BeginWith` and overlaid-`GetById` intercepts, `overlay_staged`, the per-connection updatable tags, the dispatch arm), `src/server/client.rs` (`begin_read_your_writes`, `Session::get`, `Session::read_your_writes`) | `src/server/mod.rs`'s `overlay_staged_replaces_only_matching_updatable_fields` + the extended dispatch/golden/round-trip/version-pin tests + `tests/server_transaction_integration.rs`'s three read-your-writes tests (15/15) + `tests/server_schema_driven_client.rs`'s client test (6/6) + `tests/server_protocol_version.rs` (probe at 15, client gate) — every pre-existing suite unchanged | PR #157 | Implemented/Verified |
| `SERVER-001-FR-029` | `SERVER-AUTH-AUDIT` | ADR-0029 (Accepted), `docs/design/SERVER-AUTH-AUDIT-DESIGN.md` (Accepted); the audit third of `SERVER-AUTH-DESIGN.md`'s named gap — `AuditEvent`, `AuditSink`, `AuthConfig::with_audit`, the eager handshake, `SERVER_AUDIT_LOG` | `src/server/audit.rs` (new), `src/server/mod.rs` (`AuthConfig::{with_audit, audit}`, hand-written `Debug`, eager `complete_handshake`, the gate records, `DisconnectAudit`), `src/bin/dog_server.rs` (`SERVER_AUDIT_LOG`, `audit_sink_from`) | `src/server/audit.rs`'s three unit tests + `dog_server`'s table test + `tests/server_auth_integration.rs`'s three audit tests (10/10) + `tests/server_tls_integration.rs`'s `handshake_outcomes_are_audited_with_a_reason` (13/13) — every pre-existing suite unchanged | PR #159 | Implemented/Verified |
| `SERVER-001-FR-030` | `SERVER-SESSION-VALIDATE-ON-STAGE` | `SERVER-001` v0.20.0 (ADR-0024's second revisit trigger and ADR-0027's stage-time trigger, taken as the bounded additive completion both described — no new decision); ADR-0022's rules with the flag-bit clarification — `SESSION_VALIDATE_ON_STAGE`, `ConnectionStore::validate_op`, `SessionOptions` | `src/server/protocol.rs` (the bit, `PROTOCOL_VERSION = 6`, table row 6), `src/server/mod.rs` (`validate_op` on the trait, the version-gated known-bit set, the validating staging arm), `src/server/{dog,order,employee}.rs` (`validate_op`), `src/server/client.rs` (`SessionOptions`, `begin_with`, `Session::validate_on_stage`) | `src/server/dog.rs`'s `validate_op_reports_exactly_what_commit_would` + `tests/server_transaction_integration.rs`'s two stage-time tests (17/17) + `tests/server_schema_driven_client.rs`'s `begin_with` test (7/7) + `tests/server_protocol_version.rs` (gate, pin 6) — every pre-existing suite unchanged apart from the moved unknown-flag probe | PR #163 | Implemented/Verified |
| `SERVER-001-FR-031` | `SERVER-MTLS-CLASS` | ADR-0028 (Accepted), `docs/design/SERVER-MTLS-CLASS-DESIGN.md` (Accepted); ADR-0023's first revisit trigger and `SERVER-MTLS-DESIGN.md`'s "Class from certificate" open question — the upstream `rusty_tls` accessor (`Rusty-Mill/rusty_mill` PR #148), `AuthConfig`'s exact-DER certificate-class map, `handle_connection` reading `peer_certificate_der()` | `Cargo.toml` (`rusty_tls` rev bump, no new dependency), `src/server/mod.rs` (`AuthConfig::{with_certificate_class, with_certificate_class_pem_file, class_for_certificate}`, the hand-written `Debug`, the TLS arm's `certificate_class` read and the starting-state match), `src/bin/dog_server.rs` (`certificate_classes_from_env_values`, `SERVER_AUTH_READ_ONLY_CLIENT_CERTS`/`SERVER_AUTH_READ_WRITE_CLIENT_CERTS`) | `src/server/mod.rs`'s three unit tests (`class_for_certificate_matches_by_exact_der_bytes_only`, `is_configured_is_true_with_only_a_certificate_class`, `auth_config_debug_prints_certificate_counts_not_bytes`) + `dog_server`'s `certificate_classes_from_env_values_follows_the_documented_table` + `tests/server_tls_integration.rs`'s five class-from-certificate tests (17/17) — every pre-existing suite unchanged | PR #166 | Implemented/Verified |
| `SERVER-001-FR-032` | `SERVER-AUTH-RATE-LIMIT` | ADR-0030 (Accepted), `docs/design/SERVER-AUTH-RATE-LIMIT-DESIGN.md` (Accepted); `ADR-0029`'s first revisit trigger — `MAX_AUTH_FAILURES`, `FailureTable`, `AuthConfig::with_rate_limit`, `AuditKind::{LockedOut, Throttled}` | `src/server/audit.rs` (`LockedOut`/`Throttled`, `#[non_exhaustive]` on `AuditKind`/`RequestKind`), `src/server/mod.rs` (`MAX_AUTH_FAILURES`, `MAX_TRACKED_PEERS`, `RateLimit`, `FailureTable`, `AuthConfig::{with_rate_limit, rate_limit}`, the `Authenticate` arm's throttle/lockout logic), `src/bin/dog_server.rs` (`SERVER_AUTH_RATE_LIMIT`, `rate_limit_from_env_value`) | `src/server/mod.rs`'s five unit tests (`RateLimit::parse`, three `FailureTable` tests) + `dog_server`'s `rate_limit_from_env_value_follows_the_documented_table` + `tests/server_auth_integration.rs`'s three rate-limit/lockout tests (13/13) — every pre-existing suite unchanged | This PR | Implemented/Verified |
| `SERVER-001-FR-033` | `SERVER-ACCESS-LOG` | ADR-0031 (Accepted), `docs/design/SERVER-ACCESS-LOG-DESIGN.md` (Accepted); `ADR-0029`'s second revisit trigger — `AccessEvent`, `Outcome`, `AccessSink`, `AuthConfig::with_access_log` | `src/server/access.rs` (new: `AccessEvent`, `Outcome`, `AccessSink`, `NoAccessLog`, `StderrAccessLog`, `FileAccessLog`), `src/server/mod.rs` (`AuthConfig::{with_access_log, access_log}` and its `access_log` field, `outcome_of`, the `request_kind` capture and recording call in `handle_connection`), `src/bin/dog_server.rs` (`SERVER_ACCESS_LOG`, `access_sink_from`) | `src/server/access.rs`'s three unit tests (line format/distinctness, no marker value, `FileAccessLog` append/failure) + `dog_server`'s `access_sink_from_follows_the_documented_table` + `tests/server_auth_integration.rs`'s `access_log_and_audit_log_streams_stay_disjoint` (14/14) — every pre-existing suite unchanged | this PR | Implemented/Verified |
| `SERVER-001-FR-034` | (none — bounded additive completion) | `ADR-0029`'s fourth revisit trigger (`ADR-0028` landing) — `AuditKind::Admitted.classed_by_certificate` | `src/server/audit.rs` (`classed_by_certificate` field on `Admitted`, the `line()` arm), `src/server/mod.rs` (the one construction site) | `tests/server_tls_integration.rs`'s `admitted_records_classed_by_certificate_for_a_matched_leaf_only` (18/18) — every pre-existing `Admitted` literal updated, every pre-existing suite otherwise unchanged | this PR | Implemented/Verified |
| `SERVER-001-FR-035` | `SERVER-SERVE-OPTIONS` | ADR-0032 (Accepted), `docs/design/SERVER-SERVE-OPTIONS-DESIGN.md` (Accepted); the trigger `ADR-0029` first named and `ADR-0030`/`ADR-0031` each restated — `ServeOptions` (renamed from `AuthConfig`), `with_tls`/`tls()`, `serve`'s three-parameter signature | `src/server/mod.rs` (`AuthConfig` → `ServeOptions`, the new `tls` field and its builder/accessor, `serve`/`handle_connection` signatures), `src/bin/dog_server.rs` (`main`'s TLS folding, the three decision-table functions' types), `benches/server.rs`, eight test files — every `AuthConfig` reference and `serve(..)` call site updated | The full existing suite passing unmodified in intent (`SRV-FR-006`): `cargo test --all-features` 367 lib tests + every integration suite unchanged (`tests/server_tls_integration.rs` 18/18, `tests/server_auth_integration.rs` 14/14); `cargo test` (default features) 136 lib + 2 doctests unchanged — zero assertion changes across all sixteen touched files | this PR | Implemented/Verified |
| `SERVER-001-FR-036` | `SERVER-SESSION-SNAPSHOT-ISOLATION` | ADR-0033 (Accepted), `docs/design/SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN.md` (Accepted); closes `SESS-FR-007`'s second half `ADR-0027` left standing — `SESSION_SNAPSHOT_ISOLATION`, `PROTOCOL_VERSION = 7`, `ErrorCode::Conflict`, `ConnectionStore::apply_transaction`'s new `read_set` parameter, `SessionOptions::snapshot_isolation()` | `src/server/protocol.rs` (version-7 table row, the flag bit, `MAX_TRACKED_READS`, `ErrorCode::Conflict`), `src/server/mod.rs` (`record_read_set`, per-connection `snapshot_reads` state, the `GetById`/`BeginWith`/`Commit`/`Rollback` arms, `apply_transaction`'s trait signature, `error_message`/`downgrade_for_version`), `src/server/{dog,order,employee}.rs` (`check_read_set` and the read-set re-check inside each adapter's existing exclusive/journaled section), `src/server/client.rs` (`SessionOptions`/`Session::snapshot_isolation()`) | 3 unit tests (`record_read_set`), 1 adapter unit test (`dog.rs`, atomicity with the write batch), 5 integration tests in `tests/server_transaction_integration.rs` (conflict, non-conflict, `NotFound` untracked, composed with read-your-writes, the version-7 gate) — `cargo test --all-features` 371 lib tests (367 + 4), `tests/server_transaction_integration.rs` 22/22 (17 + 5); `cargo test` (default features) 136 lib + 2 doctests unchanged | PR #174 | Implemented/Verified |
| `SERVER-001-FR-037` | `SERVER-SQL-SELECT` | ADR-0034 (Accepted), `docs/design/SERVER-SQL-SELECT-DESIGN.md` (Accepted); narrows `docs/FUTURE-GROWTH.md`'s "SQL" item to a bounded, read-only `SELECT` subset — `Request::Query`/`Response::Rows`, `PROTOCOL_VERSION = 8`, `Selection`/`Predicate`/`CompareOp`, `SchemaDrivenClient::query`/`Session::query` | `src/server/protocol.rs` (version-8 table row, `Request::Query`/`Response::Rows`, `Selection`/`CompareOp`/`Predicate`, golden vectors), `src/server/mod.rs` (`ConnectionStore::scan_all`, `validate_query`/`evaluate_query` and their helpers, the `Query` dispatch arm), `src/server/sql.rs` (new — client-side tokenizer/parser), `src/server/{dog,order,employee}.rs` (`scan_all`), `src/server/client.rs` (`SchemaDrivenClient::query`, `Session::query`, `QueryRow`, `ClientError::Sql`), `src/production.rs`/`src/generic/query.rs`/`src/generic/{mmap_store,store,mmap_scanned,production}.rs` (the new `AllIds`/`AllIds<R>` primitive and its forwarding impls) | 6 unit tests in `src/server/mod.rs`, 11 in `src/server/sql.rs`, 2 golden vectors, 1 adapter unit test in `dog.rs`, 14 integration tests in `tests/server_sql_integration.rs` (new — all three domains, every comparator, `LIMIT`, every client-side error shape, the version-8 gate, both session-isolation interactions) — `cargo test --all-features` 389 lib tests (371 + 18), `tests/server_sql_integration.rs` 14/14 (new); `cargo test` (default features) 136 lib + 2 doctests unchanged | this PR | Implemented/Verified |
| `SERVER-001-FR-038` | `SERVER-SQL-AGGREGATE` | ADR-0035 (Accepted), `docs/design/SERVER-SQL-AGGREGATE-DESIGN.md` (Accepted); narrows `docs/FUTURE-GROWTH.md`'s aggregation/`GROUP BY`/`AVG` item to a bounded slice — `Request::Aggregate`/`Response::Groups`, `PROTOCOL_VERSION = 9`, `AggregateFn`/`AggregateSpec`/`AggregateGroup`, `ScanValue::F64`, `SchemaDrivenClient::query`'s widened `QueryResult` return type | `src/server/protocol.rs` (version-9 table row, `Request::Aggregate`/`Response::Groups`, `AggregateFn`/`AggregateSpec`/`AggregateGroup`, `ScanValue::F64`, golden vectors), `src/server/mod.rs` (`validate_aggregate`/`evaluate_aggregate`/`reduce`/`reduce_extreme` and their helpers, the `Aggregate` dispatch arm — `scan_all` reused unchanged, no new storage-adjacent primitive), `src/server/sql.rs` (extended grammar — `agg_call`/`group_by_clause`, `ParsedColumnItem`/`AggregateArg`), `src/server/client.rs` (`QueryResult`/`AggregateRow`, `SchemaDrivenClient::query_aggregate`, the functional-dependency check), `src/server/audit.rs` (`RequestKind::Aggregate`) | 10 unit tests in `src/server/sql.rs`, 5 in `src/server/mod.rs` (including the empty-implicit-bucket `Min`/`Max`/`Avg` fallback), 2 golden vectors, 15 integration tests in `tests/server_sql_integration.rs` (now 29/29 — every aggregate function against a hand-computed reduction, `GROUP BY` on one/two fields, the `AVG`-equals-`SUM`/`COUNT` cross-check, `WHERE`+`GROUP BY`, both zero-match cases, `LIMIT`, the functional-dependency/`SELECT *`/non-numeric-field client-side rejections, the version-9 gate, both session-isolation interactions) — `cargo test --all-features` 404 lib tests (389 + 15), `tests/server_sql_integration.rs` 29/29 (14 prior + 15); `cargo test` (default features) 136 lib + 2 doctests unchanged | this PR | Implemented/Verified |
| `SERVER-001-FR-039` | `SERVER-REMINDER-DOMAIN` | ADR-0036 (Accepted), `docs/design/SERVER-REMINDER-DOMAIN-DESIGN.md` (Accepted); answers the owner's own question about backing `rusty_remind_me`, not sourced from `docs/FUTURE-GROWTH.md` — a fourth domain, `Reminder`, the generic schema library's first front-door appearance outside its own `research`-gated reference material, no wire/`PROTOCOL_VERSION`/`ErrorCode` change of any kind | `src/generic/reminder.rs` (new, front-door — `Reminder`/`ReminderStatus`, `DueAtField`/`StatusField`, `ReminderProductionStack`, `create_reminder_production_stack`/`open_reminder_production_stack`), `src/server/reminder.rs` (new, `server`-gated alone — `ReminderConnectionStore`, `FIELD_TITLE`/`FIELD_DUE_AT`/`FIELD_STATUS`), `src/server/journal.rs` (`CheckpointFlush` for `ReminderProductionStack`, unconditional), `src/bin/reminder_server.rs` (new — mirrors `dog_server.rs`'s shape exactly) | 3 unit tests in `src/generic/reminder.rs` (compiled and run even under default/zero-feature builds — `status_to_u32`/`status_from_u32` round trip including an out-of-range rejection, `create`-then-`get`/`filter_eq`, `update`-then-reopen durability), 5 in `src/server/reminder.rs` (`get`, `filter_eq` by `due_at_unix_ms` plus unsupported fields, `scan_field`/`update_field` on `status` including the invalid-discriminant rejection, `describe`, every relation method's `Unsupported`), 9 integration tests in `tests/server_reminder_integration.rs` (new, `required-features = ["server"]` only — `GetById` plus a `GROUP BY status` `Aggregate` count, `FilterEq` range/unsupported fields, `UpdateField` discriminant validation/unsupported fields, every relation request's `Unsupported`, a `Query` `WHERE due_at_unix_ms < ...` range filter, `Request::Transaction` via raw framing, read-your-writes and snapshot-isolation session composition, `title`'s every-capability-flag-`false` queryability), plus a real `cargo run --bin reminder_server --features server` smoke check — `cargo test --all-features` 412 lib tests (404 + 8), `tests/server_reminder_integration.rs` 9/9 (new); `cargo test` (default features) 139 lib tests (136 + 3, the `generic::reminder` unit tests) + 2 doctests | this PR | Implemented/Verified |

| `SERVER-001-FR-040` | `SERVER-ENTITY-DOMAIN` | ADR-0037 (Accepted), `docs/design/SERVER-ENTITY-DOMAIN-DESIGN.md` (Accepted); the second round in the `rusty_remind_me`-motivated line `ADR-0036` started, not sourced from `docs/FUTURE-GROWTH.md` — a fifth domain, `Entity`, one self-referential `SymmetricRelation` (`relates_to`), plus a genuinely new capability, `SchemaDrivenClient::traverse` (bounded client-side breadth-first graph walking), no wire/`PROTOCOL_VERSION`/`ErrorCode` change of any kind | `src/generic/entity.rs` (new, front-door — `Entity`/`EntityKind`, `KindField`/`MentionCountField`, `RelatesTo`, `EntityProductionStack`, `create_entity_production_stack`/`open_entity_production_stack`/`open_entity_production_stack_portable`), `src/server/entity.rs` (new, `server`-gated alone — `EntityConnectionStore`, `FIELD_LABEL`/`FIELD_KIND`/`FIELD_MENTION_COUNT`), `src/server/journal.rs` (`CheckpointFlush` for `EntityProductionStack`, unconditional), `src/server/client.rs` (`SchemaDrivenClient::traverse`, no new `Request`/`Response`), `src/bin/entity_server.rs` (new — mirrors `reminder_server.rs`'s shape exactly) | 4 unit tests in `src/generic/entity.rs` (compiled and run even under default/zero-feature builds — `kind_to_u32`/`kind_from_u32` round trip including an out-of-range rejection, `create`-then-`get`/`filter_eq`/`neighbors`, `update`-then-reopen durability including `neighbors`, portable-reopen matching `open`), 5 in `src/server/entity.rs` (`get`, `filter_eq` by `kind` including the invalid-discriminant rejection plus unsupported fields, `scan_field`/`update_field` on `mention_count`, `neighbors` plus `parent`/`children`'s `Unsupported`, `describe`), 9 integration tests in `tests/server_entity_integration.rs` (new, `required-features = ["server"]` only — `GetById` plus a `GROUP BY kind` `Aggregate` count, `FilterEq` by `kind` plus unsupported fields, `UpdateField` on `mention_count` plus unsupported fields, `Neighbors` both directions plus `parent`/`children`'s `Unsupported`, `traverse` over a real cyclic graph including the `max_depth`/`max_nodes` bounds, `traverse`'s client-side rejection against a `Reminder` server, `Request::Transaction` via raw framing, read-your-writes and snapshot-isolation session composition, `label`'s every-capability-flag-`false` queryability) — `cargo test --all-features` lib tests plus `tests/server_entity_integration.rs` 9/9 (new); `cargo test` (default features) 143 lib tests (139 + 4, the `generic::entity` unit tests) + 2 doctests | this PR | Implemented/Verified |
| `SERVER-001-FR-041` | `SERVER-ENTITY-V2-REDESIGN` | ADR-0039 (Accepted), `docs/design/SERVER-ENTITY-V2-REDESIGN-DESIGN.md` (Accepted); the concrete mechanism for `ADR-0038`'s accepted option (a) — `Entity` revised to its real, `rusty_remind_me`-MCP-verified shape: `kind` an open `String` (was a fixed enum), a second `SymmetricRelation` (`MentionedWith`) alongside `RelatesTo`, carried by a genuinely new multi-relation primitive (not the previously-assumed `Reversed`-style forwarding fix, confirmed with `rustc` to conflict, `E0119`), `PROTOCOL_VERSION` 10 (`NeighborsByRelation`/`ListRelationKinds`/`RelationKinds`, gated client-side only), `traverse`'s optional relation filter | `src/generic/entity.rs` (rewritten — `kind: String`, `MentionedWith`, `RELATION_LABELS`), `src/generic/store.rs` (`MultiSymmetric`, new), `src/generic/query.rs` (`MultiNeighbors`, new), `src/generic/production.rs` (`neighbors_by_relation`/`all_neighbors`/`relation_kinds`, new), `src/server/protocol.rs` (`PROTOCOL_VERSION = 10`, `Request::NeighborsByRelation`/`ListRelationKinds`, `Response::RelationKinds`), `src/server/mod.rs` (`ConnectionStore::{neighbors_by_relation, list_relation_kinds}`, no defaults), `src/server/{dog,employee,entity,order,reminder}.rs` (every adapter implements both), `src/server/client.rs` (`SchemaDrivenClient::{neighbors_by_relation, list_relation_kinds}`, `traverse`'s new `relation` parameter), `src/server/audit.rs` (`RequestKind` extended), `src/bin/entity_server.rs` (updated for the new `Entity` shape) | 3 rewritten unit tests in `src/generic/entity.rs`, 5 rewritten in `src/server/entity.rs`, 9 rewritten in `tests/server_entity_integration.rs` (both relations' `neighbors_by_relation`/`list_relation_kinds`, `traverse`'s relation filter, `kind`'s now-read-only `UpdateField` rejection, the no-relations-capability negative fixture) — `cargo test` (default features) 142 lib tests + 2 doctests; `cargo test --all-features` 420 lib tests plus every integration suite green including `tests/server_entity_integration.rs` 9/9; `cargo doc --all-features --no-deps` 64 warnings (baseline, unchanged). Two real deviations from the accepted text, both caught during implementation and resolved with the owner via `AskUserQuestion` (kind's dual-role blocker; the `Symmetric`-forwarding coherence conflict), plus a third, smaller one (`label`'s identifier not renamed to `name`) — see `ADR-0039`'s own implementation-log entry for the full account | PR #185 | Implemented/Verified |
| `SERVER-001-FR-042` | `SERVER-ENTITY-ALIASES` | ADR-0040 (Accepted), `docs/design/SERVER-ENTITY-ALIASES-DESIGN.md` (Accepted); closes `ADR-0039`'s own paired `aliases`/case-insensitive-name-resolution Non-goals — `Entity` gains `aliases: Vec<String>` (durable via the whole-record blob; no `FieldRef`, no wire representation since `ScanValue` has no list variant); a new secondary-index primitive, `NameIndex`/`NameIndexed`/`FindByName` (the `MultiSymmetric` precedent applied to indices — `GenericMmapStore` admits one `IndexedField` marker, `kind` holds it), rebuilt from records at every open with no file of its own, trim+lowercase normalization at build and query; `FilterEq` on `label` real against `label` or any alias; `DomainSchema` reports `label` `filter_eq: true`. **No `PROTOCOL_VERSION` change** — the existing `FilterEq`/`ScanValue::Str`/`RecordList` shapes carry the whole capability | `src/generic/query.rs` (`NameIndexed`, `FindByName`, new), `src/generic/store.rs` (`NameIndex`, `normalize`, new, with forwarding impls for `GetById`/`AllIds`/`Flush`/`FilterEq`/`ScanField`/`UpdateField`/`MultiNeighbors`/`Neighbors`), `src/generic/production.rs` (`find_by_name`, new), `src/generic/entity.rs` (`aliases` field, `NameIndexed` impl, `EntityProductionStack` rewrapped, constructors wrap in `NameIndex::new`), `src/server/entity.rs` (`filter_eq` on `FIELD_LABEL` real, `describe` flag), `src/server/journal.rs` (doc comment only), `src/bin/entity_server.rs` (sample aliases) | 2 new unit tests in `src/generic/entity.rs` (`find_by_name` across case/whitespace/alias/miss/no-prefix; a shared key returns both owners, a thrice-registered key returns one id) + 3 extended (aliases durable across `open`/`open_portable`, `UpdateField` leaves the map intact, exactly four files on disk — no fifth for the index); 1 new in `src/server/entity.rs` (`filter_eq` on `label` by label/alias/case/miss/non-`Str` → `Malformed`) + `describe` extended; 1 new in `tests/server_entity_integration.rs` (five spellings of one name, an alias, a miss, no substring match, a two-owner shared alias over a real socket) + 2 updated (`label` flags, `aliases` absent from schema, SQL `WHERE label = ..` stays exact-match) — `cargo test` (default features) 144 lib tests + 2 doctests; `cargo test --all-features` 423 lib tests plus every integration suite green including `tests/server_entity_integration.rs` 10/10; `cargo doc --all-features --no-deps` 64 warnings (baseline; three new from this round's own doc comments fixed, not accepted). Landed as proposed, no deviation from `ADR-0040` | PR #187 | Implemented/Verified |
| `SERVER-001-FR-043` | `SERVER-ENTITY-SOURCE-VERIFICATION` | ADR-0042 (Accepted), `docs/design/SERVER-ENTITY-SOURCE-VERIFICATION-DESIGN.md` (Accepted) — the two follow-ups from reading `baileyrd/rusty_remind_me@29602f1` directly (ten findings, each cited to a file:line): `ENT5-FR-001` `NameIndex::normalize` collapses internal whitespace runs (the source's own `normalize_entity_name`; `FR-042`'s trim-only rule was the exact one that repository had retired after it split one person into two entities), zero migration since the index is rebuilt from records at every open; `ENT5-FR-002` `entity_id(name) -> Uuid` over the first 16 bytes of `sha256(normalize(name))` — one indexed `GetById` resolves a canonical name in any spelling, `RecordId` untouched, a convention not a constraint. F3/F4/F5 (open-label edges, directed edges with metadata, edges-with-hops traverse result) named for a future round, not built | `src/generic/store.rs` (`normalize` rule + `pub(crate)`), `src/generic/entity.rs` (`entity_id`, `sha2` import, module doc rewritten — "source unread" caveat retired, fixed-set `RELATION_LABELS` named a known divergence), `Cargo.toml` (`sha2 = "0.10"` — the first new unconditional dependency since `subtle`, correcting the ADR's own "since `rusty_tls`" claim), caveat pointers in `docs/design/SERVER-ENTITY-V2-REDESIGN-DESIGN.md`, `docs/design/SERVER-ENTITY-ALIASES-DESIGN.md`, and `SERVER-001`'s FR-040/041/042 entries | 1 new unit test in `src/generic/store.rs` (`normalize`: tab/newline inside, doubled space, empty, whitespace-only, case, not-stripping) + 2 new in `src/generic/entity.rs` (`entity_id` determinism over four spellings, distinctness, never nil; mint-then-fetch by re-derived id from another spelling, alias via index not id, hand-picked ids untouched) + 1 new in `tests/server_entity_integration.rs` (own one-entity server: one-round-trip `GetById` on a re-derived id over a real socket, a miss for a different name, `FilterEq` collapsing internal whitespace for label and alias) + 2 existing `find_by_name` assertions extended — `cargo test` (default features) 147 lib tests + 2 doctests; `cargo test --all-features` 426 lib tests plus every integration suite green including `tests/server_entity_integration.rs` 11/11; `cargo doc --all-features --no-deps` 64 warnings (baseline; one new module-doc link warning fixed, not accepted). Landed as proposed, no deviation from `ADR-0042`; one correction to the ADR's own dependency claim, recorded in its implementation log | this PR | Implemented/Verified |
| `SERVER-001-FR-044` | `SERVER-ENTITY-ALIASES-WIRE` | ADR-0041 (Accepted), `docs/design/SERVER-ENTITY-ALIASES-WIRE-DESIGN.md` (Accepted) — `Entity::aliases` on the wire: `ENT4-FR-001` `ScanValue::StrList` (5)/`ValueKind::StrList` (4), `PROTOCOL_VERSION` 11, table row 11; `ENT4-FR-002` `FIELD_ALIASES = 3`, all-`false` descriptor, the raw stored list in `GetById`/`Query`, every write/filter/scan path `Unsupported`; `ENT4-FR-003` rule-3 content strip in `downgrade_for_version` (`Record`/`Rows` pairs, `Schema` descriptors, below 11) — the protocol's first content-rewriting downgrade; `ENT4-FR-004` no new client API; `ENT4-FR-005` no new `Request`/`Response`/`ErrorCode`; `ENT4-FR-006` golden vectors + downgrade unit tests. One implementation-time completion recorded in the ADR's log: `GROUP BY` a `StrList` field refused server- and client-side, closing the one path the design's own "never in `Groups`" invariant left unguarded | `src/server/protocol.rs` (two appended variants, constant, table row, three golden vectors, pin), `src/server/mod.rs` (`downgrade_for_version` three arms + debug assertions, `validate_aggregate` one arm, `value_matches_kind` doc), `src/server/entity.rs` (`FIELD_ALIASES`, `get`/`scan_all`/`check_read_set`/`describe` + four `Unsupported` arms, module doc), `src/server/client.rs` (`GROUP BY` kind check, `resolve_literal` doc), `src/generic/entity.rs` (docs), caveat pointers in `SERVER-001` FR-042, `ADR-0040`, `docs/design/SERVER-ENTITY-ALIASES-DESIGN.md` | 3 golden vectors in `src/server/protocol.rs` + 2 new unit tests in `src/server/mod.rs` (each of `Record`/`Rows`/`Schema` stripped at 10 and 1, untouched at 11 and `PROTOCOL_VERSION`, a `StrList`-free `Record` untouched at 1, the `Conflict` case unchanged; `StrList` in `group_by`/predicate/`MAX` each `Malformed`) + 1 new in `src/server/entity.rs` (four `Unsupported` arms, tag 4 still `UnknownField`, read-set arm) with `get`/`describe` extended to four fields + 3 new in `tests/server_entity_integration.rs` (criterion 2 read-back and SQL projection; criterion 3 `Hello { 10 }` and silent clients each see the `FR-042` three-field shape with a version-11 control; criterion 4 every client- and server-side refusal, record unchanged) + 1 new in `tests/server_protocol_version.rs` (`Hello { 10 }` `DescribeSchema`/`GetById` lack `aliases`, `Hello { 11 }` has it) + the three version pins — `cargo test` (default features) 147 lib tests + 2 doctests; `cargo test --all-features` 429 lib tests plus every integration suite green including `tests/server_entity_integration.rs` 14/14 and `tests/server_protocol_version.rs` 7/7; `cargo doc --all-features --no-deps` 64 warnings (baseline, no new warning). Landed as proposed, no deviation from `ADR-0041`; one additive completion recorded | this PR | Implemented/Verified |
| `SERVER-001-FR-045` | `SERVER-SQL-JOIN` (Part A) | ADR-0044 (Accepted), `docs/design/SERVER-SQL-JOIN-DESIGN.md` Part A (Accepted) — a `JOIN` over a declared relation within one table: `JOIN-FR-001` `Request::Join(JoinSpec)`/`Response::JoinedRows` (19/15), `JOIN-FR-002` `Request::DescribeRelations`/`Response::Relations` (20/16) + `ConnectionStore::describe_relations()` with a conservative default (`parent`/`children` omitted unless the adapter claims them; `Employee` does), `JOIN-FR-003` `validate_join`/`evaluate_join` — server-side index nested loop over the adapter's own relation methods, `limit` bounds work and response, `JOIN-FR-004` both orientations of a symmetric edge, `JOIN-FR-005` SQL aliases/qualified names/`JOIN … ON` with four parse-time rejections, `JOIN-FR-006` `SchemaDrivenClient::relations` + `query_join` + `QueryResult::Joined`, `JOIN-FR-007` no new `ErrorCode`, `JOIN-FR-008` four golden vectors, protocol 12, pins moved. Gated `Malformed` below 12 server-side per the accepted text; `right_table`/`target_table` reserved for ADR-0045 | `src/server/protocol.rs` (`JoinRelation`/`JoinSpec`/`JoinedRow`/`RelationDescriptor`, two requests, two responses, constant, table row), `src/server/mod.rs` (`describe_relations` default + `default_relation_descriptors`, `validate_join`, `evaluate_join`, two `dispatch` arms, the `< 12` gate, `outcome_of`), `src/server/audit.rs` (two `RequestKind`s), `src/server/employee.rs` (`describe_relations` override), `src/server/sql.rs` (`Token::Dot`, `table_ref`, `maybe_qualified`, `ParsedJoin`, `ParsedColumnItem::Qualified`, `ParsedCondition::qualifier`, `validate_qualifiers`, four errors), `src/server/client.rs` (`relations`, `query_join`, `QueryResult::Joined`, `JoinedRowNamed`) | 4 golden vectors + pin in `src/server/protocol.rs`; 4 unit tests in `src/server/mod.rs` over a `JoinFixture` (default list, every `validate_join` refusal, every `JoinRelation` with filters/projections/`limit`, `dispatch`); 6 in `src/server/sql.rs`; integration — `server_entity_integration` +2 (criterion 2: `relates_to` both orientations with labels, `mentioned_with`, `neighbors`, left/right filters, `LIMIT`, `SELECT *`, `aliases` as `StrList` in a joined row, three relations listed; criterion 6: seven client-side `Sql` refusals, raw `right_table: Some`/raw `Parent` `Malformed`), `server_employee_integration` +1 (criterion 3: `parent`/`children`/`collaborates_with`, a right-side salary filter, the parentless root produces no row), `server_order_integration` +1 (criterion 4: nothing listed, `parent` join `Sql` client-side and `Malformed` raw), `server_dog_integration` +1 and `server_reminder_integration` +1 (criterion 5), `server_protocol_version` +1 (criterion 6 server side: `Hello { 11 }` and silent client `Malformed` for both requests, `Hello { 12 }` served, a version-1 server never sent a `Join`) — `cargo test` (default) 147 + 2; `cargo test --all-features` 439 lib + every suite green (entity 16/16, employee 4/4, order 3/3, dog 5/5, reminder 10/10, protocol_version 8/8); `cargo doc` 64 (baseline). Landed as proposed; two consequences recorded (three exhaustive `QueryResult` matches gained an arm; four unplanned protocol-11 pins in Unit 49's tests rewritten) | this PR | Implemented/Verified |
| `SERVER-002` v0.1.0 / `SERVER-001` v0.35.1 (patch) | `SERVER-CLIENT-ECOSYSTEM` | ADR-0043 (Accepted), `docs/design/SERVER-CLIENT-ECOSYSTEM-DESIGN.md` (Accepted) — `ECO-FR-001`–`003` the `client` Cargo feature (`server` implies it; no public path changes; CI builds and tests it alone), `ECO-FR-004`–`006` `SERVER-002` the language-neutral wire specification plus `tests/fixtures/wire-vectors.txt` enforced by the golden-vector tests, `ECO-FR-007`–`009` the stdlib-only Python reference client verified offline against the fixture and live against a real server, fail-loud without `python3`, version-pinned at 12 | `Cargo.toml` (`client`/`server` features, two `[[test]]` targets), `src/lib.rs` (gate), `src/server/mod.rs` (module gates, `TlsConfigError`, the `serve` re-export), `src/server/serve.rs` (the moved server body), `src/test_support.rs` (`wire_fixture`), `src/server/protocol.rs` (golden wrappers + `introduced_at`), `.github/workflows/ci.yml` (two steps), `clients/python/rusty_multimodal_db/{codec,protocol,client}.py`, `clients/python/driver.py`, `clients/python/README.md`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-002-wire-format.md` | `tests/server_client_only.rs` 2/2 under `--features client` (framing + codec round trip with `SERVER-002` §4's bytes, the client library refusing a dead port, no server compiled); `cargo test --features client` 195 lib + 2 doctests; the fixture enforced on every `cargo test` (48 vectors; a regenerate-then-verify pair proved the round trip); `clients/python/tests/test_vectors.py` 5/5 offline (every `Request/*` and `Response/*` line decodes and re-encodes byte-for-byte; the two `SERVER-002` §4 worked examples pinned); `tests/server_python_client.rs` 1/1 live (at 12: four fields incl. the `StrList`, alias `FilterEq`, `Query`, `Aggregate`, a write, `Neighbors`, a protocol-12 `Join` of eight rows; at `Hello { 10 }`: three fields, no relations, join refused client-side; the Python write observed by the Rust client); doc at the 64-warning baseline. Landed as proposed | this PR | Implemented/Verified |
| `SERVER-001-FR-046` | `SERVER-INSERT-DESIGN`, `SERVER-INSERT` | ADR-0046 (Accepted as option (d), implemented in the same cycle), `docs/design/SERVER-INSERT-DESIGN.md` (Accepted) — runtime record insertion: `INS-FR-001` `Insert<R>`/`InsertError`, `INS-FR-002` `GenericMmapStore::insert` (log entry `fsync`ed, then the `O_APPEND` slot, then the maps), `INS-FR-003` the insert log format (`<path>.inserts`, `GENINSL\0`, tagged header, length-prefixed entries, torn tail dropped), `INS-FR-004` the fold at `open`/`read_portable_records` (idempotent by id; `create` clears a stale log; a reopen with no inserts writes nothing), `INS-FR-005` every layer forwards + `GenericProductionStore::insert`, `INS-FR-006` `ConnectionStore::insert_record`/`InsertOutcome` with a default of `Unsupported` and whole-list validation in `Reminder`/`Entity`, `INS-FR-007` `Request::Insert` (21)/`ErrorCode::Duplicate` (11)/`ErrorCode::Storage` (12) at protocol 13 with the three gates, `INS-FR-008` `SchemaDrivenClient::insert` + Python `Client.insert`, `INS-FR-009` three vectors, pins moved, `SERVER-002` v0.2.0 | `src/generic/insert_log.rs` (new), `src/generic/{mod,query,mmap_store,store,mmap_scanned,production}.rs`, `src/server/{protocol,serve,audit,reminder,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/insert_log.rs` 8 unit tests; `src/generic/mmap_store.rs` +4, `store.rs` +1, `reminder.rs` +1, `entity.rs` +1, `order_customer.rs` +2; `src/server/reminder.rs` +1, `entity.rs` +1, `serve.rs` +1, `protocol.rs` two vectors + the pin; integration `server_reminder_integration` +1 (incl. a second server on the same directory), `server_entity_integration` +1, `server_protocol_version` +1, `server_dog_integration` +1, `server_auth_integration`/`server_transaction_integration` extended, `server_python_client` extended — `cargo test --all-features` 459 lib + every suite green; `cargo test` (default) and `--features client` green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | this PR | Implemented/Verified |
| `SERVER-001-FR-047` | `SERVER-LINK-DESIGN`, `SERVER-LINK` | ADR-0047 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-LINK-DESIGN.md` (Accepted) — runtime relation insertion: `LNK-FR-001` `Link`/`MultiLink`/`LinkOutcome`/`LinkError`/`valid_relation_label`, `LNK-FR-002` `Symmetric::link` (edge log before adjacency), `LNK-FR-003` `insert_log::{append_item, read_items}`, `LNK-FR-004` the edge-log fold, `LNK-FR-005` `MultiSymmetric::link` with label creation, `LNK-FR-006` the label manifest, `LNK-FR-007` the per-label fold and `open_portable` union, `LNK-FR-008` the forwards + `GenericProductionStore::{link, link_by_relation}`, `LNK-FR-009` `ConnectionStore::link_records` (`Entity` open-label, `Employee` fixed), `LNK-FR-010` `Request::Link` (22) at protocol 14 with the three gates, `LNK-FR-011` discovery follows, `LNK-FR-012` `SchemaDrivenClient::link` + Python `Client.link` with the relation re-fetch, `LNK-FR-013` one vector, pins moved, `SERVER-002` v0.3.0 | `src/generic/{store,insert_log,query,mod,mmap_scanned,production}.rs`, `src/server/{protocol,serve,audit,entity,employee,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/insert_log.rs` +1, `store.rs` +5, `entity.rs` +1, `generic_spike/employee_impl.rs` +1; `src/server/{entity,employee,serve}.rs` +1 each, `protocol.rs` the vector + the pin; integration `server_entity_integration` +1 (incl. a second server on the same directory serving the edge and the label), `server_employee_integration` +1, `server_protocol_version` +1, `server_dog_integration` +1, `server_reminder_integration`/`server_auth_integration`/`server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` 471 lib + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | this PR | Implemented/Verified |
| `SERVER-001-FR-048` | `SERVER-MEMORY-DOMAIN-DESIGN`, `SERVER-MEMORY-DOMAIN` | ADR-0048 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-MEMORY-DOMAIN-DESIGN.md` (Accepted) — `Memory` domain: `MEM-FR-001` the eleven-field record, `MEM-FR-002` `category` indexed, `MEM-FR-003` `access_count` scannable and non-negative, `MEM-FR-004` the read-only fields and `metadata_json` as unparsed text, `MEM-FR-005` `MemoryProductionStack` with no relation of either kind, `MEM-FR-006` `MemoryConnectionStore` + `CheckpointFlush`, `MEM-FR-007` `memory_server`, `MEM-FR-008` the socket proof; no wire change | `src/generic/{memory,mod}.rs`, `src/server/{memory,mod,journal}.rs`, `src/bin/memory_server.rs`, `tests/server_memory_integration.rs`, `Cargo.toml`, `docs/specifications/server/SERVER-001-query-layer.md` | `src/generic/memory.rs` +1 (default features), `src/server/memory.rs` +4, `tests/server_memory_integration.rs` +3 (new; incl. a second server on the same directory serving an inserted memory) — `cargo test --all-features` every lib test + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 unchanged | PR #200 | Implemented/Verified |
| `SERVER-001-FR-049` | `SERVER-REPLACE-DESIGN`, `SERVER-REPLACE` | ADR-0049 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-REPLACE-DESIGN.md` (Accepted) — whole-record replacement: `REP-FR-001` `Replace`/`ReplaceError`, `REP-FR-002` `GenericMmapStore::replace` (log, slot in place, bucket moved), `REP-FR-003` the fold applying the log over the blob by id, `REP-FR-004` the forwards + `GenericProductionStore::replace`, `REP-FR-005` `ConnectionStore::replace_record`/`ReplaceOutcome` (`Memory`/`Reminder`/`Entity`), `REP-FR-006` `Request::Replace` (23) at protocol 15 with the three gates, `REP-FR-007` `SchemaDrivenClient::{replace, upsert}` + Python `Client.replace`, `REP-FR-008` one vector, pins moved, `SERVER-002` v0.4.0 | `src/generic/{query,mod,mmap_store,store,mmap_scanned,production}.rs`, `src/server/{protocol,serve,audit,memory,reminder,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/mmap_store.rs` +2, `memory.rs` +1, `entity.rs` +1, `generic_spike/employee_impl.rs` +1; `src/server/{memory,reminder,entity}.rs` +1 each, `protocol.rs` the vector + the pin; integration `server_memory_integration` +1 (incl. a restart serving the replaced version), `server_entity_integration` +1, `server_protocol_version` +1, `server_dog_integration` +1, `server_auth_integration`/`server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` every lib test + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | this PR | Implemented/Verified |
| `SERVER-001-FR-050` | `SERVER-TABLES-DESIGN`, `SERVER-TABLES` | ADR-0050 (Accepted as designed, implemented on the same branch; implements ADR-0045, Accepted), `docs/design/SERVER-TABLES-DESIGN.md` (Accepted) — more than one table on one connection: `TBL-FR-001` `serve_tables`/`table_name`, `TBL-FR-002` `Request::Use` (24) per connection, `TBL-FR-003` `ListTables` (25)/`Tables` (17), `TBL-FR-004` cross-table `Join`, `TBL-FR-005` the `Memory → Entity` instance, `TBL-FR-006` `Use` in a session refused, `TBL-FR-007` foreign labels and the server-side far-end check on `Link`, `TBL-FR-008` `Memory::mentions`, `TBL-FR-009` `SchemaDrivenClient::{list_tables, use_table}` + cross-table SQL `JOIN` + the Python client, `TBL-FR-010` three vectors, pins moved, `SERVER-002` v0.5.0 | `src/generic/{store,memory}.rs`, `src/server/{protocol,serve,audit,memory,dog,order,employee,reminder,entity,client}.rs`, `src/bin/memory_server.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/store.rs` +1, `generic/memory.rs` +1, `server/memory.rs` rewritten +1, `protocol.rs` the vectors + the pin; integration `server_memory_integration` +2 (incl. the two-table suite with a restart), `server_protocol_version` +1, `server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` every lib test + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | PR #202 | Implemented/Verified |
| `SERVER-001-FR-051` | `SERVER-DELETE-DESIGN`, `SERVER-DELETE` | ADR-0051 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-DELETE-DESIGN.md` (Accepted) — runtime deletion: `DEL-FR-001` `Delete`/`Detach`/`DeleteError`, `DEL-FR-002` `GenericMmapStore::delete` (tombstone, slot retired), `DEL-FR-003` insert-log format version 2 with tombstones and a lazy upgrade, `DEL-FR-004` the folds and every layer's forward (relation layers dropping the id's edges), `DEL-FR-005` `MultiSymmetric::detach`, `DEL-FR-006` `Request::Delete` (26) at protocol 17 with the three gates, `DEL-FR-007` the cross-table cascade via `detach_record`, `DEL-FR-008` `SchemaDrivenClient::delete` + Python `Client.delete`, `DEL-FR-009` one vector, pins moved, `SERVER-002` v0.6.0 | `src/generic/{insert_log,slot_file,query,mod,mmap_store,store,mmap_scanned,production}.rs`, `src/server/{protocol,serve,audit,memory,reminder,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/insert_log.rs` +2, `mmap_store.rs` +2, `store.rs` +2, `entity.rs` +1, `memory.rs` +1, `generic_spike/employee_impl.rs` +1; `src/server/{memory,reminder,entity}.rs` +1 each, `protocol.rs` the vector + the pin; integration `server_memory_integration` +1 (the two-table delete suite incl. the cascade, a re-insert, and a restart), `server_protocol_version` +1, `server_dog_integration` +1, `server_auth_integration`/`server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` every lib test + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | PR #204 | Implemented/Verified |
| `SERVER-001-FR-052` | `SERVER-COMPACT-DESIGN`, `SERVER-COMPACT` | ADR-0052 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-COMPACT-DESIGN.md` (Accepted) — compaction: `CMP-FR-001` `Compact`/`CompactionReport`, `CMP-FR-002` `GenericMmapStore::compact` (blob, gapless slot rewrite, log removed, crash-safe order), `CMP-FR-003` the forwards, `CMP-FR-004` edge blobs folded from the adjacency (sorted), `CMP-FR-005` `GenericProductionStore::compact`, `CMP-FR-006` `ConnectionStore::compact` + `Request::Compact` (27)/`Response::Compacted` (18) at protocol 18 with the three gates, `CMP-FR-007` `SchemaDrivenClient::compact` + Python `Client.compact`, `CMP-FR-008` two vectors, pins moved, `SERVER-002` v0.7.0 | `src/generic/{slot_file,query,mod,mmap_store,store,mmap_scanned,production}.rs`, `src/server/{protocol,serve,audit,memory,reminder,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/mmap_store.rs` +1, `store.rs` +1, `entity.rs` +1, `generic_spike/employee_impl.rs` +1; `src/server/memory.rs` +1, `protocol.rs` the vectors + the pin; integration `server_memory_integration` +1 (the two-table compaction suite with a restart), `server_protocol_version` +1, `server_dog_integration` +1, `server_auth_integration`/`server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` every lib test + every suite green; default and `client` feature sets green; clippy (three feature sets) and fmt clean; Python vectors 5/5 | PR #206 | Implemented/Verified |
| `SERVER-001-FR-053` | `SERVER-DATA-DIR-DESIGN`, `SERVER-DATA-DIR` | ADR-0053 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-DATA-DIR-DESIGN.md` (Accepted) — a durable data directory: `DDR-FR-001` `open_or_create_{memory,entity}_production_stack` (portable reopen when the slot file exists, empty creation when not), `DDR-FR-002` `SERVER_DATA_DIR` on `memory_server` (durable when set, the scratch-and-samples default when not; any failure a startup error naming the file), `DDR-FR-003` the startup line | `src/generic/memory.rs`, `src/generic/entity.rs`, `src/bin/memory_server.rs` | `src/generic/memory.rs` +1, `src/generic/entity.rs` +1 — `cargo test --all-features`; the binary proven by hand over a real socket (write, kill, restart, read back), recorded in `docs/PROJECT-STATUS.md` item 157 |
| `SERVER-001-FR-054` | `SERVER-GUARDED-REPLACE-DESIGN`, `SERVER-GUARDED-REPLACE` | ADR-0054 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-GUARDED-REPLACE-DESIGN.md` (Accepted) — a guarded replace: `GRD-FR-001` `GenericProductionStore::replace_if` under the write lock, `GRD-FR-002` `GuardedReplace`, `GRD-FR-003` `ConnectionStore::replace_record_if` + `ReplaceIfOutcome` on `Memory`/`Reminder`/`Entity`, `GRD-FR-004` `validate_predicate` in `dispatch`, `GRD-FR-005` `Request::ReplaceIf` (28)/`ErrorCode::GuardFailed` (13) at protocol 19 with the three gates, `GRD-FR-006` `SchemaDrivenClient::replace_if` + Python `Client.replace_if`, `GRD-FR-007` two vectors, pins moved, `SERVER-002` v0.8.0 | `src/generic/{mod,production}.rs`, `src/server/{protocol,serve,audit,memory,reminder,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/production.rs` +1; `src/server/memory.rs` +1, `protocol.rs` the vectors + the pin; integration `server_memory_integration` +1 (last-writer-wins over the wire with a restart), `server_protocol_version` +1, `server_dog_integration` +1, `server_auth_integration`/`server_transaction_integration`/`server_python_client` extended — `cargo test --all-features` |
| `SERVER-001-FR-055` | `SERVER-PAGE-DESIGN`, `SERVER-PAGE` | ADR-0055 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-PAGE-DESIGN.md` (Accepted) — one ordered keyset page: `PAG-FR-001` `Request::Page` (29) answered by `Rows`, `PAG-FR-002` `page_rows` + `ConnectionStore::page` defaulting over `scan_all` + `PageRow`, `PAG-FR-003` `validate_page` in `dispatch`, `PAG-FR-004` protocol 20, gated as `Query`, `RequestKind::Page`, `PAG-FR-005` `SchemaDrivenClient::page` + Python `Client.page`, `PAG-FR-006` one vector, pins moved, `SERVER-002` v0.9.0 | `src/server/{protocol,serve,audit,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/server/serve.rs` +1 (`page_rows`), `protocol.rs` the vector + the pin; integration `server_memory_integration` +1 (pages by `updated_at` over the wire), `server_dog_integration` +1 (the default on `Dog`), `server_protocol_version` +1, `server_python_client` extended — `cargo test --all-features` |
| `SERVER-001-FR-056` | `SERVER-MEMORY-SYNC-FIELDS-DESIGN`, `SERVER-MEMORY-SYNC-FIELDS` | ADR-0056 (Accepted — the owner's option (a) after a design-only proposal), `docs/design/SERVER-MEMORY-SYNC-FIELDS-DESIGN.md` (Accepted) — `Memory`'s sync fields: `SYN-FR-001` `deleted_at_unix_ms` (`0` live, non-negative) and `node_id` (`""` unattributed) at tags 11/12, `SYN-FR-002` `SCHEMA_TAG` `memory::Memory@2` refusing a twelve-field-layout directory distinctly with no upgrade, `SYN-FR-003` `describe` at thirteen fields and no wire change | `src/generic/memory.rs`, `src/server/memory.rs`, `src/bin/memory_server.rs` | `src/generic/memory.rs` +1 (the old-tag blob refused on both reopen paths, nothing rewritten); `src/server/memory.rs` counts moved to thirteen; integration `server_memory_integration` +1 (sentinels, `Page`/`Query`/`GROUP BY` over the fields, a negative stamp, a restart) — `cargo test --all-features` |
| `SERVER-001-FR-057` | `SERVER-COUNT-EDGES-DESIGN`, `SERVER-COUNT-EDGES` | ADR-0057 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-COUNT-EDGES-DESIGN.md` (Accepted) — a global edge count: `CNT-FR-001` `MultiNeighbors::edge_count` + `GenericProductionStore::count_edges`, `CNT-FR-002` `ConnectionStore::count_edges` on `Memory`/`Entity`, `CNT-FR-003` `Request::CountEdges` (30)/`Response::Count` (19) at protocol 21 gated as `Page`, `CNT-FR-004` `SchemaDrivenClient::count_edges` + Python `Client.count_edges`, `CNT-FR-005` two vectors, pins moved, `SERVER-002` v0.10.0 | `src/generic/{query,store,production}.rs`, `src/server/{protocol,serve,audit,memory,entity,client}.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-00{1,2}-*.md` | `src/generic/memory.rs` +1 (the store count across a link, a delete, a reopen), `protocol.rs` the vectors + the pin; integration `server_memory_integration` +1 (two tables, a link, a cascading delete), `server_protocol_version` +1, `server_dog_integration` +1, `server_python_client` extended — `cargo test --all-features` |
| `SERVER-001-FR-058` | `SERVER-RELATION-DOMAIN-DESIGN`, `SERVER-RELATION-DOMAIN` | ADR-0058 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-RELATION-DOMAIN-DESIGN.md` (Accepted) — the `Relation` domain: `REL-FR-001` the record and its rules, `REL-FR-002` `subject` indexed / `updated_at_unix_ms` scannable, `REL-FR-003` the stack and constructors, `REL-FR-004` `RelationConnectionStore` with seven fields and every front-door write, `REL-FR-005` `memory_server`'s third table | `src/generic/relation.rs`, `src/server/relation.rs`, `src/generic/mod.rs`, `src/server/mod.rs`, `src/server/journal.rs`, `src/bin/memory_server.rs` | `src/generic/relation.rs` +1, `src/server/relation.rs` +2; integration `server_memory_integration` +1 (the three-table server: out-edges, in-edges, a page, the merge, a count, a delete, a restart) — `cargo test --all-features` |
| `SERVER-001-FR-059` | `SERVER-ORDERED-INDEX-DESIGN`, `SERVER-ORDERED-INDEX` | ADR-0059 (Accepted as designed, implemented on the same branch), `docs/design/SERVER-ORDERED-INDEX-DESIGN.md` (Accepted) — a memory-only sorted index behind `Page`: `ORD-FR-001` `OrderedField`, `ORD-FR-002` `PageBy`/`page_by`, `ORD-FR-003` the `Ordered` layer exact through every write, `ORD-FR-004` `Memory`/`Relation` stacks over `updated_at_unix_ms`, `ORD-FR-005` the adapters' `page` from the index, `page_by_scan` for every other field | `src/generic/traits.rs`, `src/generic/query.rs`, `src/generic/store.rs`, `src/generic/production.rs`, `src/generic/memory.rs`, `src/generic/relation.rs`, `src/server/serve.rs`, `src/server/memory.rs`, `src/server/relation.rs` | `src/generic/memory.rs` +1, `src/generic/relation.rs` +1; integration `server_memory_integration` extended (the scan path agrees with the index) — `cargo test --all-features`; measured in `RESULTS.md` |
| `SERVER-001-FR-060` | `SERVER-WRITE-BATCH-DESIGN`, `SERVER-WRITE-BATCH` | ADR-0060 (Accepted as designed, option (c), implemented on the same branch), `docs/design/SERVER-WRITE-BATCH-DESIGN.md` (Accepted) — a runtime-write batch: `WBT-FR-001` `Request::WriteBatch`/`WriteOp`/`MAX_BATCH_OPS`, `WBT-FR-002` `Response::BatchResults`/`WriteResult`/the pipelined default, `WBT-FR-003` the atomic single-lock override on the three write domains, `WBT-FR-004` both clients, `WBT-FR-005` the pins and `SERVER-002` v0.11.0 | `src/server/protocol.rs`, `src/server/serve.rs`, `src/server/audit.rs`, `src/server/memory.rs`, `src/server/entity.rs`, `src/server/relation.rs`, `src/server/client.rs`, `clients/python/**` | `server_protocol_version` +1 (the 22 gate), `server_memory_integration` +1 (pipelined, atomic success, atomic abort applies nothing); two golden vectors — `cargo test --all-features` |
| `SERVER-001-FR-061` | `SERVER-SQL-ORDER-BY-DESIGN`, `SERVER-SQL-ORDER-BY` | ADR-0061 (Accepted as designed, option (a), implemented on the same branch), `docs/design/SERVER-SQL-ORDER-BY-DESIGN.md` (Accepted) — `ORDER BY` in the SQL subset compiled to `Request::Page`: `OBY-FR-001` `order_by_clause` grammar/`ParsedQuery::order_by`, `OBY-FR-002` the three parse-time exclusions (`OrderByWithFilter`/`OrderByWithJoin`/`OrderByWithAggregate`), `OBY-FR-003` client-side field-kind/limit/version validation, `OBY-FR-004` `query_ordered` compiling to one or a looped `Page` calls plus projection, `OBY-FR-005` the unchanged `QueryResult::Rows` shape, `OBY-FR-006` no Python/wire/server change | `src/server/sql.rs`, `src/server/client.rs` | `src/server/sql.rs` +5 unit tests, `tests/server_sql_integration.rs` +6 (`LIMIT`-bearing round trip matching `page()`, named-column projection, a 2,500-record no-`LIMIT` multi-page walk, every client-side refusal, the protocol-20 gate) | this pass, 2026-09-14 | Implemented/Verified |
| `SERVER-001-FR-062` | `SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN`, `SERVER-WRITE-BATCH-CRASH-ATOMICITY` | ADR-0063 (Accepted as designed, option (a), implemented on the same branch), `docs/design/SERVER-WRITE-BATCH-CRASH-ATOMICITY-DESIGN.md` (Accepted) — crash-atomic `WriteBatch` on `Memory`/`Entity`/`Relation` when journaled: `WBJ-FR-001` journal format version 2 with a leading kind byte (`0` `TransactionOp`, `1` `WriteOp`), `WBJ-FR-002` the journaled atomic `write_batch` branch, `WBJ-FR-003` replay via `prepare_write`/`apply_prepared`, `WBJ-FR-004` unchanged behavior with no journal, `WBJ-FR-005` a real crash-injection test. Mechanism corrected during implementation from the design's own pre-decided-guard sketch to journaling the raw `WriteOp` list — see `ADR-0063`'s own "Acceptance and implementation" | `src/server/journal.rs`, `src/server/{memory,entity,relation,dog,order,employee,reminder}.rs` | `src/server/journal.rs` +2 unit tests (8 existing pass unmodified), `src/server/memory.rs` +1 crash-injection test (`write_batch_atomic_is_crash_atomic_via_the_journal`) | this pass, 2026-09-14 | Implemented/Verified |
| `SERVER-001-FR-063` | `SERVER-METRICS-DESIGN`, `SERVER-METRICS` | ADR-0064 (Accepted as designed, option (a), implemented on the same branch), `docs/design/SERVER-METRICS-DESIGN.md` (Accepted) — process-wide observability: `MET-FR-001` `Request::Metrics`/`Response::Metrics` at protocol 23, gated as any authenticated class; `MET-FR-002` `ServerMetrics` (six atomics) as a plain `ServeOptions` field (corrected from the design's sibling-`Arc`-parameter sketch — `ServeOptions::rate_limit` already holds live per-request state); `MET-FR-003` the `handle_connection` connection-open/close and post-dispatch counters; `MET-FR-004` deterministic Prometheus text rendering; `MET-FR-005` the read-only gate | `src/server/metrics.rs`, `src/server/protocol.rs`, `src/server/serve.rs`, `src/server/audit.rs`, `src/server/client.rs`, `clients/python/**`, `docs/specifications/server/SERVER-002-wire-format.md` | `src/server/metrics.rs` +4 unit tests, 2 new `protocol.rs` golden vectors, `tests/server_metrics_integration.rs` (3/3 real socket), Python `test_vectors.py` (140/140 subtests) | this PR | Implemented/Verified |
| `SERVER-001-FR-064` | `SERVER-BACKUP-DESIGN`, `SERVER-BACKUP` | ADR-0065 (Accepted as designed, option (a), implemented with three corrections, same branch), `docs/design/SERVER-BACKUP-DESIGN.md` (Accepted) — live server-side backup: `BAK-FR-001` `Request::Backup`/`Response::BackedUp` at protocol 24, gated like `Compact`; `BAK-FR-002` `backup_source`/`with_backup_source` on `Dog`/`Memory`/`Entity`/`Relation` (corrected to a single adapter-level path plus a shared prefix-glob copy, not a per-layer `Backup` trait); `BAK-FR-003` `ServeOptions::backup_root`/`SERVER_BACKUP_ROOT`, wired into `memory_server.rs` only (`Dog` has no durable binary to wire it into); `BAK-FR-004` path-component validation before any I/O; `BAK-FR-005`/`006` refuse-existing-target and copy under the write lock, via one whole-directory atomic rename (corrected from per-file temp-then-rename); `BAK-FR-007` restore as documentation, not code | `src/server/serve.rs`, `src/server/protocol.rs`, `src/server/{dog,memory,entity,relation}.rs`, `src/server/client.rs`, `src/bin/memory_server.rs`, `clients/python/**`, `docs/specifications/server/SERVER-002-wire-format.md` | `tests/server_backup_integration.rs` (5/5 real socket: reopen-and-compare, every path-traversal shape, existing-target refusal, unconfigured-root `Unsupported`, `ReadOnly` `Unauthorized`), Python `test_vectors.py` (140/140 subtests) | this PR | Implemented/Verified |
| `STORAGE-013-FR-001..007` | `EXTERNAL-DB-BENCHMARK` | ADR-0015 (Accepted) — `benches/external_db.rs`, `external-db-bench` Cargo feature | `benches/external_db.rs` (new), `Cargo.toml` (`rusqlite`/`duckdb`/`postgres` dev-dependencies, `external-db-bench` feature, `external_db` `[[bench]]` entry) | `cargo check --bench external_db --features research,external-db-bench` clean + a real `cargo bench --features research,external-db-bench --bench external_db` run against a local Postgres instance, all three workload groups (`get`, `scan_ages`, `neighbors_one_hop`) × all three sizes × all four systems, no panics | PR #66 | Implemented/Verified |
| `STORAGE-013-FR-008` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.2.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_two_hop_vs_external_db` Criterion group | `benches/external_db.rs` (`run_neighbors_two_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_two_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_two_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and a real, investigated Postgres join-strategy finding in `RESULTS.md`'s `### Graph traversal, two-hop` subsection | PR #72 | Implemented/Verified |
| `STORAGE-013-FR-009` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.3.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_three_hop_vs_external_db` Criterion group, private `three_hop_neighbors` helper | `benches/external_db.rs` (`three_hop_neighbors`, `run_neighbors_three_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_three_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_three_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and confirmation that the v0.2.0 Postgres join-strategy finding reproduces at this depth in `RESULTS.md`'s `### Graph traversal, three-hop` subsection | PR #74 | Implemented/Verified |
| `STORAGE-013-FR-010` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.4.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_four_hop_vs_external_db` Criterion group, private `four_hop_neighbors` helper | `benches/external_db.rs` (`four_hop_neighbors`, `run_neighbors_four_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_four_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_four_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and the new Postgres/SQLite crossover finding (confirmed via `EXPLAIN (ANALYZE, BUFFERS)`, not assumed) in `RESULTS.md`'s `### Graph traversal, four-hop` subsection | PR #76 | Implemented/Verified |
| `STORAGE-013-FR-011` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.5.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_five_hop_vs_external_db` Criterion group, private `five_hop_neighbors` helper | `benches/external_db.rs` (`five_hop_neighbors`, `run_neighbors_five_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_five_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_five_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and confirmation (via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass at all three sizes) that the v0.4.0 crossover reversed rather than persisted, in `RESULTS.md`'s `### Graph traversal, five-hop` subsection | PR #78 | Implemented/Verified |
| `STORAGE-014-FR-001..005` | `PRODUCTION-STORE-PORTABILITY` | ADR-0016 (Accepted) — companion record blob (`src/durability/record_blob.rs`, new), `ProductionStore::open_portable`, `DurabilityError::RecordBlobUnreadable` | `src/durability/record_blob.rs` (new: `RecordBlob`/`EncodedRecordBlob`, `companion_path`, header/version/fingerprint/decode checks, crash-safe `write`, header-only `is_current_at`; v0.2.0: `BLOB_VERSION = 2`, inline `Fnv1a64`, `RecordBlob::fingerprint`), `src/durability/mod.rs` (`pub(crate) mod record_blob`, `RecordBlobUnreadable` variant), `src/production.rs` (`create` writes the blob, `open` compares the header fingerprint and rewrites only if stale — v0.2.0; v0.1.0 byte-compared a full serialization — new `open_portable`); `src/durability/mmap_store.rs` untouched (empty diff) | `record_blob.rs`'s 12 unit tests (round trip incl. `breed`/edges, missing/short/wrong-magic/wrong-version/truncated/fingerprint-mismatched blobs all typed errors not panics, whole-file replacement consuming a stale temp file, header-only `is_current_at` incl. an age-only change reported as current, a version-1 blob reported as a version mismatch and as stale, the fingerprint pinned to a known value / order-sensitive / age-insensitive) + `production.rs`'s 6 tests (`open_portable` from the path alone, both files copied to a fresh directory, missing blob is `RecordBlobUnreadable` naming the companion path while `open` still works, `open` heals a legacy ages-only directory, `open` rewrites the blob only when the record set changed — mtime and bytes unchanged otherwise, including after an age-only change; a version-1 blob upgraded in place by `open`) + `MmapAgeStore`'s existing suite passing unmodified + `cargo test`/`cargo test --all-features`/`cargo fmt --check`/`clippy --all-targets --all-features -D warnings`/`cargo doc` clean + a release-build `create`/`open`/`open_portable` cost measurement at 1K/100K/1M in `RESULTS.md`'s `### ProductionStore file portability` subsection, re-measured for v0.2.0 in its `#### Follow-up: header fingerprint` subsection | PR #84 | Implemented/Verified |
| `STORAGE-019-FR-001..007` | `SCHEMA-MIGRATION-DESIGN`, `SCHEMA-MIGRATION` | ADR-0066 (Accepted as designed, option (a), implemented on the same branch), `docs/design/SCHEMA-MIGRATION-DESIGN.md` (Accepted) — schema migration tooling: `MIG-FR-001` the three-primitive pattern (a caller-defined old-layout struct implementing `SchemaTag`; the existing `open_..._portable` read path typed over it; the existing `create_..._production_stack` write path, unmodified), `MIG-FR-002` a hand-written per-migration `Old → New` conversion function, `MIG-FR-003` output to a fresh path only, refused if it exists, `MIG-FR-004` reuse of `create_memory_production_stack` unmodified, `MIG-FR-005` a real CLI (`examples/migrate_memory_v1_to_v2.rs`), `MIG-FR-006` a regression test against `Memory`'s real production code path, `MIG-FR-007` zero new dependency/protocol change/`src/` module | `examples/support/migrate_memory_v1_to_v2_lib.rs` (new — `MemoryV1`, `open_memory_v1_stack_portable`, `migrate_memory_v1_to_v2`, `migrate`), `examples/migrate_memory_v1_to_v2.rs` (new, the CLI), `tests/schema_migration.rs` (new, 3 tests), `Cargo.toml` (`[[test]]`/`[[example]]` entries) | `tests/schema_migration.rs` (3/3 — the flagship round trip through `Memory`'s real `open_memory_production_stack_portable`; the existing-destination refusal; the already-`@2`-tagged-source refusal with the existing `RecordBlobUnreadable` message) + a real `cargo run --example migrate_memory_v1_to_v2` smoke test against a genuine on-disk fixture (usage message + exit 1 with no args; the existing error message + exit 1 against a nonexistent source; a real four-file `Memory@2` directory written and reported against a real two-record fixture) | this session | Implemented/Verified |
| `SERVER-001-FR-067` | `SERVER-REPLICATION-DESIGN`, `SERVER-REPLICATION` | ADR-0067 (Accepted as designed, option (a), implemented with one correction, same branch), `docs/design/SERVER-REPLICATION-DESIGN.md` (Accepted) — round one of replication/high availability: `RPL-FR-001` `Request::FetchSnapshot`/`Response::Snapshot { files }` at protocol 25, reusing `Backup`'s `with_exclusive` critical section; `RPL-FR-002` a new `TokenClass::Replication` (a third enum variant, not a separate `AuthConfig` field) never satisfied by `ReadOnly`/`ReadWrite`, gated in `handle_connection` by a dedicated class-exactness check, `ServeOptions::{with_replication_token, replication_token}`, `SERVER_AUTH_REPLICATION_TOKEN`; `RPL-FR-003` `read_table_files`/`ReadTableFilesError` reusing `copy_table_files`'s prefix-glob enumeration to read bytes instead of copy files; `RPL-FR-004` `ConnectionStore::fetch_snapshot` (default `Unsupported`) on `Dog`/`Memory`/`Entity`/`Relation`, reusing `backup_source`; `RPL-FR-005` `SchemaDrivenClient::fetch_snapshot()` and the Python client's `fetch_snapshot`; `RPL-FR-006` the operator recipe documented, no daemon shipped; `RPL-FR-007` `MAX_SNAPSHOT_BYTES` (8 MiB, fixed constant) checked before any byte is read, `ErrorCode::TooLarge`. **One correction found during implementation**: `ServeOptions::is_configured()` originally omitted `replication_token`, so a replication-token-only server left `Authenticate` a no-op and defaulted every connection to `ReadWrite` — fixed before shipping, caught by the integration test's own success-path case | `src/server/protocol.rs`, `src/server/serve.rs`, `src/server/{dog,memory,entity,relation}.rs`, `src/bin/memory_server.rs`, `src/server/client.rs`, `clients/python/rusty_multimodal_db/{protocol,client}.py`, `clients/python/driver.py`, `tests/server_replication_integration.rs` (new), `tests/server_python_client.rs`, `tests/server_client_only.rs`, `tests/fixtures/wire-vectors.txt`, `docs/specifications/server/SERVER-002-wire-format.md` | `tests/server_replication_integration.rs` (5/5: reopened-snapshot identical-record-set flagship proof; no-token/`ReadWrite`/`ReadOnly` all `Unauthorized`; over-ceiling `TooLarge`), `tests/server_python_client.rs`'s extended refusal-path assertions at both the current version and a hand-negotiated 10, `tests/server_client_only.rs`'s updated `Hello` pin, two new `ServeOptions` unit tests (`is_configured_is_true_with_only_a_replication_token`, `check_classes_a_replication_token_distinctly`), `RMDB_REGENERATE_VECTORS=1`-regenerated `wire-vectors.txt` (73 vectors) + `clients/python/tests/test_vectors.py` (146 subtests) — `cargo fmt`/`cargo clippy --all-features -- -D warnings` clean, `cargo test --all-features --no-fail-fast` 529 lib tests + every integration target green | same session | Implemented/Verified |
| `SERVER-001-FR-068` | `SERVER-FILTERED-PAGE-DESIGN`, `SERVER-FILTERED-PAGE` | ADR-0068 (Accepted as designed, option (a), implemented with two corrections, same branch), `docs/design/SERVER-FILTERED-PAGE-DESIGN.md` (Accepted) — combining `WHERE` with `ORDER BY`: `FPG-FR-001` `Request::FilteredPage { order_by, after, limit, filter: Vec<Predicate> }` (35) at protocol 26, answered `Response::Rows` (reused, no new response variant); `FPG-FR-002` `validate_filtered_page` composing `validate_page`'s four checks plus per-predicate `validate_predicate`, no new validation function; `FPG-FR-003` one shared default evaluation (`scan_all()` → filter via `predicate_matches` → `page_rows`) correct for every domain, zero adapter overrides; `FPG-FR-004` `ConnectionStore::filtered_page` a trait method with a working default (`page`'s own precedent — corrected during implementation from the design's own self-contradictory "default `Unsupported`" text); `FPG-FR-005` `sql.rs`'s `OrderByWithFilter` exclusion removed, `WHERE` + `ORDER BY` compiles to `FilteredPage` (`OrderByWithJoin`/`OrderByWithAggregate` unchanged); `FPG-FR-006` `SchemaDrivenClient::fetch_filtered_page` plus a `fetch_ordered_page` dispatcher reusing `query_ordered`'s existing chunked-loop shape, no new client-side predicate evaluator; `FPG-FR-007` gated like `Page` — `Malformed` below 26, a read, no `SessionOpen` gate. **Two corrections found during implementation**: `FPG-FR-004`'s text (see above); and the design's Non-goals wrongly claimed no Python client change was needed (conflated `ADR-0061`'s lack of a SQL compiler with the actual precedent — every wire-touching round since `ADR-0057` gives Python a raw per-request method) — corrected, `clients/python/` gains `Client.filtered_page` | `src/server/protocol.rs` (`Request::FilteredPage`, module-doc row 26, golden vector, `PROTOCOL_VERSION = 26`), `src/server/serve.rs` (`ConnectionStore::filtered_page`, `validate_filtered_page`, dispatch arm, version-26 gate), `src/server/sql.rs` (`validate_order_by`'s exclusion removed), `src/server/client.rs` (`fetch_filtered_page`, `fetch_ordered_page`), `src/server/audit.rs` (`RequestKind::FilteredPage`), `clients/python/rusty_multimodal_db/{protocol,client}.py` (`FilteredPage`, `Client.filtered_page`), `clients/python/driver.py`, `docs/specifications/server/SERVER-002-wire-format.md` | `tests/server_sql_integration.rs` (40 tests — a filtered-ordered round trip cross-checked against both `page()` and `Query`, a no-`LIMIT` filtered multi-page walk over 2,500 records, every client-side refusal, the protocol-26 gate, an explicit unfiltered-`Page`-unaffected regression check), `tests/server_python_client.rs` extended (`filtered_page`), `tests/server_client_only.rs`'s updated `Hello` pin (25 → 26), `RMDB_REGENERATE_VECTORS=1`-regenerated `wire-vectors.txt` (74 vectors) + `clients/python/tests/test_vectors.py` (offline round trip) — `cargo fmt`/`cargo clippy --all-features -- -D warnings` clean, `cargo test --all-features --no-fail-fast` 529 lib tests + every integration target green | same session | Implemented/Verified |
| `SERVER-001-FR-069` | `SERVER-METRICS-HTTP-DESIGN`, `SERVER-METRICS-HTTP` | ADR-0069 (Accepted as designed, option (a), implemented with one correction found before the build, same branch), `docs/design/SERVER-METRICS-HTTP-DESIGN.md` (Accepted) — an opt-in HTTP `/metrics` listener, entirely separate from the binary wire protocol: `MHTTP-FR-001` `ServeOptions::metrics_http`/`with_metrics_http(listener: TcpListener)`, absent by default; `MHTTP-FR-002` `GET /metrics` → 200 + `ServerMetrics::render()` verbatim + `Content-Type: text/plain; version=0.0.4` + `Connection: close`, else 404; `MHTTP-FR-003` `rusty_http` (path dependency, default features, `server`-gated), the `ADR-0014`/`rusty_tls` reuse template; `MHTTP-FR-004` thread-per-connection over `std::net::TcpListener`, malformed/oversized/truncated heads close with no reply and no panic; `MHTTP-FR-005` no auth/TLS on the new listener, a named tradeoff; `MHTTP-FR-006` `SERVER_METRICS_HTTP_ADDR` wired into all four `src/bin/*_server.rs`, fatal bind error on a typo. Built by Codex via `codex-build`, independently reviewed and re-verified by Claude. **Correction found before the build**: the design's own acceptance criterion asserting the HTTP scrape body byte-identical to a same-instant wire `Metrics` response was unsatisfiable as written (`Metrics` renders before counting its own request; `dogserver_uptime_seconds` can cross a wall-clock second boundary) — corrected to compare individual counter values, excluding uptime, before the build ran. | `src/server/metrics_http.rs` (new), `src/server/serve.rs` (`ServeOptions::metrics_http`/`with_metrics_http`, `serve_tables`'s second accept thread), `src/server/mod.rs` (`mod metrics_http`), `src/bin/{dog,memory,reminder,entity}_server.rs` (`SERVER_METRICS_HTTP_ADDR` wiring), `Cargo.toml` (`rusty_http` optional path dependency, `server` feature), `tests/server_metrics_http_integration.rs` (new) | `cargo fmt -p rusty_multimodal_db -- --check`/`cargo clippy -p rusty_multimodal_db --all-features --tests --bins --lib -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` 769 tests green (up from 758, 0 failed) — 5 new integration tests + 5 new unit tests in `metrics_http.rs` + 1 in `serve.rs`; `cargo tree` confirms `rusty_http` adds no transitive dependency | PR #218 (design), build PR (implementation) | Implemented/Verified |
| `STORAGE-020-FR-001..006` | `SERVER-RESTORE-DESIGN`, `SERVER-RESTORE` | ADR-0070 (Accepted as designed, option (a), implemented on the same branch — Codex via `codex-build`, independently reviewed by Claude), `docs/design/SERVER-RESTORE-DESIGN.md` (Accepted) — a real `restore_backup` CLI closing `ADR-0065`'s own "restore needs no new code" claim: `RST-FR-001` `examples/support/restore_backup_lib.rs` (no `main`) plus a thin `examples/restore_backup.rs` CLI, no new `src/` library module; `RST-FR-002` copies every backup file into the target's parent, names unchanged; `RST-FR-003` crash-safe per file — stage everything into one temp directory first, only then rename each staged file individually into place (per-file atomic, not one whole-group swap, a named limitation since the target may already hold sibling tables' own files); `RST-FR-004` refuses a target with any existing file at the stem's own prefix, before any copy; `RST-FR-005` verifies via a real reopen through the named domain's own portable production constructor, `AllIds::all_ids().len()` for the record count, files retained on failure; `RST-FR-006` prints the `SERVER_DATA_DIR`/restart next step. No deviation from the accepted design. | `examples/support/restore_backup_lib.rs` (new — `Domain`/`RestoreReport`/`RestoreError`/`restore`), `examples/restore_backup.rs` (new, the CLI), `tests/restore_backup.rs` (new, 10 tests), `Cargo.toml` (`[[test]]`/`[[example]]` entries, no `required-features`, no new dependency) | `tests/restore_backup.rs` (10/10 — 5 offline: domain parsing, target-prefix refusal before reading a missing backup, missing-backup cleanup, empty-backup verification failure per domain, no-filename error; 5 live over a real socket: a full `Memory`/`Entity`/`Relation` round trip each via a real `Request::Backup` and an independent production reopen checking every field and relation/`mentions` edge, a retry refusal with byte-identical target/backup bytes, `Entity`'s sibling-table coexistence, a staging-copy-failure case leaving no target file and no leftover temporary directory, a wrong-domain case carrying the real schema-tag `DurabilityError` with every copied file retained) + a real `cargo run --example restore_backup` smoke test against a genuine fixture — `cargo fmt -p rusty_multimodal_db -- --check`/`cargo clippy -p rusty_multimodal_db --all-features --tests --bins --lib --example restore_backup -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` 779 tests green (up from 769, 0 failed), every existing suite's count unchanged | PR #219 (design), build PR (implementation) | Implemented/Verified |
| `MVCC-SPIKE-FR-001..006` | `MVCC-SPIKE-DESIGN`, `MVCC-SPIKE` | `ADR-0071` (Accepted as designed), `docs/design/MVCC-SPIKE-DESIGN.md` (Accepted): research-only versioned writes, snapshot reads, atomic write-write conflict detection, explicit bounded GC, no production wiring. Built by Codex via `codex-build`, independently reviewed and re-verified by Claude before merge. | `src/generic_spike/mvcc_spike.rs`; one module declaration in `src/generic_spike/mod.rs` under the existing research gate | Nine spike unit tests covering acceptance criteria 1–6 and boundary cases. Independently re-run by Claude: `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --all-features -- -D warnings` clean; `cargo test -p rusty_multimodal_db --all-features --no-fail-fast` **all 779 tests green, 0 failed** (Codex's own build report claimed one Python-client failure; that was an artifact of its sandbox having no runnable `python3`/`python`, not a real defect — it passes clean here); `cargo test -p rusty_multimodal_db --lib --features research --no-fail-fast` 361/361 green, including all nine new `mvcc_spike` tests. The bare `--features research` command (without `--lib`) still fails to compile — `tests/server_replication_integration.rs` needs the separate `server`/`client` feature, not `research` — confirmed to reproduce identically on the pre-change `main` baseline (`757690286`), i.e. pre-existing and untouched by this round | this PR | Implemented/Verified |
| `MVCC2-FR-001..012` | `MVCC-PRODUCTION-DESIGN`, `MVCC-PRODUCTION` | `ADR-0072` (Accepted as designed, revised through nine rounds of Codex-found and one Claude-found correction before/during the build — see `ADR-0072`'s own "Acceptance and implementation"), `docs/design/MVCC-PRODUCTION-DESIGN.md` (Accepted): real MVCC (`SESSION_MVCC_ISOLATION`, protocol 27) for `Memory`/`Entity`/`Relation` — per-`(record, field)` version chains adapted from `MVCC-SPIKE`'s proven shape, a shared per-table `AtomicU64` txn counter assigned at *apply* time (round nine's own correction — assigning at journal-append time left a cross-path ordering race; the fix also removed the need for any new insert-log/journal entry kind), activation with a one-time baseline scan, full mutation-path coverage (single-shot writes, session `Commit`, atomic `WriteBatch`, journaled and non-journaled), a write-write conflict check reusing `ErrorCode::Conflict`, and `Compact`'s own flush-before-clear to a persistent `<mmap_path>.mvcc` history store. `ConnectionStore` gained four new default-`Unsupported`/no-op methods, so `Dog`/`Order`/`Employee` needed zero changes. Implemented directly by Claude, not Codex, after Codex's sandbox execution environment became persistently unusable (confirmed via direct log inspection, unrelated to this spec) — owner picked direct implementation per this crate's own host-takeover convention. **Completed across PRs #234–#241 (all merged 2026-09-18)**, each independently verified by Claude (diff read, fresh `fmt`/`clippy`/`test`) before merge: #234 the core, `Memory`/`Entity`/`Relation`; #235 `Entity`/`Relation` wire-level tests; #236 client support — Rust `SessionOptions::mvcc_isolation()` and the Python client (`MVCC2-FR-011`); #237/#238 the insert-log restart-recovery gap closed by a per-domain `open_with_mvcc` (`docs/design/MVCC-OPEN-HOOK-PROPOSAL.md`, option (b): a pre-open read of the pending log via existing `pub(crate)` primitives — the feared `GenericMmapStore::open` hook was not needed, zero generic-layer change); #239 `Dog`/`Order`/`Employee` wired at the owner's call, reversing this ADR's original deferral (their only mutation path is `UpdateField`/`Commit`, no insert log or `Compact`); #240 a real correctness bug found only by the deployment test in #241 — no non-journaled write path ever flushed `<mmap_path>.mvcc` (only `Compact` and journal checkpoints did), so a session `Commit` on an unjournaled table was silently absent from MVCC's own history after a restart while a plain read showed the right value — fixed in all six domains by flushing after every MVCC-recording write inside the same exclusive section; #241 `SERVER_MVCC_ISOLATION` on `memory_server` (fresh-create → `with_mvcc`, reopen → `open_with_mvcc`), with real-compiled-binary restart tests. **Still open, deliberately and named**: reconstruction from a pending *journal* remainder (distinct from the insert-log remainder, now closed — `MVCC-OPEN-HOOK-PROPOSAL.md`'s own open question); `SERVER_MVCC_ISOLATION` and `SERVER_TXN_JOURNAL_PATH` are mutually exclusive per table (`memory_server` refuses the pair at startup); independent Codex review of the whole line — its sandbox could not spawn processes throughout (`ADR-0072` round eleven). | `src/server/mvcc.rs` (new), `src/server/journal.rs` (`Turn::journal_entries`, `CommitGroup::entries_since_checkpoint`), `src/server/protocol.rs` (`SESSION_MVCC_ISOLATION`, `PROTOCOL_VERSION = 27`), `src/server/serve.rs` (`ConnectionStore::{apply_transaction_mvcc,mvcc_supported,mvcc_begin,mvcc_release,mvcc_get}`, session/dispatch wiring, `MvccReleaseGuard`), `src/server/{memory,entity,relation,dog,order,employee}.rs` (`with_mvcc`/`open_with_mvcc`, full mutation-path wiring, per-write and `Compact`/checkpoint flush), `src/server/client.rs` and `clients/python/**` (`mvcc_isolation`), `src/bin/memory_server.rs` (`SERVER_MVCC_ISOLATION`), `tests/server_{memory,entity,relation}_mvcc_integration.rs`, `tests/memory_server_mvcc_integration.rs` (all new), `tests/server_client_only.rs`/`tests/server_transaction_integration.rs` (protocol bump), `docs/specifications/server/SERVER-002-wire-format.md`, `docs/design/MVCC-OPEN-HOOK-PROPOSAL.md` | Per PR: `cargo fmt -p rusty_multimodal_db -- --check` and `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean, full `cargo test -p rusty_multimodal_db --features server,research` green, 0 failed. At `main` after #243: 604 lib tests; 6+6+6 real-socket MVCC tests (`Memory`/`Entity`/`Relation` — version gate, activation baseline, snapshot survives an ordinary concurrent write, post-snapshot insert invisible, session-commit conflict, no false conflict); 3 real-binary `memory_server` tests (env-var gate, fresh-create path under a concurrent ordinary write, restart with a fresh MVCC session seeing the pre-restart commit — the last one is what exposed #240's bug); 10 `mvcc.rs` unit tests; MVCC unit tests in all six adapters including per-path restart-safety tests for the #240 flush | PRs #234–#241 | Implemented/Verified (independent review owed) |
| `SERVER-001-FR-070` (`QPL-FR-001..007`) | `SERVER-QUERY-PLANNER-DESIGN`, `SERVER-QUERY-PLANNER` | `ADR-0073` (Accepted as designed and implemented, option (a) — `Request::Query` only; one recorded deviation), `docs/design/SERVER-QUERY-PLANNER-DESIGN.md` (Accepted): query planner step one — `QPL-FR-001` `plan_query(schema, filter) -> QueryPlan::{FullScan, IndexEq(i)}`, the first `Eq` predicate in wire order on a field the domain's `describe()` reports `filter_eq: true`, deterministic and value-blind (no cost model); `QPL-FR-002` `query_candidates` — `scan_all` for `FullScan`, the adapter's own `filter_eq` bucket read back via per-id `get` for `IndexEq` (a vanished id dropped exactly as `scan_all`'s own `all_ids`-then-`get` shape already drops it — same consistency class); `QPL-FR-003` every predicate re-checked by the unchanged `evaluate_query`, so the index narrows what is read, never what is returned (`Entity::label`'s normalized index is a superset of exact `Eq`; the re-check keeps `Query` exact); `QPL-FR-004` a `filter_eq` refusal falls back to the scan, never surfaces; `QPL-FR-005` result-set equality proven per shipped indexed field; `QPL-FR-006` row order and the `LIMIT`-kept subset may differ from v0.57.0, within the existing "unspecified order" contract; `QPL-FR-007` no trait/adapter/wire/protocol/client change, `PROTOCOL_VERSION` stays 27. **Deviation**: `QPL-FR-004`'s named `debug_assert!` omitted — the fixture test proving the fallback would trip it; the adapters' `describe()`/`filter_eq` agreement is proven by the per-domain tests instead. Built directly by Claude (Codex's sandbox could not spawn processes all session); **no independent review yet** — owed. | `src/server/serve.rs` only (`QueryPlan`, `plan_query`, `query_candidates`, the `Request::Query` dispatch arm; `PlannerFixture` + 5 unit tests), `tests/server_sql_integration.rs` (+4 tests, Memory/Entity/Relation/Reminder starters), `benches/server.rs` (`memory-planner` rows), `docs/specifications/server/SERVER-001-query-layer.md` (v0.58.0, FR-070, plus a named header gap for the never-appended v0.51.0–v0.57.0 entries), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 604 (up from 599), `server_sql_integration` 44 (up from 40), every other target unchanged and green, 0 failed. Criterion 5 measured via `cargo bench --bench server` (`memory-planner`: `WHERE category = 'c7'` through the index vs. `WHERE source = 'c7'` as the equal-selectivity unindexed control, 100K `Memory` records): 576.5 µs vs. 104,492.7 µs per query, the same 1,000 rows returned either way — ~181×, over a real loopback socket; full table in `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-071` (`QPC-FR-001..007`) | `SERVER-QUERY-PLANNER-CONSUMERS-DESIGN`, `SERVER-QUERY-PLANNER-CONSUMERS` | `ADR-0074` (Accepted as designed and implemented, option (a) — all three consumers; no deviation), `docs/design/SERVER-QUERY-PLANNER-CONSUMERS-DESIGN.md` (Accepted): query planner step two — `QPC-FR-001` one private `indexed_candidates(store, schema, filter)` = `query_candidates(store, plan_query(schema, filter), filter)`, `Query`'s arm refactored onto it; `QPC-FR-002` the `Aggregate` arm feeds it to the unchanged `evaluate_aggregate` (own per-row re-filter); `QPC-FR-003` the `filtered_page` trait default's body gathers candidates the same way then re-checks and `page_rows` — signature untouched, no adapter override, **zero observable difference** (`page_rows` orders the filtered set); `QPC-FR-004` `evaluate_join`'s left loop planned against the *left* store's schema, `left_filter` re-check kept, right side untouched; `QPC-FR-005` proven per consumer over real sockets against oracles computed from unfiltered requests (which plan `FullScan`); `QPC-FR-006` `Aggregate` group order / `LIMIT`-kept groups and `Join` pair order / `LIMIT`-kept pairs may differ, inside their designs' own "unspecified order" contracts — no pre-existing test pinned either; `QPC-FR-007` no trait-signature/adapter/wire/protocol/client change, `PROTOCOL_VERSION` stays 27. Built directly by Claude (Codex's sandbox still cannot spawn processes); **no independent review** — owed. | `src/server/serve.rs` only (`indexed_candidates`; the `Query` and `Aggregate` arms; the `filtered_page` default body and doc; `evaluate_join`'s left loop; `PlannerFixture`'s `neighbors` stub + 3 unit tests), `tests/server_sql_integration.rs` (+3, `relates_to` edges on the entity starter), `tests/server_memory_integration.rs` (+1, cross-table), `benches/server.rs` (`memory-planner` rows for `count(*)` and `fpage-50`), `docs/design/SERVER-SQL-JOIN-DESIGN.md` (one sentence), `docs/design/SERVER-FILTERED-PAGE-DESIGN.md` (one qualification), `docs/specifications/server/SERVER-001-query-layer.md` (v0.59.0, FR-071), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 607 (up from 604), `server_sql_integration` 47 (up from 44), `server_memory_integration` 14 (up from 13), every other target unchanged and green, 0 failed. Criterion 4 measured via `cargo bench --bench server` (`memory-planner`: `query`, `count(*)`, `fpage-50`, `category` index vs. unindexed `source` control, 100K `Memory` records): `COUNT(*)` 419.9 µs vs. 99,583.2 µs (~237×), `FilteredPage` `LIMIT 50` 535.7 µs vs. 104,109.9 µs (~194×), `Query` re-measured 546.6 µs vs. 100,524.0 µs (~184×), over a real loopback socket — `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-072` (`QPR-FR-001..007`) | `SERVER-QUERY-PLANNER-RANGE-DESIGN`, `SERVER-QUERY-PLANNER-RANGE` | `ADR-0075` (Accepted as designed and implemented, option (a) — the range candidate step for every consumer; no deviation), `docs/design/SERVER-QUERY-PLANNER-RANGE-DESIGN.md` (Accepted): query planner step three — `QPR-FR-001` `RangeBy::range_by(lower, upper)` on `Ordered`, a guarded `BTreeSet::range` over `(key, id)` pairs (empty, never a panic, for an inverted or equal-exclusive range), exposed on `GenericProductionStore`; `QPR-FR-002` `ConnectionStore::range_field()`/`range_ids()` with defaults (`None`/`Unsupported`), `Memory`/`Relation` answering `updated_at_unix_ms` via `uuid_pair_bounds` (`Uuid::nil()`/`max()` sentinels) — server-side, not a `FieldCapabilities` flag; `QPR-FR-003` `QueryPlan::IndexRange` chosen by `plan_query(schema, range_field, filter)` only after the unchanged equality rule (first `Gt`/`Ge`/`Eq` lower, first `Lt`/`Le`/`Eq` upper, `Ne` never), so no request that planned `IndexEq` changes plan; `QPR-FR-004` `range_bounds` + `query_candidates` walk-then-`get` with the scan fallback, `indexed_candidates` threading `range_field` so all four consumers gain the path; `QPR-FR-005` proven per domain and per consumer over real sockets against the unfiltered scan and the never-range-indexed `created_at` twin, through runtime `Insert`/re-keying `Replace`/`Delete`; `QPR-FR-006` `Query`/`Aggregate`/`Join` order now ascending `(updated_at, id)` on a range plan (unspecified either way), `FilteredPage` zero difference; `QPR-FR-007` no wire/protocol/`FieldCapabilities`/client/`Page` change, `PROTOCOL_VERSION` stays 27. Built directly by Claude (Codex's sandbox still cannot spawn processes); **no independent review** — owed. | `src/generic/query.rs` (`RangeBy`), `src/generic/store.rs` (`impl RangeBy for Ordered`), `src/generic/production.rs` (`range_by`), `src/generic/memory.rs` (+1 test), `src/server/serve.rs` (`range_field`/`range_ids` defaults, `QueryPlan::IndexRange`, `plan_query`, `range_bounds`, `query_candidates`, `indexed_candidates`, `uuid_pair_bounds`/`UuidPairBounds`, the `filtered_page` doc, `PlannerFixture` + 6 tests), `src/server/memory.rs`, `src/server/relation.rs` (the two overrides), `tests/server_sql_integration.rs` (+4), `tests/server_memory_integration.rs` (+1), `benches/server.rs` (`memory-planner` `index-range` rows), `docs/specifications/server/SERVER-001-query-layer.md` (v0.60.0, FR-072), `docs/FUTURE-GROWTH.md`, `docs/design/SERVER-ORDERED-INDEX-DESIGN.md`/`SERVER-QUERY-PLANNER-DESIGN.md` (one open-question note each), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 614 (up from 607), `server_sql_integration` 51 (up from 47), `server_memory_integration` 15 (up from 14), every other target unchanged and green, 892 tests across 39 targets, 0 failed. Measured (acceptance criterion 7, `benches/server.rs`'s `memory-planner` rows extended with an `index-range` pair — the two-sided 1%-selective range `50000 <= updated_at_unix_ms < 51000` through the `Ordered` walk vs. the identical bounds on the never-range-indexed `created_at_unix_ms` twin holding identical values, on the same 100K `Memory` table, over a real loopback socket): `Query` 543.6 µs vs. 94,205.6 µs (~173×), `COUNT(*)` 400.6 µs vs. 84,703.1 µs (~211×), `FilteredPage` `LIMIT 50` 498.9 µs vs. 89,241.0 µs (~179×) — `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-073` (`FPW-FR-001..006`) | `SERVER-FILTERED-PAGE-WALK` | `ADR-0076` (Accepted as implemented — merged unchanged as PR #276 on 2026-09-21 under the owner's `/goal`, option (a); option (b) taken the same day as `ADR-0077`), `docs/design/SERVER-FILTERED-PAGE-WALK-DESIGN.md`: `ADR-0075`'s own option (b) — `FPW-FR-001` eligibility (`order_by` is the range field, every predicate a non-`Ne` bound on it); `FPW-FR-002` the start cursor as the later of the client's cursor and the tightest lower bound (`Gt k` → `(k, max)`, `Ge`/`Eq k` → `(k - 1, max)`, none for `i64::MIN`); `FPW-FR-003` `page_by(start, limit)` → `get` → the longest matching prefix (`Page`'s consistency class); `FPW-FR-004` sequence identity with the factored default proven in-process (26 shapes, 3 pinned) and over a socket via SQL on `Memory`/`Relation` through runtime writes; `FPW-FR-005` every ineligible shape is the default, byte-for-byte; `FPW-FR-006` no generic/wire/protocol/client change, `PROTOCOL_VERSION` stays 27. Built directly by Claude; **no independent review** — owed. | `src/server/serve.rs` (`filtered_page_by_candidates`, `bounded_walk_applies`, `bounded_walk_start`, `bounded_filtered_page`, the trait default now a one-line call, the doc comment; +2 unit tests), `src/server/memory.rs` (the override; +1 unit test), `src/server/relation.rs` (the override), `tests/server_sql_integration.rs` (+1), `benches/server.rs` (`memory-planner` `index-since` row, `measure_planner_pair` generic over the request count), `docs/specifications/server/SERVER-001-query-layer.md` (v0.61.0, FR-073), `docs/FUTURE-GROWTH.md`, `docs/design/SERVER-FILTERED-PAGE-DESIGN.md`/`SERVER-QUERY-PLANNER-RANGE-DESIGN.md` (one note each), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 617 (up from 614), `server_sql_integration` 52 (up from 51), every other target unchanged and green, 896 tests across 39 targets, 0 failed. Measured (acceptance criterion 5, `benches/server.rs`'s new `memory-planner` `index-since` row — `FilteredPage` `WHERE updated_at_unix_ms >= 1000 ORDER BY updated_at_unix_ms LIMIT 50`, a bound admitting ~99% of the same 100K `Memory` table, vs. the `created_at_unix_ms` full-scan control, over a real loopback socket, the row added and measured on the pre-change code first): before this round 123,436.1 µs (walk every in-range record, then page; control 146,098.2 µs), after 89.4 µs (the bounded walk; control 143,975.6 µs) — ~1,380× — `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-074` (`FPM-FR-001..006`) | `SERVER-FILTERED-PAGE-WALK-MIXED` | `ADR-0077` (Proposed and implemented on one branch; the fork — (a) as implemented / (b) plus intersecting the equality index with the range / (c) decline — held open for the owner at review), `docs/design/SERVER-FILTERED-PAGE-WALK-MIXED-DESIGN.md`: `ADR-0076`'s own option (b) — `FPM-FR-001` eligibility (`order_by` is the range field and the filter does not plan the declared equality index, `plan_query`'s equality-first rule kept); `FPM-FR-002` the walked key read off the record and the resume cursor (the greatest `(key, id)` read), `bounded_walk_start` over the walked field's predicates alone; `FPM-FR-003` chunks of `limit` through `page_by`, a non-`Ne` bound on the walked field cuts, everything else is passed over, until `limit` matches, a cut, or the index's end (`Page`'s consistency class, a vanished id back-filled); `FPM-FR-004` sequence identity with the factored default proven on an `I64` fixture (with `get` counts and the three concurrency windows pinned), on `Memory` (32 shapes, 5 pinned), and over a socket via SQL on `Memory`/`Relation` through runtime writes; `FPM-FR-005` the equality-index filter is the default, byte-for-byte; `FPM-FR-006` no generic/wire/protocol/client change, `PROTOCOL_VERSION` stays 27. Built directly by Claude; **no independent review** — owed. | `src/server/serve.rs` (`bounded_walk_applies`/`bounded_walk_start`/`bounded_filtered_page` widened, `walked_key` new, the trait doc comment; +2 unit tests, 2 rewritten/extended), `src/server/memory.rs`/`relation.rs` (the overrides pass `describe()` and `order_by`; +6 oracle shapes, 2 pinned), `tests/server_sql_integration.rs` (+1), `benches/server.rs` (`since-mixed`/`since-eq` rows; every control page re-ordered by `created_at_unix_ms` so it cannot walk), `docs/specifications/server/SERVER-001-query-layer.md` (v0.62.0, FR-074), `docs/FUTURE-GROWTH.md`, `docs/design/SERVER-FILTERED-PAGE-WALK-DESIGN.md`/`SERVER-FILTERED-PAGE-DESIGN.md`, `ADR-0076` (one note each), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 619 (up from 617), `server_sql_integration` 53 (up from 52), every other target unchanged and green, 899 tests across 39 targets, 0 failed. Measured (`benches/server.rs`'s new `memory-planner` `since-mixed` row, `FilteredPage` `WHERE updated_at_unix_ms >= 1000 AND source = 'c7' ORDER BY updated_at_unix_ms LIMIT 50` over 100K `Memory` records vs. the `created_at_unix_ms` + `source` full-scan control, both binaries run back to back on an idle 4-core Linux container): before 129,732.5 µs (control 150,205.5 µs), after 3,933.2 µs (control 139,143.4 µs) — ~33×; `since-eq` 1,368.8 → 1,100.4 µs — `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-075` (`QPI-FR-001..006`) | `SERVER-QUERY-PLANNER-INTERSECT` | `ADR-0078` (Proposed and implemented on one branch; the fork — (a) as implemented / (b) plus a width guard / (c) decline — held open for the owner at review), `docs/design/SERVER-QUERY-PLANNER-INTERSECT-DESIGN.md`: `ADR-0077`'s own option (b) and `ADR-0075`'s carried open question — `QPI-FR-001` `IndexIntersect { eq, lower, upper }` when the first eligible equality and a bound on the range field are both present; `QPI-FR-002` both id lists fetched without a decode, `intersect_ids` (the smaller hashed), only the members read; `QPI-FR-003` every consumer through `indexed_candidates`, `bounded_walk_applies` standing aside; `QPI-FR-004` set identity proven on `PlannerFixture` (with `get`/scan counts), `Memory`, and over a socket via SQL on `Memory`/`Relation` through runtime writes; `QPI-FR-005` a walk refusal reads the bucket alone, a bucket refusal scans; `QPI-FR-006` `serve.rs` alone, `PROTOCOL_VERSION` stays 27. Built directly by Claude; **no independent review** — owed. | `src/server/serve.rs` (`QueryPlan::IndexIntersect`, `plan_query`, `query_candidates`, `intersect_ids`, `bounded_walk_applies`; +3 unit tests, 1 assertion rewritten), `tests/server_sql_integration.rs` (+1), `benches/server.rs` (`eq-range` rows, `intersect_filter`), `docs/specifications/server/SERVER-001-query-layer.md` (v0.63.0, FR-075), `docs/FUTURE-GROWTH.md`, `ADR-0077`/its design and `SERVER-QUERY-PLANNER-RANGE-DESIGN.md` (one note each), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 622 (up from 619), `server_sql_integration` 54 (up from 53), every other target unchanged and green, 903 tests across 39 targets, 0 failed. Measured (`eq-range`, ten of 100K records, idle 4-core Linux container): `query` 1,008.5 → 174.3 µs, `count(*)` 1,028.7 → 91.5, `fpage-50` 1,085.8 → 101.4; `since-eq` 1,270.7 → 5,030.0 — `RESULTS.md` | this session | Implemented/Verified |
| `SERVER-001-FR-076` (`QPB-FR-001..006`) | `SERVER-QUERY-PLANNER-INTERSECT-BUDGET` | `ADR-0079` (Proposed and implemented on one branch; the fork — (a) as implemented / (b) the budget as a setting / (c) decline — held open for the owner at review), `docs/design/SERVER-QUERY-PLANNER-INTERSECT-BUDGET-DESIGN.md`: `ADR-0078`'s own option (b), read as a budget on work rather than an estimate of size — `QPB-FR-001` `RangeBy::range_by_limited` on `Ordered` (abandoned at the first id past the budget, nothing materialized), `QPB-FR-002` `ConnectionStore::range_ids_limited` on `Memory`/`Relation`, `QPB-FR-003` the `IndexIntersect` arm walking with `|bucket| × INTERSECT_WALK_BUDGET` (10) and reading the bucket alone past it, an empty bucket walking nothing; `QPB-FR-004` set identity on every path proven on `PlannerFixture` (with counts, the over-budget path forced), `Memory`'s adapter, the generic `Ordered`, and the socket; `QPB-FR-005` the worst case bounded at ~1.4× the bucket alone; `QPB-FR-006` `plan_query`/wire/clients untouched. Built directly by Claude; **no independent review** — owed. | `src/generic/query.rs` (`range_by_limited`), `src/generic/store.rs` (`guarded_range`, `range_by_limited`), `src/generic/production.rs` (`range_by_limited`), `src/server/serve.rs` (`range_ids_limited` default, `INTERSECT_WALK_BUDGET`, the arm; +1 unit test, `PlannerFixture` extended), `src/server/memory.rs`/`relation.rs` (`range_ids_limited`; +1 unit test), `src/generic/memory.rs` (+1 unit test), `docs/specifications/server/SERVER-001-query-layer.md` (v0.64.0, FR-076), `docs/FUTURE-GROWTH.md`, `ADR-0078`/its design (one note each), `RESULTS.md` | `cargo fmt -p rusty_multimodal_db -- --check` clean; `cargo clippy -p rusty_multimodal_db --features server,research --all-targets -- -D warnings` clean; `cargo test -p rusty_multimodal_db --features server,research` — lib 625 (up from 622), every other target unchanged and green, 906 tests across 39 targets, 0 failed. Measured (idle 4-core Linux container): `since-eq` 5,170.9 → 1,468.9 µs; `eq-range` `query`/`count(*)`/`fpage-50` 97.5/116.2/111.2 → 129.8/131.7/106.7 — `RESULTS.md` | this session | Implemented/Verified |

Trace both directions: requirements reach evidence, and material code has
a requirement, defect, maintenance policy, or ADR. Update this table in
the same PR that changes implementation state.
