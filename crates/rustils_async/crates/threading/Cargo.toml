[package]
name = "threading"
description = "Minimal multithreading primitives: scoped-thread spawn with a decoded join outcome, and Mutex/RwLock with an explicit poisoning policy. No unsafe."
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
publish = false
license = "MIT"

[package.metadata.rusty_mill]
layer = "platform"

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
undocumented_unsafe_blocks = "deny"
