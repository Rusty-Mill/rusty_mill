[package]
name = "platform-mock"
description = "In-memory backend implementing every platform trait — the injectable test double (RFC v2 §4.1)."
version = "0.27.0"
edition = "2021"
rust-version = "1.75"
publish = false
license = "MIT"

[dependencies]
platform.workspace = true

[lints]
workspace = true

[dev-dependencies]
platform-parity.workspace = true
