{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageNoteMergeResult",
  "description": "Reply for `com.nexus.storage::note_merge`.",
  "type": "object",
  "properties": {
    "files_rewritten": {
      "description": "Referencing files whose links were redirected.",
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "links_updated": {
      "description": "Link occurrences redirected across those files.",
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "target": {
      "description": "The merged note (same as the `target` arg).",
      "type": "string"
    },
    "trash_id": {
      "description": "Forge-trash bucket id for the deleted source; `null` for\n`system` / `permanent` destinations.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "target",
    "files_rewritten",
    "links_updated"
  ]
}
