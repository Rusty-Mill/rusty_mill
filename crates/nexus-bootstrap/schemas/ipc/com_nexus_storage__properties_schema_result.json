{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StoragePropertiesSchemaResult",
  "description": "Reply for `com.nexus.storage::properties_schema` (handler `87`).\nValues are property type names: `text`, `number`, `date`,\n`date_time`, `boolean`, `list`, `link`, `tags`.",
  "type": "object",
  "properties": {
    "inferred": {
      "description": "Key → type inferred from indexed frontmatter values.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "overrides": {
      "description": "Key → type declared in `.forge/app.toml` `[properties].type_overrides`.\nOverrides win over inferred types.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "inferred",
    "overrides"
  ]
}
