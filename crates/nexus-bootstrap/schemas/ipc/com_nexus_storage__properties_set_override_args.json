{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StoragePropertiesSetOverrideArgs",
  "description": "Args for `com.nexus.storage::properties_set_override` (handler `88`).",
  "type": "object",
  "properties": {
    "key": {
      "description": "Frontmatter key.",
      "type": "string"
    },
    "property_type": {
      "description": "Type name to declare, or `null` to drop the override.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    }
  },
  "additionalProperties": false,
  "required": [
    "key"
  ]
}
