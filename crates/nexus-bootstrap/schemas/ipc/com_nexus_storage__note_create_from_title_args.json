{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StorageNoteCreateFromTitleArgs",
  "description": "Args for `com.nexus.storage::note_create_from_title` (handler `86`).",
  "type": "object",
  "properties": {
    "content": {
      "description": "Initial body. Default empty. For \"extract selection\", the selection.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "folder": {
      "description": "Forge-relative folder for the new note. Default: forge root.",
      "type": [
        "string",
        "null"
      ],
      "default": null
    },
    "title": {
      "description": "Human title; sanitised into the filename stem. Must not sanitise to empty.",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "title"
  ]
}
