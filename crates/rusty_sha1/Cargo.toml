[package]
name = "rusty_sha1"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "A zero-dependency SHA-1 (FIPS 180-1) implementation, shared by rusty_git's object hashing and rusty_term's WebSocket handshake"

[dependencies]
