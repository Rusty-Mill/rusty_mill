[package]
name = "ts-types"
description = "Core wire and API types: keys, node/peer status, netmap, DERP map (serde)"
version = "0.0.1"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/baileyrd/rusty_tailscale"

[dependencies]
serde.workspace = true
# Regular (not dev) dependency: CapMap values are arbitrary JSON.
serde_json.workspace = true
thiserror.workspace = true
